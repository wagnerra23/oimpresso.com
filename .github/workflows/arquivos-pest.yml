name: Arquivos · Pest (MySQL)

# Lane dedicada pra rodar a suíte Pest MySQL-only do Modules/Arquivos contra
# MySQL REAL. Existe porque ci.yml e modules-pest.yml rodam SQLite :memory: — e
# os testes de audit/download do Arquivos dependem de comportamento MySQL-only
# (ENUM strict mode em arquivos_audit_log.action + introspection SHOW COLUMNS),
# então lá eles dão markTestSkipped (falsa cobertura). Aqui executam de verdade
# contra o schema baseline migrado.
#
# GAP fechado (follow-up do PR #3658): o bug `signed_url_consumed` só reproduz em
# MySQL — o enum original (2026_05_10_000002) não tinha o valor, o INSERT falhava
# em strict mode e era engolido pelo try/catch do DownloadController (nenhuma
# consumação de signed URL era auditada, viola ADR 0123 §8). Os guardas de
# regressão (AuditLogActionEnumTest / DownloadAuditTest, e o teste #8 rapid-fire
# do AuditLogCommandTest) são MySQL-only BY DESIGN e, sem esta lane, ficavam
# DECORATIVOS: a genérica "modules-pest" (SQLite) os pulava. Esta lane vira o HOME
# do ratchet pra TODO teste MySQL-only do Arquivos.
# Refs: ADR 0123 (Modules/Arquivos backbone, §8 audit integral) · PR #3658.
#
# Reusa .github/actions/pest-mysql-setup (mesma do financeiro-pest / jana-pest,
# FV-F2): PHP 8.4 + deps + .env MySQL + migrate (schema baseline 2026-06-04 +
# migrations novas, INCLUI 2026_07_02_000001_widen_arquivos_audit_log_action_enum
# que amplia o enum com signed_url_consumed) + seed mínimo biz=1 (Business::first())
# + biz=2 (Tier 0 cross-tenant — ADR 0093/0101).

on:
  workflow_dispatch: {}
  # ADR 0271 onda 2 (required-readiness): pull_request SEM paths (always-run) pra
  # ser promovível a required sem deadlock "Expected — waiting". O custo (MySQL +
  # migrate + Pest) é evitado por skip-as-pass interno: dorny/paths-filter detecta
  # se algo do Arquivos mudou; se não, pula o pesado e conclui verde. push segue
  # path-filtered (contém custo no main).
  pull_request:
    branches: [main]
  push:
    branches: [main]
    paths:
      - 'Modules/Arquivos/**'
      - 'database/schema/mysql-schema.sql'
      - '.github/workflows/arquivos-pest.yml'
      - '.github/actions/pest-mysql-setup/**'

concurrency:
  group: arquivos-pest-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pest:
    name: PHP / Pest (Arquivos · MySQL)
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: oimpresso_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20

    steps:
      - uses: actions/checkout@v4

      - name: Detectar mudanças do Arquivos (skip-as-pass — ADR 0271 onda 2)
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            arq:
              - 'Modules/Arquivos/**'
              - 'database/schema/mysql-schema.sql'
              - '.github/workflows/arquivos-pest.yml'
              - '.github/actions/pest-mysql-setup/**'

      - name: Skip-as-pass (nada do Arquivos mudou)
        if: steps.changes.outputs.arq == 'false'
        run: echo "✅ Sem mudanças no Modules/Arquivos — skip-as-pass (required-ready, ADR 0271 onda 2)."

      # Setup canônico reusável (composite action). O service container MySQL fica
      # DECLARADO ACIMA no job (composite action não pode declarar services).
      - name: Setup Pest MySQL (PHP + deps + .env + migrate + seed mínimo multi-tenant)
        if: steps.changes.outputs.arq == 'true'
        uses: ./.github/actions/pest-mysql-setup

      - name: Stub Vite manifest (Inertia root view sem build do front)
        if: steps.changes.outputs.arq == 'true'
        run: |
          mkdir -p public/build-inertia
          cat > public/build-inertia/manifest.json <<'EOF'
          {
            "resources/css/inertia.css": { "file": "assets/inertia.css", "src": "resources/css/inertia.css", "isEntry": true },
            "resources/js/app.tsx": { "file": "assets/app.js", "src": "resources/js/app.tsx", "isEntry": true }
          }
          EOF

      - name: Run Pest (Arquivos · MySQL) — ALLOWLIST VERDE (catraca)
        if: steps.changes.outputs.arq == 'true'
        # phpunit.xml força DB_CONNECTION=sqlite :memory: — se não sobrescrever, os
        # testes MySQL-only (enum strict / SHOW COLUMNS) SKIPam (a falsa cobertura).
        # Exportar aqui faz rodar no MySQL real semeado.
        #
        # CATRACA: a lane roda só os arquivos comprovadamente verdes (MySQL-only do
        # Arquivos). Cada novo teste MySQL-only do Arquivos é adicionado AQUI
        # (ratchet up). NÃO incluir arquivos com RefreshDatabase/migrate:fresh —
        # eles dropam o schema (FK) e LIMPAM o biz=1 seedado, envenenando os demais.
        # Os daqui usam DB::table()/factory + cleanup no afterEach (sem refresh).
        #
        # +3 em 2026-08-10 (eixo 2 do test-lane-coverage · `--mudos`): os 3 abaixo têm
        # guard `if sqlite → markTestSkipped` e a ÚNICA lane que os alcançava era o
        # modules-pest.yml, que roda sqlite :memory: ⇒ pulavam 100% das vezes, e skip
        # sai exit 0 (verde que não prova nada). Entram AQUI, que é o lar declarado do
        # ratchet MySQL-only do Arquivos. Nenhum usa RefreshDatabase/migrate:fresh/
        # dropIfExists; ConsumersTraitTest escreve 0 linhas (reflexão sobre models).
        #
        # ERAM 5 CANDIDATOS. Rodados de verdade pela 1ª vez (run 31411208410), 2 ficaram
        # VERMELHOS e por isso NÃO entram — a catraca é "allowlist VERDE", e ligar um
        # vermelho permanente numa lane advisory só ensina o time a ignorá-la:
        #
        #   RetentionCleanupCommandTest — ✅ RESOLVIDO em 2026-08-10, entrou na allowlist
        #     acima. As 5 falhas NÃO eram defeito do teste: eram BUG DE PRODUÇÃO.
        #     RetentionCleanupCommand.php:124 abria a closure com
        #     `use ($dryRun, $retentionDays, &$stats)` e usava `$businessId` na :194, que
        #     nunca foi capturado ⇒ `Undefined variable $businessId`. O comando crashava
        #     sempre que HAVIA linha a limpar (com 0 linhas o chunk nem chama a closure —
        #     por isso passava despercebido, e o teste que provaria saía verde por skip).
        #     Fix = capturar $businessId no `use`. É o teste que agora defende isso.
        #
        #   ExportZipCommandTest — ✅ RESOLVIDO em 2026-08-10, entrou na allowlist acima.
        #     As 4 falhas eram DUAS causas distintas:
        #     (a) 3 delas: `Artisan::output()` chamado 2× no mesmo teste. Ele é
        #         BufferedOutput::fetch() por baixo — retorna E ESVAZIA o buffer, então a
        #         2ª asserção recebia string vazia. Defeito de teste; capturar 1× em
        #         $output. O sintoma (`Expected: <vazio>`) apontava pro COMANDO — quem não
        #         medisse investigaria o lugar errado.
        #     (b) 1 delas: BUG DE PRODUÇÃO, mesma classe do `signed_url_consumed` que o
        #         PR #3658 consertou. `exported` nunca esteve no enum de
        #         arquivos_audit_log.action; o ExportZipCommand insere esse valor dentro de
        #         try/catch(\Throwable) ⇒ em MySQL strict o INSERT falha, o catch engole, o
        #         comando retorna 0 e a trilha LGPD Art. 18 NÃO é gravada. Fix: migration
        #         2026_08_10_000001 amplia o enum (append-only; down() recusa estreitar com
        #         linhas gravadas).
        #
        # Os dois seguem mudos até serem tratados — dívida agora VISÍVEL e datada, em
        # vez de verde-fantasma.
        env:
          DB_CONNECTION: mysql
          DB_HOST: 127.0.0.1
          DB_PORT: "3306"
          DB_DATABASE: oimpresso_test
          DB_USERNAME: root
          DB_PASSWORD: root
        run: |
          mkdir -p test-results
          vendor/bin/pest --no-coverage --colors=always \
            --log-junit test-results/pest-arquivos-junit.xml \
            Modules/Arquivos/Tests/Feature/AuditLogActionEnumTest.php \
            Modules/Arquivos/Tests/Feature/AuditLogCommandTest.php \
            Modules/Arquivos/Tests/Feature/DownloadAuditTest.php \
            Modules/Arquivos/Tests/Feature/CuradorStatsReaderTest.php \
            Modules/Arquivos/Tests/Feature/ConsumersTraitTest.php \
            Modules/Arquivos/Tests/Feature/DedupeStatsCommandTest.php \
            Modules/Arquivos/Tests/Feature/HealthCheckCommandTest.php \
            Modules/Arquivos/Tests/Feature/RetentionCleanupCommandTest.php \
            Modules/Arquivos/Tests/Feature/ExportZipCommandTest.php


      # Passo 1 do plano da LC-13 (Gate:): a lane JA emitia JUnit e NINGUEM lia.
      # O sumario conta `assertions` por arquivo — e o corpus que falta pra medir
      # o FP do --check-assertions ANTES de arma-lo. Aqui so REPORTA (if: always()).
      - name: Sumario JUnit por arquivo (junit-summary.mjs — corpus p/ LC-13)
        if: always()
        run: node scripts/tests/junit-summary.mjs test-results/pest-arquivos-junit.xml --out test-results/pest-arquivos-junit-summary.json || true
      # ── PASSO 3 do plano do LC-13 — ARMADO (flip [W] 2026-08-16) ─────────────────
      # Guardado pela MESMA condicao do run de Pest, NUNCA pelo `if: always()` do step
      # de corpus acima: la o step roda ate quando o skip-as-pass (ADR 0271 onda 2)
      # impediu o Pest e o XML nem existe — armar ali ficaria vermelho em todo PR que
      # nao toca o modulo. Medido 2026-08-16; e a razao de isto ser um step PROPRIO e
      # nao um flag no de cima.
      #
      # MORDE quando a lane RODOU e nao provou nada: `0 failed` com 0 assertions e
      # ausencia de EXECUCAO, nao sucesso (LC-13 — "VERDE por NAO-EXECUCAO").
      # Sem `always()`/`!cancelled()`, o `success()` implicito vale: se o Pest falhou,
      # este step e pulado e a lane ja esta vermelha por conta propria (nao duplica).
      - name: Suite provou algo? (--check-assertions · LC-13)
        if: steps.changes.outputs.arq == 'true'
        run: node scripts/tests/junit-summary.mjs test-results/pest-arquivos-junit.xml --check-assertions

      - name: Upload sumario JUnit (pest-arquivos-junit)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pest-arquivos-junit-summary
          path: test-results/pest-arquivos-junit-summary.json
          retention-days: 14
          if-no-files-found: ignore
      - name: Upload JUnit (Arquivos · MySQL)
        if: ${{ !cancelled() && steps.changes.outputs.arq == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: pest-arquivos-junit
          path: test-results/pest-arquivos-junit.xml
          retention-days: 14
