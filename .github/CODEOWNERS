# CODEOWNERS — oimpresso ERP
# Mapeia SÓ os paths SENSÍVEIS para reviewer obrigatório no GitHub.
# Sintaxe: path @github-user1 @github-user2
# Referência: TEAM.md §3 + memory/reference/feedback-claude-aprova-merge-verde-criticos-nao.md
#
# ⚠️ POLÍTICA (2026-06-03): NÃO existe mais catch-all `*`. Só os paths abaixo
# exigem review de code owner. Todo o resto = sem owner obrigatório → PR verde
# (CI passando) é mergeável sem aprovação manual do Wagner — delegado ao Claude.
# Ver feedback-claude-aprova-merge-verde-criticos-nao.md ("merge verde, críticos não").
#
# ⚠️ ENFORCEMENT — este arquivo NÃO afirma o próprio enforcement em presente (LC-10).
# Quem sabe é a branch protection VIVA; leitor: `node scripts/governance/flip-required.mjs`
# (imprime require_code_owner_reviews e enforce_admins) ou o GET de protection.
# Fato datado 2026-09-22: o PR #7658 tocou `TEAM.md` — path COBERTO por este arquivo —
# e mergeou com ZERO reviews (`pull_request_read get_reviews` → `[]`), pelo caminho admin.
# Logo: code owner aqui NÃO trava quem mergeia como admin. NÃO MEDIDO: o caminho
# não-admin — pela doc do GitHub a aprovação passa a ser exigida, mas aqui não há recibo.
# Não leia este arquivo como trava universal nem como decoração; leia como "reviewer
# obrigatório pra quem tem write sem admin, reviewer sugerido pro resto".
#
# ⚠️ GitHub IGNORA code owner sem acesso de ESCRITA — isso é comportamento do GitHub,
# não estado do repo, e vale sempre.
# Fato datado 2026-09-22 (fonte: API de collaborators, não memória): o repo tem 5
# collaborators — @wagnerra23 (admin) e @LuizWr2, @SupportWR, @grokwr2, @felipewr2-cell
# (write). A redação anterior desta linha dizia "TIME AINDA NÃO É COLABORADOR DO REPO,
# só @wagnerra23 é dono válido hoje" — era verdade quando escrita (2026-06-03) e ficou
# FALSA sem avisar, e foi ela que sustentou 3 meses de `# TODO`.
# Handle se confere na API, nunca se adivinha do nome: @SupportWR é a **Maiara**.
# Pra pôr alguém como dono: (1) confirmar write na API; (2) trocar `# TODO` pelo @handle.

# ──────────────────────────────────────────────
# 💰 DINHEIRO / FISCAL / RECORRÊNCIA — Wagner (Eliana futura)
# ──────────────────────────────────────────────
Modules/Financeiro/                 @wagnerra23   # TODO: + @<handle Eliana>
Modules/NfeBrasil/                  @wagnerra23   # TODO: + @<handle Eliana>
Modules/RecurringBilling/           @wagnerra23   # TODO: + @<handle Eliana>
memory/requisitos/Financeiro/       @wagnerra23
memory/requisitos/NfeBrasil/        @wagnerra23
memory/requisitos/RecurringBilling/ @wagnerra23
memory/requisitos/Boleto.md         @wagnerra23

# ──────────────────────────────────────────────
# 🛒 VENDA — Wagner + Felipe
# ──────────────────────────────────────────────
# [W] 2026-09-21, textual: *"quem é o dono da venda então, é o Felipe"*. Registro do
# ato em TEAM.md §3.3.1; aqui é o enforcement.
#
# A venda NÃO é um módulo — `Modules/Sells` não existe. São 6 paths do núcleo
# UltimatePOS, 197 arquivos medidos em 2026-09-22 (re-rode, não cite este número):
#   git ls-tree -r origin/main --name-only -- resources/js/Pages/Sells \
#     app/Http/Controllers/SellController.php app/Http/Controllers/SellPosController.php \
#     resources/views/sell resources/views/sale_pos memory/requisitos/Sells | wc -l
#
# Por que ela é dono-obrigatório embora `resources/js/` seja zona livre: a venda é o
# chokepoint da regra Tier 0 "CÁLCULO DE VALOR ou ESTOQUE" (memory/proibicoes.md) — em
# 2026-06-05 o `Util::num_uf` inflou 16 vendas do biz=4 (cliente real) antes de alguém ver.
#
# OS DOIS handles, de propósito: qualquer um dos dois aprova. Só @felipewr2-cell
# travaria o [W], porque o GitHub não deixa ninguém aprovar PR próprio.
resources/js/Pages/Sells/                   @wagnerra23 @felipewr2-cell
app/Http/Controllers/SellController.php     @wagnerra23 @felipewr2-cell
app/Http/Controllers/SellPosController.php  @wagnerra23 @felipewr2-cell
resources/views/sell/                       @wagnerra23 @felipewr2-cell   # Blade legada (MWART pendente)
resources/views/sale_pos/                   @wagnerra23 @felipewr2-cell   # Blade legada (MWART pendente)
memory/requisitos/Sells/                    @wagnerra23 @felipewr2-cell   # SPEC · charter · casos

# ──────────────────────────────────────────────
# 🔒 LGPD / DADOS PESSOAIS — Wagner (+ Felipe em Modules/Jana/)
# ──────────────────────────────────────────────
# O `# TODO: + @<handle Felipe>` desta linha foi preenchido por ato do [W] em
# 2026-09-22 ("agora preenche o TODO da Jana e do SRS"). Handle conferido na
# fonte primária, não adivinhado do nome: @felipewr2-cell autora commits deste
# repo com `felipewr2@gmail.com`, e o corpo deles diz "maquina Felipe" /
# "conta felipewr2".
# ⚠️ O [W] nomeou a JANA, não o bloco LGPD inteiro — as 5 linhas abaixo
# seguem só com @wagnerra23 DE PROPÓSITO. Estendê-las é ato dele, não
# "completar o que faltou" (dono inventado parece canon — TEAM.md §3.3).
Modules/Jana/                       @wagnerra23 @felipewr2-cell   # ex-Copiloto (rename ADR 0088)
memory/requisitos/Jana/             @wagnerra23   # docs LGPD-crítico (SPEC, PII-REDACTION, retention). Ghost-dir requisitos/Copiloto/ migrado p/ cá em 2026-07-01 (só lápides restam, sem conteúdo LGPD)
memory/requisitos/EvolutionAgent/   @wagnerra23
Modules/Ponto/                      @wagnerra23   # ponto eletrônico = dado pessoal (ex-PontoWr2)
memory/requisitos/Ponto/            @wagnerra23   # docs ponto (dir canônico atual)
memory/requisitos/PontoWr2/         @wagnerra23   # docs ponto legacy (ainda com arquivos)

# ──────────────────────────────────────────────
# 🗝️ SEGREDOS / COFRE — Wagner + Maiara + Felipe
# ──────────────────────────────────────────────
# O `# TODO: + @<handle Maiara> @<handle Felipe>` foi preenchido por ato do [W]
# em 2026-09-22, mesmo pedido da Jana acima. Handles conferidos na fonte
# primária: @SupportWR autora commits deste repo com `name: "MaiaraWR"` (é a
# MAIARA, não o Felipe — o login engana); @felipewr2-cell como acima.
#
# ⚠️ FATO DATADO 2026-09-22 — a regra abaixo não casa arquivo nenhum hoje:
# `Modules/SRS/` tem **0** arquivos em `origin/main` (o módulo foi removido).
# Controle positivo no MESMO comando: `Modules/Financeiro` = 232.
#   git ls-tree -r origin/main --name-only -- Modules/SRS | wc -l
# Ressuscitar o SRS é PROIBIDO (§5 2026-07-29); os sucessores canônicos são
# `Modules/KB`, `Modules/Jana` e `Modules/Governance`. O que sobrou do SRS são
# 3 docs em `memory/requisitos/SRS/` (BRIEFING · DEPRECATION-PLAN · SPEC), que
# este arquivo NÃO cobre — se a posse deve valer sobre eles, é decisão do [W]
# e é uma linha. Re-meça antes de citar este número.
Modules/SRS/                        @wagnerra23 @SupportWR @felipewr2-cell

# ──────────────────────────────────────────────
# 🏗️ INFRA / CI / DEPLOY / GOVERNANÇA — Wagner (ninguém mergeia sem ele)
# ──────────────────────────────────────────────
.github/                            @wagnerra23   # workflows + este próprio CODEOWNERS
INFRA.md                            @wagnerra23
docker-compose*.yml                 @wagnerra23
memory/decisions/                   @wagnerra23   # ADR canon
CLAUDE.md                           @wagnerra23
TEAM.md                             @wagnerra23

# ──────────────────────────────────────────────
# LIVRE (sem owner obrigatório) — PR verde mergeável sem Wagner:
#   resources/js/ (frontend) — EXCETO `resources/js/Pages/Sells/` ·
#   Modules/Cms (landing) · demais Modules/ ·
#   memory/sessions/ · memory/handoffs/ · memory/requisitos não-críticos · etc.
#
# ⚠️ A venda SAIU da zona livre em 2026-09-22 (seção 🛒 acima): além de
# `Pages/Sells/`, saíram `resources/views/sell/`, `resources/views/sale_pos/` e
# `memory/requisitos/Sells/`. Este parágrafo é o único lugar do arquivo que
# enumerava a zona livre — recortá-la aqui é parte do ato, não zelo.
# ──────────────────────────────────────────────
