---
thread: "09 · PUXAR as 11 telas restantes"
dono: "[CC]"
estado: feito — 11/11 linhas medidas · 4 correções no build · divergências declaradas
base_lida: wagnerra23/oimpresso.com@main 8752fa20775a (2026-09-24)
prefixo_tocado: ponto-telas.jsx (Escalas: célula de remover) · oimpresso.com.html (?v=…-e)
---
# _saida-09 · 11 telas do Ponto

Leitura por busca dirigida (âncoras, `data-testid`, 501, IA, remover), **não** por `.tsx` inteiro. Onde não li, está escrito "não medido".

| # | Page viva (B) | protótipo | medido? | puxado | divergência declarada |
|---|---|---|---|---|---|
| 1 | `Aprovacoes/Index.tsx` (23.762, lido inteiro) | `Aprovacoes` | **sim** | ✅ tira de 6 KPIs por estado que **filtra ao clicar** · filtro **Prioridade** (Urgente/Normal) · "impacta apuração" sob o tipo · **Ver** em toda linha (antes só nas não-pendentes) · aprovar pede **confirmação** (com aviso quando impacta apuração) · rejeitar abre **Modal com motivo** 5–500 e contador (antes `window.prompt`) · vazio "Caixa vazia" × "Nenhum resultado" + Limpar filtros · Limpar só aparece com filtro ativo | protótipo **aprova e rejeita em lote** com motivo único; produção só **aprova** em lote (`/ponto/aprovacoes/lote`) — rejeição em lote é comportamento só do protótipo, não removi · filtro inicial: protótipo abre em **Pendente**, produção em **todos** · coluna "Criada" (`created_at_human`) não entrou: o mock não tem o campo e inventar data é VALOR |
| 2 | `Intercorrencias/Index.tsx` (10.913, lido inteiro) | `Intercorrencias` | **sim** | ✅ filtros **Estado** e **Tipo** (o protótipo não tinha nenhum) + Limpar · vazio em 2 casos: "Sem intercorrências" + **Criar primeira** × "Nenhum resultado" + Limpar filtros | protótipo mantém a coluna Prioridade (produção põe "Urgente" junto do estado) · coluna "Criada" não entrou (mock sem `created_at_human`) |
| 3 | `Intercorrencias/Create.tsx` (17.059) | `FormIntercorrencia` | **parcial** | — | produção chama `POST /ponto/intercorrencias-ai/classify` (sugere tipo; `Create.casos.md:32` é **BACKLOG** — sem UC sobre o que a sugestão pode fazer). **Não trouxe**: desenhar a sugestão sem UC é inventar UX |
| 4 | `Intercorrencias/Show.tsx` (6.825, lido inteiro) · `Edit.tsx` não lido | drawer de detalhe | **sim** (Show) | ✅ **Submeter** e **Cancelar** pedem confirmação, com a copy da produção ("Submeter esta intercorrência para aprovação?" · "…A ação não é reversível.") | protótipo mostra o detalhe em **Drawer** (PT-02); produção em página própria — layout, fica. Mesmas regras: editar/submeter só em RASCUNHO, cancelar em RASCUNHO/PENDENTE |
| 5 | `BancoHoras/{Index,Show}.tsx` (7.641 + 9.471, lidos inteiros) | `BancoHoras` | **sim** | nada — o protótipo já cobre: 4 KPIs (crédito, débito, com crédito, com débito), ajuste ± com observação ≥ 5, aviso append-only, histórico | **enum de movimento diverge:** produção `tipo` ∈ `CREDITO_HE · DEBITO_FOLGA · AJUSTE_MANUAL · EXPIRACAO · PAGAMENTO`; o mock usa `origem` ∈ `APURACAO · AJUSTE_MANUAL · FECHAMENTO`. Não renomeei: `FECHAMENTO` e o débito por atraso não têm par no enum real, e mapear seria inventar semântica. **Pergunta para [W]/Code:** o transporte de saldo no fechamento e o débito por atraso entram como qual `tipo`? · Só no protótipo: KPIs de teto/piso e prazo do acordo no detalhe |
| 6 | `Escalas/Index.tsx` (8.547) | `Escalas` | **sim** (remover) | ✅ com vínculo, a linha **não tem** "Remover" — só o texto `Em uso por N colaborador(es)` + `data-testid="escala-<id>-remover-bloqueado"` (UC-ESCIDX-03). Antes: botão desabilitado + "N vinculados — desvincule antes" | protótipo confirma no **Modal** do DS; produção usa `window.confirm`. Fica o protótipo |
| 7 | `Escalas/Form.tsx` (9.022, busca dirigida) | `EscalaForm` | **sim** | nada — os 5 tipos (`FIXA · FLEXIVEL · ESCALA_12X36 · ESCALA_6X1 · ESCALA_5X2`) e os limites (diária 60–600, semanal 0–3.600 min) já batem 1:1 | — |
| 8 | `Colaboradores/{Index,Edit}.tsx` (9.454 + 7.189, lidos inteiros) | `Colaboradores` | **sim** | ✅ estado vazio em 2 casos (nada cadastrado × busca/filtro sem resultado, com "Limpar busca") · ajuda literal dos 2 switches do form ("bate ponto e participa da apuração CLT" · "HE e débitos acumulam… Exige escala com banco de horas ativo") | **protótipo à frente**: a máscara de CPF/PIS (3 últimos dígitos) e o aviso "PIS não cadastrado" **saíram daqui** para a produção (D-COLAB-CPF, comentário no `Index.tsx`). Só no protótipo: filtros Escala e Situação, colunas "Último ponto" e "Saldo BH", botão Espelho, card "Dados do HRM" — comportamento sem par, não vira pedido sem [W] |
| 9 | `Importacoes/{Index,Create,Show}.tsx` (7.096 + 5.438 + 5.763, lidos inteiros) | `Importacoes` | **sim** | nada — protótipo **à frente** (diagnóstico do processamento, amostra de erros, barra de progresso, 4 KPIs) | **enum de estado diverge:** produção `ESTADO_PENDENTE · ESTADO_PROCESSANDO · ESTADO_CONCLUIDO · ESTADO_FALHOU` + `linhas_ignoradas`; o mock usa `CONCLUIDA · CONCLUIDA_COM_ERROS · PROCESSANDO · FALHOU` + `linhas_erro`. "Concluída com erros" não existe na produção. Não mudei (é VALOR de estado) — **pergunta:** a produção vai ter o estado "com erros" ou o protótipo deve cair em CONCLUIDO + ignoradas? · produção faz auto-refresh de 3 s enquanto processa; o protótipo não simula |
| 10 | `Relatorios/Index.tsx` (9.238) | `Relatorios` | **sim** (501) | nada — já estava certo | o protótipo já mostra `NAO_IMPLEMENTADO` e "em implementação" para as 7 chaves que o `RelatorioController:102` responde `abort(501)`; só o Espelho gera. Cumpre o PARAR SE da thread |
| 11 | `Configuracoes/{Index,Reps}.tsx` (6.471 + 8.061, lidos inteiros) | `Configuracoes` | **sim** | nada — protótipo à frente (card "IA do Ponto", ICP-Brasil, janela de correção) | **nomes de chave do mock ≠ produção** (`tolerancia_minutos_por_marcacao` × `tolerancia_marcacao_minutos`, etc.) — só o mock, sem efeito na tela · REPs: produção tem coluna **Ativo**, protótipo tem **CNPJ** · aqui é o destino das 5 chaves de tolerância do HRM (`RESIDUO-6`, ADR/decisão [W] 2026-09-23) — entra quando a migração acontecer, não antes |

## Âncoras
Fora de Dashboard/Espelho, as Pages do Ponto **não têm `data-contract`** (busca: 0). O protótipo tem (`escalas-escalas-cadastradas`, `importacoes-historico-de-importacoes`, `bancohoras-historico-de-movimentos`…) — protótipo à frente; nada a puxar. O único `data-testid` dessas Pages (`escala-<id>-remover-bloqueado`) entrou.

## Perguntas que ficam (não bloqueiam a thread)
1. Banco de horas: saldo transportado no fechamento e débito por atraso entram como qual `tipo` do enum real?
2. Importações: existe (ou vai existir) o estado "concluída com erros"?
3. `Intercorrencias/Edit.tsx` (13 KB) não foi lido — o rascunho do protótipo edita no mesmo form de criação.
