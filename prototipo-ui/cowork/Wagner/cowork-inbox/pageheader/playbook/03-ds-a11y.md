---
sessao: "03"
titulo: DS: TabBar com setas + PageHeader com role=banner
dono: "[CL]"
base: ebe1fc8be7e4
---
# 03 · DS: TabBar com setas + PageHeader com role=banner

Decisão [W]: **corrigir no DS** (defeitos d/e de `cowork/Felipe/divergencia-pageheader-tabbar-pt01.md` §5).

- `TabBar.jsx` (6.890 B): hoje **sem** `tablist` e **sem** `ArrowRight` (busca de 23/09 em `components/`: só `Segmented.jsx` e `PresenterMode.jsx` têm). Espelhar a mecânica do `resources/js/Components/shared/PageHeaderTabs.tsx` (←/→/Home/End, `role="tablist"`/`tab`, `aria-selected`) — ou manter `<nav>`+`aria-current` e só adicionar setas; decidir pelo que o `PageHeaderTabs` faz.
- `PageHeader.jsx`: emitir `role="banner"` no `<header>` como o repo já faz (`PageHeader.tsx:140`).
- Regenerar `_ds_bundle.js` pelo caminho normal do DS (`ds-push.mjs`), não à mão.

## Prova
As provas desta thread estão no JSON do `00-INDICE.md` — o placar as confere. Recibo: `_saida-03.md`, escrito por quem executar.
