---
sessao: "06"
titulo: Título da Caixa Unificada 14px → 22px
dono: "[CL]"
base: ebe1fc8be7e4
---
# 06 · Título da Caixa Unificada 14px → 22px

Decisão [W]: **22px (protótipo)** — medição de 08/09 em `memory/requisitos/Whatsapp/CaixaUnificadaV4-visual-comparison.md` (D4 DIVERGE real).

Hoje: `<h1 className="font-semibold text-[14px] leading-tight truncate">Atendimento</h1>` em `CaixaUnificada/Index.tsx`. Preferir adotar o `PageHeader` canon; se não couber no layout da caixa (altura), usar `var(--fs-7)` — nunca outro valor arbitrário. **Depois da thread 04** (o peso vem de lá).

## Prova
As provas desta thread estão no JSON do `00-INDICE.md` — o placar as confere. Recibo: `_saida-06.md`, escrito por quem executar.
