---
sessao: "01"
titulo: ADR 0395 — merge + flip do required
dono: "[W]"
base: ebe1fc8be7e4
---
# 01 · ADR 0395 — merge + flip do required

Decisão [W] 2026-09-23: **aprovar**. Merge do PR da ADR e depois o flip, **na ordem da própria ADR**: merge → `POST` aditivo em `required_status_checks/contexts` com payload UTF-8 sem BOM (o context tem `·` e `ó`) → `node scripts/governance/protection-drift.mjs` → `gh pr update-branch` nos PRs abertos.

⚠️ O valor exato de `status:` para "aceita" nas ADRs deste repo não foi conferido — a prova usa `status: aceito` (visto em `adr/ui/0018`). Se o repo usar outro, corrigir a prova no mesmo PR.

## Prova
As provas desta thread estão no JSON do `00-INDICE.md` — o placar as confere. Recibo: `_saida-01.md`, escrito por quem executar.
