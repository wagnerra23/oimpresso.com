---
sessao: "04"
titulo: h1 do PageHeader: padrão 600
dono: "[CL]"
base: ebe1fc8be7e4
---
# 04 · h1 do PageHeader: padrão 600

Decisão [W]: **600 (DS)**.

Medido 23/09 @ebe1fc8be7e4: `PageHeader.tsx:151` — `titleWeight === 'semibold' ? 'font-semibold' : 'font-bold'` ⇒ o **padrão é bold (700)** e já existe a prop. Inverter o padrão (semibold por omissão; `titleWeight="bold"` vira opt-in) e trocar `tracking-tight` por `-0.015em`. Atualizar o docblock `:11`.

**Raio:** toda tela com o canon. Conferir quem passa `titleWeight` explicitamente (`git grep -n titleWeight resources/js`) — essas não mudam. Baseline visual vai mexer em massa: declarar no PR.

## Prova
As provas desta thread estão no JSON do `00-INDICE.md` — o placar as confere. Recibo: `_saida-04.md`, escrito por quem executar.
