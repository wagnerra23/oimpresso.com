---
sessao: "05"
titulo: Abas do PageHeaderTabs com 36px
dono: "[CL]"
base: ebe1fc8be7e4
---
# 05 · Abas do PageHeaderTabs com 36px

Decisão [W]: **36px (DS)** — também é o alvo de toque.

Medido 23/09: `PageHeaderTabs.tsx:135` — `default: { base: 'px-3 py-1.5 text-sm', … }` (~30px). Alvo: altura 36px, padding `0 14px`, 13px (valores do `TabBar` do DS). O protótipo já mede ~36px (`.cli-moduletopnav-tab`, `clientes-page.css:82`).

Atualizar no mesmo PR `tests/pageHeaderTabsDensity.spec.tsx` e `tests/pageHeaderTabsFidelity.spec.tsx`. Não conferi se `config/pageheader-shared-baseline.json` fixa altura.

## Prova
As provas desta thread estão no JSON do `00-INDICE.md` — o placar as confere. Recibo: `_saida-05.md`, escrito por quem executar.
