---
sessao: "01"
titulo: /roles em Inertia
dono: "[CL]"
base: ebe1fc8be7e4
---
# 01 · /roles em Inertia

Hoje @ebe1fc8be7e4: `routes/web.php:882` `Route::resource('roles', RoleController::class)` e nenhuma Page `Roles/` em `resources/js/Pages/` (só a proposta em `cowork-inbox/acessos/repo/resources/js/Pages/Roles/`). Fixar `ROLES_PAGE` no índice ao abrir. `/sales-commission-agents` fica pra thread própria depois.

## Prova
No JSON do `00-INDICE.md` — o placar confere. Recibo: `_saida-01.md`, de quem executar.
