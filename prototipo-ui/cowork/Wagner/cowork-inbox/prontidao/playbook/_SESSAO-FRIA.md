# Sessão fria — Prontidão

Para quem não viu a conversa:
1. Ler `00-INDICE.md` desta pasta (bloco json = fonte).
2. Ler `scripts/qa/prototipo-readiness.mjs` inteiro: é ele que decide o que é "pronta".
3. Escolher UMA thread, rodar `/onda prontidao --thread NN`, 1 PR, fechar com `_saida-NN.md` nesta pasta.
4. Números do índice valem para a árvore dd380c33a374 (lida 2026-09-23 11:20 UTC). Se o `main` andou, **medir de novo antes de agir** (`node scripts/qa/prototipo-readiness.mjs`). Tela que já saiu da lista é recibo, não trabalho.
