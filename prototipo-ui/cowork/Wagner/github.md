repo: wagnerra23/oimpresso.com
branch: main
path: prototipo-ui/cowork

## Last sync
date: 2026-09-16T18:51:00Z
tree: c1f77b029185 (andou desde 07b1a7658571 — ~5 h)
branch: main

### Updated in this project
- ✅ **O espelho foi atualizado às 18:09:02Z de hoje, SEM o meu lote — e com os meus bytes.** `state/active-bundle.json` (52.369 → **166.177 B**): `bundleId 6270479598680fc21409b6d6703b7dc62b0b41bf356108e2055ba2d572aba006` · `baseBundleId b19625fb6c0fe1c11f7f52fc7c0a1adae304e6ab16ce4476fb73a56a05a1b615` · `mode delta` · `mirrorScope **tree**` · `unchanged: 278`. Novos no `state/`: `application-report.json` (**324.506 B**, schema `oimpresso-design-application-report/3`, `screens: 162` · `pending: 134`) + `applications.json` (37.244 B). **O meu `c8a0709…` não aparece em nenhum arquivo do `state/`** ⇒ as 44 partes que eu gerei às 13:45 **não foram aplicadas e estão superseded**.
- **Paridade conferida por sha, não por data:** 6 de 6 sha256 sorteados do meu manifesto batem no `active-bundle.json` **e** no `application-report.json` — `oimpresso.com.html` 450f420d… (34.758 B) · `app.jsx` e108dd46… (75.264) · `sidebar.jsx` 9e8a13b8… (35.006) · `clientes-page.jsx` 8086f48f… (111.804) · `ponto-telas.jsx` 97089d9a… (66.478) · `styles.css` 95ceae89… (222.644). Inclui os 2 arquivos que eu consertei em 14/09 (a11y) ⇒ o conserto pousou.
- **O gerador MUDOU e a mudança é grande:** `gerar-payload-partes.mjs` 13.082 → **14.239 B**. Novidade = flag **`--full-tree`**: importa `classificarParaSync` de `../design/importar-bundle.mjs`, varre a árvore inteira (não só o fechamento do `entry`), pula `.git`/`sync`/o próprio `--out`, **recusa link simbólico**, e estampa `mirrorScope: 'tree'` no manifesto — "inclui playbooks/contratos da conta e autoriza poda de sobras". É o que explica o `.md` no espelho: `prototipo-ui/cowork/Wagner/` agora tem **294 arquivos na raiz + `cowork-inbox/` (44), `contrato/` (3), `handoff-crm/` (9), `handoff-ds-notas/`, `handoff-sidebar/`, `ds-v6/`, `venda-v3/`, `produto-preco-especial/`, `scripts/`, `CLAUDE.md` (16.106 B)** — e `Felipe/` recebeu `venda-v3/` (13) + `produto-preco-especial/` + `handoffs/`. **O meu pacote era `--full-tree`-cego:** 281 arquivos, **0 `.md`** e **0 caminho com subpasta** — só o fechamento do host. O caminho canônico deste projeto passou a ser `--full-tree`, não o snapshot do shell.
- **`aplicar-payload.mjs` 24.437 → 24.986 B:** ganhou **`--owner Wagner|Felipe`** (via `pathsForOwner` do `bundle-transaction.mjs`; `--owner Felipe` **exige** bundle v2 — "payload parcial legado não prova isolamento") e o rodapé agora reporta `telas: N · pendentes: N · bloqueadas: N`.
- ⚠️ **Lição de ferramenta (quase repeti o erro de 09/09 invertido):** o listador de árvore **não indexa `.mjs`** — `regex_filter=^scripts/.*\.mjs$` deu **0 de 17.005**, e o mesmo `gerar-payload-partes.mjs` **lê normal** por caminho direto. Ou seja: `github_get_tree` devolvendo vazio **não prova ausência** para `.mjs`; só leitura direta com controle positivo prova. Em 09/09 eu afirmei ausência por listagem e errei; hoje a listagem sugeria que os 4 scripts do protocolo tinham sido apagados — **falso**.
- **Correção de caminho (minha, outra vez):** **não existe `cowork-inbox/` na raiz do repo** (`regex ^cowork-inbox` → 0 de 17.005). Ele mora em **`prototipo-ui/cowork/Wagner/cowork-inbox/`** — dentro do dono, exatamente como o R3 do guard permite desde 13/09. O `CLAUDE.md` deste projeto cita `cowork-inbox/` solto em 4 pontos; é atalho, não caminho.
- **`sync/` continua não existindo no `main`** (`regex ^sync/|payload\.part` → 0 de 17.005) — o transporte segue sendo ZIP/colagem, e o pacote de 13:45 deste lado virou artefato morto. Pedido marcado como ⛔ SUPERSEDIDA no topo de `cowork-inbox/recepcao-pacote/playbook/02-pacote-2026-09-16-regerado.md` (passos 1–7 mortos; só o passo 8, decisões de [W], segue vivo).
- **Não medi:** `bundle-transaction.mjs`, `importar-bundle.mjs`/`classificarParaSync`.

- **Pedido emitido (thread 07, na pasta `ancora` — anti-scatter, não pasta nova):** `cowork-inbox/ancora/playbook/07-cruzar-report-trio-contrato.md` + `_PATCH-INDICE-2026-09-16.md`. Pede **execução e números, zero script novo**: os donos já existem no `main` (`screen-coverage-map` · `casos-coverage-guard --report` · `design-coverage` · `module-surface --migracao` · `blade-migration-census --json` · `reuse-index` · `gerar-contrato`/`gerar-map` · `scripts/design/ancora.mjs` com **os dois stagings**). Entregável = 1 tabela no `_saida-07.md` (`source·target·module·state·charter?·casos?·contrato?·âncora E0–E4·componente-faltante`) + 5 contagens: **(A) executável hoje · (B) falta UC/charter · (C) falta contrato · (D) falta componente+ADR · (E) sem receptor (decisão [W])** — é essa classificação, não o `pending: 134`, que diz o que dá pra fazer amanhã. Também pede VALOR/`totalDiverge` (`tokens:build` + `ds-push` + `ds-mirror-drift`) e os 4 campos do sumário que eu não li.
- ❌ **Defeito meu achado no playbook `ancora`: as threads 01–03 apontam para um arquivo que ninguém carrega.** O `00-INDICE.md` declara `ALVO: prototipo-ui/ancora.mjs` (49.089 B) e todas as provas nesse caminho, mas o leitor real é **`scripts/design/ancora.mjs`** — medido: `post-merge-ui-smoke-required.mjs:316` importa esse caminho (e `:318` degrada citando-o), `.test.mjs:175` faz `existsSync` nele, e `block-ancora-no-olho.mjs:84`, `charter-validate.mjs:117`, 3 workflows e a skill `refutador-gt-g5` todos mandam `node scripts/design/ancora.mjs`. **Eu registrei a mudança de casa em 14/09 e não corrigi o índice** ⇒ 3 threads prontas pra editar arquivo morto. Correção vai no patch (§1) e é pré-requisito das 01–03.
- **`scripts/qa/prototipo-readiness.mjs`: NÃO confirmei.** A busca saiu **bounded** (314 de 400 arquivos, budget 10 s, 83 acima de 512 KB não varridos) e **busca truncada não prova ausência** — mesma classe do erro de 09/09. Passo 2 do pedido é confirmar ou derrubar por leitura direta com controle positivo.
- **Stagings do `ancora.mjs` são DOIS** (`.claude/workflows/migracao-layout-em-ondas.js:99`): `prototipo-ui/cowork/Wagner` **e** `oimpresso-erp-conunica-o-visual/project`. Rodar com um só subestima cobertura.

### Leitura do report — o que o Code ainda NÃO executou (18:56Z, mesma árvore)
- **Sumário medido:** `transportChanges: 424` · `screens: 162` · **`tested: 0`** · **`smoked: 0`** · `pending: 134`. **Nenhuma das 162 telas foi testada nem fumada** ⇒ o delta de 18:09 foi **transporte**, não aplicação de tela. Linhas 16–19 do sumário **não li** (4 campos) — não afirmo `applied`/`blocked`. `totals` do bundle ativo: **702 arquivos · 10.363.749 B** (o meu snapshot tinha 281 / 7.416.482 — o `--full-tree` é 2,5× em arquivos).
- **Duas classes de dívida por `applicationState`:** `pending` **134** (tem `target` real em `resources/js/Pages/**` + `module`) · `to-create` **28** (162−134; maioria com `target: "—"` e `module: null` ⇒ **sem receptor no `main`**). Nomes `to-create` colhidos na busca: `boletos` · `catalogo-qr` · `cms` · `comissionados` · `comissoes` · `connector` · `crm-blade` (3×) · `crm-blade-forms` · `crm-portal` · `documentacao` · `equipe` · `funcoes` · `notificacoes` · `orc` · `os` · `planilhas` · `ponto-fechamento` (2×) · `ponto-mobile` · `prefs` · `producao` · `programa-doc` · `prototipos/payment-gateway-ui/cobranca` · `relatorios` · `usuarios` · `voz-do-cliente`. `ponto-fechamento`/`ponto-mobile` como `to-create` **confirmam por máquina o furo F4 de 14/09** ("sem receptor"), que até agora era só leitura minha de charter.
- ⚠️ **O report NÃO carrega o que o Code precisa pra ENTENDER a tela.** Busca por `charter|casos|contract|anchor|reason|blockers|missing|readiness|trio` no indent das entradas: **0 hit**. Cada entrada tem só `source` → `target` → `module` → `applicationState`: diz *onde pousa* e *se falta receptor*, **não** se existe charter, casos (UC), contrato de tela ou fingerprint de estilo. Esse cruzamento **não tem dono de máquina hoje**.
- **Trio medido:** `resources/js/Pages/**` tem **330** arquivos `*.charter.md`+`*.casos.md` (330 de 805). Assimetrias que reprovam o trio: `Estoque/Movimentacao` tem **só `.casos.md`** · `Auditoria` 2/0 · `Nfse` 3/0 · `ConsultaOs` 1/0 · `governance` 9/2 · `Essentials/{Documents,Holidays,Knowledge,Messages,Reminders,Settings,Todo}` ≈14/0 · `kb/_components` 3/0 · `Sells/{Drafts,Quotations,Subscriptions,Caixa}` 4/0 · `Site`, `StockAdjustment`, `StockTransfer`, `TransactionPayment`, `Whatsapp/{Settings,Templates}`, `Repair/JobSheet/Index`. **Charter sem `.casos.md` = sem UC ⇒ o `prototipo-readiness.mjs` não marca ✅** — é literalmente o "complemento" que falta.
- **Contratos:** 36 `*.contract.json` para **162 telas** ⇒ ≈78% das telas sem contrato de comportamento no CI (mesma ordem do 2-para-21 do Ponto).
- **Como o Code entende token e comportamento** (lido em `prototipo-ui/design-system/HANDOFF.md`, 9.308 B, neste turno): **token** = §6 VALOR:0 — DTCG `resources/css/tokens/*.tokens.json` → Style Dictionary → `_generated-*.css`; `ds-push.mjs` monta e **sai ≠0 se VALOR>0**; `ds-mirror-drift.mjs` vigia (baseline `totalDiverge: 0` nos 4 escopos). Regra dura #1: **proibido `#hex`/`oklch()` literal ou CSS cru do espelho em arquivo de módulo** — entrega é Tailwind 4 + token existente. **Comportamento** não vem do meu CSS: vem de `.casos.md` (UC) + `*.contract.json` (seções, copy literal, estados) + os gates do §7 (`ds-guard`, `conformance`, `pt:conformance`, `a11y`, `dominio`, `no-mock`, `visreg:pixel`). Meu `page.css` **não é** contrato de comportamento.
- ⚠️ **O §5 do HANDOFF assume que o mapa componente→arquivo é pista, não fato:** 40+ linhas herdadas, **só 3 remedidas** em 31/08 e **2 das 3 estavam erradas**. Seguem **"criar"** (sem par no `main`): `PageHeader.context`/`freshness`, `Progress`, `DatePicker`, `PeriodBar`, `Breadcrumb`, `Pagination`, `FsmStepper`, `Chart`, `TaskCard`/`BoardColumn`, `Logo`, e os 5 print-craft. Tela minha que usa esses **não é "pending de aplicar": é pendente de componente + ADR**.
- **Não medi:** o cruzamento tela→trio→contrato linha por linha (162 — só agregados e assimetrias por diretório), 4 campos do sumário, `applications.json` (37.244 B), `prototipo-readiness.mjs`, `ds-push.mjs`, `ds-mirror-drift.mjs` (o próprio HANDOFF §5.4 declara que ninguém leu).

### Ciclo anterior (13:45Z, mesmo dia)
- **bundle regenerado (2026-09-16 · 281 arquivos · 44 partes)** — `sync/bundle.manifest.json` + `payload.part01…44.json`. Snapshot `oimpresso-design-manifest/2`, `bundleId c8a070942fa6cfb79615cc482dcf63155b9d08a455c7d7cec521af32a268c768`, `baseBundleId: null`, 7.416.482 B, 286 chunks, `missing: []`, `manifestSha256 0e5b108d…`, `changesSha256 2cf051a3…`. Papéis: **271 `cowork-source` + 10 `_ds/**` `preview-cache`**. **Não rodei `node`**: replico o algoritmo do gerador lendo os bytes do disco deste projeto (`crypto.subtle` SHA-256 → base64 → arquivo, nenhum byte por prosa) — **não é** saída do `gerar-payload-partes.mjs`, e o veredito é o applier do [CL].
- **Diferença essencial do ciclo de 07/09:** lá a canonicalização de `bundleId`/`manifestSha256` era **minha, inferida**; aqui **li `scripts/design-sync/bundle-contract.mjs` inteiro** e usei a função real (`sha256(stableJson(identity))`, `files` ordenado por `localeCompare` com `{path,bytes,sha256,role}`).
- **Auto-auditoria com réplica do `validateBundleParts`: 0 erro.** Sequência 1..44 · `bundle` idêntico nas 44 · `targetManifest` só na part01 · `bundleId` recomputado ✓ · snapshot com os 281 em `added` e `unchanged: 0` ✓ · base64/`bytes`/`sha256` de **286/286** chunks ✓ · **281/281** arquivos remontados batendo o manifesto ✓ · **0 parte acima do cap 262.144** (maior 261.853) · **0 abaixo do piso 61.440** (menor 133.435).
- ❌ **O pacote de 07/09 não estava "defasado" — estava INAPLICÁVEL, e o defeito era meu.** Medido antes de apagar: **31 das 43 partes acima do cap** (maior **308.280 B**, e o `get_file` corta em 256 KiB ⇒ a guarda do `lerPayload` sai rc=2) · manifesto com `mode: snapshot` **e** `baseBundleId: 5023b274…` ⇒ `validateManifest` lança *"snapshot não pode declarar baseBundleId"* **antes** de qualquer escrita · `changes` declarando 26 `added`+100 `modified` sobre snapshot ⇒ *"snapshot precisa declarar todos os arquivos como added"*. Eu vinha reportando "bundle regenerado (281 arquivos)" como fato cumprido e "auditoria 0 defeito" — a auditoria era contra a **minha** régua, não contra `bundle-contract.mjs`. Os 44 arquivos antigos foram **apagados** deste lado (sem lote misto).
- **Por que snapshot e não delta:** `--previous sync/bundle.manifest.json` abortaria rc=2 (manifesto antigo inválido) · a base real de um delta é `scripts/design-sync/state/active-bundle.json` (52.369 B), **não lido neste turno** ⇒ base não provada ⇒ delta morreria em "base divergente" · `validateBundleParts` aceita snapshot **sem** estado-base. A partir daqui dá delta: base = `c8a0709…`.
- **Lido INTEIRO no `main` neste turno** (`07b1a7658571`): `scripts/design-sync/gerar-payload-partes.mjs` (13.082 B) · `bundle-contract.mjs` (11.888 B) · `payload-dependency-graph.mjs` (5.872 B) · `aplicar-payload.mjs` (24.437 B) · `design-sync/state/README.md` (2.913 B). **Não lido ⇒ não verificado:** `bundle-transaction.mjs`, corpo de `destinoDoBundle` (`cowork-mirror-freshness.mjs:615` — só a assinatura + 4 casos do `.test.mjs`), `importar-bundle.mjs`, `bundle.schema.json`, `status.mjs`, `state/active-bundle.json`.
- **Grafo do host fechado e medido daqui:** fechamento transitivo a partir de `oimpresso.com.html` devolve **281 alcançáveis · 0 ausente · 0 arquivo fora do contrato build-only · 0 conteúdo duplicado** entre os 271 `cowork-source` (o R4 do `cowork-ssot-guard` não deve disparar). O grafo usado é a mesma função do applier, replicada (`?v=` cortado, especificador nu = externo).
- **Pedido emitido:** `cowork-inbox/recepcao-pacote/playbook/02-pacote-2026-09-16-regerado.md` (thread 02 — 8 tarefas na ordem: limpar lote velho → receber 45 arquivos → `--dry --require-complete-shell` → aplicar → guardas → `_saida-02.md`; §2 explica o snapshot, §3 condena o lote de 07/09, §4 confere as regras dele contra o código, §6 o que não resolve) + `_PATCH-INDICE-2026-09-16.md` (patch, porque a pasta no `main` pode estar à frente da cópia local).
- ⚠️ **O transporte segue sendo o furo:** `sync/` **não existe no `main`** e eu **não escrevo no git**. 45 arquivos (≈10 MB de JSON) precisam descer por ZIP/colagem de [W] ou `cowork-inbox/`. E a catraca que evitaria o próximo congelamento (thread 01) segue travada em `D-RECEPCAO-FALHA` e `D-QUEM-REGENERA`, ambas de [W].

## Sync anterior
date: 2026-09-14T13:31:38Z
tree: 73182439581f (andou desde ffe69844cb71)
branch: main

### Updated in this project
- **O read-order do `CLAUDE.md` apontava 4 caminhos MORTOS.** Os 5 process-docs saíram de `prototipo-ui/` e moram em **`memory/reference/prototipo-ui/`** (`COWORK-ESTRUTURA-E-TELAS` 10.882 B · `PRE-FLIGHT-TELA` 4.707 · `FRESCOR-PRODUCAO-vs-PROTOTIPO` 7.219 · `PROTOCOL` 38.121 · `CLAUDE_DESIGN_BRIEFING` 10.960). A raiz de `prototipo-ui/` hoje tem **só** `cowork/` e `design-system/`. Efeito medido: o "leia o `main` no início do chat" **falhava em silêncio** (3 × Not found) e eu seguia de memória — o furo mais caro do processo, porque desarma a própria defesa contra memória velha. Corrigido nos 3 itens + no ponteiro do topo + no §Papéis.
- ❌ **Retratação 3 (a mais séria): o "R1 zero `.md` em `cowork/`" que eu policiava foi REVOGADO por [W] em 2026-09-13.** Lido inteiro: `scripts/governance/cowork-ssot-guard.mjs` (5.403 B) — **R1** = raiz de `prototipo-ui/` só `cowork/`+`design-system/` · **R2** = só donos `Wagner`/`Felipe` · **R3** = `.md` **permitido dentro de um dono** · **R4** = zero bytes duplicados. Motivo no cabeçalho do script: do pacote de 2026-09-11, **400 pousavam e 416 eram descartados, 337 deles `.md`** — o `cowork-inbox/` chegava pela metade. Eu **recusava ordem de serviço em `.md` citando regra derrubada**, e 3 dos 4 números estavam errados ("bundle datado `cowork-*`" e "`prototipos/` fora do allowlist" não existem neste script). Emenda à ADR 0397 D3 **não lida ⇒ número não afirmado**.
- ❌ **Retratação da retratação: `financeiro-unificado.intent.json` EXISTE** — `governance/design/contracts/financeiro-unificado.intent.json` (2.816 B @`42547eb92313`), **fora do glob `*.contract.json`**; o conflito #7 original estava certo e a errata de 13/09 estava errada. Diretório real tem **38 arquivos** (36 `*.contract.json` + `EXEMPLO` + `contract.schema.json`), não 35+1. O `prototipo-ui/contrato/` que eu jurava existir aparece só **dentro de pacotes espelhados** (`cowork-inbox/{acessos,modulos}/repo/prototipo-ui/contrato/`) — provável origem da falsa memória.
- **`ancora.mjs` mudou de casa:** não existe mais em `prototipo-ui/`; o `main` invoca **`scripts/design/ancora.mjs`** (step "ancora selftest" do `design-memory-gate.yml`). **Controle positivo antes de afirmar ausência** (minha regra de 10/09): `scripts/design-sync/gerar-payload-partes.mjs` (13.082 B) lê normal ⇒ a ferramenta vê `.mjs`, logo a ausência é real.
- **PONTO medido — export sem carga.** 7 de 7 arquivos do build com **tamanho idêntico** ao `main` (`ponto-data` 25.849 · `fechamento` 17.729 · `mobile` 17.717 · `page.css` 26.024 · `page.jsx` 33.942 · `telas` 62.342 · `ui` 17.003) ⇒ nada novo a descer. Comparei **tamanho**, não sha256 — veredito duro é do `cowork-mirror-freshness.mjs`.
- **Âncoras do Ponto lidas nos charters** (42 linhas, busca `^(related_prototype|bundle_source|visual_source|status|contract):`): **21 charters → 3 destinos** — `ponto-telas.jsx` **17×** · `ponto-page.jsx` **3×** · `n/a` 1× (`Welcome`, motivo declarado). **`bundle_source` e `visual_source`: ZERO** no módulo. **`status: draft` em 21 de 21.** 5 furos: F1 **5 dos 7 arquivos do build não são âncora de ninguém** (e o `--check-orfaos` é DELTA ⇒ órfão herdado é invisível por desenho) · F2 1 âncora para 17 telas (D2 de pé) · F3 **2 contratos para 21 telas** · F4 `fechamento`+`mobile` sem receptor · F5 **Ponto ausente do FRESCOR** (o passo 1 da ROTINA não tem resposta pro módulo).
- **Furo de CI novo, 3º caso da mesma família:** `memory/reference/prototipo-ui/**` **não está no `paths:`** do `design-memory-gate.yml` (o gatilho tem `prototipo-ui/**`) — mexer no read-order não dispara a lane de design. Mesma causa já registrada no YAML pro `ds-guard` e o `integrity-check` ("a migração pra `scripts/design/` tirou-o de lá e nenhuma linha o repôs", 0 de 2 cobertos, 13/09). Somado: os **dois leitores de âncora rodam `continue-on-error: true`** — podem estar vermelhos há semanas sem ninguém ver.
- **Pedido emitido (medição, não layout):** `COLAR-NO-CODE-PONTO-ANCORAS-LEITURA-CRUZADA.md` — pede ao [CL] rodar `scripts/design/ancora.mjs` + `anchor-content-check.mjs` no Ponto e devolver **os números dele** em formulário fechado (A1–A3 · B1–B5 confirma/derruba · C1–C4 baseline · D próximo passo). Declarado no próprio pacote: **ALVO não medido ⇒ sem os 10 blocos, não autoriza pixel**; e **não li** os dois scripts de âncora nesta sessão (só a invocação no YAML) — todo número meu vem de ler charter, que é exatamente o motivo do pedido.
- ⚠️ **Ciclo fechado SEM pacote regenerado, e agora o build MUDOU** (`sidebar.jsx` + `app.jsx`, conserto de a11y abaixo) ⇒ o pacote está defasado **por fato deste turno**, não por data herdada. E **retifico**: eu não posso dizer "defasado desde 2026-08-24" — `sync/` **não existe no `main`** e **não está no `.gitignore`** (medido: `^sync/` → 0 de 16.918, controle positivo `.json` OK). Não roda daqui (ADR 0374): `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`.
- ❌ **Retratação 4, e a mais caro: "eu não inspeciono o protótipo deste lado" é FALSO.** Sondei o protótipo servido neste ciclo (rota `ponto`, dark, **T1 estável 755=755** após `__oiLazyDone`): `svg` sem nome **3 → 1** · `div` clicável **0** · `[aria-live]` **1** · botão sem nome acessível **0**. Consertei os 2 que eram meus (`aria-hidden`/`focusable` nos inline `<svg>` de `sb-collapse-handle` e `sb-mobile-toggle`, que já tinham `aria-label`) — **`sidebar.jsx` e `app.jsx` mudaram**. O 1 restante é do **DS bundle** (`_ds_bundle.js:730`, ícone `warn`): pendência de DS, espelho não se edita. Consequência de processo: eu declarei **2 ciclos** de ALVO como "bloqueado, não inspeciono" sem ter tentado — o bloco 2 dos 10 eu **consigo** fechar; o que falta é emitir no **formato do `style-fingerprint`** (§19.1 #5), não a capacidade.
- **`ponto-page.css` medido contra o predicado do `ds-guard §8`:** **0 tokens de cor bespoke** (o "paleta inventada" de ≥4 **não dispara**) e **8 cores cruas**, todas dentro do `@media print` do `.pt-folha` (tinta de papel: `#222 #111 #eee #555 #777 #f5f5f5` + `#fff3cd`). Declarado como resíduo, não repintado por conta: `#fff3cd` é o único que parece paleta emprestada.
- **Duplicata de bytes MEDIDA e negativa:** `entrega-sidebar-code/build/` e `entrega-sidebar-01-02/` guardavam cópias **antigas** (`sidebar.jsx` −19.833 B · `app.jsx` −304 · `styles.css` −3.139) ⇒ R4 não disparava por bytes, mas o host não as declarava ⇒ **órfãos**. **APAGADAS neste turno (22 arquivos)** por ordem de [W] — e o `playbook/` delas era justamente o par duplicado que o cabeçalho do guard mediu em 13/09 (*"10 pares idênticos entre `cowork-inbox/sidebar/playbook/` e `entrega-sidebar-code/playbook/`"*); o `main` está à frente nessa pasta (tem `_saida-04/05/06` + `recibos/`), logo nada foi perdido.
- **AP9 FECHADO no host, e ele era dívida declarada em 8+ pacotes meus.** `app.jsx:940` era `<div className="main">` ⇒ **0 `<main>`** no documento, e eu vinha exportando isso como *"host/shell, sem dono"* em relatorios, repair, estoque 1.1 e 1.2, visão-geral, CRM e jana. Virou `<main className="main">`. **Medido depois, T1 estável 755=755 com `__oiLazyDone`: `main` = 1 (tag MAIN).** Defeito meu no caminho, registrado: troquei a abertura e não o fechamento — `Expected corresponding JSX closing tag for <main>` derrubou a página, consertado no mesmo turno e reconferido com console limpo.
- **A2 era FALSO-POSITIVO meu, salvo pelo caso de sanidade:** minha sonda acusou **41** elementos com `outline:none`; medindo **em repouso**. Focando um deles nasce `outline solid 2px` + `box-shadow oklch(0.32 0.06 295) 0 0 0 3px` com `:focus-visible` verdadeiro ⇒ anel do DS presente, zero defeito. Sem o controle positivo eu teria exportado 41 defeitos inexistentes.
- **2 pendências de DS declaradas (espelho não se edita):** ícone `warn` do bundle sai sem nome acessível (`_ds_bundle.js:730`) · o `Widget` nasce com **`h3`**, o que faz o salto **h1 → h3** na rota do Ponto (A8) — mesma família do `TabBar` sem `role="tab"` e da cor crua no `Avatar`.
- **Norma ganhou §17, §18 e §19** (`COLAR-NO-CODE-PROTOCOLO-COWORK-EXPORT.md`, 25 títulos `##` conferidos por grep na mesma edição): transporte/recepção (zip ≠ prova · pasta de pouso · bytes ≠ digest · apply não apaga) · **aplicar por PAPEL, nunca por classe** ([W]) · as 6 máquinas do Code a que eu tenho de me adaptar + o catálogo do que já deu errado com número.
- **§14 (MAPA DE DESTINOS) REESCRITO — ele mandava tudo pra 2 rotas MORTAS.** Apontava `prototipo-ui/` raiz e `prototipo-ui/design-docs/`: a raiz é proibida pelo **R1** e `design-docs/` **não existe mais** (dobrada em `cowork/Wagner/cowork-inbox/`). Agora todo destino é dentro do dono, com 4 invariantes (R1 raiz · R3 `.md` dentro do dono · R4 zero bytes duplicados · playbook em pasta, e pasta que já existe recebe **patch**). Também corrigido: **uma** pasta de contrato (`governance/design/contracts/`, 38 arquivos), não as duas que eu descrevia — `prototipo-ui/contrato/` e `design-docs/contrato-cowork/` **não existem na árvore**.
- **ÂNCORA CRIADA NAS 18 TELAS: `ponto-telas.jsx` tinha ZERO `data-contract`, agora tem 26** (um por Card, id derivado de `função + título`: `aprovacoes-fila-de-aprovacoes`, `bancohoras-saldos-por-colaborador`, `configuracoes-regras-clt-reforma-trabalhista`…). Sem isso as 18 telas não tinham âncora verificável nenhuma — o `map.json` ancora por `data-contract`, não por linha. Medido no render: `aprovacoes-fila-de-aprovacoes` presente no DOM, console limpo. 2 ids nasceram feios (`intercorrencias-card`, `escalaform-card`, Cards sem título) e ficam declarados.
- **Auditoria de contrato do Painel: zero drift** — os 4 ids do `dashboard-index.map.json` estão todos no DOM (T1 estável 755).
- **2 defeitos MEUS que o próprio gap achou, corrigidos no build** (o alvo não é sagrado): paginação de Aprovações era **15/pág** contra os **20/pág** que o charter declara como Goal ⇒ virou 20 · rejeição individual aceitava motivo de 1 caractere ⇒ agora exige **mín. 5** (regra do charter) e grava `.trim()`. O `window.prototype`→`Modal` do DS fica declarado, não feito (troca de primitivo, não de valor).
- **ALVO emitido na FORMA do vetor, com um eixo provado impossível daqui:** bbox/linhas/overflow/cor/radius/borda/fonte por seção, **tema dark e light medidos** (cor foi de `oklch(0.94 0.005 90)` para `oklch(0.22 0.01 80)`). **O eixo de LARGURA não gira deste lado** — forcei 1280/1440/375 e as três devolveram os **mesmos** números (647px nas 3 seções) ⇒ exige o dispatch local com browser (ADR 0290). Virou `D-ALVO-1280`; sem ela, nenhuma thread de layout é legítima. **Testei antes de declarar impossibilidade** — foi a lição do erro anterior.
- **Pacote emitido (3 arquivos de ponte):** `cowork-inbox/ponto/playbook/_PATCH-INDICE-2026-09-14.md` (patch do índice — 4 threads, 3 decisões, o **frescor do Ponto que o quadro canon não tem**, e a fila das 17 telas restantes com símbolo+faixa) · `…/ponto/playbook/16-gap-aprovacoes.md` (proposta do `aprovacoes-index-gap.md` com 6 regiões + 5 divergências + contrato de comportamento de 6 elementos; lado vivo **TODO** porque não li o `.tsx`) · `cowork-inbox/ds-atomos/playbook/06-widget-nivel-titulo.md` (pedido **no primitivo, por papel**: `Widget` nascendo `h3` causa o A8, e o ícone `warn` anônimo).
- **FILA DAS 18 TELAS FECHADA — 8 ondas, threads 16 · 20–26.** Cada uma com regiões, `data-contract`, divergências fechadas contra o charter e `PARAR SE`. **11 defeitos meus corrigidos no build:** 6 paginações erradas em 6 telas (15 por hábito × o número que cada charter declara: 20/25/30/20/25/20) · ordenação por saldo desc que não existia · 2 validações auditadas (`motivo` e `observação` ≥ 5 chars) · e **2 factuais escritos NA TELA** — a fonte do config (`Modules/Ponto/Config/config.php` → **`config/pontowr2.php`**, caminho que eu nunca li) e o **formato do identificador do REP** (`AAAAMMDDHHMMSSNNN` inventado → **CNPJ 14 + sequencial 3**, Anexo I; os dois têm 17 chars, então a validação de comprimento escondia o erro, e quem preenchesse pelo meu placeholder cadastraria REP inválido).
- **ÂNCORA: `ponto-telas.jsx` tinha ZERO `data-contract`, agora tem 26** (id derivado de `função + título`). Sem isso as 18 telas não tinham âncora verificável — o `map.json` ancora por `data-contract`, não por linha. **Painel: zero drift** (os 4 ids do `dashboard-index.map.json` presentes no DOM).
- **19 decisões respondidas por [W] neste ciclo** (ata: `cowork-inbox/ponto/playbook/ATA-DECISOES-2026-09-14.md`), com 3 regras acima delas: **R1** Non-Goal ratificado vira guard **e** sai do protótipo no mesmo pacote · **R2** onde a decisão for contra o protótipo, quem muda é o protótipo · **R3** onde a resposta era técnica, "faça" em vez de "escolha". **Aplicadas no build e medidas:** `D-INTERC-ACOES` (Editar/Submeter fora da linha) · `D-ESC-DESTROY` (remover indisponível com vínculo + `Modal` do DS em vez de `window.confirm`) · `D-COLAB-CPF` (CPF **e** PIS mascarados na listagem). **`D-CFG-POR-BUSINESS` ADIADA** — registrada como adiamento, não como "sim", pra eu não planejar contando com tela que não vai existir.
- **BLOCO 1 aberto: rota própria funcionando.** `app.jsx` aceita o prefixo `pt-` (padrão que `pat-`/`rep-`/`hrm-` já usam) e o `ponto-page.jsx` ganhou `daRota` — a rota passou a ser a fonte, não o estado interno. Medido: `pt-aprovacoes` abre a aba · **`pt-espelho-1` abre o detalhe por deep link** · `ponto` volta ao painel. **1 das 9 páginas fechada**; as 8 restantes na thread **28** com tabela de rota, regex única e 1 PR cada.
- ❌ **ADR 0383 violada no meu build por 3 semanas, achada ao listar as telas para [W] aprovar e corrigida.** `ponto-mobile.jsx` exigia **selfie obrigatória** (estado, botão, bloqueio do `Bater ponto`, `selfie_min_kb: 100` — que é o `SELFIE_MIN_BYTES` que a ADR manda deletar — coluna "Selfie (hash)"). Li a ADR inteira antes de agir: `aceito` · `canonical` · `ativo`, ratificada [W] 28/08, executada no PR #6393. **Por que ninguém pegou:** o guard do `Wave28MobileMarcacaoTest` lê o fonte **do Service/Controller PHP** e **não lê o protótipo**. Removido; medido depois: **0 menções de selfie**, `Bater ponto` habilitado, 8 cabeçalhos coerentes. A coluna virou **"Hash da marcação"** (hash encadeado do NSR, Art. 85, é legítimo; o proibido é hash **derivado de biometria**) e o header passa a citar a base correta — **LGPD Art. 5º II + Art. 11**, não o Art. 9º que circulava errado.
- ✅ **A proposal `ponto-contratos-retidos` FECHOU — 24 dias depois de aberta (21/08 → 14/09).** [W]: *"vai"* nos defaults e *"sim aceito as 5"*. **D0** rota `/ponto/conformidade` read-only (a única das 4 telas sem risco jurídico, e pré-requisito prático do fechamento) · **D1** `ponto.fechar` próprio e **"Reabrir" não existe na v1** · **D2** a palavra **"assinada" sai** e o ato continua com nome e data · **D3** recusar = **`ORIGEM_ANULACAO`** (precedente do módulo, não escolha de gosto) · **D4** **AFD/AEJ fora** do fechamento. **R2 cumprida no mesmo turno e medida:** `/Reabrir/` ausente · `/assinad/i` **zero** na tela · `ORIGEM_ANULACAO` presente · passo 4 = *"AFD / AEJ — em Relatórios"*. Pedido executável na thread **30** (5 PRs em risco crescente; os 2 contratos retidos entram **junto das telas**, nunca antes).
- **Pedido de guard que nasce desta falha:** estender a varredura dos termos da ADR 0383 a `prototipo-ui/cowork/**`, no `ds-guard`/`integrity-check` que já varrem o espelho (LC-19, sem script novo). **Regra geral proposta:** toda ADR com cláusula *"como se reconhece violação"* varre **os dois lados** — o protótipo é ponto cego hoje, e é o alvo que a produção copia.
- **3 erros meus de conta, corrigidos pela medição do [CL]:** o `paths:` **não eram 3 linhas, eram 8 arquivos nomeados** (a minha conta deixaria `PROTOCOL`, `REGISTRY_DS_COMPONENTES`, `ARQUITETURA`, `LICOES_CC` e `FRESCOR` fora do gatilho, e o glob de pasta que eu pediria foi recusado como "cobertura de mentira") · o manifesto era **"a mergear"** (#7117), não "a fazer" · e **A1/A2 já estavam respondidos**: 21 charters · 20 com âncora · 3 destinos · **0 âncora podre provado por MUTAÇÃO** — régua mais dura que a minha. **A pergunta do ciclo morreu com resposta: o Code lê a âncora igual.**
- ❌ **"R1" era rótulo, não endereço — e travou um item por um ciclo.** O repo tem 15+ regras chamadas R1. O meu é **`scripts/governance/cowork-ssot-guard.mjs`**, comentário `// R1 — a raiz do protótipo contém somente as duas áreas autorizadas`, check `!['cowork','design-system'].includes(e.name)`. **Regra nova minha:** citar **`arquivo#Rn` com a linha do check**, nunca "R1" solto.
- **`sync/`: os dois retratos estavam certos sobre coisas diferentes.** Medido às 18:27Z (árvore `32af4af112a4`): `^sync/|bundle.manifest|payload.part` → **0 de 16.920**, e `sync/` **não está no `.gitignore`**. O pacote de 278 arquivos existe **no disco do [CL]** (o zip habilitou o gerador, que só roda de onde os arquivos estão) e **não no git** — é o próprio #7117. **E já nasceu defasado:** 8 arquivos do build mudaram depois do zip.
- ⚠️ **Ciclo fechado SEM pacote regenerado, e agora com 8 arquivos do build mudados** (`sidebar` · `app` · `ponto-page` · `ponto-telas` · `ponto-fechamento` · `ponto-mobile` + os 2 de a11y). Não roda daqui (ADR 0374): `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`.

## Sync anterior
date: 2026-09-13T22:37:04Z
tree: ffe69844cb71
branch: main

### Updated in this project
- ❌ **RETRATAÇÃO DUPLA (a pior deste projeto até aqui): o caminho dos Contratos de Tela que eu citei por vários ciclos não existe.** Medido em `ffe69844cb71` com filtro `contract\.json$|contrato/` sobre 16.569 arquivos (39 hits): **`prototipo-ui/contrato/` NÃO EXISTE**; os contratos moram em **`governance/design/contracts/`** — 35 contratos + `EXEMPLO.contract.json`. E o **`financeiro-unificado.intent.json` que eu apresentei como conflito #7 não existe em lugar nenhum da árvore**. Não li arquivo errado: **citei de memória e depois usei a citação como prova** — espelho invertido do erro do `ds-push.mjs` (lá inventei ausência, aqui inventei presença). Corrigido em `CLAUDE.md` (2 lugares + regra nova "não afirmar caminho de memória"), na tela Documentação (alerta `danger` datado + linha `contrato` + conflito #7 reescrito) e na thread 06.
- **Conflito #7 reescrito com o número real:** 35 contratos para **188** charters = **19%** do corpus travado no CI. E a tela de maior charter do sistema — `Financeiro/Unificado/Index` (31.116 B) — **não tem contrato**; o nome próximo (`caixa-unificada.contract.json`, 2.022 B) é outra tela. Não promover por semelhança de nome.
- **Thread 06 do playbook `ancora` reescrita** (`06-contrato-fora-do-glob.md`): abre com a errata, pede o **número** (gate imprime `contratos_carregados` + `charters_encontrados`, o 2º tem de bater 188) + guarda de pasta com arquivo plantado + declara a lacuna do Financeiro. Explicitamente **não** pede gerar 153 contratos.
- **Playbook do Financeiro aberto** (`cowork-inbox/financeiro/playbook/00-INDICE.md`, onda = seção por decisão de [W]): 6 seções enumeradas por **leitura estática** do `financeiro-page.jsx` (KPI strip · period bar · toolbar de filtros · ageing · header · tabela+drawer). Âncoras medidas: charter 31 KB + `casos.md` com UC-F01…F05 e UC-FUNI-01…04; protótipo `financeiro-page.jsx` (related+bundle no mesmo arquivo, sem conflito).
- ⚠️ **O ciclo de export NÃO fechou, e o bloqueio é meu:** o **ALVO não foi medido** (blocos 2 e 3 dos 10) porque medir exige sondar o protótipo servido, e eu não inspeciono deste lado. Então **nenhuma thread de layout foi emitida** — o pacote está marcado `alvo_medido: NAO — BLOQUEANTE` e diz, nele mesmo, que não autoriza pixel. Seção 06 (tabela+drawer) também não levantada (grep truncou em 20 KB). Duas decisões em [W]: `D-FIN-ALVO` (roda a passada de medição?) e `D-FIN-CONTRATO` (contrato antes ou depois das ondas?).
- ⚠️ **Ciclo fechado SEM pacote regenerado.** `sync/` segue em 07/09.

## Sync anterior
date: 2026-09-13T22:28:30Z
tree: ffe69844cb71
branch: main

### Updated in this project
- **Varredura de cobertura FECHADA nas 14 áreas cegas** (governance 9 · RecurringBilling 6 · Purchase 4 · Nfse 3 · Whatsapp 3 · Suporte 2 · Site 2 · StockAdjustment 2 · StockTransfer 2 · Auditoria 2 · TransactionPayment 3 · User 1 · Vestuario 1 · ConsultaOs 1 = **41 charters**): 19 com âncora · 22 `n/a` declarado — **soma fecha**. Nenhuma das 15 buscas saiu *bounded* (as de 10/09 saíram; por isso a diferença).
- **Financeiro varrido inteiro** (módulo escolhido por [W]): 21 charters / 22 declarações → **7 com âncora · 14 `n/a`**, soma fechando. `Unificado/Index` = related+bundle no MESMO arquivo (`financeiro-page.jsx`, sem conflito) · `Cobranca`→`pg-cobranca-page.jsx` · **`Conciliacao`+`Dre`+`Fluxo`+`Impostos` → 1 só arquivo** (`financeiro-telas-extras.jsx`) · `ProvaViva`→ HTML legado (única do corpus) · `AssinaturaAtualizar` = `n/a` com errata: o `.tsx` existe no espelho mas é porte reverso (310 vs 309 linhas, difere só na API do PageHeader).
- **2 classes de conflito novas, achadas nesta varredura** (publicadas na tela, #8 e #9): **#8** `related_prototype: n/a` convivendo com `bundle_source` apontando arquivo no mesmo charter — `Vestuario/Etiquetas`, `StockAdjustment`×2, `StockTransfer`×2 (5 casos); **#9** âncora que se anula no próprio campo — 3 charters de `RecurringBilling` cujo `visual_source` diz "RASTRO, NÃO DESIGN APROVADO" (aba é placeholder de 3 elementos + porte reverso): máquina lê ✓, humano lê ✗.
- **2º caso do padrão BOM de âncora:** `Purchase/{Index,Show}` → `compras-page.jsx (ComprasPage)` / `(DrawerView)` — uma tela cada, por símbolo, no mesmo arquivo (o outro é `Sells/Caixa`). É a forma que resolve o muitos-para-um.
- **Estado da classificação:** 62 de 188 charters fechados e somados (14 áreas + Financeiro); **126 seguem por amostra** — vão fechando módulo a módulo pela fila.
- ⚠️ **Ciclo fechado SEM pacote regenerado** (`documentacao-page.jsx`). `sync/` segue em 07/09.

## Sync anterior
date: 2026-09-13T22:22:10Z
tree: ffe69844cb71 (andou desde 6fc8b8fac31d)
branch: main

### Updated in this project
- **DENOMINADOR FECHADO pela máquina: 188 charters** em `resources/js/Pages/**`, 34 áreas (filtro `\.charter\.md$` na árvore `ffe69844cb71` → "188/808"). Contagem da ferramenta, não soma minha. Corrige meu número anterior (189) e substitui todo "~" na tela Documentação → Âncoras de tela.
- **3 prefixos que eu declarei "não varridos" NÃO EXISTEM** em `Pages/`: `Tarefas`, `Copiloto`, `superadmin`. Eu inventei ausência — mesma família do erro do `ds-push.mjs`. Registrado na própria tela. Áreas realmente não varridas (existem e nunca foram lidas): Nfse 3 · Purchase 4 · RecurringBilling 6 · Site 2 · Stock* 4 · Suporte 2 · User 1 · Vestuario 1 · Whatsapp 3 · governance 9 ≈ **35 charters** ⇒ cobertura de âncoras ~80%.
- **Partição em 14 módulos publicada na tela** (fila de export, 1 pacote por módulo), somando exatamente 188 sem sobra: RH&Ponto 37 · Financeiro 27 · Oficina 24 · Fiscal 16 · Governança&BI 13 · Jana&KB 13 · Vendas&PDV 12 · Estoque&Compras 9 · Produtos 8 · CRM 7 · Produção&ComVisual 7 · Plataforma&Conta 7 · Patrimônio 5 · Atendimento 3. **Módulo ≠ pasta** (HRM em `Essentials`, Oficina partida em `Repair`+`OficinaAuto`).
- ⚠️ **NÃO emiti os 14 pacotes.** Cada um exige 4 denominadores + charter + ALVO medido; emitir tudo agora seria 14 pedidos sem medição. Fila desce um por ciclo — aguardando [W] escolher o primeiro.
- ⚠️ **Ciclo fechado SEM pacote regenerado** (`documentacao-page.jsx`). `sync/` segue em 07/09.

## Sync anterior
date: 2026-09-11T14:32:08Z
tree: 6fc8b8fac31d (leitura do ciclo anterior; NADA lido do git neste turno)
branch: main

### Updated in this project
- **Pedido pro Code emitido:** `cowork-inbox/shell-usermenu/playbook/` — `00-INDICE.md` (fonte JSON embutida no 1º bloco) + 3 threads: **01** Aparência abre/escolhe tema · **02** Sair confirma e encerra de verdade · **03** "+ Adicionar empresa" navega ou desabilita (`role="menuitem"` sem ação nos DOIS dropdowns). 2 decisões travadas em [W]: `D-CMD-BARRA` (⌘/ morto no `Sidebar.tsx:1273`) e `D-PRESENCA` (sem receptor de status no `main` lido).
- ⚠️ **Declarado no próprio pacote: NÃO é export de layout.** Sem ALVO medido neste ciclo → **sem os 10 blocos**, não autoriza pixel. Pedido de comportamento, e o bloco 7 lista o que não resolve (atmosfera por vibe não medida · ícone do trigger inferido · nada de layout).
- **Ficou deste lado, não virou PR:** colisão de prefixo `.pf-` (perfil × `acessos-page.css`, que define `.pf-body{display:grid;230px 1fr}` — todo card do Perfil em 190px nas 4 abas) resolvida renomeando pra `.upf-`; alinhamento do rail (cert no eixo de 56px, barra horizontal, dica em camada fixa); menu do usuário passando a dirigir o tweak do shell via `window.__setTweak` em vez de estado paralelo. Defeito meu = conserto meu.
- **2º pedido emitido:** `cowork-inbox/recepcao-pacote/playbook/` — recepção do ZIP confere projeto × `sync/bundle.manifest.json` por sha256 em 4 baldes que **têm** que somar, e falha ao divergir; 2 decisões em [W] (`D-RECEPCAO-FALHA` severidade · `D-QUEM-REGENERA` autoria do pacote). Declarado no pacote: **nada foi lido do `main` neste turno** — todo caminho/nome de guarda precisa ser reconferido pelo [CL]; se já existir script com esse papel, estender aquele (anti-scatter).
- ⚠️ **Ciclo fechado SEM pacote regenerado.** Mudaram `sidebar.jsx`, `app.jsx`, `styles.css`, `icons.jsx`, `perfil-page.{jsx,css}`, `documentacao-page.{jsx,css}`, `COLAR-NO-CODE-PROTOCOLO-COWORK-EXPORT.md`. O `sync/` segue em 07/09. Comando (não roda daqui — exige os arquivos em disco, ADR 0374 ativa): `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`.

## Sync anterior
date: 2026-09-10T21:02:26Z
tree: 6fc8b8fac31d (andou 3× no turno: ed4398d77437 → c436fc5540b7 → 6fc8b8fac31d)
branch: main

### Updated in this project
- **PUXADO do vivo: a 4ª cascata "Modo de trabalho" (Vibes)** no user dropdown do `sidebar.jsx` (+ ícone `palette` no `icons.jsx`, 6 regras no `styles.css`). Fonte medida em `6fc8b8fac31d`: `Components/cockpit/Sidebar.tsx` — trigger `:1257-1275`, `VibesSubpanel` `:1348-1420`, `vibeAccent` `:1415-1419` — e `shared.ts` `:153-158` (os 3 ids) + `:177` (`TW_VIBE: 'oimpresso.cockpit.tweaks.vibe'`). Copy e cores **literais**: workspace "Denso e formal — padrão" `oklch(0.55 0.15 295)` · daylight "Tons quentes, mais ar" `oklch(0.72 0.13 60)` · focus "Alto contraste, monocromático" `oklch(0.45 0.02 240)`. Persiste na mesma chave do vivo.
- **O ⌘/ NÃO era conflito com o ⌘K — é atalho morto no vivo.** Medido: `Sidebar.tsx:1273` **renderiza** o kbd `⌘/`, mas o `AppShellV2.tsx` liga só `⌘K` (`:389`, paleta) e `⌘\` (`:395`, sidebar); `useSidebarShortcut.ts:135` **descarta** evento com `metaKey`. O `/` **sem modificador** é "focar busca da página" em 5+ telas (`Cliente/Index:742`, `Essentials/{Todo,Licencas,Knowledge}`, `Financeiro/{Cobranca,Unificado}`) e o próprio `Cliente/Index:2581` o documenta assim. **Não portei o kbd** — exportar atalho que não liga é exportar dívida com selo. Vira **resíduo pra [W]**: ou o vivo liga o ⌘/, ou remove o rótulo.
- **Declarado, não portado:** a atmosfera por vibe (o que o `cockpit.css` reescreve) — aqui só marco `data-vibe` no `<html>` e persisto; pintar por conta seria invenção. Também **não medi** o ícone do trigger no vivo (linha 1263 não voltou na leitura): usei `palette`, inferido da lista de imports Lucide do arquivo — inferência, não medição.
- **Busca bounded 2×** neste turno (363/400 de 17.461 e 358/400 de 926): as ausências acima são **piso, não teto**.
- **Nova página na tela Documentação: `tec-ancoras` — "Âncoras de tela"** (grp Técnico, `auth: derivado`, `git: prototipo-ui/ancora.mjs`): os 3 campos (`related_prototype`/`bundle_source`/`visual_source`), tabela de âncoras 1:1, **7 conflitos medidos** e a cobertura declarada. Numeração/contadores da tela são derivados — nada escrito à mão.
- **Âncoras lidas do `main` neste turno** (~150 declarações, busca `^(related_prototype|bundle_source|visual_source):` em `resources/js/Pages/`): varredura ampla saiu **bounded** (343 de 400 de 808), completada por prefixo em Ponto (21) · OficinaAuto (9) · Patrimônio (5) · Sells (11) · Produto (10) · Repair (17). **Não varridos ⇒ não verificados:** Nfse, Purchase, RecurringBilling, Site, Stock*, Suporte, Tarefas, User, Vestuario, Whatsapp, governance, Copiloto, superadmin.
- **Conflitos que viram fila de decisão:** muitos-para-um (`ponto-telas.jsx` 15 charters · `fiscal-page.jsx` 7 · `repair-page.jsx` 6 · `patrimonio-page.jsx` 5) · `related` × `bundle` discordando em `Fiscal/{Config,Dfe,Eventos,Sped}` · `Produto/Index` pendurado na tela errada de propósito (`ancora.mjs:266-278`) · 6 portes reversos · `visual_source` órfão em 3 telas · `financeiro-unificado.intent.json` fora do glob `*.contract.json` (32 contratos para 189 charters).
- **Documentado "onde mora o DS"** em dois lugares deste projeto: `CLAUDE.md` → nova subseção `### 📍 Onde mora o DS no main` (8 títulos `##` conferidos por grep na mesma edição) e a **tela Documentação** do app (`documentacao-page.jsx`, doc `tec-front` — blocos novos "Onde o DS mora no git" e "Âncoras de tela", `upd` → 2026-09-10).
- **Medido no `main` neste turno** (árvore `ed4398d77437`): DS compilado `prototipo-ui/design-system/_ds_bundle.js` (348.172 B @`3aa50565ca52`) + `colors_and_type.css` (19.917 B @`79c31435983c`) + `cockpit_domains.css` + `styles.css` + `support.js` + `_ds_manifest.json`; cópia byte-idêntica (mesmos blobs) em `scripts/design-sync/mirror-snapshot/`. DS fonte: `resources/css/tokens/{base,semantic}.tokens.json` → `_generated-{cockpit,foundations,inertia}-*.css`; `resources/css/cockpit.css` + `foundations.css`; `Layouts/AppShellV2.tsx` (+ charter/casos). Âncoras de tela do `design-system/HANDOFF.md` §3–§4 (lido inteiro).
- ❌ **RETRATADO (mesmo dia, por [CL]):** eu declarei que `scripts/design-sync/ds-push.mjs` não existia. **Existe em `ed4398d77437`, com teste.** A afirmação nasceu de uma listagem de diretório que não o devolveu — *listagem incompleta não é prova de ausência*, mesma classe do erro de busca de 09/09. Regra: ausência exige leitura direta do caminho + controle positivo. Corrigido em `CLAUDE.md` e na tela Documentação.
- **Segunda retratação, mesma família — ADR 0374 está ATIVA.** A errata do [CL] (`prototipo-ui/CODE_NOTES.errata-0374-nao-revogada-2026-09-08.md`, lida inteira neste turno) mede em `11eff17f13`: `status: aceito` · `lifecycle: ativo` · `superseded_by: []`; nenhuma 0375→0392 a supersede; a emenda real é a **0389** (`decided_at: 2026-09-03`), que toca **uma linha** (escrita inline quando não há rota de máquina, sob 4 condições). Meu `COLAR-NO-CODE-PROTOCOLO-COWORK-EXPORT.md` declarava "revogada em 2026-09-07" em **4 linhas** — **corrigidas neste turno** (§6, §8 proibições, §12 do comando), como a errata pediu (a fonte é deste lado; o [CL] não edita o corpo do pacote). O fato de 07/09 (281 arquivos · 43 partes) fica preservado: gerar o pacote e "a ADR foi revogada" são afirmações diferentes, e só a primeira tinha recibo.
- **E eu misturei dois fluxos:** `ds-push.mjs` é **git → Design** (monta/valida; envio depende de login interativo); a exportação **Design → Code** é o protocolo do `cowork/` + `gerar-payload-partes.mjs`. Donos diferentes.
- **Não lido ⇒ não verificado:** `README.md`/`NOTAS_INTERNAS.md` do espelho, `_ds_manifest.json`, `scripts/governance/ds-mirror-drift.mjs`, os contratos em `prototipo-ui/contrato/`.
- ⚠️ **Ciclo fechado SEM pacote regenerado** — `documentacao-page.jsx` mudou. `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`. Não roda daqui.

## Sync anterior
date: 2026-09-10T17:44:51Z
tree: ed4398d77437 (o `main` andou: era af09f7c3a0fd às 10h38)
branch: main

### Updated in this project
- **A3 fechado no build:** `icons.jsx` ganhou `...rest` + `aria-hidden="true"`/`focusable="false"` por padrão. O [CL] confirmou a causa que eu tinha registrado como não-feito: o `Icon` desestruturava só `d/size/stroke/className/style` e **descartava** o resto, então `aria-hidden` por chamada sumia. Vale pro app inteiro.
- **Defeito estrutural do meu próprio playbook, apontado pelo [CL] e confirmado no `main`:** li `prototipo-ui/design-docs/cowork-inbox/_scripts/README-placar.md` — `contem`/`nao_contem` **não fecham thread** ("estrutura não prova execução", regra de 08/09). As 6 threads nasceram só com provas estruturais, então `entregue 0 de 6` era do desenho, não do andamento. §7 reescrito com os contratos reais: `execucao` (recibo JSON + summary da máquina que rodou), `revisao` (só pra tarefa que escreve apenas `.md`/`.contract.json`), `comparacao` (design-diff com `--contrato --check --check-shell --json`).
- **Ordem das vagas invertida:** 01·02·03 escrevem `.jsx`/`.css`, logo fecham por `comparacao`, que exige o `cockpit-sidebar.contract.json` — produzido pela **06**. A 06 deixou de ser o último passo e virou **pré-requisito** das outras.
- **Resíduos reclassificados (UI-0029):** 1 e 3 nunca foram decisão de [W] — abri as duas como "bloqueada em [W]" contra o corolário 1, que proíbe devolver essa pergunta; ambas `respondida: true`, threads 04 e 05 destravadas. 4 respondido e executado. **Novo RESÍDUO-5:** o rail do alerta de certificado é invenção minha (o vivo não tem variante de 56px) — se a 04 promover, [W] decide.
- **Leitura do `main` neste turno:** `_scripts/README-placar.md` (inteiro) + buscas em `scripts/qa/`. **Não lido ⇒ não verificado:** a UI-0029 em si (aceitei o relato do [CL]), o PR #7186 da emenda da 0180, o `placar-indice.mjs`.
- ⚠️ **`sync/` segue defasado** (`icons.jsx` + host mudaram): `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`. Não roda daqui (ADR 0374).

## Sync anterior
date: 2026-09-10T16:55:31Z
tree: af09f7c3a0fd
branch: main

### Updated in this project
- **Thread 03 do playbook Sidebar EXECUTADA** (RESÍDUO-4 respondido por [W]: importar): slot `NfeCertBadge` no protótipo, entre `.sb-top` e `.sb-body` — a posição que o vivo documenta em `NfeCertBadge.tsx:25`. Estado de `MOCK.NFE_CERT` (`data.jsx`), espelhando `shell.nfe_cert_status`. Recibo: `_saida-03.md`.
- **Portado, não inventado:** 4 estados (`sem_cert`/`ok`/`vencendo`/`vencido`), silêncio nos não-críticos, cores exatas (exceção R-DS-002), copy literal, singularização dia/dias, destino `/fiscal/config`. Medido nos 4 estados, expandido e rail.
- **Uma invenção declarada:** o vivo **não tem variante rail** do badge (só existe expandido; em 56px o texto não cabe). Criei `NfeCertBadgeRail` — ícone 40×40 com o texto no `aria-label`. Virou pergunta nova no resíduo 4 do índice, caso a thread 04 promova isso pro vivo.
- **Leitura do `main` neste turno:** `Components/cockpit/NfeCertBadge.tsx` (inteiro).
- **Pendente da própria 03:** o diff bidirecional do `CompanyPicker` (protótipo × vivo) que o playbook pedia — fui direto ao slot. Divergência estrutural fechada; a de detalhe segue não medida.
- ⚠️ **Ciclo fechado SEM pacote regenerado** — o build mudou de novo (`sidebar.jsx` · `data.jsx` · `styles.css` · host), então o `sync/` segue defasado. `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`. Não roda daqui (ADR 0374).

## Sync anterior
date: 2026-09-10T12:43:33Z
commit: (não sei — a leitura foi por árvore/busca; `github_get_tree` resolve hash de árvore, não de commit)
tree: af09f7c3a0fd
branch: main

### Updated in this project
- **Threads 01 e 02 do playbook Sidebar EXECUTADAS no build** (`sidebar.jsx` · `styles.css` · `app.jsx` · bump de `?v=` no host). Recibos: `cowork-inbox/sidebar/playbook/_saida-01.md` e `_saida-02.md`.
- **a11y (01):** 42 linhas de menu deixaram de ser `div role="link"` e viraram `<button>`; idem menu do usuário e os dois dropdowns de empresa. `.sb-kbd`/setas/avatares silenciados, `WipMark` com `role="img"`, "⋯ mais N" com nome literal. **Layout idêntico, medido** (h34px · pl 10/18px · 13px IBM Plex Sans · w233px · aside 260px). Código morto: `SidebarTabs`/`SidebarChat`/`ConvRow` + 150 linhas de CSS órfão + `ChatPage`/`ConvTabsBar` + as 2 chaves de storage que ninguém lia.
- **Paridade com o vivo (02):** auto-rail em **1280 inclusive** (`AUTO_RAIL_MQ` de `shared.ts`, era `< 1280`) e **só a escolha manual persiste** (`escolherModo`); sem chave, o modo segue a largura ao vivo.
- **Regressão introduzida e corrigida no mesmo turno:** `escolherModo` resolvia o updater contra um `sbMode` congelado pelo closure do `useEffect([])` → ⌘\ virou mão única. Conserto por `sbModeRef`; ciclo completo medido nos dois sentidos (expanded↔rail↔hidden). Pega pelo verificador, não por mim — minha prova era um clique numa direção.
- **Leitura do `main` neste turno:** `Components/cockpit/shared.ts` (inteiro — `AUTO_RAIL_MAX_W = 1280`, `SidebarMode = 'expanded' | 'rail'`, `LS.SB_MODE`). **Não lido ⇒ não verificado:** `Sidebar.tsx` linha a linha, `cockpit.css`, ADR 0180 nesta sha.
- ⚠️ **Ciclo fechado SEM pacote regenerado** — desta vez o build **mudou** (3 arquivos + host), então o `sync/` está **defasado**. Comando: `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`. **Não roda daqui** (ADR 0374) — não afirmo que regenerei.

## Sync anterior
date: 2026-09-10T10:38:13Z
commit: (não sei — `github_get_tree` resolveu a árvore `af09f7c3a0fd`, que é hash de árvore, não de commit)
branch: main

### Updated in this project
- **Playbook do Sidebar emitido** (`cowork-inbox/sidebar/playbook/`, 7 arquivos): 3 threads executáveis na vaga 1 (01 corpo/a11y · 02 auto-rail · 04 modo `hidden` no vivo), 2 na vaga 2/3 e 1 bloqueada em [W] (ghosts × ADR 0180). **Absorve e supera** `handoff-sidebar/PEDIDO-CODE.md` (28/08) — anti-scatter, sem doc novo.
- **O pedido de 28/08 relido contra o código, não contra si mesmo: 3 dos 4 deltas caíram.** (A) aba Chat não está órfã no vivo — foi **removida de propósito** (UI-0011 single-pane, `Sidebar.tsx:5-7,519`; conv switcher em `Pages/Copiloto/Chat.tsx`), logo quem está atrasado é o build daqui; (B) atalho `G X` **feito** (`useSidebarShortcut` importado no `AppShellV2`, `.sb-kbd` em `Sidebar.tsx:537`); (C) o conflito de ghosts **mudou de lado** — o vivo implementou `GHOST_TETO = 5` (`Sidebar.tsx:542-544,569`) contra a letra da ADR 0180/AP19; (D) modo `hidden` + `SidebarReopenHandle` **segue de pé** — `SidebarMode` é só `rail|expanded` e o único uso vivo está em `Pages/Financeiro/_cowork-bundle/shell-app.jsx:509`.
- **Produção à frente em 4 pontos, virados em thread de PUXAR:** auto-rail por largura (UI-0030, `matchMedia(AUTO_RAIL_MQ)`) · persistir **só** a escolha manual (`chooseSidebarMode` — o build daqui grava todo valor, defeito medido no espelho em 2026-09-02) · `<nav className="sb-body" aria-label="Navegação principal">` (o protótipo usa `div`) · slot do `NfeCertBadge` após o `CompanyPicker`.
- **Leitura do `main` neste turno** (árvore `af09f7c3a0fd`): `Layouts/AppShellV2.tsx` (inteiro) · buscas em `Components/cockpit/` e `resources/js/` · `app/Sidebar/` (4 arquivos, só a árvore). **Não lido ⇒ não verificado:** `Components/cockpit/Sidebar.tsx` linha a linha (li por busca), `shared.ts`, `useSidebarShortcut.ts`, `cockpit.css`, **ADR 0180 nesta sha** (AP19 citada pela leitura de 28/08). Duas buscas voltaram **bounded** — a ausência de `SidebarReopenHandle` no shell vem de ler o `AppShellV2`, não do zero-match.
- ⚠️ **Ciclo fechado SEM pacote regenerado** — nenhum arquivo do build mudou neste turno (só `.md` de ponte), mas registro a regra: `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`. Não roda daqui.

## Sync anterior
date: 2026-09-09T11:46:22Z
tree: 752041ac450d (a última busca do turno já respondeu de 7742b9621c32 — a árvore andou)

### Updated in this project
- **Auditoria dos vínculos de tela do `ancora.mjs`** (`prototipo-ui/ancora.mjs`, 49.089 B, lido inteiro). Denominador: **189** charters em `resources/js/Pages/**`; **~146** declarações de `related_prototype`/`bundle_source`/`visual_source` lidas. **Todo** caminho citado que li **existe** no git — inclusive os formatos sujos (`hrm-extras.jsx (Metas) …`, `Financeiro - Prova Viva (primitivos).html`, que só passa porque o valor cru é testado antes do regex).
- **3 defeitos medidos.** **D1** — a perna do `-page.jsx` está guardada em `if (stagingDir)` e não cai no `LUGAR_FIXO`: **14** telas cujo único vínculo é `bundle_source`/`visual_source` saem "sem protótipo" sem `--staging`, com o arquivo no git (`repair-page.jsx` 46.532 B · `governance-page.jsx` 20.212 B · `oficina-page.jsx` · `oficina-os-page.jsx` · `produtos-page.jsx`) — `--list` foi consertado em 2026-08-28, o comando de 1 tela não, e as duas portas discordam. **D2** — `norm()` derruba `/index` e o loop aceita `comp.includes(q)`/`relc.includes(q)`: `Ponto/Index` → `q="ponto"` → casa 21 charters e o **último na ordem de `walk`** vence, sem aviso, exit 0, selo `✓`. **D3** — `--list` carimba `hasSource` sem chamar `caminhoDaAncora`/`existsSync`, e o 3º fallback (`mockupJsx(fm.component)`) é tautológico (âncora = a própria tela).
- **Limite estrutural declarado, não resolvível por PR:** 20 charters do Ponto → 2 arquivos · `Financeiro/{Conciliacao,Dre,Fluxo,Impostos}` → 1 · 5 do Patrimônio → 1 · 7 do Fiscal → 2. A skill já diz "responde QUAL ARQUIVO, nunca QUAL VIEW" ⇒ contrato de tela **não** é decidível pela âncora hoje. Único charter do corpus que resolve: `Sells/Caixa/Index` → `vendas-extras.jsx · função VendasCaixaPage (linhas 123-354)` — mesma ancoragem por símbolo das threads 13–15 do Ponto. Virou decisão `D-SIMBOLO` ([W]), não pedido.
- **Playbook emitido: `cowork-inbox/ancora/playbook/`** (4 arquivos, JSON embutido no 1º bloco do índice): **01** perna do bundle no `LUGAR_FIXO` · **02** query ambígua devolve candidatos e sai 2 · **03** `--list` ganha `caminho`/`existe` e mede o fallback `component`. **Não são paralelas** — as 3 escrevem no mesmo arquivo, então 01 → 02 → 03 com remedição de sha por thread, e a API pública (que o hook `post-merge-ui-smoke-required.mjs:316-322` importa) é guarda.
- **Retratação preventiva registrada no pedido:** `Jana/Chat → jana-merge.jsx` **não** é mis-anchor — `jana-merge.jsx` é dono de `window.JanaPage` (`app.jsx:810`) e `chat-jana.jsx:722` se declara peça reusada por ele. Minha suspeita inicial era hipótese, não medição.
- **Cobertura declarada (não medido ⇒ não verificado):** `kb`, `Modules`, `NfeBrasil`, `Nfse`, `Purchase`, `RecurringBilling`, `Site`, `Stock*`, `Suporte`, `Tarefas`, `TransactionPayment`, `User`, `Vestuario`, `Whatsapp`, `Essentials/{Holidays,Reminders,Knowledge/Index}`, `Manufacturing/{Report,Settings}`; a varredura A→Manufacturing saiu parcial (328 de 400 arquivos). **Não rodei `node`** — nenhum veredito de `--selftest`/`--list` é afirmado aqui.
- **Paridade do Patrimônio medida no denominador** (árvore `f8e6e02876fc`): **5** `Inertia::render` (`Patrimonio/{Index,Bens,Alocacoes,Manutencoes,Configuracoes}`), 5 trios completos, `_shared/PatrimonioSubNav.tsx` 4.373 B (deriva **6** ghosts de `shell.menu`, não as 7 abas do protótipo) — e **17** Blades vivos em `Resources/views`. **6 lacunas:** (1) escrita ainda Blade nos 4 CRUDs (8 sítios `create`/`edit`), Non-Goal **declarado** nos charters ⇒ nova decisão `D-FORMS`; (2) **a fusão da thread 09 ficou pela metade** — `RevokeAllocatedAssetController:108` ainda devolve `asset_revocation.index`; (3) Garantias sem rota nem tela (`D-GARANTIAS`); (4) Auditoria (`D-AUDITORIA`); (5) **8 chamadas** a `assetmanagement::{show,create,edit}` cujos arquivos **não estão na árvore** (raiz de views só tem `index.blade.php`, 203 B); (6) provas de 09/10/11 pedem subpasta e o `main` é **flat** ⇒ o placar marca "pendente · arquivo ausente" para 3 telas em produção (mesmo defeito que a errata de 08/09 corrigiu só para Bens).
- **Ondas 1–3 emitidas como DELTA** (a pasta local tinha **7** arquivos contra **24** no `main` — cache atrás; mandar a local descer sobrescreveria o índice de 22.024 B, as 2 erratas do [CL] e as 10 saídas): `cowork-inbox/patrimonio/playbook/_PATCH-INDICE-2026-09-09.md` (7 objetos p/ §7.threads + 1 decisão `D-FORMS` + 7 linhas p/ §2) + **14** provas flat · **15** views fantasma (medição, com caso de sanidade obrigatório) · **16** revogações na aba (fusão de **tela**, não de rota — rota é [W]) · **17–20** formulários **nascendo BLOQUEADAS** por `D-FORMS`. **8 arquivos novos**; a pasta vai de 24 → 31, e fecha em ~38 com os `_saida`.
- **Ordem que não é gosto:** 14 antes de tudo (com o placar mentindo, o denominador de "o que falta" está errado) · 16 depois da 15 (fusão pela metade é pior que Blade inteiro) · 19 depois do D1 (migrar Manutenções com o `&&` vivo entrega tela nova e inacessível ao técnico, que é quem usa).
- **Não medido ⇒ não verificado:** paridade **por seção** das 5 telas (exige T1 estável + `getComputedStyle` no protótipo servido), conteúdo dos 5 `.tsx` e dos 10 `_saida-*`. Nenhum comando rodado daqui.
- ⚠️ **Ciclo fechado SEM pacote regenerado** — o `gerar-payload-partes.mjs` exige os arquivos em disco. Não afirmo que regenerei. Comando: `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`.

## Sync anterior
date: 2026-09-09T11:04:51Z

### Updated in this project
- **Ponto: 4 ondas de DS no build** (`ponto-ui.jsx` deixou de ser paralelo e delega ao bundle; `ponto-page/telas/fechamento/mobile.jsx` só call-site). 7 átomos + **62** botões → `Button`/`Tooltip` · **38** campos → `Input`/`Select`/`Textarea` · **8** `pt-check` → `Checkbox` · **7** `pt-toolbar` → `Toolbar`. Zero arquivo novo, zero CSS novo, API de `window.PontoUI` inalterada.
- **Retratação 1:** eu afirmei que o `Input` do DS não repassa `min`/`max`/`maxLength`/`accept` e que era lacuna **pro Code**. Lido no turno: `ui/input.tsx` e `ui/textarea.tsx` fazem `{...props}` — **passa em produção**. O fechado é o componente compilado do **espelho**. Zero pedido por isso.
- **Retratação 2:** eu disse que "não existe `PontoSubNav`". Existe em produção (`Pages/Ponto/_shared/PontoSubNav.tsx`, 2.296 B, ADR 0182 → `PageHeaderTabs`, **5 ghosts + ⋯ Mais**). Eu respondi pelo protótipo e falei como se fosse do módulo.
- **Achado que muda o pedido:** **o Ponto de produção já é React inteiro e já compõe o DS** — 21 Pages `.tsx` + 21 charters, 24 `Inertia::render`, 2 contratos vigentes, imports `@/Components/ui/*` + `@/Components/shared/*`. O outlier era o meu protótipo. Logo o pedido "Ponto usa DS" **não tem objeto**.
- **Pedido emitido no eixo certo: `cowork-inbox/ds-atomos/playbook/`** (4 arquivos, JSON embutido). [W] pediu "o visual igual ao seu" → a diferença é **anatomia de átomo**, não escolha de componente: 3 threads no primitivo em vez de 21 PRs por tela. **01** `ui/card.tsx` (+`badge`/`note`/`flush`, 1.987 B @733033864088) · **02** `shared/KpiCard.tsx` (+`variant="filter"`, 11.135 B @670b3f645b9b) · **03** `shared/Toolbar.tsx` (CRIAR — produção tem `PageFilters`, que é outra peça) · 04 tabela BLOQUEADA (`D-GRADE`) · 05 `StatusBadge` NÃO MEDIDA (17.674 B não recortados).
- **Lei da pasta: aditivo ou nada**, com **prova de guarda** por thread — `Card`/`KpiCard` são consumidos fora do Ponto (Backup · Financeiro/Advisor · Financeiro/Unificado; varredura **parcial**: 360 de 928 arquivos, então é piso, não teto).
- **A11y do alvo corrigida no build antes de exportar (§5-bis), com sonda validada por caso de sanidade** (branco sobre `--surface` = **13,62**): `.pt-tbl th` em `--text-mute` dava **3,18** (falha AA) → `--text-dim` = **5,49**, e **9,5px → 11px**; os outros **24** `color:var(--text-mute)` do `ponto-page.css` tinham o mesmo 3,18 e foram com ele. Label roxo do KPI = **4,85** ✓, não mexido.
- **Defeito da própria migração, achado na verificação e corrigido:** `.pt-toolbar → PtBarra` arrastou **7** `<span className="pt-sp">` do CSS antigo, convivendo com o spacer do `Toolbar` — 82px de gap fantasma. Removidos (page 1 · telas 5 · fechamento 1). **O bloco C da thread 03 tinha sido emitido em cima disso e mentia em 3 números** ("7 filhos, todos `flex:none`, `nowrap`") — remedido: 6 filhos, `0 1 auto`/`1 1 260px`/`1 1 0%`, barra **1215×71px a 1280px**. Toda medida do playbook passa a citar a largura.
- **KPI-filtro:** `variant="filter"` usa o dicionário próprio do bundle (`primary/amber/rose/emerald/violet`), não o `tone` semântico — sem isso os 6 tiles saíam todos roxos. Abriu `D-KPI-LABEL` (label accent 13.3/400 do bundle × 11px/600 uppercase muted da ADR 0110, que o próprio `KpiCard.tsx` documenta): fila de [W].
- **`COLAR-NO-CODE-ponto-ondas.md` reescrito** (anti-scatter, segue ponteiro): mapa **espelho → `main`** de 12 linhas — os três que **não** casam são `Widget`×`ui/card.tsx`, `Toolbar`×`PageFilters.tsx` e o KPI. **W12 morreu** (`BulkActionBar` aceita `children`, o "motivo do lote" cabe dentro). W9 virou divergência medida (13 abas planas × 5+overflow). W11/W13 mantidas, W14 nova.
- **Colisão de prefixo declarada:** as reescritas caíram em `ponto-page/telas.jsx` (threads 08/09) e `ponto-mobile.jsx`+host (thread 10), cujo `base:` é `e86130722de1` — quem abrir **remede antes de escrever**. **Thread 10 segue violada:** `ponto-mobile.jsx` ainda tem selfie (`:38`, `:72-76`, `:184`, `:261`) contra a ADR 0383.
- **Promovido à NORMA, para todos os módulos** (pedido [W] no mesmo turno): **§15 · ACERTOS** no `COLAR-NO-CODE-PROTOCOLO-COWORK-EXPORT.md` (21 títulos `##`, nenhum perdido) + **C13** na `CONSTITUICAO-COWORK.md` (citação atualizada a C1–C13; a linha declara que C13 **nasceu no §15**, porque nenhuma lei nasce na constituição). O canal virou a **5ª saída** do §6 (eram "4 saídas, e só 4") e o **item 9 do DoD** (§4 bloco D): ciclo que leu o `main` sem bloco de acerto **não fechou** — e "nenhum acerto medido" é resposta válida, silêncio não é. Zero script, zero gate de CI: é arquivo que se acrescenta.
- **Canal novo, pedido por [W]: `COLAR-NO-CODE-ACERTOS-E-LICOES.md`** (acumulativo, bloco por ciclo, mais novo em cima). O sistema catalogava erro (`LICOES_CC`), ausência (placar) e proibição — **faltava o que a produção acertou e não se refaz**. Ciclo 09/09 entrou com **8 acertos com sha** (A1 Ponto já compõe o DS · A2 `{...props}` no `input.tsx` · A3 `PontoSubNav` 5+⋯ · A4 `BulkActionBar` com `children` · A5 semântica do KPI-filtro pronta · A6 o arquivo documenta melhor que meu pedido · A7 `PageFilters` é outra peça · A8 precedente do Fiscal 03/09) e **5 lições com regra colada**. Registrado no §14 (mapa de destinos) e no read-order do §2-quater da norma.
- **Reincidência medida, e é minha:** "produção está atrás do protótipo" é minha hipótese-padrão e **falhou 2 de 2** quando testada contra leitura (Fiscal 03/09 · Ponto 09/09). Regra nova: antes de afirmar "o módulo X não tem Y" → controle positivo na busca + ler 1 arquivo real. Custo 3 chamadas; custo de errar = pacote no eixo errado, que foi o que aconteceu neste turno antes da correção.
- **Causa-raiz do erro do dia, mecânica:** busquei `from "@/Components/…"` com aspas **duplas**; o repo usa **simples** → "No matches" virou o fato "o Ponto não usa o DS", errado em 21 arquivos. A regra do §5-bis ("toda sonda roda um caso de sanidade antes do veredito") passa a valer para **busca de código**, não só sonda de DOM.
- **Reexport do Ponto com ancoragem dupla por SÍMBOLO:** threads **13** (Painel · `Dashboard/Index.tsx :: DashboardIndex(:183)` @`19c5ad41dc7c`) · **14** (Espelho lista · `Espelho/Index.tsx :: EspelhoIndex(:42)` @`5e4a3c209d3f`) · **15** (Aprovações · `Aprovacoes/Index.tsx :: AprovacoesIndex(:119)` @`6dc451b3c541`), cada uma com o **NÃO É** em caixa alta: paridade de **forma**, não de inventário de seção — `PresenceStrip`/`ActivityFeed`/`AlertInbox`/`MonthHeatmap` **ficam**, com prova de guarda. Ficha das **19 Pages** medida em `_delta-indice-13a15.md`: 20 threads no eixo (3 emitidas · `Espelho/Show` DIVIDE por 6 símbolos · 14 medidas e não emitidas · 3 bloqueadas).
- **Delta em vez de índice:** o `00-INDICE.md` do Ponto **local tem 24.411 B e o do `main` 24.929** — 518 B de divergência, com o `main` à frente. Editar o local e mandar descer sobrescreveria isso ⇒ desce **patch** (3 objetos no §7.threads, 2 no §7.decisoes, 3 linhas no §2). A pasta local do playbook é cache e acabou de provar.
- ⚠️ **Ciclo fechado SEM pacote regenerado** — 6 arquivos do build mudaram e o `gerar-payload-partes.mjs` exige os arquivos em disco. Não afirmo que regenerei. Comando: `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`.

## Sync anterior
date: 2026-09-08T14:52:00Z

### Updated in this project
- **Playbook do Patrimônio emitido pelo fluxo do §13** (`cowork-inbox/patrimonio/playbook/`, 7 arquivos): 5 threads CABE + 1 BLOQUEADA, com âncora recortada (`arquivo :: símbolo :: faixa :: sha`) em cada uma. O root `COLAR-NO-CODE-patrimonio-ondas.md` virou **ponteiro de 1,3 KB** (padrão HRM/Ponto).
- **Achado novo, Tier 0 em produção:** `AssetAllocationService.php:112` — a subconsulta de revoke não filtra `business_id` enquanto a externa filtra (`:107`); saldo disponível fica maior do que é, silenciosamente. Virou a thread 01.
- **Um pedido de 04/09 morreu no passo 0:** o `&&` da permissão (D1) não se reconfirmou nas 40 ocorrências lidas hoje — virou thread de medição, não PR. Segundo módulo seguido em que releitura mata pedido (Compras: 6 de 8).
- **Teto do índice corrigido com caso registrado** (§13.2): passa a ser 8 KB de **prosa** (o bloco json é para máquina, não conta). Patrimônio 5.505 ✅ · Compras 9.545 ❌ · Ponto 16.614 ❌ · HRM 18.038 ❌ — os 3 antigos ficam como **dívida nomeada**, não exceção.
- **Protocolo partido em NORMA × DOSSIÊ** (2026-09-08): `COLAR-NO-CODE-PROTOCOLO-COWORK-EXPORT.md` fica com as 19 seções de *o que se faz* (47.722 B) e `DOSSIE-PROTOCOLO-COWORK.md` recebe as 6 de *por que se sabe* (§1 · §7 · §9 · §9-bis · §9-ter · §9-quater, 16.666 B). **Numeração preservada** — toda citação antiga resolve. Alvo declarado era norma ≤15 KB e **não foi atingido**: 62 KB → 47,7 KB.
- **`CONSTITUICAO-COWORK.md` (3.335 B) criada e as 8 cópias do `§0` podadas**: relatórios · patrimônio · fiscal · Forja · CRM · repair · compras · jana passam a citar C1–C12 numa linha e mantêm só a lei do próprio módulo (as medições de C1 ficaram, como medição). Guarda rodada na mesma edição: contagem de `^## ` idêntica antes/depois nos 8 arquivos — **zero cabeçalho perdido**.
- **Gate de escrita adotado** (do §9-quater): documento de processo novo só se destravar thread nomeada com PR em ≤7 dias; cada ciclo registra `docs de processo : PRs mergeados`. Janela 09/08→08/09 medida: **61 : 7**.
- **Playbook do Compras emitido** (`cowork-inbox/compras/playbook/`, 6 arquivos): 2 threads executáveis (E2E · Margem sem fonte) e 3 bloqueadas em [W]. Corrige o doc de 04/09, cujos 6 de 8 pedidos já estavam feitos no `main`.

## Sync anterior
date: 2026-09-07T20:41:00Z

### Updated in this project
- **Paridade `Jana.Painel` medida e as 4 correções do alvo APLICADAS aqui** (não exportadas): `chat-jana.jsx` (CTAs das ações → `Revisar régua/proposta/recorte`; card `cheq` sem fonte → `metodos`) · `jana-merge.jsx` (toggle + `cfg` de `metodos`; copy do upsell; `Jana Pro` e `Conversar com a Jana` no cabeçalho de METAS, este com `data-contract="painel-cta-conversar"` e `onGoTab` como prop nova do `JmMetasSecao`). Verificado no render (dark, 0 erro de console).
- **bundle regenerado (2026-09-07 · 281 arquivos)** — ADR 0374 **revogada por [W] neste turno**, então gerei aqui: `sync/bundle.manifest.json` (schema `oimpresso-design-manifest/2`, `bundleId 3fe98b64…`, `baseBundleId 5023b274…`, mode `snapshot`, 281 arquivos · 7.366.728 B, `changes`: 26 added · 100 changed · 0 removed) + **43 partes** `sync/payload.partNN.json` (schema `oimpresso-design-bundle/2`, pedaço de 131.072 B, teto de 196.608 B por parte — parâmetros **inferidos das 43 partes vigentes**, não lidos do gerador). Lista tirada do host (`oimpresso.com.html` É o manifesto) + o conjunto `_ds` do manifesto anterior.
- **Auditoria do pacote gerado:** 43/43 partes remontadas, `atob`→sha256 de **todos** os pedaços confere, 281/281 arquivos cobertos, soma de bytes e contagem de pedaços por arquivo corretas, `bundleId`/`manifestSha256` idênticos nas 43 partes, 0 defeito. **Declarado:** a canonicalização de `bundleId`/`manifestSha256`/`changesSha256` é minha (`sha256` sobre `path:sha\n` + `JSON.stringify`), pois o gerador não estava disponível pra conferir — se o validador do Code usar outra, ele recusa e o pacote se regera lá.
- **Defasagem do pacote MEDIDA por sha256, não estimada** (comparação `sync/bundle.manifest.json` × arquivos deste projeto, 2026-09-07): manifesto de **2026-08-24T22:49Z**, 255 declarados → **155 iguais · 100 mudaram · 0 ausentes** + **26 declarados no host e inexistentes no manifesto** (`cli-tabs/cli-seg/cli-pagehead/cli-kebab`, `jana-metas*`, `jana-telas-novas*`, os 16 `forja-*` da quebra do `forja-page.jsx`, `produto-detalhe`, `produto-catalogo.css`). **126 arquivos fora do pacote.** Único "órfão" é o próprio `oimpresso.com.html`, que entra como `entry` — falso-positivo esperado. **Não regenerei** (ADR 0374: o gerador exige os arquivos em disco; escrever pelo contexto do agente é transcrição).
- **Pacote de export reescrito** (anti-scatter, 3ª vez) em `COLAR-NO-CODE-jana-tabs-cor-e-icone.md`: 10 blocos, ancoragem dupla, **2 ondas · 2 arquivos** (§1.1 token do pill · §2.1 `de <alvo>` + `<pct>% do alvo` no `MetaCard` do `Index.tsx`). Retrato de paridade ficou no chat, não em arquivo (mapa é COMANDO, ADR 0256 / L-42).
- **Leitura do `main` neste turno** (árvore `43b76c1ec327`): `Modules/Jana/Http/Controllers/IndexController.php` · `Pages/Jana/Index.tsx` · `_components/JanaKpiCard.tsx` · `_components/useJanaConfig.ts` · buscas em `_components/` (JanaCockpit §análises/§ações, JanaConfigDrawer, JanaDrillDrawer). **Não lido ⇒ não verificado:** `JanaCockpit.tsx` inteiro (47.668 B — li por busca, não linha a linha), `Index.charter.md`, `Index.casos.md`, `PainelContratoTest.php`.
- **Divergências medidas (11)**, com 2 pontos de **produção à frente** (CTAs `Revisar …` das ações HITL; conjunto de análises com `metodos` real) e 1 defeito interno do build daqui (upsell fala em 5 análises incluindo "cheques", que não tem fonte no `SellsCockpitAggregator`).
- Nenhum pacote de export gerado — a decisão de virar pedido é de [W].

## Sync anterior
date: 2026-09-06 (hora não registrada — ciclo do alvo Jana.Painel, árvore fb2240427978)

### Updated in this project
- **Alvo `Jana.Painel` medido e pacote de export reescrito** em `COLAR-NO-CODE-jana-tabs-cor-e-icone.md` (o mesmo arquivo do módulo — anti-scatter; a onda das abas virou §1.1). 10 blocos + ARQUIVOS A EDITAR/REUSAR/NÃO TOCAR/PARAR SE + placar. **Vira pedido: 1 onda · 1 arquivo · 1 linha** — pill do contador inativo em `PageHeaderTabs.tsx` usa `var(--border-2)` e o `TabBar` do DS manda `var(--bg-2)` (autoridade DS → protótipo → produção).
- **Leitura do `main` neste turno** (árvore `fb2240427978`): `prototipo-ui/FRESCOR-PRODUCAO-vs-PROTOTIPO.md` · `prototipo-ui/PRE-FLIGHT-TELA.md` · `Pages/Jana/Index.tsx` · `_components/JanaAreaHeader.tsx` · `_shared/JanaSubNav.tsx` · `Components/shared/PageHeaderTabs.tsx`. **Não lido ⇒ não verificado:** `JanaCockpit.tsx` (47.668 B, dono de brief/KPIs/análises/ações), `Index.charter.md`, `Index.casos.md`, `SidebarGhost.php`.
- **`main` à frente em 2 pontos, e as duas melhorias foram PUXADAS pro build daqui:** (a) `cli-tabs.jsx` acrescenta `role="tablist"` no nav + `role="tab"`/`aria-selected`/roving `tabIndex` + nav ←/→/Home/End (o `TabBar` do DS só escreve `aria-current="page"`; medido antes: **0 de 6** `role="tab"` no `nav.jm-tabs`); (b) `chat-jana.jsx` — `svg.jc-spark` ganhou `aria-hidden`. Medido depois (1103 nós, duas leituras iguais, dark): 6/6 `role="tab"`, ativa `tabIndex=0` e inativas `-1`, 0 svg anônimo, cores inline do DS intactas.
- **Erro meu registrado:** a 1ª medição de `role="tab"` contou a página inteira em vez do `nav` e devolveu "5 de 6" — escopo de seletor errado quase transformou defeito real em "pendência do DS, nada a fazer". Está no §2/§8 do pacote.
- **Pendências do DS (bundle é espelho, não se edita aqui):** cor crua `rgb(255,255,255)` no `Avatar` dentro do `<h1>` · `TabBar` sem `role="tab"` de origem.
- ⚠️ **Ciclo fechado SEM pacote regenerado** — 2 arquivos do build mudaram e o `gerar-payload-partes.mjs` exige os arquivos em disco (ADR 0374). Não afirmo que regenerei. Comando: `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`.

## Screen map
| Tela | Arquivos do build | Âncora no `main` |
|---|---|---|
| Shell · Sidebar | sidebar.jsx · app.jsx (modos + drawer mobile) · styles.css (bloco `.sb-*`) | resources/js/Components/cockpit/Sidebar.tsx · shared.ts · useSidebarShortcut.ts · Layouts/AppShellV2.tsx · resources/css/cockpit.css · app/Sidebar/*.php (contrato v2, ADR 0180) |
| Fiscal · Cockpit | fiscal-page.jsx · fiscal-actions.jsx · fiscal-page.css | resources/js/Pages/Fiscal/Cockpit.tsx (+ `_components/FxShell.tsx`, `WriteOffAuditoriaCard.tsx`, `SavedViewsChips.tsx`) |
| Fiscal · NF-e/NFC-e | fiscal-page.jsx (`FxNotasPage` preset 55/65) | resources/js/Pages/Fiscal/Nfe.tsx (+ `NotaDrawer.tsx`, `InutilizacaoModal.tsx`) |
| Fiscal · NFS-e | fiscal-page.jsx (preset NFS-e) | resources/js/Pages/Fiscal/Nfse.tsx (+ `NFSeDrawer.tsx`) |
| Fiscal · Manifesto DF-e | fiscal-subpages.jsx (`FxDfePage`) | resources/js/Pages/Fiscal/Dfe.tsx + `DfeController` |
| Fiscal · Eventos | fiscal-subpages.jsx (`FxEventosPage`) | resources/js/Pages/Fiscal/Eventos.tsx + `EventosController` |
| Fiscal · Certificado e config. | fiscal-subpages.jsx (`FxConfigPage`) | resources/js/Pages/Fiscal/Config.tsx + `ConfigController` |
| Fiscal · SPED e livros | fiscal-subpages.jsx (`FxSpedPage`) | resources/js/Pages/Fiscal/Sped.tsx + `SpedController` + `SpedIcmsIpiGeneratorService` |
| Fiscal · mutações e ⌘K | fiscal-actions.jsx | `AcoesController` · `PaletteSearchController` · `_components/CmdKPalette.tsx` |
| Ponto · átomos (todas as 13 abas) | ponto-ui.jsx | `Components/ui/{button,card,input,select,textarea,checkbox}.tsx` · `Components/shared/{KpiCard,StatusBadge,EmptyState,PageFilters,BulkActionBar}.tsx` |
| Ponto · Painel | ponto-page.jsx | resources/js/Pages/Ponto/Dashboard/Index.tsx (+ `_components/{PresenceStrip,ActivityFeed,AlertInbox}.tsx`) |
| Ponto · Espelho (lista + mês) | ponto-page.jsx · ponto-telas.jsx | Pages/Ponto/Espelho/Index.tsx · Show.tsx (+ `_components/MonthHeatmap.tsx`) |
| Ponto · Intercorrências · Aprovações | ponto-telas.jsx | Pages/Ponto/Intercorrencias/{Index,Create,Edit,Show}.tsx · Aprovacoes/Index.tsx |
| Ponto · Banco de horas · Escalas · Colaboradores | ponto-telas.jsx | Pages/Ponto/BancoHoras/{Index,Show}.tsx · Escalas/{Index,Form}.tsx · Colaboradores/{Index,Edit}.tsx |
| Ponto · Importações · Relatórios · Configurações | ponto-telas.jsx | Pages/Ponto/Importacoes/{Index,Create,Show}.tsx · Relatorios/Index.tsx · Configuracoes/{Index,Reps}.tsx |
| Ponto · Fechamento · Conformidade | ponto-fechamento.jsx | **sem receptor** — threads 04/05 bloqueadas em W1–W4 |
| Ponto · REP-P (celular) | ponto-mobile.jsx | `Api/MobileMarcacaoController.php` (existe **sem rota**) · 7 rotas `abort(501)` |
| Ponto · sub-nav | (13 abas planas em `ponto-page.jsx`) | Pages/Ponto/_shared/PontoSubNav.tsx → `shared/PageHeaderTabs.tsx` (5 + ⋯ Mais) |

## Sync history (resumo — narrativa completa em `DOSSIE-PROTOCOLO-COWORK.md` §14)

## Sync anterior
date: 2026-09-03T20:45:00Z

### Updated in this project
- **Pacote de EXPORT do módulo Fiscal reescrito na forma padrão de 10 blocos** (`COLAR-NO-CODE-fiscal-notafiscal-ondas.md` — o mesmo arquivo, anti-scatter §2-ter; as 11 perguntas ⛔ [W] preservadas). Agora com **ancoragem dupla**: alvo de layout = protótipo medido aqui; âncora de implementação = arquivo real do `main`, com `ARQUIVOS A EDITAR / REUSAR / CRIAR / NÃO TOCAR / PASSO A PASSO / DADO / PARAR SE` por onda. **10 ondas**, cada uma = 1 seção ≤ 1 PR de 300 linhas.
- **Leitura do `main` neste turno** (árvores `1582cb14ca3a` · `8ce4de791752`): `Pages/Fiscal/` (21), `_components/` (11), `_lib/` (6), `Cockpit.tsx`, `Nfe.tsx`, `FxShell.tsx`, `botao-fiscal.ts`, `chip-filtro.ts`, `Modules/Fiscal/**` (44), `Routes/web.php`.
- **Correção do meu diagnóstico anterior:** o PR-A1 ("matar a camada `fx-*`/`var(--fis)` rosa") está em boa parte **entregue** — `Cockpit.tsx` e `Nfe.tsx` já usam `Button`/`Input`/`Select`/`Checkbox` do DS através de `_lib/botao-fiscal.ts` e `_lib/chip-filtro.ts`, com o motivo escrito no próprio `_lib` (armadilha layered × unlayered do `cowork-fields.css`). O que sobra de `fx-*` é CSS de layout, não pele de controle.
- **Gap real medido, que eu não tinha:** `Cockpit.tsx` recebe `alerts` e **só o conta** (`totalRej`) — os 4 alertas fiscais do protótipo **não têm superfície no vivo**; e `interface Sparklines` existe com a prop **não desestruturada** (confirma a decisão ⛔ [W] 2). Viraram Ondas 1 e 4.
- **Onda 0a — a11y do ALVO corrigida no build, não exportada como dívida** (§5-bis: exportar `TR` clicável e ícone anônimo é exportar defeito com selo). Medido antes: **8 linhas clicáveis com 0 `tabindex`** · **4 de 4** svg de botão sem `aria-hidden` · drawer sem `aria-modal` e foco no `BODY` · `[aria-live]` = **0** na carga (o container nascia junto do 1º toast). Depois: `tr[tabindex="0"]` **8** · svg sem nome **0/4** · `aria-modal="true"` com foco inicial no "Fechar", Tab preso e foco devolvido · `[aria-live]` **1** · **0 erro de console**.
- **T1 estável:** três leituras iguais de `querySelectorAll('*').length` = **1099**, rota `fiscal`, dark, após `__oiLazyDone`. Alvo por seção fechado com filhos **na ordem** e tokens resolvidos (`--accent` dark `oklch(0.70 0.15 295)`; `--fis` do port não existe mais no protótipo).
- **Declarado, não afirmado:** `Dfe/Eventos/Config/Sped/Nfse.tsx` **não lidos neste turno** — as ondas 6–9 abrem com `LER NO TURNO`; `screen-coverage` do Fiscal não medido; A2 (`outline:none`) não auditado por seção.
- ⚠️ **Ciclo fechado SEM pacote regenerado** — 4 arquivos do build mudaram e o `gerar-payload-partes.mjs` exige os arquivos em disco (ADR 0374). Não afirmo que regenerei. Comando: `node scripts/design-sync/gerar-payload-partes.mjs --root <dir> --out sync/ --previous sync/bundle.manifest.json`.

| data | ciclo |
|---|---|
| 2026-09-03 | Forja dividida em 1 arquivo por tela — 3 ondas, tudo medido no app rodando |
| 2026-09-01 | Playbook do **módulo Nota fiscal** completado para **9 ondas · 28 PRs** (`COLAR-NO-CODE-fiscal-notafiscal-ondas.md`): +  |
| 2026-09-01 | Playbook de ondas do **módulo Nota fiscal** escrito como PONTE (`COLAR-NO-CODE-fiscal-notafiscal-ondas.md`): 7 ondas · 2 |
| 2026-09-01 | (sem bullet) |
| 2026-09-01 | (sem bullet) |
| 2026-09-01 | (sem bullet) |
| 2026-09-01 | (sem bullet) |
| 2026-09-01 | 24px |

> **9 blocos** (‑70635 chars de narrativa) foram para o dossiê em 2026-09-08. Motivo: `github.md` é **recibo parseável** (repo · branch · path · Last sync · Screen map), não diário. Estava com 70.636 chars — o maior arquivo da raiz.
