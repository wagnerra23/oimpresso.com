# STATUS.md — Espinha do Cowork (single source of truth)

## 🧭 COMO PENSAR — núcleo ativo (consolida REGRA-0 + L-25/**26/27**/29/30/31/32; [W] 2026-06-03 "as lições passivas não bastam, evolua" · 06-04 auditoria de gatilho)

> ## ⭐ A LEI DE UMA TELA — pré-voo de TODO turno (auditoria [CC] 2026-06-08 · `Auditoria CC - 2026-06-08.html`)
> **Esta sequência roda ANTES de qualquer afirmação, plano ou arquivo. É a reorganização das 6 regras abaixo numa ORDEM obrigatória — não uma lista nova. Diagnóstico que a gerou: numa só sessão violei a Regra 6 três vezes (Sells/Oficina #2417/Compras: afirmei estado do git lendo CÓPIA LOCAL stale) e construí um Mapa de 6 fases sobre essa premissa errada. Regra que não trava no momento da ação não vale — igual ao lint aspiracional que consertamos no código.**
> **O padrão a matar:** _afirmo / planejo / produzo ANTES de estabelecer a verdade viva — e quanto maior o artefato, pior o estrago._ Velocidade na fase errada.
>
> **PORTÃO 1 · VERDADE** — antes de afirmar fato sobre o repo: li `@main` NESTE turno? Tag `✓lido` ou `⚠não-verifiquei`. ⛔ Arquivo LOCAL do Cowork (`resources/css/*`, `*.tsx`, cópia de charter) = **fotocópia que envelhece, sósia do original** — NUNCA cito como estado do repo. Sem leitura viva → digo "não verifiquei", nunca afirmação confiante.
> **PORTÃO 2 · PROPORÇÃO** *(o portão NOVO desta auditoria)* — o tamanho do que vou produzir cabe na minha certeza? Premissa não-verificada → **menor sondador possível** (uma pergunta, um grep), nunca catedral. ⛔ **Nenhum artefato > 1 arquivo (Mapa, plano N-fases, handoff longo) sem `✓lido` da premissa neste turno.** Foi a catedral-sobre-areia que mais custou hoje.
> **PORTÃO 3 · MANDATO** — já foi decidido? Decidido (contrato/[W]) → **EXECUTO, não pergunto**. ⛔ "Quer que eu…?" sobre o decidido = a desculpa; repetir pergunta já respondida desperdiça o tempo do [W]. Pergunto só em subjetivo/Tier-0 aberto.
> **PORTÃO 4 · PROVA** — vi 🔴 e 🟢, escopo COMPLETO? Todo ✅ com número/screenshot no mesmo turno; teste rodado em TUDO antes de declarar; gate visto falhar E passar.
>
> _Por que melhor: (1) sequência-trilho, não lista esquecível — não dá pra produzir no P2 sem passar pelo P1. (2) tem o portão que faltava (Proporção). (3) é barata (um read < um Mapa errado), então sobrevive ao calor do momento. (4) aplica em mim o que aplicamos no seu código: disciplina mecânica, não aspiracional._

> Estas 6 valem **ACIMA** da lista de lições. Se eu hesitar ou for declarar algo, leio isto primeiro. **O número/teste manda, não a minha palavra.**
> **🎯 HIERARQUIA ÚNICA DE REGRA (M1 · 2026-06-04, [W] ratifica):** estas **6 = a lei** (o always-read). **Regra de Ouro** (§ abaixo) = o **pré-flight** delas. **Bateria §9 + `memory-health.js`** = o **check mecânico** (não 4ª lista pra ler). **L-01…L-32 = histórico** que vira check. _Uma superfície só — sem listas concorrentes (mata S1/C9)._
> 1. **PROVA antes de afirmar.** Nunca "feito/funciona/resolvido" sem número/screenshot anexo NO MESMO turno. Não medi → digo **"não verifiquei"**.
> 2. **Mede o escopo COMPLETO antes de declarar — não depois.** Rodo o teste em TUDO que carrega ANTES de falar "pronto". Declarar num escopo parcial e deixar o teste me corrigir (101→19→1→drawer→878) = **whack-a-mole, o erro recorrente.**
> 3. **Contrato aprovado decide → conformo, NÃO pergunto.** "Quer que eu…?" sobre o já-decidido = a desculpa. Pergunto só em subjetivo/Tier-0 genuinamente em aberto.
> 4. **100% = 100%.** Não estaciono residual como "sua decisão" nem "exceção" quando existe token/técnica (color-mix etc.). Exceção só com PROVA de que não há token, declarada.
> 5. **Testar o teste, DOIS lados.** Todo ✅ tem que ter sido visto 🔴 num bug **E** 🟢 num caso legítimo (sensibilidade + especificidade). Senão não vale.
> 6. **Fato sobre repo/git/estado externo = `⚠ inferido` até `✓ lido NESTA sessão`.** Memória local é **cache que envelhece** — causa-raiz recorrente ("execução 40", "o gate pega o pixel": afirmei de cabeça, o git desmentiu). Antes de dizer que arquivo/tela/token/PR existe ou faz X → `github_read_file`/grep no `main` desta sessão, ou falo **"não verifiquei"**. **Agressivo/rápido vale pra EXECUTAR (PRs reversíveis em paralelo), NUNCA pra AFIRMAR** — velocidade no fazer ≠ velocidade no alegar. **Esta regra VENCE a REGRA-0 quando colidirem** (a REGRA-0 mata o "quer que eu…?" sobre o decidido; NÃO licencia afirmar fato não-lido). _(promovida do Gate 6 — reincidiu 2× em 2026-06-04, §12 sobe de tier.)_ **⚠ REINCIDÊNCIA TRIPLA 2026-06-08 (Sells, Oficina #2417, Compras): afirmei "X está stale/falta no git" lendo ESPELHO LOCAL (`resources/css/*` do Cowork) — os 3 já estavam migrados no `main`. Espelho local do Cowork ≠ git, SEMPRE. Estado de QUALQUER `resources/css/*` ou Page só por leitura @main no turno. [W] previu o furo 2×. Não enfileirar handoff sem reler @main do alvo no mesmo turno.**
> _Executo + provo; não intelectualizo instrução explícita; não declaro vitória parcial; **não afirmo fato externo de cabeça**; **não proponho guarda/componente/regra sem ler o que o repo já tem**._

> **7. CONHECER o terreno ANTES de propor (não reinventar).** O repo tem suíte ENORME de guardas/componentes/scripts (`MAPA_CAPACIDADES_REPO.md`). Antes de propor QUALQUER guarda/regra/componente/script novo → ler `package.json` scripts + `scripts/` + `Components/ui/` + o MAPA, e confirmar no `main`. **Reincidência 2026-06-08:** propus reinventar `--accent`-guard (já existe `conformance-gate.mjs` hue roxo + `foundation-guard.mjs` token-def allowlist) e PageHeader-guard (já existe `pageheader:guard`) — só não foi pro Code porque [W] mandou "resolver nos padrões do projeto" e eu li os scripts a tempo. Regra: **estender/referenciar o que existe, nunca recriar** (M-AP-6 "não inventar componente"). Esta regra é a face-PROPOR da Regra 6 (que é a face-AFIRMAR)._


> **LER ISTO PRIMEIRO em todo chat novo.** Espelho-fonte do `Painel Cowork - Estado Atual.html`.
> **🗺️ LER TAMBÉM no início (anti-reinvenção):** `MAPA_CAPACIDADES_REPO.md` — índice do que o repo JÁ TEM (suíte de guardas, componentes, scripts, tokens). Conferir aqui ANTES de propor qualquer guarda/regra/componente novo. Senão, reinvento o que existe (erro 2026-06-08).
> **🥇 DS = UM SÓ, NOME = `ds-v6` (consolidado 2026-06-04 · [W] autorizou o rename · ver `ds-v6/_LEIA.md`):** **`ds-v6/` é a pasta ÚNICA do DS** — `tokens.css` (fonte de valores que o host carrega) + `components.css`/`doc.css`/`interactive.js` (implementação) + `showcase/gabarito/receita/mapa` (vitrine). **`ds-v5/` não existe mais** (movido pra ds-v6 · host+`Produção - Tela Real.html` repontados · testado render OK). Roxo `oklch(0.55 0.15 295)` (ADR 0235) idêntico ao git. Cor/token novo = só em `ds-v6/tokens.css`, Tier 0, só [W]. **Git ainda diz `ds-v5` (ADR 0244) → rename enfileirado pro [CL] em COWORK_NOTES (D-06: Cowork=git).**
> **🧹 FAXINA 2026-06-04 ([W] "retirar todos os conflitos"):** 11 HTMLs soltos da raiz → `_arquivo/exploracoes-2026-06-04/` com lápide-INDEX (host intacto, testado). **`prod-page`≠`producao-page`** (2 módulos, não dup). **Rename de nomes bagunçados EXECUTADO+TESTADO:** `financeiro-app`→`financeiro-page` · `boleto-contas-app`→`boletos-page` · `prod-page`→`produtos-page` · `inbox-v2-{extras,ai,cur,out}`→`inbox-{extras,ai,cur,out}` (move + host `<script src>`; exports `window.*` inalterados; verificado: FinanceiroPage/ProdListPage/BoletosPage/Inbox* existem; render limpo). Padrão: tudo `<modulo>-page.jsx`+`<modulo>-data.jsx`+`<modulo>.css`. **Refs `.md`/mirror/`app.jsx`-comentário NÃO foram reescritas (não quebram runtime; doc).**
> **🌱 LER TAMBÉM (raiz do método, anti-regressão):** `PROCESSO_MEMORIA_CC.md` — duas velocidades (Register↔Charter↔ADR), anéis de decisão, e §5 REGRESSÕES PROIBIDAS. Conferir §5 ANTES de propor qualquer coisa. _REGRESSÃO É INACEITÁVEL._
> Atualizar ao fim de cada sessão: estado de tela mudou? decisão tomada? → reflete aqui.
> **2026-06-07 · AVALIAÇÃO + REFINO dos PRIMITIVOS DE LAYOUT (ADR 0253) + correção de DRIFT.** [W]: "lei os componentes primitivos → leu do git? avalie com notas, gere relatório → refine e complete, envie o relatório e o porquê pra memória." **DRIFT achado (REGRA 6):** o git (`@main 1f53d49`) estava **à frente da espinha local** — `DESIGN.md` reescrito **06-06** ("grande inspeção") + **ADR 0253 (primitivos de layout `Components/layout/` Box/Stack/Inline/Grid/Container/Text)** + **ADR 0254 (grade determinístico)** + `MANUAL-IDENTIDADE.md` + `MANUAL-CSS-JS.md` v1.1 — **nada disso estava em STATUS/MEMORY_INDEX (06-04).** Eu tinha lido só `ds-v6/` local (régua visual CSS), que é camada DIFERENTE dos primitivos de CÓDIGO. **Entregue:** (1) relatório `Avaliacao - Primitivos de Layout (ADR 0253).html` — nota **7.6/10**, gaps P0/P1; (2) **refino v2** dos 6 primitivos em `prototipo-ui-patch/resources/js/Components/layout/` (Box+superfície · Stack/Inline+divider · Grid+auto-fit `min` · Container `max-w-*` v4-safe · Text+mono/tabular/tons `success/warning/destructive`/escala 4xl-5xl) — fiado nos tokens REAIS do `@theme` (não `--pos/--neg` da régua), aditivo, projeção 9.2/10; (3) **porquê** = `memory/decisions/_PROPOSTA-amend-0253-primitivos-layout-completos.md` (proposta, [CL] numera, [W] decide) + log `memory/sessions/2026-06-07-*`; (4) ponte zero-toque `prototipo-ui-patch/PROMPT_PARA_CODE_PRIMITIVOS-LAYOUT-V2.md` (URLs+git, [W] cola 1×). **Tier 0 isolado:** `--font-mono` no `@theme`. **NÃO commitado** (read-only). **Aberto:** risco Container TW v4 inferido-não-testado (refino já corrige); critério-de-pronto ADR 0253 (tela piloto/doc/REGISTRY) = [CL]; atualizar MEMORY_INDEX T2 c/ ADR 0253/0254 + DESIGN.md 06-06.
> **2026-06-08 (k) · PAINEL [W] = tela-padrão durável.** `Painel de Controle.html` = a ÚNICA tela que [W] abre (PT simples: sua única ação / esperando você / feito). **CONVENÇÃO DURÁVEL: ao FIM de toda sessão, [CC] REFRESCA o `Painel de Controle.html` do estado real (STATUS + fila COWORK_NOTES)** — nunca envelhece. Automação honesta: `prototipo-ui-patch/COLE_ISSO_NO_CODE.md` = 1 prompt → Code faz a fila inteira + merge autônomo (URLs públicas ~1h via get_public_file_url; regenerar se expirar). Durabilidade real = **memória + loop** ("continua" retoma qualquer sessão); o painel é descartável/regenerável. [CC] não commita — o 1 paste de [W] é o único toque humano.
> **2026-06-08 (f) · MODELO ÚNICO DE IDENTIDADE — RATIFICADO [W] ("só deve existir um modelo").** UM modelo de governança de cor, 2 camadas: **chrome = roxo `oklch(0.55 0.15 295)` único** (botão/foco/link/ativo; runtime `app.jsx accentHue`; proibido `--accent` por módulo) + **semântica = N tokens governados** (`--origin-*`/`--stage-*`/status). **MORRE** accent-por-módulo (Vendas-verde 155 · CRM-indigo 268 · Sells-azul 220 → viram `--origin-*` ou somem); âmbar Oficina FICA (já é `--origin-MFG`, não `--accent`). Defesa: redefinir `--accent` fora do canon = lint. Doc proposta: `memory/decisions/_PROPOSTA-modelo-unico-identidade-2-camadas.md` (→ [CL] numera ADR). Levantamento+backlog 6-fases + decisão: `Mapa de Identidade ERP - CC.html`. **F0 decidida.** ⚠️ **CORREÇÃO (i) 2026-06-08 — censo contaminado por espelhos LOCAIS; git OK:** lido @main: `sells-cowork.css@main` e `cowork-canon-financeiro-bundle.css@main` **JÁ são roxo 295**. O "Sells azul 220" + "2 bundles fin dup" + "Compras já aliased" eram cópias LOCAIS stale do Cowork (`resources/css/` daqui ≠ git). **Handoff Sells azul→roxo = VOID** (deletado). **Única ilha git confirmada = Compras** (`cowork-compras-bundle.css@main` = hex `#1f3a5f`). **Lição (vira regra):** estado do repo só por `github_read_file @main` — os `resources/css/` locais driftam. Programa de identidade é MENOR do que o Mapa pintou; re-baselinar contra git. Git `Sells/Create.tsx` = alto-craft (`raw_oklch:0 raw_hex:0`), dívida real = primitivos F4; o "bug da savebar" era do mock Cowork, não do Create.tsx.
> **2026-06-12 (b) · PRODUTOS — análise + P0 tokenização + 2 fixes dark + P1 acabamento 9.75 ([W] "essa tela está no padrão?" → "sim" → "p1").** P0: `prod-mec.css` (79 cruas, 0 dark) → tokens; 2 bugs dark (var() em transition trava Chromium; **app.jsx fixava --accent-soft claro inline app-wide vencendo [data-theme=dark]** → desacople hue×luminância via `--accent-h` nos 2 blocos do tokens.css, conserta TODA tela dark). **P1:** lentes (ícone-quadradinho accent-soft) nas 7 seções do drawer (= gramática show/Create); empty-state padrão DS; seleção robusta box-shadow inset + foco/--ease; tokenização das cruas do drawer (supp/var-table + best-badge); floor 9.5→fs-1. Hosts: `produtos-page.jsx?v=p1`, `prod-mec.css?v=p1b`, `prod-page-extras.css?v=p1`, `ds-v6/tokens.css?v=v6-3`, `app.jsx?v=accentdark2`. Grid é o + completo = candidato a **DataGrid shared (P2, aguarda aval [W])**. Sessão: `memory/sessions/2026-06-12-produtos-analise-p0-tokens.md`.
> **2026-06-12 (a) · VENDAS/LISTA — 3 comentários [W] (totalizadores · filterbar · nav Comercial).** (1) **Totalizadores:** barra `.vd-totalbar` full-width abaixo da tabela (NÃO tfoot — scroll-x quebra colSpan), soma reativa ao filtro: N vendas/pagas/a receber + COMISSÃO + TOTAL DO FILTRO. (2) **Filterbar:** status-tabs + Foco/Hoje + **busca no canto direito** fundidos numa linha `.vd-filterbar` (input liga no `query` que já filtrava). (3) **Nav:** VdModNav ganhou **CRM** (início) e **Oficina** (depois) como irmãos do grupo COMERCIAL via `__selectRoute`, separados por `.vd-modnav-div`. Hosts: `vendas-page.jsx?v=fb2`, `vendas-extras.jsx?v=fb1`, `vendas.css?v=filterbar2`. **⚠️ LIÇÃO AMBIENTE:** edição direta do [W] persiste arquivo sob nome literal `arquivo.jsx?v=X`, mas meu preview serve o PLANO — consolidei literal→plano via `copy_files move:true` + href novo. SEMPRE consolidar quando o system avisar de edição direta. 2 comentários [W] abertos: "plano de contas deve ir para…" + "sync now endereço cliente↔venda". Sessão: `memory/sessions/2026-06-12-vendas-lista-filterbar-totalizadores.md`.
> **2026-06-11 (h) · SUB-NAV DO MÓDULO VENDAS ([W] "quais telas vinculadas?" → "ok" → 2 refinos no review ao vivo).** Caixa do dia/Devoluções/Comissões/Relatórios saíram de trás do "Visões ▾" → tabs sublinhadas. **v2 ([W] "page header fica abaixo do header" · "visões sai fora" · "Imprimir caixa pra dentro do caixa"):** tabs viraram `VdModNav({here})` em página, **ABAIXO do page header** de cada tela (lista + 4 subs); VdSubBreadcrumb removido das subs; **"Visões ▾" DELETADO do toolbar**; "Imprimir caixa" movido pro header do Caixa ("Imprimir caixa (Z)"); **toolbar FOCO desceu pra logo acima da lista** (KPIs→FOCO→status-tabs→tabela, ?v=modnav3). ?v=modnav2 nos 3 arquivos. Validado view ao vivo + reload. **Lição de ambiente:** capture do iframe reporta paint/classes inconsistentes pra mudança de classe in-place — estado pós-interação só por eval no view do usuário ou reload. Sessão: `memory/sessions/2026-06-11-vendas-create-reskin-show.md` (adendos).
> **2026-06-11 (g) · VENDAS/CREATE RE-SKIN NA LINGUAGEM DO SHOW ([W] "a venda tá feia, teria que refazer por completo? o show está bonito").** Diagnóstico: NÃO refazer — domínio sólido, casca feia. Re-skin v2 (`vendas-create-page.css` reescrito + edits cirúrgicos no jsx, host ?v=cr3): **← Vendas (esc)** no header (a "volta" que faltava) · total hero mono fs-7 · pills→tabs sublinhadas (=vd-drawer-tab) · ícone-quadradinho accent-soft nas 5 seções (=lentes 9.75) · tempero sh/ease/ramp · **FIX overflow-x da fila 06-10** (min-width:0 + savebar wrap) · G8 snap micro-fonts→fs-1. Probe **0 🔴** light+dark (G5 lista cruas do SHELL, fora da rota). F2 [W] pendente; port F3 leva conceitos, não CSS (Create.tsx@main já é alto-craft). Sessão: `memory/sessions/2026-06-11-vendas-create-reskin-show.md`.
> **2026-06-11 (f) · ACABAMENTO FINO + VARREDURA POR ESTADO — 0 🔴 FINAL.** Pós-(e): Histórico data única `dd/mm · HH:MM` · headers/composer de comentários harmonizados · Fiscal no kv-card+copy. Bateria POR ESTADO (drawer linhas distintas) pegou em série: vd-link 11px→fs-2 (G8) · regra stale vd-step-num (G3) · vd-cob-* pg-styles→vendas (G7) · os-drawer max-width 92vw (G4) · G10 exceção outline-chip (negative ✓) · **sweep das 9 `background:-fg` do vendas.css** (7→av-5 · 2 primários AZUIS recibo/orçamento→accent). Verifier done. Operacional: needs_work em série = varrer a CLASSE por grep, não a instância. Sessão: `memory/sessions/2026-06-11-drawer-metodo-975-refinos.md`.
> **2026-06-11 (e) · VIDA SISTÊMICA + BATERIA v2.4 ([W] "cor fosca é sistêmico… revisão em TODOS os componentes, tem teste pra isso").** (1) Raiz do fosco achada na FONTE `ds-v6/tokens.css`: chroma subiu light+dark (pos .12→.17 · neg .18→.21 · warn .12→.16 · softs →.08-.11 · accent-soft/line →.045/.09 · origins) — roxo canônico intocado, ?v=v6-2. (2) Vínculos 1-linha + ficha `.fin-kv-card`. (3) **qa-conformance v2.4: G10 chip fosco · G11 select fantasma · G12 grid órfão · G13 texto cortado** (controle-negativo ✓) — reclamações do [W] mecanizadas (L-41 nova: 2ª ocorrência = gate, não conserto avulso). Bateria rodada (fin lista+drawer+4 sub-telas · vendas · compras) → consertado: .fin-kbd-acc G3 · KVEdit select absoluto sobre espelho (G11 raiz: max-content esticava track) · vendas G2 accent-color + G3 av-5 · G9 flutuante=fixed/absolute/sticky. **Placar 0 🔴 em todas as rotas.** Tokens novos já servem no gabarito FA-1/FA-5.
> **2026-06-11 (d) · F2 DO DRAWER 9.75 + HANDOFF FA-5 ([W] "ok f2").** Pacote drawer (J/K+n/N+R kbds visíveis · recibo brand · CopyVal · KVEdit · hero calmo+chip frescor · lentes hue · xchips wayfinding · polish grid/footer) aprovado → ponte `prototipo-ui-patch/PROMPT_PARA_CODE_FA5-DRAWER-975.md` (**onda FA-5**, depende FA-1 pelos tokens --sh-2/--ease; paraleliza com FA-2..4; FX-2/3/4 implementar 1× citando FA-4). Total programado: **20 ondas** (FA 5 · Q 5 · Caixa 10). NÃO commitado — [W] cola 1×.
> **2026-06-11 (c) · DRAWER FINANCEIRO — MÉTODO 9.75 APLICADO + BENCH + R1-R3 ([W] "aplicar o método no drawer" → "pode criar aqui primeiro" → "ancorado nos melhores?" → "pode fazer… harmonizado, cores complementares").** Protótipo, verifier ✅2×: **J/K + ↑n/N↓ + R com kbds visíveis** (P2/N3) · **recibo brand** (P5/S3) · FX-2/3/4 provados aqui (maio hardcoded morto · "vencida há Nd" · zero sem sinal). **Bench** `Bench Drawer Financeiro.html`: 8,4 vs Linear/Stripe/Mercury/Attio/Front → **refinos R1 copy-em-tudo (CopyVal) · R2 KV inline (KVEdit + FIN_EDIT_OPTIONS) · R3 hero calmo (badge fora, chip frescor) + harmonização** (sh-2, gradiente roxo, lentes hue por domínio, xchips wayfinding venda-roxo/OS-âmbar/compra-verde) ≈ **9,5**. F1 — aguarda F2 [W] p/ handoff. Sessão: `memory/sessions/2026-06-11-drawer-metodo-975-refinos.md`.
> **2026-06-11 (b) · FINANCEIRO — DIFF COMPLETO ×MAIN + ONDAS FA-1..FA-4 ([W] "compare e verifique o que falta aplicar, divida em ondas").** Releitura @main `8f5d24b4` no turno (imports temporários deletados — IT8): **pacote F2 de telas LANDOU INTEIRO** — lentes 029 ✓ · Impostos ✓ · drawer 3 camadas (hero fixo/Conciliação-lente/LenteFiscal) ✓ · ramp `--fs-1..9` em **`foundations.css` (EXISTE agora — censo 06-04 "não existe" = stale)** + `fontRampCheck` ratchet ✓ · Exportar XLSX/PDF ✓. **FALTA (medido):** (1) **§TEMPERO não landou** (--sh/--ease/--t/--atmo = 0 em foundations/cockpit/inertia — PR-4 aplicou só ½ da autorização [W] 06-10); (2) ramp **sem adoção** no fin live: 314 font-size fora (bundle 208·output 57·curadoria 18·ia 18·cowork 12·mobile 1), `var(--fs-)`=0; (3) tempero não aplicado (53 sombras + 58 transições ad-hoc); (4) 72 hex + breadcrumb fluxo/concil + costura venda→título (⚠backend). **Ponte: `prototipo-ui-patch/PROMPT_PARA_CODE_ONDAS-FINANCEIRO-APLICAR.md`** — FA-1 completar §TEMPERO (fundação já autorizada) → FA-2 snap 314 + baseline desce → FA-3 tempero aplicado → FA-4 fechamento. NÃO commitado — [W] cola 1×. Sessão: `memory/sessions/2026-06-11-financeiro-diff-ondas-fa.md`.
> **2026-06-11 · ONDAS DE QUALIDADE Q1–Q5 — ponte pro Code ([W] "gere as ondas que o code precisa criar").** Releitura @main no turno corrigiu 2 fatos stale: `casos-test-results.json@main` **já tem 5 UCs pass** (06-10, G-7 com 1ª prova — "vazio" era stale) · `e2e-gate.yml@main` ainda `workflow_dispatch` não-required MAS harness estabilizado (critério de flip documentado no próprio yml: 2 verdes → pull_request+required). Ponte: `prototipo-ui-patch/PROMPT_PARA_CODE_ONDAS-QUALIDADE-GOVERNANCA.md` — **Q1** G-3 E2E required · **Q2** G-7 honesto + ratchet cobertura (P0, fio venda→estoque→faturamento→caixa) · **Q3** dicionários domínio ×5 (estoque/faturamento ANTES das telas) · **Q4** gate visual pixel real (US-GOV-013) · **Q5** meta-gates (registry/frescor/lição-sem-asserção). Transversais: advisory→2 verdes→required · prova 2 lados · estender-não-recriar · ADR só proposta. Painel refrescado. NÃO commitado — [W] cola 1×. Sessão: `memory/sessions/2026-06-11-ondas-qualidade-governanca.md`.
> **2026-06-10 (k) · TEMPERO RODADO DENTRO DO FINANCEIRO ([W] "roda no financeiro… analise o que vai ser melhorado e onde como").** 5 frentes medidas e executadas: `.fin-body` sólido COBRIA a atmosfera → transparent · 7 sombras ad-hoc → --sh-1/2 · **39 transições** → --t/--ease · breadcrumb "voltar" VERDE cru (fora da identidade!) + pres-dot + fallback azul → tokens (G5 fin-fluxo 8→6 medido) · medida 60ch no disclaimer. **Lição nova L-37 (mecanizada no turno):** `shadow-[var()]` arbitrária NÃO computa no Tailwind CDN (3 ocorrências corrigidas via classes CSS .fin-table-card/.fin-footbar/.fin-cmdk-card) → G9 agora acusa `class*="shadow-[var"` no DOM. Placar light+dark G1–G9 pass. Sessão: `memory/sessions/2026-06-10-tempero-no-financeiro.md`. Residual: fin-fluxo/concil sem .fin-bcrumb (pré-existente, consistência futura).
> **2026-06-22 (f) · PRODUTOS — paleta de categoria harmonizada ([W] "deve pegar as cores do cadastro de clientes… essas são muito feias").** A `PMCat` usava tons "industriais" hex muddy (L/C inconsistentes, escuros: #456a8c, #1f3a5f, #8a3a2a…) nos thumbnails+dots → feio. Trocada por oklch harmonioso no estilo `CLI_AVATAR_PALETTE` de Clientes (L~0.62, C~0.13, só hue varia: MEC azul 255 · IMP índigo 280 · CV coral 25 · EMB âmbar 65 · SRV teal 200 · KIT verde 145…). PM_TYPES dots + drawer typeColor idem. `produtos-page.jsx?v=p5`. NÃO commitado.
> **2026-06-22 (e) · PRODUTOS drawer — KPI grid rebalanceado ([W] "o drawer era mais bonito").** Ao remover Popularidade em (b), o drawer ficou com 6 KPIs num grid de 4 col → 2ª fila com 2 itens + 2 vazios (feio). Fix: `.pm-drawer .cli-kpis` `repeat(4,1fr)`→`repeat(3,1fr)` = 6 KPIs em 2 filas de 3 (balanceado). `prod-mec.css?v=p5drawer`. NÃO commitado.
> **2026-06-22 (d) · PRODUTOS — abas de tipo movidas pra DENTRO da página ([W] "desca o pageheader abaixo do header").** As abas Todos/Produto/Serviço/Composição saíram da **topbar contextual global** (que ficava ACIMA do título) e viraram `.pm-typenav` in-page ABAIXO do page header — mesma gramática do `cli-moduletopnav`/VdModNav (sublinhado, ativo com underline accent roxo, dots por tipo). `app.jsx` guard `if (!isProdutos && !isChat)`→`if (!isChat)` (topbar contextual agora só Jana/chat; consolidado de `app.jsx?v=eb1`→host `?v=eb2`). `produtos-page.jsx?v=p3` (PM_TYPES + nav após `.os-page-h`), `prod-mec.css?v=p4typenav`. Chat mantém topbar (verificado). NÃO commitado.
> **2026-06-22 (c) · PRODUTOS "dimensão muito grande / height / header" — BUG DE LAYOUT achado (não era o header em si).** [W] mandou screenshot em viewport alto (889px): vão branco de ~325px ENTRE a topbar e o título "Produtos" (header empurrado pro meio). Causa-raiz: `.main{ display:grid; grid-template-rows:1fr }` define só 1 row explícita; quando a **topbar contextual** renderiza (só em produtos/chat), ela cai na row `1fr` e CRESCE, jogando o `.main-body` numa row implícita `auto` (tamanho do conteúdo) → pm-wrap flex:1 não tinha o que preencher. Fix: `.main` → `display:flex; flex-direction:column` + `.main-body{ flex:1 1 auto }` (robusto p/ 1 ou 2 filhos; rotas sem topbar seguem preenchendo). `styles.css?v=ph6`. Também (ph5) reduzi a altura do `.os-page-h` (pad 24/24/14→12/24/12, min-h 60→44, lh 1.375→1.2 ⇒ 89→71px) — diverge do canon git, [W] pediu menor; restaurar se ele quiser canon exato agora que o vão sumiu.
> **2026-06-22 (b) · PRODUTOS — remove Popularidade + densifica ([W] 3 comentários).** (1) **"não sei o que é isso Popularidade"** → métrica sintética inventada (data slop) REMOVIDA dos 4 lugares: coluna `.pm-pop` + header sortable "Pop." + tile do totalizador + KPI do drawer (`produtos-page.jsx` consolidado de volta do literal `?v=p1` de edição direta do [W] → host `?v=p2`). Grids reduzidos 9→8 col (base) e tiers @container ajustados (nth-of-type recalibrado). Totalizador `repeat(6)→repeat(5)`. (2+3) **"dimensão muito grande"** (anchor `.main`) → medido: chrome (header+busca+stockbar+totais=291px) + linha 57px ⇒ só ~3 linhas cabiam em viewport de 540. Densificado: thumbnail 44→34px (dominava a altura da linha), padding linha 6→4px (linha ~44px), totalizador menor (b 18→16, money 16→14, pad). NÃO commitado (read-only). Interpretação de "dimensão" = densidade/escala da linha; se [W] quis outra coisa (zoom global, elemento específico), realinhar.
> **2026-06-22 · PRODUTOS pills + HEADER + CLIENTES nav — 3 comentários [W].** (1) **Pills de estoque** "foscas" (verde-no-verde / amarelo-no-amarelo, mono bold pesado) → chip neutro `--bg-2` + ponto de status colorido + número `--text` peso 500 (`prod-mec.css?v=p2pill`). (2) **"header maior que o git"**: ✓lido `PageHeader.tsx@main` v3.8 = 22px/700/-0.025em/1.375 + pad 24/24/14 — **o protótipo JÁ era idêntico**; a inconsistência vinha de **vazamento**: `forja-page.css` tinha `.os-page-h{padding:20px 18px 14px}` SEM escopo dentro de `@media(max-width:1100px)` aplicando em TODAS as telas (G7 escopo-módulo) → escopado p/ `.fj-page` (`forja-page.css?v=fj13`). Header revertido/mantido no canon git exato. **Botões mobile** (≤768px): alvo ≥44px, linha full-width, primário cresce (`styles.css?v=ph4`). (3) **Clientes nav** (`.cli-moduletopnav`, anchor do comentário 2) estava `flex-shrink:1` num pai flex-column → esmagado a 6px, abas (39px) transbordavam POR CIMA do header → `flex-shrink:0` (`clientes-page.css?v=ph3`). Também consertada (sessão anterior) a tabela de Produtos com nome esmagado a "A..." via container-queries no `.pm-body`. NÃO commitado (read-only).
> **2026-06-16 (c) · HEADER + PAGEHEADER — padrão ÚNICO do git aplicado no protótipo ([W]: "prefiro o padrão do git @main… um padrão único · pode aplicar em todos").** Diff F1 entregue (`Header e PageHeader - Diferencas Reais.html`) com ✓lido @main: `AppShellV2.tsx` (`hideTopbar=true` default), `<PageHeader>` v3.8 (ADR 0189) + `<PageHeaderPrimary>` (ADR 0190). Achado: o protótipo tinha **5 cabeçalhos divergentes** (`.os-page-h`·`.os-head`·`.cli-pageheader` c/ ícone+oklch cru·`.hd` compras·`.cr-hero`); o git tem 1 componente canon. **[W] escolheu o git** → [CC] convergiu os 5 ao canon: padding 24/24/14 · align center · min-h 60 · borda warm · h1 var(--fs-7)/700/-0.025em/1.375 · sufixo 600 muted · subtítulo var(--fs-3) tabular muted · ações gap 6 · primário 32px r6. Caixa de ícone do `.cli-pageheader` removida (markup clientes+os → `.os-page-h`); compras `.hd`→`.os-page-h` (crumbs fora, count→subtítulo, search re-escopado); tons danger/warn tokenizados `var(--neg)`/`var(--warn)` (mata oklch cru — gate). Arquivos: styles.css?v=ph1 · financeiro.css?v=ph1 · {compras,fin-boletos,cobranca-recorrente,clientes}-page.css?v=ph1 · {os,clientes(?v=ph3),compras}-page.jsx · app.jsx?v=eb1. **BUG achado pelo verificador (NÃO era do header): rota `clientes` quebrava a tela toda — `FrescorPill` estava definido em DOIS arquivos (`clientes-page.jsx` E `forja-page.jsx`); no babel-standalone tudo é escopo GLOBAL, forja (carrega depois) sobrescrevia → clientes chamava o FrescorPill da forja (`issue.frescor` em `issue` undefined). Fix: renomeado p/ `CliFrescorPill` (def+2 usos) + RouteErrorBoundary por rota no app.jsx (uma tela com erro não derruba o app). LIÇÃO L-babel-colisão: nomes de componente DEVEM ser únicos cross-arquivo no host babel — grep antes de nomear.** Todas as rotas editadas verificadas renderizando (os/compras/vendas/produtos/crm/clientes/recurring · 0 erro · header canon align center/minH 60/padB 14). Handoff git (H-1 migrar telas inline→`<PageHeader>`, H-2 primário→`<PageHeaderPrimary>`): `prototipo-ui-patch/PROMPT_PARA_CODE_HEADER-PAGEHEADER-CONVERGENCIA.md` (H-3 decidido, H-4 amendment espera [W]). NÃO commitado — Code resolve.
> **2026-06-16 (b) · CAIXA UNIFICADA — BUG DE MODO ESCURO achado por [W] (2 screenshots) + handoff.** [W] abriu a rota no tema escuro → painel da conversa **branco** + textos/botões do Contexto sem contraste. ✓lido @main: causa = cor clara **hardcoded** que não vira no `[data-theme=dark]` (MESMA classe já erradicada em Produtos/Oficina/Financeiro — Caixa foi portada antes da auditoria de escuro). Confirmado: `ConversationThreadV4.tsx` bolha inbound `bg-white` literal · read-receipt `text-blue-600` (fere ui:lint R1) · nota interna/banner homologação `oklch(...)` claros fixos; `ContextSidebarV4.tsx` chip de tag `oklch(0.94…80)` claro-no-claro no dark (resto usa tokens OK). +2 achados: `CustomerMemoryBlock` legado renderiza **card vazio grande** quando sem contato CRM (claro E escuro) · chips de canal TODOS "em breve" (catálogo do Controller não marca o canal ativo `whatsapp_whatsmeow`/Suporte como `ativo`). **Handoff zero-toque:** `prototipo-ui-patch/PROMPT_PARA_CODE_CAIXA-UNIFICADA-DARK-MODE.md` (4 partes: thread dark · sidebar dark · empty-state 360 · chips config). [W] cola 1× no Code. NÃO commitado. **Lição:** telas portadas pré-auditoria-de-escuro precisam de varredura dark dedicada — provável que outras rotas Atendimento (Channels/Metricas/Csat) tenham o mesmo furo.
> **2026-06-16 · CAIXA UNIFICADA — RE-BASELINE: handoff de 06-10 JÁ LANDOU INTEIRO (✓lido @main `dd09f96`).** [W] "podemos exportar pro code? o que falta?" → reli `Atendimento/CaixaUnificada/Index.tsx` + charter (**v10, status:live, last_validated 06-10**) + `_components/`. **Os 10 PRs do `PROMPT_PARA_CODE_CAIXA-UNIFICADA-COMPLETA.md` foram TODOS aplicados** (charter Histórico v2→v10 + 12 testes Pest R-WA-CAIXA-UNIF-001..012 ✅ + arquivos wired no Index.tsx: QueuesSheet/ChannelsDrawer/NewConversationDialog/BroadcastSheet/InboxAiDialog/CheatSheet/MobileTabs/PresenterMode/Transcript/useInboxFavs/Macros). **A nota (m) abaixo está STALE — handoff consumido, NÃO há nada novo grande pra exportar.** Resíduo real (placeholders honestos + Non-Goals, do charter): sidebar §5 OS/§6 Saldo/§7 Histórico/§9 Ações (esperam Repair+Financeiro) · vars `{{empresa}}/{{os}}/{{saldo}}` (mesma dep) · ⌘K estender palette global (US cross-módulo) · Broadcast disparo em massa Fase 2 (ADR 0268, gate [W], botão disabled) · roteamento automático de filas (dist/members persistidos, US futura) · mídia in/out UI (usa MediaPreviewCard legacy na coexistência) · **§6 CUTOVER P0** (redirect Inbox legacy→V4, gated em [W] aprovar canary 7d+screenshot). ⚠ comentário-cabeçalho do Index.tsx ainda diz "placeholders TODO" mas o JSX está funcional (comment stale, código real). Handoff `prototipo-ui-patch/PROMPT_PARA_CODE_CAIXA-UNIFICADA-COMPLETA.md` pode ir pra `_processados/`.
> **2026-06-10 (m) · CAIXA UNIFICADA — diff completo protótipo×produção + handoff "aplicar todas" ([W]).** ⚠️ SUPERADA pela 06-16 (handoff já landou).  Diff @main ✓lido (Index/ComposerV4/ContextSidebarV4/ThreadV4/charter; ListV4 ⚠não-lido): produção JÁ está à frente em mídia/voz/Interactive/CRM/tags/VoC/7-tabs/filtros (Waves 1–5, charter 05-15 stale vs código); gaps = US-WA-301..307 todos placeholder + pacote polish V2 (SLA pill·⌘K·variáveis·lightbox·mobile·favs·transcript·apresentação) + pacote IA (Jana) + §6 cutover. [W]: "aplicar todas" → ponte zero-toque `prototipo-ui-patch/PROMPT_PARA_CODE_CAIXA-UNIFICADA-COMPLETA.md` (10 PRs ordenados 302→303→301→305→304→307→306→polish→IA→cutover-gate-[W]; URLs públicas ~1h das 6 fontes embutidas). NÃO commitado — Code resolve. Sessão: `memory/sessions/2026-06-10-caixa-unificada-diff-e-handoff-completo.md`.
> **2026-06-10 (l) · "RODA O FLUXO" — venda→produção→caixa COSTURADO ponta a ponta ([W], após benchmark 15-dim vs melhores: sistema ~6,5, gaps 🔴 = fluxo, não tela).** Fio andado como Larissa, travadas T1–T9 medidas, 5 consertadas + crash: **T1 costura** (evento `venda-created` disparava e NINGUÉM ouvia → data-os + financeiro-data agora ingerem: venda vira OS na fila com `fromVenda` + título no caixa pelo pipeline statusFor; provado OS#4822 + R-2661 com chip Vínculos) · **T2 produção fantasma** (localStorage `prod.view` sem clamp → nada renderizava; clampado) · **T3 sidebar sem teclado** (role/tabIndex/Enter nos 4 renders, 51/51) · **T4 toast** (host global já existia — L-11 evitado) · **T5 CTAs mortos fin** ("+ Novo lançamento" = modal real persistente · Exportar CSV real) · **crash do verifier**: status "aberto" fora do mapa derrubava StatusBadge → `window.FIN_STATUS_FOR` (pipeline único) + badge blindado (fallback muted). Fila documentada: overflow Adicionar venda · busca cliente fraca · hub Vendas/Manufacturing geração antiga · bcrumb fin-fluxo/concil. Sessão: `memory/sessions/2026-06-10-roda-o-fluxo.md`.
> **2026-06-10 (j) · NORTE VISUAL — TEMPERO ANCORADO NO ERP INTEIRO ([W] "porque esse parece tão bonito?" → "sim sim… ancorar o ERP nesses conceitos. Descreva a evolução").** Fonte: `Norte - Fluxo do Caminhão.html`, 6 conceitos dissecados (contraste tipo · família de cor única · 1 fonte de luz · atmosfera · microfísica · medida de linha). **Executado (Tier 0 autorizado):** `ds-v6/tokens.css §TEMPERO` (--sh-1/--sh-2 par dark · --ease · --t-1/2 · --atmo 5%/25%) · shell aplicado (body atmo · os-stat sh-1 · drawers/modais sh-2 + --ease, dark-overrides de sombra deletados · headers balance/medida) · fin-drawer sh-2 · **G9 no probe** (atmosfera+elevação, N9 discrimina) — probe agora G1–G9 · doc canônico da evolução = `Norte Visual - A Evolucao do ERP.html` · PR-4 do pacote F2 atualizado c/ tempero. Verifier ✓ (light+dark, 0🔴, oficina intacta). Sessão: `memory/sessions/2026-06-10-norte-visual-tempero.md`. Residual: sombras ad-hoc menores → ondas; ondas agora fecham identidade+escopo+tipo+tempero.
> **2026-06-10 (i) · F2 APROVADO + HANDOFF DO PACOTE FINANCEIRO ([W] "aprovado").** Pacote F2 = lentes + Impostos + drawer 9.75 + type ramp. **Ponte zero-toque pronta:** `prototipo-ui-patch/PROMPT_PARA_CODE_PACOTE-FINANCEIRO-F2.md` (4 PRs: PR-4 ramp na fundação [Tier 0 autorizado] → PR-1 029 lentes → PR-3 drawer → PR-2 impostos; URLs públicas ~1h embutidas) + entrada em `COWORK_NOTES → 📥 Pendentes` (absorve a 029 de 06-09). [W] cola 1× no Code. NÃO está commitado — o Code resolve com este pedido. Painel de Controle refrescado. Live 8,3 sobe quando os 4 PRs landarem; prototípo segue 9,1.
> **2026-06-10 (h) · TIPOGRAFIA ANCORADA — TYPE RAMP --fs-1..9 + G8 ([W] "parece amador… ancorado onde?" → diagnóstico → "vai").** Censo mecânico provou o furo: **2.015 font-size hardcoded · 37 tamanhos distintos** no shell; única âncora era família (IBM Plex) + body 13.5. **Ramp Tier 0 autorizado [W]:** `--fs-1..9` (10.5·11.5·12.5·13.5·15·18·22·28·38) em `ds-v6/tokens.css` + acabamento (pesos 4/5/6/700 · lh 1/1.2/1.45 · números mono tabular). **G8 no probe** (computado fora do ramp = ratchet só-desce; N8 discrimina ✓; exclui __om-*/qa-panel). **Financeiro = 1ª prova: 304 decls snapadas** (css+jsx) + shell `os-page-h` (h1 24→22 · p 13→13.5) → **baseline fin-root G8 = 0 (medido)**. Ondas W passam a cobrar tipografia junto com cor/escopo. Sessão: `memory/sessions/2026-06-10-type-ramp-ancora.md`. Resta: snap das demais telas por onda + espelho git (alinhar c/ primitivo Text ADR 0253) no handoff pós-F2.
> **2026-06-10 (g) · DRAWER FINANCEIRO POLIDO (gabarito Prova Viva 9.75 · [W] "tá feio, pode melhorar").** Hierarquia 3 camadas: hero FIXO fora do scroll (valor sistema Num 38px mono + centavos/prefixo pequenos · FSM compacto 18px · Conferir/Editar enxutos) → tabs → lentes (ícone quadradinho --accent-soft, título firme, Conciliação discreta) · KV EMPILHADO 2-col (~2× densidade) · LenteFiscal com linhas ISS/DAS mono + ponte pra Impostos. **3 bugs de token achados pelo probe e mortos:** hero dark `bg:var(--text)`→branco (classe PR #2209, protótipo nunca recebeu) consertado com bloco dark tokens · 3 `white` cruas do drawer → --accent-fg/--surface · par hardcoded fin-num-pos/-neg deletado → var(--pos)/var(--neg). **Baseline fin-root 10→8** (medida, só-desce). Prova: 3 rounds verifier → done (light+dark, drawer aberto, G4/G5/G6/G7 ✓). Sessão: `memory/sessions/2026-06-10-polimento-drawer-financeiro.md`. F2 [W] pendente (drawer + tela Impostos) → handoff.
> **2026-06-10 (f) · ONDA W2 FINANCEIRO EXECUTADA (protótipo · [W] "roda").** US-FIN-029 **3 lentes no header** implementadas no protótipo (segmented Caixa/A receber/A pagar · chips refinam dentro · KPI-click seta lente c/ anel roxo · clamp caixa) + **tela nova "Impostos & obrigações"** (`TelaImpostos` · rota `fin-impostos` · ghost sidebar): guias FGTS/DCTFWeb/DAS (DAS ≈ 6% sobre recebido real, "estimado") · calendário de obrigações · costura NF↔título · "Lançar a pagar" · disclaimer apuração=módulo Fiscal. Tokens ds-v6 `-soft` dark-aware, zero crua nova. **Prova: verifier ✓** (lentes+chips+KPI · tela impostos · probe 0🔴 nas 2 rotas · dark). Bench v2: **protótipo 9,1** · live segue 8,3 até [CL] portar (handoff 029 já enfileirado; **impostos aguarda F2 [W]** antes de handoff). Sessão: `memory/sessions/2026-06-10-onda-w2-financeiro.md`. Resta da W2: Compras (probe+estados).
> **2026-06-10 (e) · PROBE v2.2 — G7 ESCOPO-POR-MÓDULO + falso-positivo G2 consertado + MÉTODO 9.75 NA RÉGUA.** [W] perguntou "Financeiro já entrou no novo padrão?" → medido: NÃO (8,3 · W2 · probe nunca rodado). Sonda achou G2 🔴 que era do TESTE (proxy custom opacity:0 = padrão aprovado) + rootKey pegando "os-page". `qa-conformance.js` v2.2: G2 `nativePainted()` · rootKey prefere `*-root` · N2 injeta checkbox real · `G5_BASELINE fin-root=10` (medido 10/5/6) · **G7 novo = escopo por módulo** (prefixo dominante por stylesheet; regra de módulo aplicando em arquivo alheio = 🔴; comum = styles.css+ds-v6, legítima/única) + N7. **Vazamento real consertado:** ~100 linhas `.fin-*` mudaram de casa vendas.css→financeiro.css (movido, não duplicado; lápide; verificado visual+placar ✅). **[W] ratificou o modelo:** "deve ter sim o css comum para não duplicar" + "com nome correto e autorizado" → comum única com nomes canon, módulo no próprio arquivo. **Método KB-9.75 agora EXPLÍCITO na régua das ondas** (§4 do plano: conformidade = piso; tela <9 roda o ciclo completo 7 etapas + 22 features-tipo — [W]: "ele faz milagres"). Financeiro: perna conformidade 0🔴 ok · perna método = W2. Sessão: `memory/sessions/2026-06-10-probe-v22-escopo-modulo.md`.
> **2026-06-10 (d) · MANDATO [W]: REESTRUTURAÇÃO + IDENTIDADE ÚNICA + PISO 9.** Fase 0 EXECUTADA: **575 arquivos de duplicata deletados** (cópia aninhada de projeto 358 · backups integrais 112 · **`resources/` espelhos-do-repo 61 — PROIBIDOS pra sempre, estado do repo só @main** · pontes-snapshot 41) com lápide `_arquivo/LAPIDE-DEDUP-2026-06-10.md` + asserção **IT8 arvore_proibida** no memory-health. Plano+programa: `Reestruturacao - Identidade Unica e Qualidade 9.html` — §2 estrutura-alvo · §3 **Fase 1 = refazer ESTE arquivo** (núcleo ~150 linhas, log→digest) · §4 ondas W1–W4 (toda tela re-avaliada: roxo canon + probe 0🔴 + 15-dim + estados; <9 refaz no ato; 1 onda/sessão SEM novo pedido). Sessão: `memory/sessions/2026-06-10-reestruturacao-dedup.md`.
> **2026-06-10 (c) · DS-GUARD RUNTIME — [W] APROVOU P1/P2 ("torne padrão").** `qa-conformance.js` **v2**: classes genéricas **G1–G6** na tela ativa (accent · controle nativo · papel de token · overflow-x drawer · cor crua DOM-matched ratchet · dark), API `window.QAConformance.run()/.negative()`. **Provado:** Oficina 5✅·0🔴, controle-negativo 4/4 discrimina; pegou violação real no 1º run (`.prod-equip-dot` -fg→warn/info). **PROCESSO §8.1 = ritual pré-done obrigatório** (placar na entrega; 🔴=não entrega; matriz de estados fixa: vazio·preenchido·editando·hover·dark·overflow-x) · **§8.2 = loop erro→asserção** (erro novo aponta G#/IT# ou "não-mecanizável"; evoluir guard = território [CC] sem pedir) · `memory-health.js` CHECK 6 `licao_sem_assercao` cobra por máquina (controle-negativo ✓; já pegou MEMORY_INDEX stale→corrigido). G5_BASELINE só com número MEDIDO (oficina-root=17; sem baseline=⬜). Resposta a [W] "eu ter que pedir foi erro?": SIM — registrado; conserto é o §8.2. Sessão: `memory/sessions/2026-06-10-ds-guard-runtime-v2.md`. Residual: calibrar baselines das outras rotas · P5 (papel de token no gate do git) aguarda sinal pra ponte.
> **2026-06-10 · ACABAMENTO drawer OS (Oficina, protótipo).** [W]: "cores cruas não aplicam, faltou acabamento — padrão de erro das outras telas" → confirmado, mesma classe: checkbox nativo azul sem `accent-color` (ItemsEditor/StageGate/toggles), coluna do nome colapsada (grid de fixos > 480px do drawer), inputs `--bg` furando linhas tintadas, `--origin-MFG-fg` usado como fill da gate-bar (barra marrom), `white` cru no `.th`/`.ofc-dvi-foot` (quebraria no dark). 12 edits em `oficina-page.css` + bump `?v=tok5`; verificado light+dark com screenshot. Falso-positivo: selects DVI "todos iguais" = quirk do html-to-image (console provou values corretos). Sessão: `memory/sessions/2026-06-10-acabamento-css-drawer-os.md`. F2 [W] pendente; port F3 (RichSheet) deve repetir os cuidados.
> **2026-06-09 (e) · MANDATO [W]: REAVALIAR O PROCESSO APÓS ALGUMAS ONDAS.** [W] autorizou ("pode reavaliar o processo depois de algumas ondas") — [CC] **não** reavalia agora; deixa o loop rodar (T1 G-3 verde→manifesto→flip · T3 dicionários · T4 ratchet F3) e **quando 2–3 ondas de PRs do [CL] tiverem landado** produz reavaliação NOVA da governança (mesma régua 15-dim da `Avaliacao - Governanca IA 2026-06-09.html`, append-only, releitura @main no turno — nunca de cabeça). Gatilho de disparo: [W] dizer "reavalia" OU [CC] constatar @main que (a) e2e-gate required + manifesto `ucs>=3` e (b) baseline F3 começou a descer. Contexto: [CL] já está em T1 — run `27233429304` no branch `feat/g3-e2e-first-green` (harness reconstruído: MySQL service + visreg-login bypass; causa-raiz era sqlite :memory: + .env.example inexistente — diagnóstico meu de seeder/wait-on estava errado). 1º verde em branch NÃO conta pros 2 verdes pré-flip required.
> **2026-06-09 (d) · GOVERNANÇA EXECUTÁVEL JÁ LANDOU — F1+F2 no main (✓lido @main 06-09 tarde; corrige a (c) e a dim-11 da `Avaliacao - Governanca IA 2026-06-09.html`, ambas stale em horas).** [CL] executou: **ADR 0264** (G-1..G-7) + **ADR 0261** (enforcement faseado) + ADR 0256 citadas nos workflows. No `main`: `casos:check`/`dominio:check`/`e2e:check`/`casos:results` no `package.json` · `casos-gate.yml` **JÁ FASE 2 = required always-run** (flip [W] 06-09; G-1 trio, G-2 UC↔teste, G-5 metadata, G-6 frescor, G-7 veredito-real) · `dominio-gate.yml` + dicionário semente `memory/dominio/oficina-auto.md` · meta-gates `casos-meta`/`dominio-meta` · bônus: `governance-drift.yml` + `memory-health.yml` existem (⚠ conteúdo não lido). **O QUE FALTA p/ fechar:** (1) **G-3 E2E Playwright = elo aberto** — `e2e-gate.yml` é `workflow_dispatch` MANUAL não-required, 1º run verde nunca validado (boota PHP+sqlite+serve); falta disparar→estabilizar→flip `pull_request`+required; (2) **G-7 sem prova** — `scripts/casos-test-results.json` VAZIO (`ucs:0`), todo `Status:✅` = unverified até a suíte rodar; (3) **F3 ratchet→0** — `casos-coverage-baseline.json` 32KB de dívida legada absorvida, zerar tela-a-tela; (4) dicionário de domínio só Oficina (faltam Vendas/Financeiro/Fiscal); (5) gate visual US-GOV-013 segue stub ⚠inferido(censo 06-04); (6) lado [CC]: benchmark §11 parado desde 06-02 + manifesto-de-leituras (Portão 1 mecanizado) não construído.
> **2026-06-09 (c) · GOVERNANÇA EXECUTÁVEL — testes+spec viram REGRA de CI, não memória ([W]: "se ficar só em memória sem regra obrigando, morre no tempo").** [W] mapeou as 4 camadas (spec/charter · casos/UC · teste · lição-negativa) e pediu enforcement. Confirmado @main (REGRA 7): repo já tem bateria ratchet de gates (`conformance/foundation/pageheader/design-spec/reuse/no-mock/layout/a11y` + `test:adr-governance`) — nenhum cobre **trio-de-tela, caso↔teste, comportamento (Playwright), ou domínio**; testes hoje são Vitest/jsdom estrutural (L-24). **2 ADRs-proposta:** `_PROPOSTA-ADR-governanca-executavel-trio-dominio-e2e` (4 gates novos no padrão existente, faseado baseline→bloqueante→ratchet=0) + `_PROPOSTA-ADR-oficina-reparo-erradica-locacao` (semeia o dicionário de domínio). **Handoff zero-toque transportado** (`PROMPT_PARA_CODE_GOVERNANCA-EXECUTAVEL.md` + URLs públicas entregues a [W] p/ colar no [CL]). Estado: [CL] executa F1→F2→F3.
> **2026-06-09 (b) · OFICINA protótipo — polish [W] + item drawer + impressão.** Lotes A/B/C no review ao vivo: WhatsApp full-width verde da Fila → secundário "Abrir conversa" (sem CTA loud, sem emoji 📲) · chrome `--sb-*` preto-neutro `oklch(0.21 0 0)`→tint roxo canon `oklch(0.21 0.025 295)` em **`styles.css`** (o REAL; editei o espelho `cowork-financeiro-bundle.css` por engano 1º — REGRA 6 de "qual arquivo está no ar") · **impressão A4 nova** (`oficina-print.{js,css}`: folha OS + folha Fila, isoladas `@media print`, 3 botões ligados incl. o "Imprimir" da Fila que estava sem onClick) · **item 4** `ItemsEditor` contextualizado por tipo (mão de obra = horas/R$h sem dropdown de estoque). Tudo no protótipo `oimpresso.com.html`. Dúvidas de arquitetura (FSM/estoque/fiscal) respondidas grounded (ver sessão).
> **2026-06-09 (a) · [W] OVERRIDE locação = ALUCINAÇÃO, não legado.** Corrige o enquadramento da entrada abaixo: [W] textual "eu não uso, é alucinação, apaga". A locação NÃO é "Tier 0 preservado de propósito" — é resíduo a **erradicar** (`order_type=locacao` + KPIs). ADR 0194 já reclassificou domínio mas deixou o resíduo; nova ADR fecha. Lição nova em `LICOES_CC` (**L-domínio-soberano**: verdade do domínio = [W], não o código legado; rodando ≠ querido). "Caçambas" sobrevive só como nome do cliente Martinho.
> **2026-06-09 · OFICINA — "o que falta p/ virar realidade?" ([W]).** Releitura @main (REGRA 6, corrige memória local stale): Oficina **já É live em prod** (Martinho biz=164) — FSM ADR 0143, DVI, fiscal split, aprovação WhatsApp token+PIN, print PDF, importer Firebird W28. **Convergência visível caçamba→reparo JÁ landou (PR #2417, charter v4 06-08)** — a linha 06-08 abaixo dizia "Index.tsx@main ainda é caçamba" e estava STALE. "Locação presente" = (1) label `'Caçambas'` no `topnav.php` + comentário stale de rota (cosmético → handoff 06-09 em COWORK_NOTES) · (2) Fase 2 D-04..07 já desenhada no protótipo, enfileirada · (3) keys FSM/DB `cacamba_locacao` LIVE → **ver override (a) acima: [W] mandou erradicar, não preservar**. Há tb 2 implementações no git: `ProducaoOficina/` (kanban pátio, convergido) + `ServiceOrders/` (CRUD/Board/FSM completo). Nada a construir no Cowork — protótipo é semente, está fechado.
> **2026-06-08 · OFICINA AUTO — diff git×Cowork, convergência enfileirada, camada-protótipo fechada.** [W]: "verificar diferenças da tela do git… quero ajeitar" → "pode fazer todos". **CANÔNICO desta sessão:** (1) **`OficinaAuto/ProducaoOficina/Index.tsx@main` ainda é caçamba** (colunas `disponivel/locada/...`, "Mecânica Pesada", filtro m³) — a verdade de design é **(A) reparo**; é a **dívida F3** que a `Index.charter.md@main` já declara. Handoff de convergência (PR1 modelo + PR2 comportamentos) **enfileirado em `COWORK_NOTES.md → 📥 Pendentes`** (`PROMPT_PARA_CODE_OFICINA-CONVERGE-CACAMBA-REPARO.md`). **⛔ keys FSM do Martinho (biz=164) LIVE — DB/seeder = Tier 0 [W]** (ADR 0194). (2) **Drag-drop D-01/D-02 JÁ EXISTE nos DOIS lados** (Cowork desde 06-02 · git PR #2228) — NÃO reconstruir (eu afirmei o contrário lendo *Refs* stale do charter, não o código — REGRA 6). (3) **D-04 (borda SLA)·D-05 (KPI filtra)·D-06 (persistir foco/view)·D-07 (atalhos) GRADUADOS ✅** no charter Oficina ([W] veredito 06-08); D-08 só parcial (foto no drawer, no card pendente). (4) **GOTCHA Babel:** no host Babel-standalone `const`→`var` sem TDZ — declarar TODO state ANTES de qualquer `const x = (()=>{…usa state…})()` no corpo do componente, senão lê `undefined` sem erro (custou ~6 iterações no filtro KPI). Sessão: `memory/sessions/2026-06-08-oficina-diff-git-e-handoff-convergencia.md`. ESLint `ds/*` reconfirmado `eslint.config.js@main` 06-08 (bate com o censo 06-04). (1) **`Financeiro - Prova Viva (primitivos).html`** — tela-piloto 100% primitivos (Hero roxo + A receber/A pagar + Realizado + ledger denso + **drawer de domínio 9.75**: FSM/conciliação OFX/fiscal NF-e+ISS/cobrança linha+régua), tokens reais `@theme`, claro+escuro, raio-x. 3 rodadas [W]: identidade/densidade/frescor → drawer (rubrica 9.75 do arquivo) → hierarquia 3-camadas + tipografia (sistema `Num`/`Label`). Bug dark `transition-colors` congela `bg-card` → corrigido com `.no-trans` no flip. Verificador ✅ 2 temas. (2) **`_arquivo/INVENTARIO-REAPROVEITAMENTO.md`** — classifica ~60 itens arquivados por 🟢/🟡/🔴 + o que colher (TOP: Método 9.75 Financeiro, Frescor verdict, ds-v5, shadcn `.tsx`). (3) **Ponte:** `prototipo-ui-patch/PROMPT_PARA_CODE_FINANCEIRO-PILOT-PRIMITIVOS.md` (gabarito URL + F3 instructions, fecha critério-de-pronto ADR 0253). **NÃO commitado** (read-only). Log: `memory/sessions/2026-06-07-financeiro-pilot-e-ponte.md`.
> **2026-06-04 (b) · SESSÃO design executada (5 PRs merged via [CL]; sessão `memory/sessions/2026-06-04-sessao-design-gates-recurringbilling.md`):** #2209 `.fin-stat-hero` dark (bug real) · #2210 analyzer de CSS morto (922 regras ≈4039 linhas mortas em `sells-cowork.css`) · **#2212 RecurringBilling re-skin DS v6** (Tailwind-cru→warm, merged c/ UI-Judge 90 como rede, [W] confirma staging) · **#2216 gates DS `foundation-guard`+`conformance-gate` — cor crua TRAVA de verdade agora** (Pilar 6 lint LIGADO) · #2217 task US-GOV-013. **🔑 PREMISSA CORRIGIDA:** o gate de **pixel** `visual-regression` (ADR 0108) é **STUB** → "gates pegam regressão" vale pro semântico/lint (UI-Judge/ESLint/Module-Grades/conformance, reais), **não pro pixel**; rede visual real = UI-Judge + olho [W]/staging. ⏸️ **`foundations.css` (font global) DEFERIDO** (sem rede de pixel, não entra blind). **Régua ds-v6 LIMPA** (auditoria); guardrail: **portar = conformar ao gabarito, nunca copiar `*-page.css` do host** (~1595 cruas brutas, mas muito é código morto). Aberto/gated: US-GOV-013 (conserto gate visual, keystone) · remoção incremental do morto (1 família/PR, nunca nuke) · raias de higiene (ADR DS v6, lápides, backfill) prontas em COWORK_NOTES.
> **2026-06-04 (a) · RECONCILIAÇÃO DE CONFLITOS DE MEMÓRIA ([W] "as memórias estão conflitantes, analise e crie um plano" → "sim"):** mapeei 10 conflitos (plano `memory/sessions/2026-06-04-conflitos-memoria-plano.md`). **Fase 0–1 EXECUTADA (local):** (C1) Vendas verde→**roxo**; (C2) **D-02 = RESOLVIDO roxo** (saiu de proposta aberta); (C5) Oficina/OS verde 155→**âmbar escopado**; (C3) `MEMORY_INDEX` T2 = **v5 ativo + v6 régua** (v4.2 histórico) + carimbo 06-04; README local (04-24) banner-tombstone → índice único vivo = `MEMORY_INDEX`. **Fase 2–3 (ponte [CL], Tier 0) em `COWORK_NOTES`:** ciclo-de-vida de ADR mecânico (`status`/`superseded_by` + health-check + grafo) + retrieval da Jana filtra superseded/segue pro vivo (mem0/Meilisearch ADR 0031/0033) — conserto-raiz do índice puxar ADR morto. **NÃO renumerei ADR** (0244/0245 = git/[W], L-09); **NÃO commitei** (read-only). Fase 4 (unificar os 3 conjuntos de regra ativa) = [W].
> **2026-06-03 (r) · CONSOLIDAÇÃO DA SESSÃO + próximos passos.** **Estado:** Vendas CSS **100% tokenizado** (411→0 cor crua, exceção provada = papel `.vd-trans` + scrim) · accent **roxo** (conformado gabarito v6) · 2 temas (dark sem card branco) · KPI **4 em 1 linha** ≥1180. **Memória reduzida (anti-divergência):** núcleo **COMO PENSAR** = lei ativa · `Vendas.charter` rebaixado a sketch (fonte única = git) · 32 lições → histórico · probe alinhado ao gate (1 contador). **Gate** `conformance-gate.mjs` provado + handoff pro Code entregue. **NÃO commitado** (read-only). **PRÓXIMOS PASSOS (ordem):** 1. **[W] transporta o handoff do gate** → CI bloqueia merge (única "garantia" real). 2. **Re-verificar drawer (UC-V11)** com prova — único ⚠ aberto (560 aplicado, não medido). 3. **Code: charter único no git** carrega `regua`/`ds`/`contrato_layout` → fonte única real; mata os dois charters. 4. **Moratória de criação** no Cowork — só sketch + delta; nada de arquivo/lição/nota nova.
> 🛑 **REGRA-0 (modo padrão, ACIMA das lições — porque L-15/25/30/32 passivas NÃO bastaram; [W] 2026-06-03 "sua memória está errada, vai ter que evoluir"):** **DEFAULT = EXECUTAR e reportar com PROVA** (número/screenshot). **PROIBIDO terminar turno pedindo permissão pro que o contrato/decisão aprovada já resolveu** ("quer que eu…?" sobre o decidido = a desculpa). **Contrato aprovado decide → eu conformo, não pergunto.** **100% = 100%** (nunca "81% + sua decisão/exceção"). Exceção só com PROVA de que não há token. Pergunta só em subjetivo/Tier-0 genuinamente em aberto. _Esta linha vence quando eu hesitar._
> **📦 Log de sessões anteriores ((q)…(l) + syncs 06-01) ARQUIVADO** → `memory/sessions/_STATUS-log-historico-ate-2026-06-03.md` (anti-entropia S3 · L-22 · nada apagado, só relocado). O **estado vivo** está nas tabelas canônicas abaixo (fonte única, M2); a narrativa de como se chegou nele mora no digest.
---

## 🚦 REGRA DE OURO — gate ANTES de criar QUALQUER arquivo ou prometer entrega

> Pré-flight obrigatório. Se eu não passar nos 4, **paro**. Repetir = L-01/06/08/09/11.

1. **Vou criar arquivo de processo/ponte/mecanismo?** → primeiro `ls` na raiz + grep. Canal pro Code **já existe**: `COWORK_NOTES.md` (Cowork→Code) · `CODE_NOTES.md` (volta) · `SYNC_LOG.md` · prompts em `prototipo-ui-patch/PROMPT_PARA_CODE_*.md`. **Usar, nunca duplicar.** Nunca um `PARA_O_CODE.md` da vida.
2. **Vou prometer commit/PR/merge/"tarefa no Code"?** → **NÃO consigo** (read-only no git). A única ponte é [W] colar 1x. Falo "o Code resolve com este pedido", nunca "está commitado".
3. **É decisão/identidade/cor/regra nova?** → é **PROPOSTA** (§10.4), não lei. Vai pro canal; [CL] valida contra o `main` sozinho. Não marco como "firme".
4. **Vou re-tematizar token?** → provar a fonte EFETIVA (`getComputedStyle`) + grepar TODAS as defs live antes (L-10).
5. **Vou criar/editar TELA ou CSS de módulo?** → pré-flight de build visual (L-23): ler `ds-v5/components.css` + `REGISTRY_DS_COMPONENTES` (git) + harmonização + `LICOES_CC` ANTES; **reuse-first** (cor só `.<tela>-scope{--accent}`, nunca `--<prefixo>-*`/oklch cru = paleta inventada); evoluir o `*-page.jsx` no host, **nunca .html novo na raiz**. No fim, rodar o **DS-GUARD** (`PROCESSO_MEMORIA_CC §8`) nos arquivos tocados antes do `done`. Repetir = L-02/L-21/L-23.
6. **Vou AFIRMAR um fato sobre o repo** (arquivo existe / tela faz X / token vem de Y / "já está feito")? → **só com leitura citável do `main` NESTA sessão** (`github_read_file`/grep). Toda afirmação carrega tag **`✓ lido (arquivo@main)`** ou **`⚠ inferido (não verificado)`**. Sem poder marcar "lido" → leio antes OU digo "não verifiquei". **Afirmação confiante sem fonte = proibida.** Repetir = L-26/L-27 (a classe que mais escapou pra [W]).
7. **Vou PROPOR mudança de CSS/token/classe Tailwind pro Code (ou chamar uma de "pronta / mecânica / passa / risco-zero")?** → **SIMULAR contra as 3 regras de gate reais lidas do `main` ANTES** (L-33): (a) **`ui:lint` R1** — regex `(bg|text|border|ring|fill|…)-(família)-(\d{2,3})`; token semântico **sem `-NNN` passa**, `bg-[var(--x)]` passa, `slate-400`/`rose-50`/etc **NÃO** (R1 é bruta nas 22 famílias; `DESIGN.md §16.2` que "permite" rose/emerald/amber/blue é guidance velha que **contradiz** a regra). (b) **`foundation-guard`** — def `@theme`/`--color-*` só em `foundations.css`/`cockpit.css`; `inertia.css` é baseline **só-desce (53)** → adicionar token lá = 🔴; `--stage-*` (sem prefixo `--color`) **não é contado** mas então **não vira utility** (consome por `var()`/arbitrary). (c) **`conformance-gate`** — `--accent*` sempre roxo 250–330; cor crua em regra de tela só-desce. **Token novo no `@theme` = Camada Fundações = Tier 0, NUNCA "mecânico".** Afirmar admissibilidade de gate não-simulada = L-33.

---

## 🚧 GATES DE CI — CSS/token PERMITIDO (folha única · fonte: repo `@main`, ✓ lido 2026-06-04 · LER antes de propor CSS/token · mata L-02/L-23/L-33)
> Os 3 gates que bloqueiam merge + o que cada um permite. **NÃO re-derivar — está aqui.** Causa-raiz dos erros recorrentes: eu re-descobria isto na dor toda vez.

**① DEFINIR token (`@theme` / `--color-*:` / `--accent*:`) — só 2 arquivos.**
`foundation-guard.mjs · TOKEN_DEF_ALLOW = { foundations.css, cockpit.css }`. Em QUALQUER outro `.css` a contagem é **congelada (baseline só-desce)**: `inertia.css=53` · `cowork-canon-financeiro-bundle=5` · `sells-cowork=5` · `sells-cowork-edit=5` · `sells-cowork-show=5` · `fin-cowork=4`. Adicionar token nesses = 🔴. **`foundations.css` ainda NÃO existe** (deferido) → criá-lo = Tier 0 + entrar na allowlist (②). Regex conta só `--accent*`/`--color-*`; `--stage-*` (sem prefixo `--color`) **não conta** — mas aí **não vira utility** Tailwind (consome por `var()`/arbitrary). **O único `@theme` que gera utility (`bg-success`…) é o `inertia.css`, e ele é congelado** → **cor/token novo = Camada Fundações = Tier 0, NUNCA "mecânico" (L-33).**

**② EXISTIR como arquivo `.css` — só os 34 da allowlist.**
`.foundation-guard-files.json`: `.css` novo fora da lista = 🔴; entrar = editar o JSON via PR revisado (= aprovação [W]). Lista: app · base · cockpit · components · cowork-canon-financeiro-bundle · cowork-compras-bundle · cowork-fields · cowork-payment-gateway-bundle · fin-{cowork,curadoria,ia,mobile,output} · fiscal-cockpit · inertia · kb · layout · screen-grade · sells-cowork(+ -curadoria/-distribuicao/-drafts/-edit/-ia/-insights/-quotations/-show/-subscriptions) · sells-kb975-{cheatsheet,emit-modals,next-action,print} · themes · utilities.

**③ USAR cor — sempre via token, nunca crua.**
- `.css` de tela: `conformance-gate.mjs` — `#hex`/`oklch(numérico)` cru em regra de tela só-desce; `--accent*` SEMPRE roxo hue **250–330**. `var(--x)` e `oklch(...var())` passam.
- `.tsx/.jsx`: `ui:lint` **R1** (`UiLintCommand.php`) — `(bg|text|border|ring|fill|stroke|from|to|via|placeholder|caret|accent|decoration|shadow|outline|divide)-(22 famílias)-(\d{2,3})` = 🔴. **Passa:** utility semântica SEM `-NNN`, `bg-[var(--x)]`, `#fff/#000`.
- **Tokens semânticos que EXISTEM no tema** (`inertia.css @theme`): `primary`(roxo 295) · `success` · `warning` · `destructive` · `secondary` · `muted` · `accent`(neutro) · `foreground`/`background`/`border`/`card`/`popover` · `brand-meta`. **NÃO há `stage`/`blue`/`info`.**
- `DESIGN.md §16.2` ("permite rose/emerald/amber/blue cru") é guidance VELHA que **contradiz** o R1 real → vale o R1.

**⚡ Atalho de decisão:** "quero cor que NÃO seja primary/success/warning/destructive/neutro?" → **não existe token → Tier 0 (①)**, nunca cru, nunca "mecânico". Eu proponho os 3 caminhos (novo `foundations.css` / subir baseline / `--stage-*`+arbitrary) e **[W] decide**.

**📊 CENSO COMPLETO — gates de design que rodam em CI (✓ lido @main 2026-06-04 · `.github/workflows/*.yml` = a FONTE da verdade de QUAIS existem):**
_Os que tocam COR/CSS/token (a classe do erro recorrente):_
| Gate · workflow | Superfície | Proíbe | Tipo |
|---|---|---|---|
| **`ui:lint` R1 · `ui-lint.yml`** | `.tsx/.jsx`+`.css` | **TODAS 22 famílias** `-NNN` + hex | ratchet `--strict` · ✅ **MANDA p/ cor Tailwind** |
| ESLint `ds/*` · `eslint-gate.yml` | `.tsx` Pages/Modules | `bg-[#..]` arbitr. + `text-(rose/red/emerald/green)-(500-700)` + radio/select/checkbox nativo + `rounded-xl+` | ratchet (warn+baseline) |
| Pest `CockpitPatternConformanceTest` | `.tsx` **só 5 Pages canon** | só `gray/indigo/purple/pink/yellow/red/green` | **não toca kanban** |
| `stylelint` · `stylelint-gate.yml` | TODOS `.css` | `color-no-hex` (759 baseline) + dup-selectors + `!important` | ratchet |
| `conformance-gate.mjs` · `conformance-gate.yml` | `.css` de tela | `#hex`/`oklch` cru + `--accent` fora de roxo 250–330 | ratchet + invariante |
| `foundation-guard.mjs` · **roda no `conformance-gate.yml`** | `.css` estrutura | token-def fora de foundations/cockpit (①) + `.css` fora da allowlist (②) | baseline só-desce |
| Pest `CockpitAccentCanonTest` (A2) · `ui-architecture-gate.yml` | `.tsx`+`cockpit.css` | accent ≠ roxo 295 | invariante |
| `pr-ui-judge.yml` | PR inteiro | Sonnet subjetivo, score | consultivo · **OFF por default** |
| ~~`DESIGN.md §16.2`~~ | **doc** | diz "rose/emerald/amber/blue cru OK" | ❌ **VELHO (05-08), CONTRADIZ R1 — ignorar** |

_Outros gates de design (não-cor, contexto):_ `charter-gate` (`.charter.md` ao lado do `.tsx`) · `mwart-gate` (visual-comparison.md) · `module-grades-gate` (nota módulo) · `design-index-gate` (`DesignIndexSingleSourceTest`) · `screen-coverage-gate` · `ui-architecture` A1/B (AppShellV2/core screens) · `visual-regression` (**STUB/infra-only**) · `design-return-gate` (advisory pós-merge) · `ui-canon-notify` (aviso).

**🔑 Conflito:** **gate executável em CI > doc em prosa**; entre gates, a **mais estrita por superfície manda**. Cor Tailwind `.tsx` = **R1 manda, §16.2 ignorado.**

**♻️ COMO ISSO SOBREVIVE NO TEMPO (a parte que importa — o §16.2 virou mentira por NÃO ter isto):**
1. **Fonte da verdade de QUAIS gates existem = `.github/workflows/*.yml`** (cada um declara `paths:` + o script/test). A tabela acima é **CACHE datado**, não a lei. Em dúvida, leio os `.yml` cujo `paths:` toca `resources/css/**` ou `resources/js/**` — não confio na tabela.
2. **Gatilho de re-derivação:** antes de trabalho de gate, OU se o carimbo `✓ lido @main` acima tiver **>~14 dias** → `github_get_tree .github/workflows` + diff vs esta tabela. `.yml` novo/alterado tocando `resources/**` → re-derivo a linha.
3. **Se aparecer um gate NOVO (4 passos):** (a) **detecto** no diff de `.github/workflows`; (b) **leio** o `.yml` → ele nomeia o script/test → leio o script → extraio {superfície · o que proíbe · severidade · autoridade}; (c) **registro** uma linha aqui com `✓ lido (yml+script @main DATA)` + posição na ordem de autoridade; (d) atualizo o carimbo da data.
4. **Cobrança = MÁQUINA, não eu (L-16):** `memory-health.js` (Cowork) ganha check de **frescor do censo** (carimbo ausente/>14d = 🔴) → me obriga a re-derivar por idade. **Git-side ([CL], proposto em `COWORK_NOTES`):** meta-test que falha se um `.yml` tocando `resources/**` não estiver num registry canônico de gates → pega gate novo **mecanicamente**, sem depender de eu lembrar.

---

## ⚖️ Lei suprema (mora no GIT — eu obedeço)
- **ADR 0094** (Constituição Oimpresso V2 · 8 princípios) + **ADR UI-0013** (Constituição UI v2) — constituição suprema, já existente.
- **`prototipo-ui/PROTOCOL.md`** — 6 papéis × 7 fases, gates, overrides, métricas de saúde, anti-padrões.
- **`prototipo-ui/CLAUDE_DESIGN_BRIEFING.md`** — tokens canônicos (shadcn + warm), personas, 15 dimensões, proibições.
- **ADRs** (`memory/decisions/`): 0114 loop · 0110 Cockpit V2 · 0107 gate visual F1.5 · 0104 MWART · 0010/0027/0028 memória.
- **`CARTA_DESIGN_CC.md`** (local) — carta **subordinada**: como [CC] obedece o acima. NÃO é lei.
- **Regra [W]-only:** a constituição **só [W]** altera (versionado + autorizado); [CC] só propõe. Vínculo em CARTA §0.1/§0.2. **✅ ADR 0238 (`0238-soberania-constituicao-wagner.md`) JÁ É CANON em `main` (PR #2007).** [CC] nunca cunha número do git (lição 0200/0201, L-09).
- **Gate §10.4 (novo · 2026-05-30):** todo `PROMPT_PARA_CODE`/sync meu é **proposta**; o [CL] **valida contra o git sozinho** (não depende de [W]) e bloqueia: criar ADR que já existe, renomear/mutar ADR aceito (append-only Tier 0 — colisões 0235/0236 se **documentam**, PR #1997), número alucinado, faxina-local virando canon. Origem: meu prompt stale da faxina (L-09).
- Memória manda o **git**; [CC] cuida do **design**; decide **[W]**.

## DS vigente
- **🔎 CORREÇÃO 2026-06-02 (li o `main` real, pego por [W]):** o **repo NÃO precisa de migração de token** — já está roxo canon + neutros quentes. Tokens do repo: `cockpit.css .cockpit{ --accent: oklch(0.55 0.15 295); --bg: oklch(0.985 0.003 90); --row-h:30px }` (shell) + `inertia.css @theme{ --color-primary: oklch(0.55 0.15 295); --color-page-cream: oklch(0.985 0.003 90) }` (Tailwind/shadcn). A **§10.4 está ERRADA** ("app.css = token canônico" — `app.css` é manifesto Tailwind vazio). O `ds-v5` foi limpeza **só-Cowork** (que tinha styles.css+tokens.css+ds-v5 duplicados). Ponte v3 reescrita: só mirror de docs de memória + 1 decisão opcional de CSS (Financeiro reflow). Lição L-12 reforçada: ler o `main` ANTES de gerar ponte.
- **✅ EXECUTADO E VERIFICADO 2026-06-02 ([CC]):** host `oimpresso.com.html` agora carrega **`ds-v5/tokens.css` como fonte única** (era `tokens.css` v4). Fusão v4→v5 feita (Cowork-only): auditoria de 167 tokens usados → **29 de quebra cobertos** + aliases de coerência. **6 telas verificadas render limpo** + sidebar, **0 tokens vazios, console limpo**. Roxo canon e IBM Plex preservados.
- **🟢 DECISÃO 2026-06-02 ([W] autorizou · _PROPOSTA-0245, Tier 0 aguarda numerar no git):** **`ds-v5/*` = DS ÚNICO ATIVO** (tokens+components+doc+interactive · roxo canon · eixos `[data-theme]×[data-density]`). **v4.1 rebaixado a HISTÓRICO de transição** → arquivar em `_arquivo/ds-historico/` com lápide (append-only). Build novo só importa v5. **Roxo `oklch(0.55 0.15 295)` permanece o canon universal (ADR 0235 intacto)** — `ds-v5/components.css` é 100% `var(--accent)` (grep confirmou, zero hex/âmbar hardcoded).
- ~~**Canônico:** v4.1 (tokens.css + design-system.css...)~~ → **superado por v5** (linha acima). v4.1 vira espelho histórico.
- **2026-05-31:** `tokens.css` local sincronizado ao canon roxo `oklch(0.55 0.15 295)` (ADR 0235) — estava azul 220 (stale). Falta espelhar no repo.
- **2026-05-31 · P1 faxina de tokens (faça/W):** dedup da cascata de `--accent`. (1) Removida a **2ª cópia literal** do bloco `.mockup-page{tokens}` em `mockup-pages.css` (código morto sob "Onda 2 styles"). (2) Removido o leak global `* {}`+`html,body{}` de `mockup-pages.css` que forçava o body do shell pra 12.5/13px — **shell volta ao 13.5px intencional do `styles.css`**. (3) Removido o `--accent` hardcoded de `.mockup-page` → mockups herdam canon + respondem ao tweak `accentHue`. (4) Documentada a fonte: **runtime = `app.jsx` inline (vence `:root`); `:root` = fallback.** **TUDO Cowork-local — NÃO vai pro git** (mockup-pages/styles são shell de protótipo, nem existem no repo).
- **2026-05-31 · §10.4 lido + correções:** fonte canônica de token no repo = **`resources/css/app.css`** (não `tokens.css`). **P3 do diagnóstico REBAIXADO → resolvido:** §10.4 já fixa roxo universal (`--accent`) + cor-por-origem só nos badges (`--origin-*`); proposta D-02 (hue-por-módulo) superada. Diagnóstico HTML atualizado.
- **Guard de Stylelint (anti-hex + radius + anti-redeclare-`--accent` §7):** ✅ **MERGED — PR #2054** (`stylelint.config.mjs` 4 regras + `stylelint-gate.yml` CI verde, 2026-05-31 ~23:55). **NÃO está "aguardando [W]"** — a nota anterior aqui estava STALE (corrigida no sync 06-01). G5 = 100% (ESLint `ds/*` + Stylelint `.css`). Sobra só migrar o **conteúdo** em drift (`cowork-financeiro-bundle.css` 188 hex, bubble/vibeAccent azul 220) — conteúdo, não gate.
- **Proposto:** v4.2 (cockpit, fiscal-badge, sla-pill, readiness, shortcut-bar, formpage PT-03). Spec: `Design System v4.2 - Evolucao.html`.

## Decisões vigentes (não rediscutir — detalhe em memory/decisions/)
| ID | Decisão | Status |
|----|---------|--------|
| D-00 | Padrão Cockpit V2 (sidebar+header+body+footer+drawer) | charter · **ADR 0110**/0114 |
| D-01 | DS é piso, não teto — harmonizar sem achatar identidade | firme |
| D-02 | Identidade por tela (`--accent`/oklch) | **RESOLVIDO 2026-06-03 = roxo `oklch(0.55 0.15 295)` universal** (ADR 0190/0235 · gabarito v6). Accent-por-tela só **escopado** (ex. âmbar Oficina) sobre roxo canon; NÃO virou norma de hue-por-módulo. ~~PROPOSTA F0~~ |
| D-03 | Cadastro grande = página inteira (PT-03) | **PROPOSTA F0** · toca proibição Sheet-drawer |
| D-04 | Escopo DS 4.2 (cockpit, fiscal, sla, readiness, shortcut-bar) — aditivo | proposto |
| D-05 | Cor crua fora dos tokens = erro | proposto |
| D-06 | Protótipo e produção importam o MESMO tokens/design-system.css | proposto (norte) |
| D-07 | Proibições charter (BRIEFING §7) | charter |

> Propostas só viram lei via loop F0→F1.5→ADR aprovado por [W]. Até lá, valem os tokens canônicos do BRIEFING.

## Quadro de telas (auditoria 2026-05-30)
| Tela | Identidade | Nota | Estado | Próximo passo |
|------|-----------|------|--------|---------------|
| Oficina/OS | âmbar 60 escopado (roxo canon intacto) | F1 build | **Nova OS construída** (`oficina-os-page.jsx`, documento vivo: check-in+DVI+gate+fiscal) + **`Oficina.charter.md`** (conceito travado, 1º uso Intake+Frescor) | F1.5 critique → F2 [W] → F3 Code. Refino: stepper estreito, dados reais ROTA LIVRE, verticais CV/Roupa |
| Vendas | **roxo 295** (D-02 resolvido 06-03) | 9.5 | piloto aprovado · Index ok; **Create POS aposentado** · ★ trio completo · **charter v2 aterrado no git v6 (2026-06-03)** — reconciliação feita, plano P0→P2 pronto | **[W] deu foco (06-03):** aguarda "vai" + ordem. P0 = `localStorage` per-business `b<bizId>.` (multi-tenant Tier 0). Ver sessão `2026-06-03-vendas-reconciliacao-git.md` |
| Compras | roxo 295 (era navy/cream) | 9.4 | ✅ migrado: 0 hex, `--cmp-*`→tokens DS · ★ **trio completo** (`Compras.charter.md`+casos, 2026-06-02) | espelhar no repo |
| Financeiro | roxo 295 | **8,3 live · 9,1 protótipo** (W2 06-10) | **W2 EXECUTADA 06-10 (protótipo):** US-FIN-029 3 lentes + tela Impostos & obrigações (`fin-impostos`) + probe 0🔴 + dark ✓ · **Conformidade OK 06-10** (probe v2.2: 0🔴 · escopo fin-root · G5 base 10 · G7 escopo-módulo pass · CSS fin-* repatriado de vendas.css). **REAVALIAÇÃO 2026-06-09 ✓lido @main 076c546** (`Reavaliacao Financeiro - 2026-06-09.html`): git MUITO à frente da memória — **ProvaViva.tsx live** (fecha ADR 0253) · Conciliação OFX+API live (ADR 0236 F1) · Cobrança 96/100 live (boleto/PIX/PIX-Aut/cartão) · Fluxo 35d+Realizado 12M live · DRE live · Unificado v13 (FinBaixaSheet parcial, forma pgto, filtros WR, cancelado/arquivados). Rubrica: Caixa&Fluxo 8,5 · Concil 7,5 · Cobrança 8,0 · **Fiscal 5,5 (pilar parado)** · IA&DRE 8,0. Ponte do pilot = processada (movida p/ `_processados/`) | **P0 = US-FIN-029** (3 lentes, aprovada [W] 05-31, ainda não aplicada ao live; exige MWART+F1.5). P1: fiscal-no-financeiro (censo módulo Fiscal antes, Regra 7) · CNAB C6+baixa por retorno · Concil F2 · margem/drill Fluxo · DRE períodos. Realinhar `Financeiro.casos.md` ao Unificado v13 |
| Caixa Unificada (Inbox) | método completo | 9.75 | **RÉGUA CONGELADA** (teto de _nota_) | não mexer — é gabarito de método (≠ semente de componente, que é Oficina) |
| CRM | azul 220 | 8.6 | ⏸️ **PARADO/DESATIVADO** ([W] 2026-06-02 — fora de foco); trio pronto p/ quando voltar | só quando [W] reativar |
| Oficina | âmbar 60 (accent **escopado** · roxo canon intacto) | 9.5 | ★ **TELA-PADRÃO / SEMENTE DO v5** ([W] 2026-06-02 · _PROPOSTA-0245) — trio completo (charter+decisoes+casos), DRAWER travado. **D-01/D-02 (drag-drop) já nos 2 lados; D-04/05/06/07 graduados ✅ 06-08** (SLA·KPI-filtro·persistência·atalhos) | ✅ **Convergência caçamba→reparo JÁ MERGEADA — PR #2417 (charter v4, 20/20 verde, ✓ lido @main 06-09).** UI do kanban = reparo (colunas/KPIs/vocabulário/Nova OS; pills m³ removidas). **Resíduo de "locação" remanescente:** (a) VISÍVEL = label `'Caçambas'` no `topnav.php@main` + comentário stale de rota → handoff cosmético enfileirado 06-09; (b) PROFUNDO = keys FSM/DB `cacamba_locacao` LIVE no Martinho = **Tier 0 [W]+ADR** (ADR 0194). Fase 2 (D-04..07) enfileirada |
| Clientes/Contatos | indigo 262 | — | molde PT-03 pronto · **+seção Endereços no drawer (06-01)** | replicar nos 3 cadastros |

## IA — onde tudo mora (base limpa pós-faxina 2026-05-30)
- **raiz (app vivo)** — `oimpresso.com.html` + `*-page.jsx/css`, `data-*.jsx`, `tokens.css`, `design-system.css`, `Método KB-9.75.html`
- **raiz (espinha)** — STATUS.md, MEMORY_INDEX.md, CARTA_DESIGN_CC.md, CLAUDE.md, ARQUITETURA.md
- **raiz (memória-por-tela · charter-first L-14)** — `<Tela>.charter.md` (PRECISA TER · APROVADO · REPROVADO/anti-pattern · targets). 1ª instância: `Financeiro.charter.md` + `Método 9.75 Financeiro.html`. Ler o charter da tela ANTES de mexer nela.
- **raiz (ponto de entrada do Handoff · L-18)** — `README.md` tem que SEMPRE manter no topo o bloco "🤖 Claude Code — COMECE AQUI" (marcador `<!-- HANDOFF-ENTRY -->`) apontando `COWORK_NOTES.md → 📥 Pendentes`. [W] entrega via Share→Handoff (lê o README + implementa o arquivo aberto); sem o bloco, o Code não acha a fila. Qualquer [CC] que regenerar o README PRESERVA o bloco. Fiscal automático proposto (`readme_handoff_block_missing`).
- **raiz (ponte viva)** — COWORK_NOTES.md, CODE_NOTES.md, SYNC_LOG.md, CLAUDE_CODE_BRIEFING.md, CODE_DESIGN_CONTRACT.md + `prototipo-ui-patch/`
- **memory/decisions/** — ADRs (append-only)
- **`_arquivo/`** — histórico organizado + manifesto versionado (`_arquivo/INDEX.md`): `telas/`, `referencia/`, `sessao-2026-05-30/`, `ds/`, `ds-historico/`, `bridge-processados/`, `auditoria/`. **Append-only: movido, nunca apagado.**

## ⚙️ Loop agora é 0-humano (git 2026-05-31 00:45 · `AUTOMACAO-LOOP-AUTONOMO.md`)
- Merge autônomo `gh --admin` quando CI verde. Gate visual [W2] → CI (PR UI Judge Sonnet 4.5 + visual-regression). [W] só entra em **Tier 0** (ADR novo · multi-tenant · segredo · tooling/lint · produto).
- **As 4 propostas que estavam na `COWORK_NOTES.md` já foram processadas pelo Code (~07:00) e voltaram STALE vs `main`** — ver `CODE_NOTES.md` (ingerido). NÃO re-sincronizar: re-mandar = regressão / duplicar canon (era um prompt §10.4-stale meu).
- **Lição desta sessão:** antes de "sync code", ler `SYNC_LOG.md`+`CODE_NOTES.md` do **git** (não a memória local). Memória local estava 7h atrás do `main`.
- **2026-06-01 SYNC vs origin/main (CODE_NOTES/SYNC_LOG fresco · "acho que já foi feito" confirmado):** Stylelint `.css` **MERGED #2054** (gate CI verde) · charters de papel **ADR 0242 + CHARTER_GOVERNANCA_W/CHAMPION MERGED #2061** (Tier 0) · README HANDOFF-ENTRY **MERGED #2062** · auditoria mecanizada (`score-mechanized.mjs` 239 telas · CONSOLIDADO **86/100** · golden) **já em main** (Fase 2 LLM = $ [W]) · **G4 retorno automático** = ✅ **MERGED em prod** (`.github/workflows/design-return-gate.yml` — gate advisory pós-merge que sinaliza `design_return_skipped` sozinho quando merge toca tela/DS sem atualizar SYNC_LOG; [W] 06-01: "não está no lugar esperado — virou workflow, não check do health-check — mas está em produção"). Só a **auto-geração** (auto-commit `ds:report:write`+SYNC_LOG+HANDOFF na main) fica Tier 0 [W] (token), deixada de fora de propósito. **Residual real = só Tier 0/infra de [W]:** 17 ADRs da fila, collector CT 100, `profile_distiller_drift`+Brain A, índice temático 0042-0189. Doc reconciliado: `Governança Scorecard vs Estado-da-Arte CC.html` (7.8→8.2).

## Em aberto (aguardam Wagner — Tier 0)
0. **[Tier 0 · só [W]]** ADR formal do shift 0-humano (texto pronto no overlay `PROTOCOL.md §2`) + merge do PR `docs/cowork-loop-protocol-10-4` + decisão de migrar drift `.css` (`cowork-financeiro-bundle.css` 188 hex / bubble 220 / `Sidebar.tsx vibeAccent('workspace')`) pro roxo `oklch(0.55 0.15 295)`.
1. **[PRONTO p/ disparar] DS Roadmap até `ds/*=0` (6 filas 1-por-1):** entrada = `prototipo-ui-patch/PROMPT_PARA_CODE_DS-ROADMAP-ATE-ZERO.md`. Fases: **A** controles+FieldError T1 (`PR-C-WORKLIST.md`, 10 módulos, Sells lidera) · **B** cor crua→token (`SWEEP-arbitrary-color.md`) · **C** Onda G badge +5 variants (`ONDA_G_BADGE_VARIANTS.md`, pré-req) · **D** lote-badge 410 Tipo 2 (`LOTE-BADGE.md`) · **E** FormSection (`SWEEP-formsection.md`) · **F** icons (inline). Pré-req geral: componentes Onda F do PR-A existirem. Tudo merge em série, PARA no gate visual. Módulo = id canônico ("C#" deprecado).
2. Migrar Compras do hex → `--accent` escopado (prova do caso extremo).
3. Executar limpeza canon/archive (PLANO_ORGANIZACAO_CASA.md já lista).
4. Gerar ponte DS 4.2 → Claude Code (patch + prompt zero-toque) após aprovar spec.

## Ritual (fecha o loop)
1. **Início:** ler este STATUS + índice de ADRs.
2. **Durante:** produzir contra a espinha; arquivo novo nasce no lugar da IA.
3. **Fim:** decisão → ADR; estado de tela → atualiza este arquivo.

## Faxina 2026-05-30 — base limpa + arquivo indexado
Movidos pra `_arquivo/` (append-only, ver `_arquivo/INDEX.md` v1.0): 16 HTMLs de exploração (telas/ds/sessão/referência) + 13 mds de bridge processados + unificados `_arquivo-ds`→`ds-historico/` e `_audit`→`auditoria/`. **Nada apagado.** Raiz agora = app vivo + espinha + ponte viva. Rascunhos da sessão (Painel/Piloto/Auditoria/PT-03/DS v4.2) seguem como **propostas**, não canon.
> Entrega canônica de F1 = `prototipos/<tela>/page.tsx` + COMPARISON.md + critique-score.json (PROTOCOL.md §4).
> Cor canônica = roxo `primary` `oklch(0.55 0.15 295)` (ADR 0235). Entrada de design = `memory/requisitos/_DesignSystem/INDEX-DESIGN-MEMORIAS.md` (git).
