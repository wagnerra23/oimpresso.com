{
  "_nota": "Contrato de tela do Painel da Jana (/ia). Segundo contrato ATIVO do repo — o primeiro é caixa-unificada. Roda no CI advisory via contrato-de-tela.yml. Ver RUNBOOK-contrato-de-tela.md.",
  "tela": "Jana/Index",
  "fonte": "resources/js/Pages/Jana/Index.tsx",
  "_nota_campo_fonte": "⚠️ `fonte` aqui aponta pra TELA VIVA, não pra um protótipo. O modelo do `--map` assume que todo contrato nasce de um protótipo Cowork versionado; este nasceu da tela em produção, e o campo é o caminho real pra não mentir nem quebrar o check (`fonteOk = !!c.fonte && existsSync(...)`). Lido no mapa, o par Index.tsx → Index.tsx significa exatamente isso: a fonte da COPY é a própria tela. ⚠️ CORRIGIDO 2026-08-14 — a redação anterior dizia 'porque esta tela NÃO TEM protótipo': isso era verdade em 2026-08-10 e DEIXOU de ser em 2026-08-11 (#5572), quando o `jana-merge.jsx` desceu pro git. A tela TEM protótipo; o campo segue apontando pra tela viva porque a COPY deste contrato foi derivada dela, não porque falte fonte de design. Apontar `fonte` pro protótipo muda o que a máquina lê e a copy que ela compara — é decisão [W], não conserto de nota.",
  "_nota_fonte": "HISTÓRICO — fato datado, preservado. A âncora ERA `prototipo-ui/cowork/chat-jana.jsx` e foi REMOVIDA do charter em 2026-08-10 (lápide §5 do mesmo dia). Aberto no navegador, aquele protótipo se identifica como `OFFICEIMPRESSO · biz=164 · v1404 legacy migrado` e traz o KPI `Frota utilização` mais `caçambas` no texto. Veredito [W] 2026-08-10, textual: 'Frota utilização são alucinação, ninguém usa até a presente data' — isso governa o que se CONSTRÓI nesta tela do núcleo e SEGUE VALENDO. Medido na época: interseção de vocabulário com a tela viva = 1 de 10 termos (10%), contra 80% do par bom `Compras/Index`. Por isso a copy deste contrato deriva da TELA VIVA.",
  "_nota_fonte_atualizacao": "CORRIGIDO 2026-08-14 — dois pontos da nota acima apodreceram. (a) O charter NÃO declara mais `n/a`: desde 2026-08-11 (#5572) esta tela TEM protótipo versionado, e o `Jana/Index.charter.md` (v6) declara `related_prototype: prototipo-ui/cowork/jana-merge.jsx` — arquivo DIFERENTE do `chat-jana.jsx` acima. O ponteiro vigente se resolve por `node prototipo-ui/ancora.mjs Jana/Index`, nunca por esta nota. (b) O CRITÉRIO 'carimbo biz=NNN desqualifica o protótipo' foi REVOGADO pela lápide §5 de 2026-08-11 — é falso-positivo por construção; a prova é ESTRUTURAL (o protótipo desenha OUTRA tela?). O veredito sobre o `chat-jana.jsx` continua de pé por esse critério novo: ele desenha o cockpit de cobrança, não este Painel. Ressalva viva no `jana-merge.jsx`: ele cita 6 `Analise*Service` que não existem no repo (o `ancora.mjs` acusa) — as regras VISUAIS dele valem, o que ele diz sobre FONTE DE DADO não.",
  "alvo": [
    "resources/js/Pages/Jana/Index.tsx",
    "resources/js/Pages/Jana/_components/JanaPlanoBadge.tsx"
  ],
  "secoes": [
    {
      "id": "painel-cta-conversar",
      "_papel": "ação primária da tela — o charter diz que a Jana conversa, não que ela decide sozinha",
      "copy": [
        "Conversar com a Jana"
      ],
      "_nota": "2026-08-17 — a copy desta secao era \"Conversar agora\" e vivia DENTRO do `ProximaAcaoCard`, bloco declarado mock no proprio codigo (\"Mock pra demo\") que [W] mandou remover ao pedir a tela igual ao prototipo. A ancora nao foi perdida: passou pro botao REAL que ja levava pra /ia/conversa. A copy acompanha o hospedeiro — \"Conversar com a Jana\" e a que o botao real sempre teve, e e a mesma que a secao painel-metas-vazio ja pinava."
    },
    {
      "id": "painel-metas-header",
      "_papel": "cabeçalho do bloco de metas",
      "copy": [
        "Metas ativas",
        "Acompanhamento contínuo"
      ]
    },
    {
      "id": "painel-metas-vazio",
      "_papel": "estado vazio — hoje é o estado REAL de 100% dos tenants (0 metas cadastradas em qualquer business, medido 2026-08-09)",
      "copy": [
        "Nenhuma meta cadastrada ainda",
        "Conversar com a Jana"
      ],
      "estados": [
        "vazio",
        "com-metas",
        "aguardando-apuracao"
      ]
    },
    {
      "id": "painel-meta-apurando",
      "_papel": "meta cadastrada mas sem apuração ainda — não pode mostrar zero como se fosse resultado",
      "copy": [
        "Aguardando apuração…"
      ]
    },
    {
      "id": "painel-meta-sem-historico",
      "_papel": "sparkline sem série — mesma regra: ausência de dado se declara, não se desenha como zero",
      "copy": [
        "Sem histórico"
      ]
    },
    {
      "id": "painel-plano",
      "_papel": "estado comercial da area — diz em que plano o BUSINESS esta, e e a porta pro Configurar",
      "copy": [
        "plano Pro",
        "plano Grátis"
      ],
      "_nota": "Copy LITERAL do prototipo aprovado (`jana-merge.jsx:970` — `plano {pro ? \"Pro\" : \"Gratis\"}`), com o 'plano' em minuscula. Nao e frase minha: veio da ancora, e por isso pode ser pinada aqui. AS DUAS entram porque o selo tem dois estados e ambos sao contrato — um deles some se alguem trocar o ternario por um texto fixo, que e exatamente a regressao que este item defende. PROVENIENCIA DO DADO (2026-08-28): o selo le `jana.pro`, derivado de `jana_pro_module` na assinatura; ate 2026-08-27 ele nao existia porque NAO HAVIA fonte de plano no sistema (PARIDADE §8.1). Nao confundir com `jana_module`, que e o modulo on/off."
    }
  ],
  "ordem": [
    "painel-metas-header",
    "painel-cta-conversar",
    "painel-metas-vazio"
  ],
  "_pendente_w": [
    "BOTÃO \"Exportar\" — o que SOBROU desta pendência. ATUALIZADO 2026-08-31: o `(em breve)` SAIU do `title` (que agora é só `Exportar relatório`), e a âncora sustenta a remoção — no `jana-merge.jsx` o gatilho é `<button className=\"jm-btn ghost\">Exportar</button>`, sem tooltip nenhum; o rótulo visível já era `Exportar` dos dois lados. ⚠️ O QUE NÃO FOI RESOLVIDO é o fork original: o botão segue MUDO — sem `onClick`, sem `href`, sem `disabled`. Tirar a promessa não o fez entregar; ele clica e nada acontece SEM explicação, que o `UC-JPAIN-16` cataloga como PIOR que o `(em breve)` (a lista `$conhecidos` do `PainelContratoTest.php` segue contando `Exportar` como dívida declarada). As opções continuam sendo [W]: vira `disabled` com o motivo, ou entrega. A âncora mostra a FORMA que a entrega teria — um menu de 3 itens (`Painel em PDF`, `Metas em CSV`, `Fatos da memória (LGPD)`), os três marcados \"fora deste protótipo\". Segue FORA de `secoes`: pinar `Exportar` enquanto ele é mudo congela uma affordance morta.",
    "CONFIGURAÇÃO QUE O SERVIDOR NÃO HONRA: o drawer do UC-10 deliberadamente não oferece brief diário on/off + hora, áudio/TTS nem retenção — os três existem no protótipo (`jana-merge.jsx` §JmConfigDrawer) e NENHUM é honrado pelo back hoje (medido 2026-08-17; o brief é server-side via BriefingAgent, o TTS não existe, e o `jana:retention-purge` foi descartado por [W]). Devem virar configuração de verdade? Aí é backend: ou estende o dono que já existe (`PATCH /ia/alertas/config` → `business.essentials_settings.alertas`, per-business, Tier 0), ou o brief ganha chave própria. É produto, não wiring.",
    "ORDEM DO SELO: `painel-plano` entrou em `secoes` (presenca + copy ficam vigiadas) mas NAO em `ordem`, porque ordem e copy sao decisao de [W] (ver `_nota_ancoras`) e eu so tinha proveniencia pra copy. No prototipo o selo vem na zona DIREITA do header, depois de 'Atualizado HH:MM' e antes de Configurar/Exportar (`chat-jana.jsx:217`). Se essa posicao e contrato, basta inserir 'painel-plano' no array `ordem` — a ancora ja existe no fonte."
  ],
  "_nota_abas": "ABAS DA ÁREA (2026-09-02) — a barra de abas NÃO é elemento deste .tsx: nasce dos `ghosts` do `DataController` (Jana) e é renderizada pelo `JanaSubNav`/`PageHeaderTabs` compartilhados, então não recebe `data-contract` aqui (mesma razão do título do shell, `_nota_titulo`). Fica REGISTRADA como contrato de ORDEM e COPY, lido da âncora `jana-merge.jsx` §JmTabs (SYNC re-medido na data): `Painel · [Metas] · Conversa · Alertas · Ações · Memória · [Plataforma]` — Metas só quando `metasMode=aba` (aqui é seção do Painel) e Plataforma só com `jana.superadmin`. ESTADO: `Alertas` (ghost `alertas`, 3ª, contrato `jana-alertas.contract.json`) e `Ações` (ghost `acoes`, 4ª, contrato `jana-acoes.contract.json`) entraram; `Plataforma` é o próximo PR da mesma onda (handoff 2026-08-31 §Paridade Painel). Quem prova a ordem é o teste `UC-ALERTA-00` (AlertasContratoTest), não esta nota.",
  "_nota_titulo": "RESOLVIDO 2026-08-31 — era o `_pendente_w` nº 1 (\"Qual é a palavra?\"). A resposta estava na âncora o tempo todo: o `jana-merge.jsx` rotula a aba `{ key: \"painel\", label: \"Painel\" }` e carimba a tela com `data-screen-label=\"Jana — Painel\"`. APLICADO: o `AppShellV2` passou a receber `title=\"Jana — Painel\"`. Trocou-se a PALAVRA, não o padrão — o prefixo de área fica porque é o que a âncora escreve e é a convenção medida nas telas irmãs (`Jana — Memória`, `Jana Pro — Ativar`); pôr `Painel` cru divergiria das duas. A outra metade da pendência (\"o breadcrumb diz Dashboard\", \"sobrou em 2 lugares\") era PONTEIRO PODRE: o breadcrumb já não existe nesta tela — o array foi removido como dado morto, e a razão está no comentário logo acima do `AppShellV2` no `Index.tsx`. O que ainda carrega a palavra `Dashboard` é identificador de componente React (`export default function Dashboard`) e prosa de comentário, não copy que o usuário lê. NÃO entrou como seção `painel-titulo` (o parêntese da pendência previa isso): MEDIDO rodando o gate — `X seção \"painel-titulo\" sem âncora data-contract no alvo`. O título é PROP do `AppShellV2` compartilhado, não elemento desta tela; criar um `<span data-contract>` só pra satisfazer o gate é exatamente o que o `_nota_alvo` recusa. Se virar contrato, a decisão [W] é ONDE ancorar — não é wiring.",
  "_nota_metas_header": "MEDIDO 2026-08-31 — divergência REAL, não corrigida aqui porque copy pinada é lei [W]. A copy `Acompanhamento contínuo` desta seção NÃO tem contraparte na âncora: no `jana-merge.jsx` o cabeçalho de metas (`JmMetasSecao`) é só `METAS ATIVAS` mais os controles (período · Farol/Cadastro · Nova meta), SEM subtítulo — `jm-h2-sub` aparece uma única vez no protótipo inteiro, e é do bloco `ANÁLISES PRINCIPAIS`, não deste. Registro porque um pedido de 2026-08-31 pediu pra alinhar \"o subtítulo das análises\" trocando ESTA copy pela do bloco de análises: seria errado — poria uma instrução de análises (`clique num card…`) sobre o cabeçalho de METAS, e o protótipo não põe subtítulo nenhum aqui. O subtítulo das análises JÁ está idêntico ao protótipo (medido byte a byte em `_components/JanaCockpit.tsx` contra `jana-merge.jsx`), e não é pinado por este contrato. As saídas, se [W] quiser fechar a divergência: remover a copy (o protótipo não a tem) ou mantê-la como acréscimo consciente de prod.",
  "_nota_ancoras": "QUANDO ESTE CONTRATO NASCEU (2026-08-10) nenhuma âncora `data-contract` existia no `Index.tsx`. Adicioná-las é wiring do agente, não de [W]; de [W] é a COPY e a ORDEM acima. MEDIDO 2026-08-14: as 5 existem — `painel-cta-conversar`, `painel-metas-header`, `painel-metas-vazio`, `painel-meta-apurando`, `painel-meta-sem-historico`. Esta nota NÃO é o oráculo da contagem: quem sabe é `grep -c data-contract resources/js/Pages/Jana/Index.tsx`. E se esta lane bloqueia merge, o dono é `governance/required-checks-baseline.json` — nota de contrato não afirma o próprio enforcement.",
  "_nota_ordem": "2026-08-17 — `ordem` era [\"painel-cta-conversar\",\"painel-metas-header\",\"painel-metas-vazio\"], escrita quando o CTA vivia no `ProximaAcaoCard`, ACIMA do cabecalho de METAS. Com o mock removido e o bloco METAS movido pra posicao da ancora (logo apos os KPIs), o cabecalho passa a vir antes do CTA. `ordem` e derivada do LAYOUT — mudou o layout por decisao [W], corrige-se aqui no MESMO PR (regra de precedencia: o perdedor se corrige junto). A COPY de cada secao nao foi alterada por mim: continua sendo lei [W].",
  "_nota_alvo": "O selo de plano nao mora no `Index.tsx`: vive em `_components/JanaPlanoBadge.tsx`, porque as 3 telas da area (Painel, Conversa, Memoria) o injetam pelo slot `actions` do header compartilhado. O `alvo` e array e o coletor aceita arquivo — entao o componente entra aqui em vez de um wrapper `<span data-contract>` no Index so pra satisfazer o gate. Este contrato segue sendo o do PAINEL; o selo e verificado por ele porque e la que a tela o apresenta."
}
