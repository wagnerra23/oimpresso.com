# `prototipo-ui/_arquivo/ds/` — Design Systems aposentados (append-only)

> **Regra:** [ADR 0239](../../../memory/decisions/0239-governanca-design-system-git-ssot-regressao-ia.md) **R4** — só o **DS vigente** fica na raiz do `prototipo-ui/`; os passados moram aqui. Append-only (ADR 0003): movido, nunca apagado.

## Fonte da verdade (NÃO está aqui — está na raiz)
- `prototipo-ui/design-system.css` + `tokens.css` — **a fonte** do DS (R1).
- `prototipo-ui/Design System v4.html` — **spec visual vigente** (R4 · 1 só na raiz).

## Arquivados (versões antigas — referência histórica)
| Arquivo | Aposentado em | Era |
|---|---|---|
| `Design System v3.html` | 2026-05-30 | spec visual v3 (superseded por v4 · ADR 0235 roxo) |

> Histórico mais antigo (v1/v2) vive no export do Cowork (`_arquivo/ds-historico/`). Aqui ficam só os que estavam na raiz do git.
