repo: wagnerra23/oimpresso.com
branch: main

## Last sync
date: 2026-08-21T13:40:00Z

### Updated in this project
- **Módulo HRM (Essentials `/hrm`) importado pro app único** (rota `hrm` + ghosts `hrm-licencas` · `hrm-presenca` · `hrm-turnos` · `hrm-folha` · `hrm-feriados` · `hrm-metas` · `hrm-config`, grupo **RH** ao lado do Ponto WR2 — nenhum `.html` novo). Espelha o topnav de `nav_hrm.blade`: **Painel** (minhas licenças, minhas metas, próximos feriados, fila de aprovação, presença do dia, colaboradores por setor), **Licenças** (filtros colaborador/situação/tipo, aprovar/cancelar com notificação, drawer com saldo do tipo + histórico do activitylog, e a aba **Tipos de licença** com limite por ano/mês), **Presença** (clock-in/out web com localização obrigatória, lançamentos, por turno, por data, importador de planilha), **Turnos** (fixo/flexível, folgas, saída automática, atribuir colaboradores com vigência), **Folha de pagamento** (lotes rascunho/fechada + pagamento, contracheques com composição ganho a ganho, ganhos e deduções recorrentes), **Feriados** (por localidade), **Metas de venda** (faixas × realizado × comissão apurada) e **Configurações** (`essentials_settings`: prefixos, instruções, tolerâncias, 2 flags). Arquivos: `hrm-data.jsx` · `hrm-extras.jsx` · `hrm-page.jsx` · `hrm-page.css`.
- **Vocabulário corrigido de propósito:** `Resources/lang/pt/lang.php` do módulo traduz `leave` como **“Sair”**, `all_leaves` como “Todas as folhas”, `attendance` como “Comparecimento” e `holidays` como “Férias” — é o que o balcão lê hoje. A tela usa licença · presença · feriado e registra a correção como achado A1 pro F3.
- **Lido no `main` NESTE turno** (tree `b719732f3188`): `Modules/Essentials/Routes/web.php`, `Resources/views/layouts/{nav_hrm,partials/sidebar_hrm}.blade.php`, `dashboard/hrm_dashboard.blade.php`, `holiday/index`, `leave/index`, `leave_type/index`, `sales_targets/index`, e os controllers `EssentialsLeave`, `EssentialsLeaveType`, `EssentialsHoliday`, `EssentialsSettings`, `Attendance`, `Shift`, `SalesTarget` + árvore dos 19 controllers/87 views do módulo. `PayrollController` (60 KB) foi lido no espelho local (create/index/grupos) — não no `main`.
- 🔴 **Achados registrados na própria tela (A1–A7):** (A2) `EssentialsLeaveController::store` não tem FormRequest — fim antes do início passa, tipo de outro negócio passa; (A3) `max_leave_count` do tipo nunca é aplicado, só exibido; (A4) `ShiftController::destroy` e `EssentialsLeaveTypeController::destroy` estão **vazios** — turno e tipo não podem ser excluídos; (A5) `saveSalesTarget` aceita faixas sobrepostas e a apuração pega a primeira que casa → comissão indeterminada; (A6) presença web do Essentials **não** é o ponto da Portaria MTP 671/2021 (isso é o Ponto WR2) e os dois não se conversam; (A7) `importAttendance` insere sem a checagem de sobreposição que o `validateClockInClockOut` faz. Já corrigido no `main` e digno de nota: `EssentialsSettingsController` e `EssentialsHolidayController` **já são Inertia** (com `Inertia::defer`), e `SalesTarget`/`Shift` já têm gate Tier 0 contra IDOR cross-tenant (ADR 0093).
- **DS vivo:** `KpiCard` · `Alert` · `Switch` via leitura em tempo de render com fallback do shell (mesmo idioma do `acessos-ds.jsx`). Tabela, busca, botões, abas e drawer seguem do shell **de propósito** — trocar só nesta tela criaria dois padrões. Zero cor crua: tudo por token (`--pos/--neg/--warn/--accent`, `--r-1/--r-3`).
- **Ondas de refino HRM-O1 a O4 aplicadas no F1** (mesmo par de rotas, nenhum `.html` novo): **O1 mecânica** — ordenação e resize de coluna, paginação, seleção múltipla + BulkBar (aprovar/cancelar licenças em lote, fechar marcações no horário do turno), atalhos `/` e `n`, `Esc`, skeleton de carga e vazio com motivo em cada visão; **O2 formulários** que só existiam no Blade (`hrm-forms.jsx`): pedir licença multi-colaborador com as instruções da configuração, novo/editar feriado, novo/editar turno (fixo × flexível, folgas, saída automática), gerar folha do mês (competência + localidade + quem, bloqueando quem já tem folha), lançar pagamento por colaborador com efeito no lote, e editar faixas de meta **recusando sobreposição** que o servidor aceita; **O3 profundidade** — "o que fazer primeiro" no painel derivado do próprio dado, custo de folha por setor e por competência, composição dos ganhos, saldo de licença por tipo com risco de estouro, **espelho do mês** cruzando presença × licença × feriado × folga (relatório que o backend não tem), progresso dentro da faixa de meta; **O4 DS vivo** (`hrm-ui.jsx`) — `DataTablePro · Pagination · BulkBar · Skeleton · EmptyState · Toast · Chart · PeriodBar · DatePicker · Input/Select/Textarea · KpiCard · Alert · Switch` com fallback do shell.
- **Trio + contrato + teste + ondas escritos** em `cowork-inbox/hrm/`: `PEDIDO-CL-hrm.md` (3 decisões [W] da O0 — dono da jornada Essentials × Ponto WR2, folha gerencial × título no Financeiro, licença bloqueia marcação — + PRs das ondas O5 a O8 na ordem, com o lang PT isolado como PR-8), `Licencas.charter.md` (11 regras R1–R11 + achados) e `Licencas.casos.md` (22 UC com aceite), `Presenca.charter.md` (9 regras), `Folha.charter.md` (8 regras, non-goal explícito de encargos), `hrm-licencas.contract.json` (10 seções + copy literal + 8 proibições, ADR 0286) e `HrmLicencaTest.php` — **nasce vermelho** em UC-HRM-02/03/05/15/18 de propósito (é a prova dos achados A2/A3/A4).
- **Ondas O9 e O10 aplicadas** (aforândcia de protótipo, não vai pro F3): seletor de **papel simulado** no cabeçalho — Administrador · Gestor de equipe (`crud_all_leave` + `approve_leave` + `crud_all_attendance`) · Colaborador (`crud_own_leave` + `view_own_attendance`) — e de **estado da tela** (Com dados · Primeira vez · Demonstração). Efeitos reais: colaborador vê só as próprias licenças e marcações, folha vira "meus contracheques", turnos/metas/configurações respondem com **bloqueio que diz o motivo** (`EmptyState no-perm`) em vez de item escondido; gestor aprova mas não cadastra turno, feriado nem meta; primeira vez traz os 6 vazios de estreia explicando o que é cada cadastro; demonstração desliga criar/importar/excluir, tira o atalho `n` e oculta valor de salário. O respiro lateral dos blocos do módulo também foi acertado (24 px, convenção do shell) e a página ganhou rodapé de 40 px.
- Export do build em `prototipo-ui/cowork/hrm/` (só jsx/css).
- ⚠️ Não commitado — as tools de GitHub aqui são read-only; ponte pro `main` = cola zero-toque ou Issue `cowork-intake`.

### Leva anterior (2026-08-19T20:20:00Z)
- **Módulo Conector (API) importado pro app único** (rota `connector` + ghosts `conn-docs`, `conn-saude`, `conn-modulo` — nenhum `.html` novo): abas **API clients** (lista de clients OAuth do Passport com segredo oculto por padrão + revelar/copiar, tokens ativos em 24 h, criar client com as regras do `StoreOauthClientRequest` — obrigatório, 191 caracteres, aviso de nome duplicado, campos fixos `http://localhost`/`password_client` —, excluir com a consequência dita, e “Regenerar chaves” como ação destrutiva explícita), **Documentação** (catálogo dos 47 endpoints em 7 grupos lido do `Routes/api.php`, com método/rota/controller/observação, o passo a passo do `POST /oauth/token`, as regras transversais `throttle:120,1 · auth:api · log.delphi · timezone` e os 3 formatos de corpo do `DelphiSyncService`), **Saúde** (os 3 checks do `connector:health` com limiares e agenda 06:15) e **Módulo** (estado, versão 2.0, 2 migrações, instalar/atualizar/desinstalar com o `passport:install --force` pós-migração + 7 achados). Arquivos: `connector-page.jsx` · `connector-api.jsx` · `connector-page.css`; export em `prototipo-ui/cowork/connector/`.
- **DS vivo:** `KpiCard` · `Alert` · `Modal` via `acessos-ds.jsx` (mesmo idioma das telas do grupo Usuários). Tabela, busca, kebab, abas e botões seguem do shell **de propósito** — trocar só nesta tela criaria dois padrões; o campo de nome fica nativo (o `Input` do DS não expõe `aria-invalid`/ref do jeito que a validação inline usa).
- **Lido no `main` NESTE turno** (tree `728f789b8fb1`): tree `Modules/Connector/**` (101 arquivos), `Resources/views/clients/index.blade.php`, `ClientController`, `ConnectorController`, `DataController`, `InstallController`, `Routes/web.php`, `Routes/api.php`, `module.json`, `StoreOauthClientRequest`, `Resources/lang/pt/lang.php`, `Console/Commands/ConnectorHealthCommand`, `Services/DelphiSyncService` + os docs de rotina (`COWORK-ESTRUTURA-E-TELAS.md`, `PRE-FLIGHT-TELA.md`).
- 🔴 **Achados registrados na aba Módulo:** (1) `client_secret` impresso em texto puro na tabela do Blade; (2) `connector.access` existe no `DataController@user_permissions` mas rota/controller/FormRequest checam `superadmin` — delegar acesso técnico é impossível hoje; (3) `destroy` apaga `oauth_clients` e **não revoga** `oauth_access_tokens`; (4) o botão “Regenerate doc” executa `passport:install --force` (o `scribe:generate` está comentado) — rótulo mente e derruba toda integração; (5) excluir não pede confirmação no legado; (6) o primário do menu aponta `/connector/client/create` e `create()` devolve `connector::create`, view que **não existe** no módulo; (7) não há rotação de segredo por client.
- Não commitado — tools de GitHub aqui são read-only; ponte = cola zero-toque ou Issue `cowork-intake`. Trio (charter + casos + contrato) do Conector **ainda não escrito** — próximo passo natural.
- **Trio + contrato + teste + ondas escritos** em `cowork-inbox/connector/`: `Index.charter.md` (12 regras R1–R12, 7 achados A1–A7, non-goals/anti-hooks, props da Inertia), `Index.casos.md` (20 UC com aceite e rastreabilidade), `connector-api.contract.json` (15 seções + copy literal + 10 proibições + 5 acordos semânticos, ADR 0286), `ApiClientsPanelTest.php` e `PEDIDO-CL-connector-trio.md` rev. 2. Âncoras `data-contract` instrumentadas no F1.
- **5 decisões [W] ratificadas em 2026-08-19 e já aplicadas no F1 + trio:** D1 emitir credencial **fica em superadmin** (a permissão órfã `connector.access` sai do catálogo) · D2 excluir client **revoga os tokens** em cadeia · D3 **sem rotação de segredo** — não existe e não pode passar a existir · D4 **regenerar chaves sai da tela** (botão removido; `ClientController::regenerate` + rota GET a remover; vira operação de servidor) · D5 `/docs` substituído pela aba Documentação. Ondas renumeradas: O1 prova · O2 segurança da ação · O3 tradução Inertia · O4 menu/rotas mortas · **O5 apagar o legado** (lista arquivo por arquivo: `clients/index.blade.php`, `layouts/master.blade.php`, stubs `create/show/edit/update`, `ConnectorController` inteiro, chaves de lang mortas nos 16 idiomas, assets de 0 byte — só **depois** do screenshot [W2]) · O6 DS vivo.
- **Refino de estados na tela + 2 ondas novas + 1 proposta:** a lista de clients ganhou os três estados com motivo (`EmptyState` do DS) — **primeira vez** (explica o que é um API client e emite o primeiro), **sem resultado** de busca e **demonstração** (`APP_ENV=demo`: KPIs zerados, criar indisponível, atalho `n` desligado, nenhum segredo no payload), com seletor de estado no cabeçalho (afordância de protótipo, não vai pro F3); a confirmação de exclusão passou a refletir a D2 ("os N acessos abertos caem na hora"). Casos novos UC-CONN-21/22 e seções `aviso-demo` + `vazio` no contrato. Ondas novas: **CONN-O7** "quem está usando esta credencial" (`oauth_access_tokens` já tem `user_id`/`updated_at` — revogar deixa de ser decisão no escuro) e **CONN-O8** saúde com histórico (publicar o `connector:health` em `memory/governance/connector-health.json` → série de 14 dias + desvios do Delphi).
- 🔴 **Proposta separada** `cowork-inbox/connector/PROPOSTA-licencas-equipamentos.md`: não existe tela de licença/equipamento do WR Comercial em módulo nenhum, e a leitura do `LicencaComputadorController` (NESTE turno) achou **L1** `index()` = `Licenca_Computador::all()` sob `auth:api` — devolve equipamento de **todos os negócios** (Tier 0, ADR 0093) — e **L2** `senha`/`contra_senha` do cliente gravadas em claro; mais L3 máquina nasce bloqueada e liberar só por UPDATE manual, L4 HD compartilhado atualiza todas as linhas, L5 `dt_validade`/`gera_mensalidade`/`valor` sem tela nem aviso, L7 hard delete sem escopo. Modelo é do **Officeimpresso** → espera decisão de escopo [W].
- **Decisões [W] D6 e D7 (2026-08-19), aplicadas no F1 + trio:** **D6 ninguém vê segredo — nem o administrador**: a coluna Segredo virou selo "guardado · não é exibível", revelar/copiar saíram do kebab (entrou "Emitir credencial nova"), o segredo aparece **uma única vez** na criação e o mock da tela nem guarda mais o valor. **Correção da mesma data:** a primeira versão da D6 propunha `Passport::hashClientSecrets()` — [W] vetou, porque **o Delphi já está instalado nos clientes e não pode ser alterado**: credencial emitida nunca para de autenticar. A **CONN-O2b** virou "fechar o caminho de leitura sem tocar no valor guardado" (remover `makeVisible('secret')`, nenhuma rota devolve `secret`, auditoria sem segredo) e ganhou proibição explícita de hash/rotação/validade de client/troca de grant. Testes novos: `test_segredo_nao_e_hasheado_credencial_em_campo_continua_valendo` e `test_client_preexistente_ainda_obtem_token`; caso UC-CONN-24 e a seção "Restrição dura — o Delphi em campo" no charter. **D7 licenças/equipamentos é do Officeimpresso com permissão de suporte** (assunto diferente da API); `senha`/`contra_senha` saem do payload e da tabela. Testes novos: `test_nenhuma_rota_do_painel_devolve_o_segredo` e `test_segredo_do_banco_esta_hasheado` (nascem vermelhos); UC-CONN-02 reescrito e UC-CONN-23 (perder o segredo tem caminho).

### Leva anterior (2026-08-19T19:46:00Z)
- **Módulo CMS (Site) importado pro app único** (rotas `site` + ghosts `cms-blog`, `cms-depoimentos`, `cms-leads`, `cms-detalhes`, `cms-modulo` — nenhum `.html` novo): páginas/blog/depoimentos com ordem por arrasto, seleção múltipla, filtros, drawer de edição (prévia computador/celular, medidor de 160 caracteres, sanitização declarada, blocos da home item a item, histórico), Leads do formulário público, Detalhes do site nas 8 seções do `settings/index.blade` e aba Módulo (instalar/atualizar/desinstalar, os 3 checks do `cms:health`, importador WP com simulação padrão). Arquivos: `cms-page.jsx` · `cms-extras.jsx` · `cms-page.css`; export em `prototipo-ui/cowork/cms/`.
- **DS vivo:** `KpiCard` · `Switch` · `Checkbox` · `Alert` · `EmptyState` · `Modal` · `BulkBar` via `acessos-ds.jsx`. Tabela, abas, page header, campos e drawer seguem do shell **de propósito** (trocar só nesta tela criaria dois padrões) — a troca por `DataTablePro`/`TabBar`/`PageHeader`/`Drawer`/`Input` está mapeada como trabalho de F3 no pedido. Raios religados nos tokens do DS (`--r-1`/`--r-3`; `--r-md`/`--r-lg` não existem no DS vivo).
- **Trio + contratos + teste escritos** em `cowork-inbox/cms/`: `PEDIDO-CL-cms-trio.md` (6 PRs, comandos, 4 decisões [W], 4 pedidos de DS), `Index.charter.md`/`Index.casos.md` (13 regras, 18 UC), `SiteDetails.charter.md`/`.casos.md` (6 regras, 8 UC), `cms-content.contract.json` + `cms-site-details.contract.json` (ADR 0286) e `CmsPainelAdminTest.php` (14 casos, **nasce vermelho de propósito** em UC-01/05/08/09). Contexto em `cowork-inbox/CMS-F1-2026-08-19.md`.
- **Lido no `main` NESTE turno** (tree `60a9b423ac4a`): tree `Modules/Cms/**`, `Modules/Cms/Resources/js/Pages/Site/Page.charter.md`, `resources/js/Pages/Site/`, `prototipo-ui/contrato/`, `package.json`, workflows `contrato-de-tela` · `governance-script-tests` · `mv-metabolismo` · `design-memory-gate`. **Duas correções que só a leitura do `main` pegou:** (1) o módulo Cms é dono das próprias páginas Inertia — `Modules/Cms/Resources/js/Pages/Site/**` com charter ao lado, e `resources/js/Pages/Site/` só tem Login/Register; o destino dos charters do painel virou `Modules/Cms/Resources/js/Pages/Admin/**`. (2) convenção de charter do repo (`related_prototype` pro gate `pt_declarado`, seções "(faz)/(NÃO faz)", "Pendências antes de status: live") aplicada nos dois charters. Também corrigi a retenção que eu havia chutado: `Config/retention.php` = `leads_days 730`, `contacts_days 1095`, `activity_log_days 2555` — e é **política declarada sem executor** (sem comando de purge agendado).
- 🔴 **Achado bloqueante lido no `main`:** `StoreCmsPageRequest.php:31` e `UpdateCmsPageRequest.php:37` validam `type` com `in:page,post,banner`, enquanto o domínio do módulo é `page|blog|testimonial` (`nav.blade`, `CmsController` 87/112/203/213/241/261, `CmsPageRepository:42`, `StoreBlogPostRequest:53`, `CmsServiceProvider:28`, migração de dados padrão). **Salvar blog ou depoimento pelo painel é recusado hoje** com "Tipo inválido. Use page, post ou banner."; `post`/`banner` não são lidos por nenhuma consulta. Registrado como §3.b do pedido + UC-CMS-19 + teste `test_store_aceita_os_tres_tipos_do_dominio` (nasce ❌). Achados menores: `meta_description` aceita 500 no servidor (os 160 são regra de SEO da UI) e `feature_image` até 5 MB — viraram R14/R15 do charter.
- Não commitado — tools de GitHub aqui são read-only; ponte = cola zero-toque ou Issue `cowork-intake`.

### Leva anterior (2026-08-19T18:20:00Z)
- **Módulo de notificações importado pro app único** (rota `notificacoes`, grupo SISTEMA — nenhum `.html` novo): espelho de `/notification-templates` do UltimatePOS. 11 modelos nos 3 grupos (geral/cliente/fornecedor), campos fiéis (`subject · cc · bcc · email_body · sms_body · whatsapp_text · auto_send/auto_send_sms/auto_send_wa_notif`), `extra_tags` clicáveis (com as famílias de campos personalizados expandindo em chips), editor de corpo HTML (Visual/HTML), validação de tag desconhecida, contador de segmentos SMS com GSM-7→UCS-2 (160→70 com acento), teste de envio, busca no rail com `/`, selos `vazio`/`auto`/alterado e prévia por canal (e-mail, SMS, WhatsApp em telefone). Fonte lida no **espelho local** (controller, Model, `views/notification_template/**`) — não no `main`.
- **DS vivo:** `Switch` · `Alert` · `Toast` · `Tooltip` · `Input` do bundle bound (via `acessos-ds.jsx` onde já havia adaptador); rail, segmented, page header e botões seguem do shell **de propósito**. Pedido de DS registrado: `Input`/`Textarea` sem `ref`/`onFocus` impedem 100% DS nos campos que recebem inserção de tag.
- **Trio + contrato escritos:** `cowork-inbox/NOTIFICACOES-F1-2026-08-19.md` (charter com 11 regras, 24 casos de uso com aceite, 16 testes mínimos, 5 decisões [W], 7 achados do legado — inclui seed em inglês indo pro cliente, `store()` sem whitelist de `template_for`, `email_body` sem sanitização e `cc/bcc` sem validação server-side) + `cowork-inbox/notificacoes/notificacoes.contract.json` (6 âncoras `data-contract` já instrumentadas no F1). Build exportado em `prototipo-ui/cowork/notificacoes/`.
- **Lido no `main` NESTE turno:** `package.json` (scripts de gate), `scripts/governance/cowork-ssot-guard.mjs` (R1 `.md` fora do `cowork/` · R2 sem bundle datado · R3 allowlist de `prototipos/`), `prototipo-ui/contrato/contract.schema.json` + `EXEMPLO.contract.json`, `memory/governance/prototipo-readiness.json`, `PRE-FLIGHT-TELA.md`, `COWORK-ESTRUTURA-E-TELAS.md`. Não commitado — tools de GitHub aqui são read-only.

### Leva anterior (2026-08-19T14:53:00Z)
- **Fuso horário: resposta com leitura do `main` neste turno** (`config/app.php`, `app/Http/Middleware/Timezone.php`, `app/Http/Kernel.php`, `Console/Kernel.php`, `BusinessController.php`, `SetSessionData.php`, `RecurringExpense/RecurringInvoice`, `NfeController`, `SocialAuthController`, `SellController`). **Não existe configuração de data por usuário** — fuso e formato são do negócio (`business.time_zone`, `date_formats`). **O servidor tem o próprio**: `config('app.timezone')` com fallback **`Europe/London`** — a origem do erro de 3 h. 8 achados (T1–T8) + 4 guardas (G1–G4) no trio: fallback Londres; middleware `timezone` só cobre rotas com sessão (API cai no fallback); ~50 schedules hardcodam `America/Sao_Paulo`; `date_default_timezone_set` dentro do loop de `RecurringExpense/Invoice` vaza o fuso entre negócios; NF-e e login social hardcodam SP; o bug já tem memória própria (`feedback_carbon_timezone_bug`) — é reincidente.
- **Diagnóstico na tela:** `prefs` → *Formato e região* mostra três relógios (empresa · servidor UTC · navegador), destaca a divergência e explica que fora do middleware roda no fuso do servidor.
- **Conversão para o DS vivo** das telas do grupo Usuários via `acessos-ds.jsx` (idioma do `superadmin-page.jsx`: leitura em tempo de render + fallback do shell): `KpiCard` · `Switch` · `Checkbox` · `Alert` · `Progress` · `EmptyState` · `Modal` · `BulkBar`. Tabela, botões, filtros, kebab e drawer seguem do shell **de propósito** (mesma decisão da onda 4 do Superadmin — trocar só aqui criaria dois padrões).
- Trio atualizado (`cowork-inbox/ACESSOS-F1-2026-08-19.md`, seções 9 e 10) e export do build em `prototipo-ui/cowork/acessos/`. Não commitado — tools de GitHub aqui são read-only.

### Leva anterior (2026-08-19T14:33:20Z)
- **Achado que corrige os dois syncs anteriores:** `/superadmin/usuarios` (Usuário 360) **já existe em Inertia** no `main` — `Modules/Superadmin/Resources/js/Pages/superadmin/Usuario360/{Index,Show}.tsx` **com charter** (`Index.charter.md`, `Show.charter.md`), `Usuario360Controller` + `UserLockoutService`, 5 rotas (index · 360 · lock · unlock · history), lock com motivo obrigatório e `user_lockouts.snapshot`, e teste `Wave26SaturationTest` tratando Usuario360 como exceção Inertia. **Não refiz** (FRESCOR: 🔵 puxe o vivo). Registrei como resolvido: era "sem tela F1" nos syncs de 12:20 e 12:55.
- **Grupo Usuários completado no app único** (nenhum `.html` novo): rotas `comissoes` (Apuração de comissão) e `prefs` (Preferências) + drawer de detalhe do usuário, convite por e-mail e guardas de exclusão. Arquivos novos `comissoes-page.jsx` e `prefs-page.jsx`; `usuarios-page.jsx`, `comissionados-page.jsx` e `acessos-page.css` estendidos.
- **Apuração de comissão** (aprovada por [W] neste turno): período → base por agente → extrato venda a venda com a conta aberta (base, % aplicado, comissão) → fechamento do período → título a pagar no Financeiro. Três regras: % fixo sobre faturado, % por faixa de meta e % sobre margem. A tela declara em banner que **nada disso existe no backend** — o legado guarda só `cmmsn_percent` (decisão D6 do trio).
- **Guardas de exclusão** ([W]: "tenho medo do excluir"): excluir usuário ou comissionado é **bloqueado quando há venda/OS no nome dele**, com a contagem e o motivo ("o histórico ficaria órfão"), oferecendo desativar/inativar; sem movimento, exige digitar o nome de usuário para confirmar. Cobre o D5 (hard delete de `users` no `SalesCommissionAgentController::destroy`).
- **Preferências** é desenho novo e diz isso na tela: não li as views do `/prefs` no `main` neste turno — campos precisam de conferência contra `BusinessController` e as preferências do Essentials.
- Export do build atualizado em `prototipo-ui/cowork/acessos/` (só jsx/css). Não commitado — tools de GitHub aqui são read-only.

### Leva anterior (2026-08-19T14:12:00Z)
- **Grupo Usuários fechado no app único:** rotas novas `funcoes` (Funções e permissões) e `comissionados`, sub-itens de **Usuários** na sidebar. Arquivos: `funcoes-page.jsx` + `funcoes-perms.jsx` (catálogo de permissões) + `comissionados-page.jsx` + `acessos-page.css`. Nenhum `.html` novo. Export do build em `prototipo-ui/cowork/acessos/` (só jsx/css).
- **Catálogo real de permissões** (lista de produção colada por [W]: 53 grupos) normalizado em 5 formas de controle em vez de checkbox-soup: escopo (colapsa "ver todos"/"ver próprio"), CRUD numa linha, toggle, seletor exclusivo (os 5 filtros de cliente sem venda, as 4 situações de venda, os 3 níveis de reversão da auditoria) e chave crua. Os 9 `Desativar …` do PDV são exibidos invertidos como liberação. Marca de risco + barra de diferenças `+N −M desde o padrão`.
- **Lido no `main` NESTE turno:** `routes/web.php` (323, 327, 599, 715), `RoleController.php`, `SalesCommissionAgentController.php`. Diagnóstico: `/roles` e `/sales-commission-agents` são **Blade + DataTables ajax** (onda de tradução, diferente de `/modulos`); `/user/profile` legado segue Blade mas a tela Inertia de "Meu perfil" já existe (web.php:327).
- **Trio de prontidão escrito:** `cowork-inbox/ACESSOS-F1-2026-08-19.md` — 2 charters (10 regras R1–R10 de papéis + 7 regras C1–C7 de comissionados, lidas do controller), 26 casos de uso com critério de aceite, 23 testes mínimos, contrato de tela e 9 achados. Críticos: **D1** `__createPermissionIfNotExists()` cria qualquer nome de permissão que chegar no POST (sem whitelist, tabela global); **D5** excluir comissionado é hard delete de `users` (vendas órfãs); **D2** `store()` lê `radio_option` duas vezes e duplica a permissão de grupo de preço (divergindo do `update()`); **D3** 27 permissões sem lang string (Fiscal 7, PaymentGateway 10, RecurringBilling 10) aparecem cruas no `/roles`.
- **Modelo que muda premissa de UI:** comissionado **é** uma linha de `users` com `is_cmmsn_agnt=1` e `allow_login=0` (não é vínculo); `is_service_staff` é coluna do papel, não permissão; grupo de preço é **radio**, não checkbox; papel `is_default` não é editável exceto `Cashier#biz`, e editá-lo zera o `is_default`.
- Não verificado neste turno: as views `role/*.blade.php`, o seeder/catálogo real da tabela `permissions`, `SellingPriceGroup` e o relatório do agente comercial. Não commitado — tools de GitHub aqui são read-only; ponte = cola zero-toque ou Issue `cowork-intake`.
### Leva anterior (2026-08-19T12:55:00Z)
- **Pacote completo pro Code fechado** em `cowork-inbox/modulos/`: `PEDIDO-PARA-CODE.md` (7 PRs na ordem: doc → trio/testes → fix da verdade → RBAC → fila → contrato/CI → DS+limpeza, com 5 decisões [W] e checklist pós-merge) + `aplicado/` com as **cópias do `main` já patchadas** e comentadas (`ModuleManagerService.php` P1 reversão da flag no catch + P2 `error` alcançável + P4 versão instalada via `System::getProperty`; `Index.tsx` P3 marcador "sem menu"; `ModuleManagementController.php` P5 `can('manage_modules')`) + `InstalarModuloJob.php` (P6: lock por módulo, `tries=1`, estado no cache) + `ModuleErroFixtureTest.php` (prova do P2 com fixture `Modules/__ErrFixture__`).
- Ressalvas registradas no pedido: fila com worker em produção **não verificada** (PR-5) e o bug adjacente do alias kebab em `isModuleInstalled()` (`oficina-auto_version` vs `oficinaauto_version`) que o P4 encosta.

### Leva anterior (2026-08-19T12:40:00Z)
- **Buracos de `/modulos` resolvidos como arquivos prontos** em `cowork-inbox/modulos/` (README com destino e ordem de aplicação): `Index.casos.md` (16 UC Dado/Quando/Então + rastreabilidade — o arquivo que faltava pro `ciclo-completo`), `Index.charter.md` **v2** (11 regras R1–R11, A1–A4, non-goals/anti-hooks, 4 pendências [W]), `ModuleManagementTest.php` (Pest, UC-MOD-01..04/11..15, com backup/restore do `modules_statuses.json` e Artisan fakeado), `ModuleManagerServiceTest.php` (ordem, chave órfã, registered, heurística de área, `runModuleInstallCommand`), `modulos.contract.json` (7 âncoras + copy literal + estados + proibições) e `PATCHES.md` com 8 patches em diff: P1 install que falha volta a flag · P2 status "Com erro" alcançável · P3 `has_datacontroller` ⇒ "sem menu" · P4 versão instalada · P5 RBAC unificado em `manage_modules` · P6 install em fila · P7 6 skills que apontam pra `/manage-modules` · P8 chaves órfãs do JSON.
- Não commitado (tools de GitHub aqui são read-only) — ponte = cola zero-toque ou Issue `cowork-intake`.

### Leva anterior (2026-08-19T12:20:00Z)
- **Trio de prontidão + ondas de `/modulos` escritos.** `cowork-inbox/MODULOS-F1-2026-08-19.md` (charter reforçado com 11 regras de domínio + A1–A4, 14 casos de uso com critério de aceite, 14 testes mínimos, rascunho de `modulos.contract.json`, mapa do que é caseiro no F1, 4 decisões [W]) e `cowork-inbox/MODULOS-F3-ONDAS-PARA-CODE.md` (MOD-O0 decisões · O1 prova mínima · O2 verdade da tela · O3 segurança da ação · O4 DS vivo · O5 contrato+CI · O6 limpeza do legado + "o que mais precisa"). Export do build em `prototipo-ui/cowork/modulos/` (só jsx/css).
- **Achados desta leitura:** `Index.casos.md` **não existe** (causa direta da reprovação do `ciclo-completo`) e não há teste de HTTP do `ModuleManagementController`; `install()` faz `setActive(true)` **antes** do migrate → falha deixa a linha "Ativo" mentindo; `error` do `list()` só é preenchido em `Throwable` de leitura ⇒ o status "Com erro" é praticamente inalcançável; `has_datacontroller` vem na prop e a tela ignora; RBAC da rota é `is_admin`/`Admin#biz` enquanto existe a permissão `manage_modules` (duas leis); nenhum dos 32 `module.json` declara `version` ⇒ `v0.0` em toda linha; 6 skills ainda mandam o operador em `/manage-modules`.

### Leva anterior (2026-08-19T11:56:34Z)
- **`/modulos` (Gerenciador de Módulos) aberto no protótipo.** Lido no `main` NESTE turno (tree `0b125f09bd96`): `resources/js/Pages/Modules/Index.tsx` + `Index.charter.md` (draft, tier B), `app/Http/Controllers/ModuleManagementController.php`, `app/Services/ModuleManagerService.php`, `routes/web.php:916-926`, `modules_statuses.json` e os 32 `Modules/*/module.json`. Resposta à pergunta de [W]: **sim, já existe em React** — `/manage-modules` do UltimatePOS foi substituído por `/modulos` (Inertia `Modules/Index`), e `LegacyMenuAdapter.php:287` já redireciona.
- Novo `modulos-page.jsx` (`window.ModulosPage`) + `modulos-page.css`, rota `modulos` no `app.jsx`, item **Módulos** no grupo SISTEMA do `data.jsx`. Nenhum `.html` novo. Espelha a tela viva: contagens no header, 4 StatCards, FilterDropdown Área/Status + chips ativos, busca debounced 300 ms, tabela com tile de área, alias · v0.0 (module.json não declara `version` → fallback do Service), badge de status, contagem de migrations, switch de ativo e kebab Instalar/Reinstalar/Desativar com a copy literal dos confirms e toasts do controller.
- Dados = estado real do `main`: 32 módulos em `Modules/`, todos registrados e ativos (os `false`/ausentes do `modules_statuses.json` — Accounting, CustomDashboard, Ecommerce, FieldForce, Hms, InboxReport — não têm pasta, então não aparecem, igual ao Service).

### Leva anterior (2026-08-19T10:33:10Z)
- **Superadmin (SaaS/licenciamento) reconstruído no app único.** Lido no `main` NESTE turno (tree `9d99d9b2405f`): árvore de blades por módulo (417 telas de nível ≥2) + `Modules/Superadmin/Resources/views/{superadmin,business,packages,superadmin_subscription,communicator,layouts/nav}`. Novo `superadmin-page.jsx` (`window.SuperadminPage`) + `superadmin-page.css`, rota `superadmin` com ghosts `sa-negocios` · `sa-assinaturas` · `sa-pacotes` · `sa-comunicador` · `sa-config` (grupo SISTEMA da sidebar). Nenhum `.html` novo.
- Traduções de padrão: `small-box` AdminLTE → KPI cards por token; `btn-group data-toggle=buttons` (Hoje/Semana/Mês/Ano) → segmented `sa-seg`; DataTable server-side → `os-table` densa com filtros `cli-fdrop-*` e kebab `cli-kebab-*`; cards `box box-success` de pacote → grid de cards com tags privado/avulso/ativo; partials de `superadmin_settings` → nav lateral de seções (Aplicação · SMTP · Gateways · Pusher · Cron/backup · JS/CSS extra); modais sweetalert → ações no kebab. Sem inglês na UI, sem emoji.
- **Levantamento pedido por [W] antes disso:** o que ainda é Blade e não tem rota no shell — módulos inteiros (Ponto, Essentials, Repair, Manufacturing, AssetManagement, Superadmin *(agora coberto)*, Cms, Spreadsheet, Woocommerce, Connector, VozDoCliente), parciais (CRM schedule/campaign/proposal/order_request/contact_login, Officeimpresso licenças + catalogue, Jana metas/alertas) e o core UltimatePOS (`sale_pos` 61 · `report` 50 · `product` 31 · devoluções · transferência · caixa · despesas).
- **Ondas 1–4 de refino aplicadas no Superadmin** (mesmo par de arquivos, nenhum `.html` novo): (1) mecânica — ações reais nos kebabs, confirmação destrutiva que diz a consequência, seleção múltipla + BulkBar, ordenação por coluna (data dd/mm/aaaa e valor como número), paginação 6/página, skeleton + vazio com motivo, `/` foca busca e `n` abre novo; (2) formulários que o Blade tem — negócio (novo/editar, máscara CNPJ/telefone, e-mail validado, pacote + trial), pacote (preço/intervalo, 4 limites com "0 = ilimitado", módulos, privado/avulso/ativo), assinatura (mudar status com motivo · editar vigência); (3) profundidade — funil trial→pago com perda por etapa, churn 30 d com motivos, receita por pacote no MRR, fila "Vencendo ou vencido" com ação por linha, "O que fazer primeiro" navegável, comunicador com prévia do e-mail, agendamento e taxa de abertura; (4) primitivos do DS vivo no lugar das peças caseiras — `PageHeader · KpiCard · StatusBadge · Chart · Pagination · BulkBar · Modal · Input/Select/Textarea · Switch · Alert · Progress · Skeleton · EmptyState · Toast · Checkbox · RegistrationMark` (tabela/botões/filtros seguem os do shell de propósito: trocar só esta tela criaria dois padrões).
- **Trio de prontidão escrito:** `cowork-inbox/SUPERADMIN-F1-2026-08-18.md` — charter (norte por view, 11 regras de domínio, non-goals), 16 casos de uso com critério de aceite, testes mínimos (DQE/borda/plural PT-BR/mono/permissão), rascunho do Contrato de Tela (`superadmin.contract.json`) e a lista de peças ainda caseiras com motivo. 3 decisões pendentes [W]: impersonar, destino de `pages`/`pricing`, dono de `subscription/pay`.
- **Plano de ondas F3 escrito pro [CL]:** `cowork-inbox/SUPERADMIN-F3-ONDAS-PARA-CODE.md`. Leitura nova do `main` (tree `d4ad08042926`) mudou o diagnóstico: o **backend do Superadmin já está refatorado e testado** — `SuperadminDashboardService` · `SubscriptionLifecycleService` · `PackageManagerService` · `BusinessAuditService`, 6 FormRequests, `PackagePolicy`, `BusinessAutoSubscriptionObserver`, 2 listeners Asaas, `throttle:superadmin`, spans `OtelHelper`, 14 feature tests (4 de cross-tenant saturation). Só a camada de view segue Blade/AdminLTE; o único Inertia do módulo é `Resources/js/Pages/Site/Pricing.tsx` (que serve de precedente de onde a página + charter moram). Logo: **6 ondas de tradução de tela** (`SA-O1` dashboard · `SA-O2` negócios índice+drawer, a grande · `SA-O3` forms de negócio · `SA-O4` assinaturas+pacotes · `SA-O5` comunicador · `SA-O6` config+limpeza do Blade), numeradas `SA-O*` pra não colidir com as "Wave 18/23/25/27" de backend do repo. Nenhuma onda de arquitetura.
- **`SA-O0` = 3 decisões [W] antes de codar**, porque 3 widgets meus não têm origem no banco: **trial não é status** (`status` é `waiting`/`waiting_approval` → `approved` → `expired` + `cancelled`; trial vive em `package_details.trial_days` — recomendo derivar por cálculo, sem migration); **motivo de churn** não tem coluna (`cancel($reason)` recebe mas não grava — recomendo migration `cancel_reason`); **taxa de abertura** do comunicador não existe (recomendo trocar por entregues/falharam em vez de pixel — LGPD). Documentei o mapa status DB ↔ copy PT-BR pra UI nunca mostrar enum cru, e sinalizei que `toggle-active`/`destroy` são **GET** hoje (ação destrutiva em GET → virar POST/DELETE na `SA-O3`).
- **Achado fora do protótipo:** `/superadmin/usuarios` (`Usuario360Controller`, 5 rotas: index · 360 · lock · unlock · history) existe no `main` e **não tem tela F1** — é desenho novo, não tradução; pedi briefing (provável `SA-O7`).
- ⚠️ Não commitado — vive só neste projeto Cowork; ponte pro `main` = cola zero-toque ou `cowork-inbox`/Issue.

### Sync 2026-08-20 — "Visão geral" vs. Home/Blade
date: 2026-08-20T12:53:39Z

### Updated in this project
- **`/dashboard-legacy` NÃO está desligada.** Lido `routes/web.php` no `main` NESTE turno: `Route::get('/dashboard-legacy', [HomeController::class,'index'])->name('home.legacy')` dentro do grupo `auth`, e `/home` (name `home`, preservado pra ~30 `route('home')`) faz 302 pra `/ia` se o usuário tem `jana.access`, **senão pra `/dashboard-legacy`** — é a porta de entrada pós-login de quem não tem a permissão (fix do incidente 2026-07-28, Maiara em 403). A tela Blade só renderiza com `?legacy=1`; sem query cai na Inertia `Home/Index`. Endpoints vivos: `/home/get-totals`, `/home/product-stock-alert`, `/home/{purchase,sales}-payment-dues`, `/calendar`.
- **Nome antigo confirmado = "Home"**: `resources/views/home/index.blade.php`, `@section('title', __('home.home'))`.
- **Sidebar** (`data.jsx`): "Visão geral" (`dash-legacy`) subiu pra posição acima de Atendimento; Atendimento **permanece** nos atalhos (IA · Forja · Visão geral · Atendimento).
- **Vínculo na tela** (`dash-legacy-page.jsx`): a linha meta agora diz "Substitui o antigo **Home** (Blade) — /dashboard-legacy?legacy=1", com link e `title` apontando o arquivo Blade + name `home.legacy`.
- Componentes da tela seguem 100% DS vivo (`KpiCard`, `Chart`, `DataTablePro`, `PageHeader`, `TabBar`, `Drawer`, `EmptyState`, `Alert`, `PeriodBar`, `StatusBadge`, `Select`, `Switch`, `Button`).
- ⚠️ Não commitado — vive só neste projeto Cowork; ponte pro `main` = cola zero-toque ou `cowork-inbox`/Issue.

### Sync 2026-08-18 (leva anterior)
date: 2026-08-18T18:43:51Z

### Updated in this project
- **Plano Blade → React ratificado + inventário L1.** Lido no `main` NESTE turno (tree `6b923a26d904`): `prototipo-ui/COWORK-ESTRUTURA-E-TELAS.md`, `FRESCOR-PRODUCAO-vs-PROTOTIPO.md`, `PRE-FLIGHT-TELA.md`, árvore de `resources/views/**` (~660 arq.) e `Modules/*/Resources/views/**` (289 telas de nível 2), `routes/web.php`. Escritos: `cowork-inbox/PLANO-BLADE-PARA-REACT.md` (6 etapas E0–E6, 11 lotes, template da Ficha de Tela) e `cowork-inbox/INVENTARIO-L1-VENDAS-PDV.md` (24 telas + 6 anexos com ID/rota/blade/controller). Decisões [W]: 1 Blade = 1 tela React · [CC] lê o Blade e propõe a Ficha · testes = DQE + borda/validação + permissões · escopo = tudo entra menos install/vendor/emails/myfatoorah/*_old.
- **Ficha BL-home-index (dashboard legado).** Lidos `HomeController.php` (index/indexLegacy/getTotals/getProductStockAlert/get{Purchase,Sales}PaymentDues/getCalendar), `resources/views/home/index.blade.php` (91,5 KB) + 8 partials, `public/js/home.js`. `cowork-inbox/FICHA-BL-home-index.md`: 12 eventos, 9 funções/regras (NET = vendas − a receber − despesas; vencimento ≤ 7 dias; devolução líquida), estados, 7 UC, 10 testes, validação por campo, permissões de 9 papéis, 8 anti-padrões. **Achado:** `/dashboard-legacy` só cai no Blade com `?legacy=1` — sem a query renderiza a Inertia `Home/Index` (F6 Soft, US-DASH-001).
- **Tela reconstruída no app único:** `dash-legacy-page.jsx` (`window.DashLegacyPage`, rota `dash-legacy` = atalho "Visão geral" na sidebar). PT-05 com componentes do DS vivo: PageHeader + 8 KpiCard (hero no Líquido) + 2 Chart (30 dias / ano fiscal) + as 9 grades do Blade em TabBar → DataTablePro + Drawer no lugar do modal legado (PT-02) + EmptyState `no-perm` e seletor de papel simulado (admin/Eliana/Larissa/sem `dashboard.data`). Nenhum `.html` novo.
- Cópia de apoio dos fontes legados em `blade-legado/dashboard/` (index + partials + calendar/modais + `home.js`) — leitura, não fonte: o canon é o `main`.
- ⚠️ Não commitado — vive só neste projeto Cowork; ponte pro `main` = cola zero-toque ou `cowork-inbox`/Issue.

### Sync 2026-08-17 (leva anterior)
- **Venda V3 de produção puxada pro app único** (lido no `main` NESTE turno, tree `c23a910e1c19`): a venda em produção é `resources/js/Pages/Sells/CreateV3.tsx` (49.504 B) + `_components/v3/**` (16 arq.) + `CreateV3.charter.md`/`.casos.md`; o build correspondente vive em `prototipo-ui/cowork/venda-v3/` (21 arq.). Os 20 arquivos trazidos batem BYTE A BYTE com o `main` (sells-create.jsx 50.312 · sells-item-detail.jsx 40.315 · sells-data.js 39.820 · …) — zero drift do espelho local.
- Integração sem `.html` novo: `venda-v3.jsx` concatena o build (data · ui · comissão · item-detail · parcelas · entrega · lançamento · fsm · colunas · create) num **único IIFE** e só expõe `window.VendaV3Create` — os arquivos originais conversavam por globais (`Button`, `Sec`, `Grid`, `brl`, `Icon`, `Money`…) que colidiriam com o shell. `venda-v3.css` traz só as camadas 3 (campos `.dsfa`, tabela de dados, overlays) e 4 (módulo `.venda-*`); camadas 1–2 (fundações/shell) ficaram fora — o app único já as tem.
- `sells-ui.jsx` apontava pro DS antigo (`OfficeImpressoDesignSystem_d7f886`) → religado no DS vivo bound (`OfficeImpressoPontoWR2DesignSystem_019dd0`). `lucide` UMD 0.460 entrou no host (a iconografia do build resolve por nome PascalCase).
- Rota: Vendas → **Nova venda** (`sub=create`) agora renderiza a V3, com page header próprio (título + "Voltar para Vendas" + esc) no lugar do que o `sells-app.jsx` fazia; `create` deixou de ser visão persistida (era beco sem saída no `localStorage`). O `vendas-create-page.jsx` antigo fica como fallback se o script não carregar. Não trouxe `sells-app.jsx` (shell próprio com sidebar/tema — o host já entrega) nem `sells-telas.jsx` (Index/Caixa/Cotações/Rascunhos do guia; a lista do app único é a viva). Os 20 arquivos crus do build não ficam no projeto (esteira, não armazém): vivem no `main`, aqui só o compilado `venda-v3.jsx`.
- Não veio no handoff do git: `sells-roteiro.jsx` (define `CuDrawer`) — stub inerte, e o modo meta/CU fica desligado (produção). ⚠️ Não commitado — vive só neste projeto Cowork.
- **Jana — puxada do vivo (só o que a produção passou).** Lido no `main` NESTE turno (tree `0ac689e2d80e`): `Pages/Jana/{Index,Chat,Memoria,Pro}.tsx`, `_components/JanaDrillDrawer.tsx`, `_shared/JanaSubNav.tsx`, `components/JanaAreaHeader.tsx`. Aplicado em `jana-merge.jsx`/`.css`:
  - **Conversa:** filtros 4 → **2 reais** (`todas`/`arquivadas` — "minhas" era tautológica, "compartilhadas" sempre vazia, 403 em 4 pontos do ChatController); busca acento-insensível com pill ⌘K; seções **Fixadas / Recentes|Arquivadas**; botão Nova conversa (⌘N) no header do histórico; sumiram "da equipe"/"compartilhada com" (não existe compartilhamento); **Propostas de metas** (`sugestoesPendentes` + PropostaCard: dificuldade, métrica/período/alvo, racional, dependências, "Escolher esta meta"/"Rejeitar" com a copy literal dos toasts).
  - **Memória:** taxonomia da produção (`meta · preferencia · restricao · contexto · acao_pendente`) com chips **derivados do dado**, e relevância **/10** (decisão [W] 2026-08-07: a produção é a fonte, o protótipo se adapta); `desde` com data+hora.
  - **Metas:** farol **cinza** = "não dá pra dizer" quando a janela não tem apuração (+ card "Aguardando apuração…"); o drawer agora cita `ApuracaoService::farol` — o antigo `MetricasApurador::farol` não existe.
  - **Drill "de onde vem esse número":** fontes reais do `SellsCockpitAggregator` (tabelas · regra · calculado por) no lugar dos `Analise*Service` fictícios; para churn/frota/cheques (sem apurador em produção) o drawer diz isso em vez de nomear classe inexistente; footer "Conversar com a Jana" **sem semear** a pergunta (a rota não aceita).
- **Nova tela do vivo que o protótipo não tinha:** `jana-pro.jsx` + `jana-pro.css` — **Jana Pro em modo FOCO** (espelho de `Pages/Jana/Pro.tsx`): hero + card de prova dark com os 3 ângulos, tabela Grátis vs Pro, preço + confiança, footer sticky com CTA idle→ativando→feito, ⌘↵ ativa · Esc volta. Preço de concorrente **não** foi copiado (Tier 0 redigido na fonte). Rota = `tab="pro"` dentro do app único; entradas: upsell do Painel e Configurar → Plano.
- ⚠️ Não commitado — vive só neste projeto Cowork.
- **"Igualar o módulo da Jana" — o padrão virou peça** (lido no `main` NESTE turno: `prototipo-ui/COWORK-ESTRUTURA-E-TELAS.md` + `prototipo-ui/FRESCOR-PRODUCAO-vs-PROTOTIPO.md`): novo `modulo-padrao.jsx` (`window.ModuloPadrao`) + `modulo-padrao.css` extraem de `chat-jana.jsx`/`jana-merge.jsx` o header de módulo com contexto e reapuração, as abas de área dentro da página, Resumo do dia, KPIs com drill, análises, ações sugeridas, estados vazio/erro, skeleton, drawer de origem, toast e `useAba`/`useAviso`. Mesmas classes `jc-*`/`jm-*` — zero paleta paralela, nenhum `.html` novo.
- **Etapa 1 aplicada em Compras** (`compras-page.jsx`): abas Painel · Pedidos · Fornecedores; Painel apurado do próprio mock (a pagar, trânsito, volume, a conferir + concentração por fornecedor, pipeline por estágio, próximos vencimentos) e "o que fazer primeiro" apontando o gap conhecido da grade tam×cor; a lista antiga ficou intacta como aba Pedidos, com região rolável própria (`.mp-body`).
- **Trava do FRESCOR respeitada:** Atendimento/CaixaUnificada e Cliente/CRM são 🔵 (produção à frente, "não repintar") — não receberam o padrão. Próximas etapas propostas: Produção/OP → Financeiro → Vendas/PDV → Oficina → Orçamentos.
- ⚠️ Não commitado — vive só neste projeto Cowork.
- **Leva 1 do espelho — Fluxo · DRE · Conciliação** (modo "só as faltas", lido no `main` NESTE turno: charters `Fluxo/Index` v2, `Dre/Index`, `Conciliacao/Index` v2 + `BalancoView.tsx` + `BalanceteView.tsx` + ADR 0236):
  - **Fluxo** ganhou a ótica que faltava: pill `Projetado · próx 35 dias` | `Realizado · últ 12 meses` (US-FIN-014c) com 4 KPIs (Saldo 12M · Entradas · Saídas · Baixas registradas), barras gêmeas por mês (mês atual mais escuro), tabela "Detalhe por mês" com linha total e rótulo `atual`; KPI "Saldo hoje" agora nomeia a conta principal + "outras N"; empty state dos próximos eventos; breadcrumb + Esc (a tela não tinha volta).
  - **DRE** ganhou as abas da Fase 4: `Demonstrativo` | `Balanço patrimonial` | `Balancete de verificação`, cada uma com o banner obrigatório "Versão gerencial" ([W] 2026-05-21). Balanço em 2 colunas (Ativo | Passivo + PL) + rodapé da equação A = P + PL; Balancete hierárquico por plano de contas com D/C e total geral.
  - **Conciliação** virou a lista de DUAS origens (ADR 0236): chip `Banco`/`OFX` por linha, KPI **Ignoradas** no lugar do 4º card mudo, ação **Ignorar/Reabrir**, busca por descrição e **match score calculado** (0,7·valor + 0,3·data) no lugar do "95%" chumbado; sumiu o "Desfazer conciliação" (Non-Goal append-only do charter).
  - Higiene: 4 classes quebradas (`bg-[var(--pos-soft)]0`) consertadas; `useEffect` faltando no escopo do arquivo de telas extras.
  - Novo arquivo de build: `financeiro-relatorios.jsx` (Realizado · Balanço · Balancete · pill compartilhado) — registrado no host antes de `financeiro-telas-extras.jsx`.
- **Próximas levas pedidas por [W]:** 2) Cobrança · Caixa · Impostos e obrigações · 3) Relatórios · Plano de contas + Categorias · Contas bancárias + Extrato · 4) Advisor (portal contador). Modo: só as faltas.
- ⚠️ Não commitado — vive só neste projeto Cowork.

### Sync 2026-08-17 (Visão Unificada)
- **Módulo Financeiro sincronizado com o `main`** (lido NESTE turno, tree `660602c31fe2`): `Unificado/Index.charter.md` **v22**, `Index.casos.md`, `unificado-gap.md` (2026-07-01, paridade ~96%), ADR 0313 (subnav unificada), `contrato/financeiro-unificado.intent.json`, `_components/FinPillContaIndefinida.tsx`, `_cowork-bundle/README.md`, CHANGELOG do módulo. Conferência de bytes: os 10 arquivos locais do financeiro são **idênticos** aos de `prototipo-ui/cowork/` no `main` (zero drift do export); o atraso era só em relação ao **vivo** (v17→v22, jul/2026).
- Aplicado no protótipo (aqui o vivo estava à frente): (a) **US-FIN-031/v17** — rodapé de lote com as 5 ações (baixar · categoria · plano de contas · cancelar com confirmação destrutiva "N títulos totalizando R$ X" · exportar CSV da seleção) + teto 500/lote + `cancelado` append-only fora da lista/totais; (b) **v21** — tipo escolhido ANTES do form (menu no primário + ações no ⌘K), nunca a rota legada `/unificado/novo`; (c) **US-FIN-038/v22** — pill "Conta indefinida" (warning leve por token, o próprio pill é CTA) na coluna Conta e no drawer quando a baixa não tem conta bancária.
- Pendente do vivo, **não** portado (registrado): toggle **Arquivados** (mostrar só cancelados, v13) · KPIs excluindo cancelado · drag&drop + multi-upload nos anexos (P7a do gap) · motivo de rejeição inline (P8a). Nenhuma mudança de backend proposta.
- ⚠️ Não commitado — vive só neste projeto Cowork; ponte pro `main` = cola zero-toque ou `cowork-inbox`/Issue.

### Sync 2026-08-13
- `main` andou `9a121aea5957` → **`cacf627639cd`**: as 10 correções do pedido dark rev.2 **estão aplicadas** no `Index.tsx` (farol/badge/KPI strip em token, tint opaco `color-mix` sobre `--color-card`) + as **5 âncoras `data-contract`** foram adicionadas. Charter **v5** ratificado por [W] 2026-08-12 (revoga a prescrição de cor do "Demo polish v2").
- Escrito: `cowork-inbox/JANA-CICLO-COMPLETO-PRODUCAO-2026-08-13.md` — ciclo completo de `/ia`. Vereditos derivados da leitura dos gates (não rodei nada): `ciclo-completo` = **1 de 6** (`pt_declarado` reprova porque `related_prototype` é `jana-merge.jsx` e o gate casa só `/PT-0[1-5]/`; `Index.casos.md` **não existe**; `golden_live` impossível — PT-04 é `draft`, gate = screenshot [W] / ADR 0107). `contrato:check` **passa** (5/5 âncoras + copy + ordem).
- 🔴 Buraco aberto: `config/eslint-baseline.json:234` ainda dá `Jana/Index.tsx|no-restricted-syntax: 6`; o ratchet só falha em regressão (sem shrink-check) → **crédito de 6 violações grátis**, o gradiente pode voltar com o gate REQUIRED verde. Conserto: `lint:baseline:write`.
- 🔴 Âncora `jana-merge.jsx` **envenenada** (medido 2026-08-13, nota do charter v5): renderiza "FROTA UTILIZAÇÃO"/`Locadas`/"caçambas" (`frota` 8× · `caçamba` 7×) e o `dominio-gate` **não pega** — `forbidden_ui_paths` não inclui `prototipo-ui/`.
- Cor crua ainda viva no módulo: `Chat.tsx:90-92` e `Memoria.tsx:49` (este **sem par `dark:`** → quebra no escuro). Hooks conferidos: registrados em `.claude/settings.json` PreToolUse, `gate selftest` REQUIRED.

### Sync 2026-08-12
date: 2026-08-12T20:50:00Z

### Updated in this project
- Leitura do `main` NESTE turno (tree `9a121aea5957`): tree `Modules/Jana/Resources/views` e `resources/js/Pages/Jana`, grep TODO/STUB nos controllers, `memory/requisitos/Jana/AUDIT-GAPS-2026-08-10.md` (555/555 arquivos auditados), `Pages/Jana/Index.tsx`.
- **Nada mudou desde 10/08 no eixo UI:** 8 blades de tela seguem vivas, `AcaoHitlModal`/`JanaConfigDrawer` ausentes, `alertas/config` não persiste (Cluster C prova por quê: `AlertaService`+evento+listener são código morto).
- Correção de premissa minha: as fronteiras JÁ estão definidas em git (`SCOPE.md`, ADR 0363, ADR 0093, precedente US-COPI-147) — o que falta é execução de UI e conserto de registro, não decisão [W]. Drift registrado: `SCOPE.md` roteia pra `Modules/ADS` (removido), `db_tables_owned` 13×59, `module.json` afirma prefixo `copiloto.*` falso.
- Escrito: `cowork-inbox/JANA-PAINEL-DARK-PARIDADE-2026-08-12.md` **rev 2** — lidos `JanaCockpit.tsx`, `KpiCard.tsx`, `inertia.css` e os dois `tokens/_generated-inertia-*.css`. **Errata da rev 1:** o cockpit e o KpiCard estão limpos (zero cor crua); a causa real é colisão de duas famílias de token no escuro — `--accent` é roxo no cockpit e cinza de hover no Tailwind; `--color-primary` clareia 0.55→0.70 só de um lado; `bg-primary/5` do brief compõe sobre `--color-background` (0.26) em vez do card (0.30). 10 correções, 8 locais + 2 Tier 0 de token.

### Sync 2026-08-10
- Releitura do `main` (tree `aec4d8d5ac70`) pra situar a Jana nas ondas: `MetasController`, `AlertasController`, `package.json`, `scripts/governance/module-surface.mjs`, tree de `Modules/Jana/Resources/views` e `resources/js/Pages/Jana`, busca de STUB nas views.
- **Fechados desde 2026-08-09:** S-1 (`reapurar` despacha `ApurarMetaJob` com `business_id` explícito), S-2 metade honesta (mensagem "Ainda não é possível salvar…"), S-3 (zero "STUB"/"spec-ready" nas views), A-1 (`Views (Blade)` com `listar:true` desde 09/08), A-3 (`--migracao` restaurado — tinha sido deletado por acidente no rebase #5327 e ficou 3 dias morto).
- **Aberto:** as 8 blades de tela seguem vivas em `@extends('layouts.app')`; `AcaoHitlModal.tsx` e `JanaConfigDrawer.tsx` ainda ausentes. Novo dono do eixo "servido": `blade-migration-census.mjs` (ADR 0277) — A-2 vira consumo dele, não medidor novo. Item novo S-1b (reapurar por range = contrato de rota novo, Tier 0).
- Atualizado: `cowork-inbox/JANA-MODULO-ONDAS-PR-2026-08-09.md` §0.a "Onde estamos" + aviso de colisão de numeração (repo usa "Wave N"; as minhas são [CC]-0..12).

### Sync 2026-08-09

### Updated in this project
- Leitura do `main` NESTE turno (tree `f1a9606cca64`): `Modules/Jana/**` (tree 554 arq.), `Http/routes.php`, `IndexController` · `MetasController` · `PeriodosController` · `AlertasController` · `SuperadminController`, as 8 views Blade de `Resources/views/`, tree `resources/js/Pages/Jana/`.
- Achado: 8 blades órfãos vivos em `/ia/*` (metas ×4, alertas ×2, superadmin, fontes) fora do shell Inertia/DS; `reapurar` não despacha o `ApurarMetaJob` que existe; `alertas/config` não persiste; fatias D/E do FASE2 não aplicadas (`AcaoHitlModal`/`JanaConfigDrawer` ausentes).
- Leitura de governança (tree `00a25298e597`, main andou na sessão): `package.json`, `scripts/governance/module-surface.mjs`, os 37 `memory/requisitos/**/SUPERFICIE.md`, `governance/route-hits.json`, `Index.tsx`, `JanaAreaHeader.tsx`.
- Achados de instrumento: `SUPERFICIE.md` da Jana já diz `Views (Blade) — 9` × `Telas (Inertia) — 4`, mas o papel Blade tem `listar:false` (só contagem + link de pasta); `npm run migracao:report` está morto (`--migracao` não existe no parser → `exit(2)`); semente do Cliente anota "25 Blades VIVAS".
- Escrito: `cowork-inbox/JANA-MODULO-ONDAS-PR-2026-08-09.md` **revisão 2** — inventário atômico (A/B/S/U/N/G + tier + tamanho), anexo dos 8 blades (quem chega, hits, o que se aproveita), **Onda 0 transversal** (auditoria derivada no `module-surface.mjs`) + ondas 5–12 com cláusula de entrada/saída, 11 cláusulas gerais, fila de 8 decisões [W]. Substitui a tabela de ondas do FASE2 §2 (fatias D–L preservadas).

### Sync anterior (2026-08-08, ~21:35Z)
- Leitura do `main` NESTE turno: `Forja/Roadmap/Gantt.tsx` + `Gantt.charter.md` (tree do Roadmap; a própria página documenta por que abriu vazia em produção — Page sob /forja/* precisa montar ForjaHub) e `config/core_topnavs.php` (turno anterior desta sessão).
- Handoff pro Code preparado: pedido consolidado (PR-0..7 + ordem + relato) em `cowork-inbox/FORJA-TOPNAV-3GRUPOS-LEVA1-2026-08-08.md`; build exportado em `prototipo-ui/cowork/forja/`.
- Protótipo: **Gantt** copiado como 3ª sub-visão de Trabalho (`GanttView`): barras por módulo, progresso por status, prazo arrastável (só due, B2), bloqueio sinalizado, drawer único. Pedido do [CL] ganhou PR-7; charter T12.

### Sync anterior (2026-08-08, ~20:06Z)

### Updated in this project
- Leitura do `main` NESTE turno: `Tasks/Index.tsx` (28.8KB) — o conceito de Tarefas da produção.
- Protótipo: aba **Tarefas** copiada fiel ao conceito canon (`forja-tarefas.jsx`): todas as mcp_tasks, KPIs (Total/P0/Fazendo/Bloqueadas), Backlog|Quadro internos, agrupamento com “— sem onda —”, sort STATUS_ORDER→prio→id, ActorSeal humano×agente por nome, estimate_h, Lock em bloqueada, bulk → status, drag no quadro (4 colunas ativas), densidade, drawer com mudança de status.

### Sync anterior (2026-08-08, ~20:00Z)
- Leitura do `main`: `ForjaQuadro.tsx`, `ForjaBacklog.tsx`, `taskTokens.ts`, `taskBadges.tsx`, `config/core_topnavs.php` + buscas por `/forja/aprovacoes` (nada aterrissou dos pedidos).
- Achados: quadro canon é F0→F3.5 **só de telas**; trabalho geral usa status canon; topnav da Forja com **10 itens** — Roadmap (Gantt) entrou 2026-08-06 e abriu vazia em produção.
- Protótipo corrigido: Quadro com 2 eixos, fase só no pipeline, StatusPill canon. Charter v2 (T9) e pedido do [CL] (PR-1.5 + Roadmap, 10→3) atualizados.

### Sync anterior (2026-08-08, ~16:30Z)
- Leitura do `main`: charters de `Team/Index`, `Scorecard/Index` e `CcSessions/Index` (o que a produção já monitora: tokens/quotas BRL · Facts+Checks sem série · sessões com custo por dev).
- Protótipo: landing **Hoje** (fila de decisões + próximos 3 + pulso) ganhou o **placar da equipe de agentes** (heartbeat, custo/quota, critique F1.5, retrabalho, entregas) — tudo derivável de dado existente.
- Pedido pro [CL] atualizado: `cowork-inbox/FORJA-TOPNAV-3GRUPOS-LEVA1-2026-08-08.md` (PR-0 Hoje agora inclui o placar).

### Sync anterior (2026-08-08, ~16:14Z)
- Leitura do `main` (Forja × produção): tree `Pages/team-mcp/`, `Forja/Cockpit.tsx` + `Cockpit.charter.md` (errata 2026-07-27).
- Achado: hub real tem 9 itens de topnav (5 próprios + 4 TeamMcp absorvidos), Saúde fundida no `/team-mcp/scorecard`, dupe Backlog×Tarefas, Handoffs enterrado na aba MCP.
- Protótipo: leva 1 (rank híbrido+pin, roll-up de épico, massa, autocomplete) + topnav 7→3 grupos em `forja-page.jsx`.

### Sync anterior (2026-08-07)
- Leitura do `main` (conferência Jana): `Pages/Jana/` (tree), `Index.tsx` + `Index.charter.md`, `components/JanaAreaHeader.tsx`, `_shared/JanaSubNav.tsx`, `Modules/Jana/Http/routes.php`, grep em `_components/` e `Http/Controllers/`.
- Achado: ondas 1–4 da fusão APLICADAS no main — `Painel.tsx`/`Cockpit.tsx`/`Dashboard.tsx` não existem mais; `/ia` é o Painel (`IndexController`), `/ia/conversa` é o chat, 301 de `/ia/painel|dashboard|cockpit`. Novo `_components/JanaDrillDrawer.tsx` (fatia B) e filtros/J/K/⌘⇧H no `Chat.tsx` (fatia C).
- Comparativo protótipo × produção entregue em chat (sem arquivo novo).

### Sync anterior (2026-08-06)
- Lido no espelho local da pasta anexada (NÃO no `main` neste turno): `Modules/Jana/Http/routes.php`, `PainelController`, `DashboardController`, `ChatController`, `Pages/Jana/{Chat,Cockpit,Dashboard,Painel,Memoria,Pro}.tsx` + charters.
- Achado: o cockpit está implementado 2× (`Cockpit.tsx` e `JanaCockpitV2` dentro de `Dashboard.tsx`) e `Painel.tsx` é só um hub de 3 links.
- Fusão prototipada no app único (`jana-merge.jsx` + `.css`, rota `chat`): abas Painel · Conversa · Memória, Metas como seção do Painel.
- Pedido zero-toque pro [CL]: `cowork-inbox/JANA-FUSAO-2026-08-06.md`. Build exportado em `prototipo-ui/cowork/`.

## Screen map
| Tela (rota) | Arquivos deste projeto | Fonte no repo |
|---|---|---|
| Vendas — Nova venda V3 (`vendas` · sub `create`) | `venda-v3.jsx` · `venda-v3.css` | `resources/js/Pages/Sells/CreateV3.tsx` + `Sells/_components/v3/**` + `CreateV3.charter.md`/`.casos.md` · build `prototipo-ui/cowork/venda-v3/` |
| Financeiro — Fluxo · DRE/Balanço/Balancete · Conciliação · Plano de contas · Impostos (rotas internas do módulo) | `financeiro-telas-extras.jsx` · `financeiro-relatorios.jsx` · `financeiro-ops.css` | `resources/js/Pages/Financeiro/{Fluxo,Dre,Conciliacao}/**` (charters + `Dre/_components/Balan{co,cete}View.tsx`) · ADR 0236 |
| Financeiro — Visão unificada (`financeiro`) | `financeiro-page.jsx` · `financeiro-ops.jsx` · `financeiro-ops.css` · `financeiro.css` · `financeiro-data.jsx` · `financeiro-icons.jsx` · `financeiro-curation.jsx` · `financeiro-ai.jsx` · `financeiro-output.jsx` · `financeiro-telas-extras.jsx` | `resources/js/Pages/Financeiro/Unificado/**` (Index.tsx + charter v22 + casos + `_components/`) · `memory/requisitos/Financeiro/unificado-gap.md` · `prototipo-ui/contrato/financeiro-unificado.intent.json` · ADR 0313 |
| Forja (`projects`/`teammcp`) | `forja-page.jsx` · `forja-page.css` · `forja-data.jsx` · `forja-aprova.jsx` · `forja-tarefas.jsx` · `forja-mcp.jsx` · `forja-integra.jsx` | `resources/js/Pages/team-mcp/**` (Forja/Cockpit + _components · Tasks · Team · Scorecard · CcSessions) · `config/core_topnavs.php['Forja']` |
| Compras (`compras`) | `compras-page.jsx` · `compras-page.css` · `modulo-padrao.jsx` · `modulo-padrao.css` | `prototipo-ui/FRESCOR-PRODUCAO-vs-PROTOTIPO.md` (Compras 🟠) · `resources/js/Pages/Compras/components/GradeMatrixInput.tsx` (órfão) |
| Padrão de módulo (transversal) | `modulo-padrao.jsx` · `modulo-padrao.css` | derivado de `resources/js/Pages/Jana/**` (Painel/Conversa/Memória, fusão ondas 1–4) |
| Jana — fusão (`chat`) | `jana-merge.jsx` · `jana-merge.css` · `chat-jana.jsx` · `chat-jana.css` | `resources/js/Pages/Jana/*` · `Modules/Jana/Http/routes.php` |
| Documentação (`documentacao`) | `documentacao-page.jsx` · `documentacao-page.css` | `memory/GUIA-DO-SISTEMA.md` |
| Dashboard legado — Visão geral (`dash-legacy`) | `dash-legacy-page.jsx` · rota em `app.jsx` · atalho em `data.jsx` | `resources/views/home/index.blade.php` + `home/partials/*` + `public/js/home.js` + `app/Http/Controllers/HomeController.php@indexLegacy` · Ficha `cowork-inbox/FICHA-BL-home-index.md` |
| Superadmin — visão · negócios · assinaturas · pacotes · comunicador · configurações (`superadmin`, `sa-*`) | `superadmin-page.jsx` · `superadmin-page.css` · rota em `app.jsx` · nav em `data.jsx` | `Modules/Superadmin/Resources/views/**` (superadmin/index · business/index · packages/index · superadmin_subscription/index · communicator/index · superadmin_settings/partials/* · layouts/nav) |
| Módulos — gerenciador (`modulos`) | `modulos-page.jsx` · `modulos-page.css` · export `prototipo-ui/cowork/modulos/` · trio `cowork-inbox/MODULOS-F1-2026-08-19.md` · ondas `cowork-inbox/MODULOS-F3-ONDAS-PARA-CODE.md` | `resources/js/Pages/Modules/Index.tsx` + `Index.charter.md` · `app/Http/Controllers/ModuleManagementController.php` · `app/Services/ModuleManagerService.php` · `modules_statuses.json` · `Modules/*/module.json` |
| Modelos de notificação (`notificacoes`) | `notificacoes-page.jsx` · `notificacoes-page.css` · rota em `app.jsx` · nav em `data.jsx` · export `prototipo-ui/cowork/notificacoes/` · trio `cowork-inbox/NOTIFICACOES-F1-2026-08-19.md` · contrato `cowork-inbox/notificacoes/notificacoes.contract.json` | `app/Http/Controllers/NotificationTemplateController.php` · `app/NotificationTemplate.php` · `resources/views/notification_template/index.blade.php` + `partials/{tabs,tags}.blade.php` · `app/Utils/NotificationUtil.php` · `app/Console/Commands/AutoSendPaymentReminder.php` |
| Site (CMS) — conteúdo · blog · depoimentos · leads · detalhes · módulo (`site`, `cms-*`) | `cms-page.jsx` · `cms-extras.jsx` · `cms-page.css` · rota em `app.jsx` · nav em `data.jsx` · export `prototipo-ui/cowork/cms/` · trio `cowork-inbox/cms/` + `cowork-inbox/CMS-F1-2026-08-19.md` | `Modules/Cms/Http/Controllers/{CmsPageController,SettingsController,InstallController,CmsController}.php` · `Entities/{CmsPage,CmsPageMeta,CmsSiteDetail}.php` · `Http/Requests/SubmitContactFormRequest.php` · `Services/{CmsLeadService,SiteContentService}.php` · `Console/{ImportWpOfficeImpressoCommand,Commands/CmsHealthCommand}.php` · `Resources/views/{page/*,settings/*,layouts/nav}.blade.php` · precedente `Modules/Cms/Resources/js/Pages/Site/Page.charter.md` |
| Conector (API) — clients OAuth · documentação · saúde · módulo (`connector`, `conn-*`) | `connector-page.jsx` · `connector-api.jsx` · `connector-page.css` · rota em `app.jsx` · nav em `data.jsx` · export `prototipo-ui/cowork/connector/` · trio `cowork-inbox/connector/` | `Modules/Connector/Resources/views/clients/index.blade.php` · `Http/Controllers/{ClientController,ConnectorController,DataController,InstallController}.php` · `Http/Requests/StoreOauthClientRequest.php` · `Routes/{web,api}.php` · `Services/DelphiSyncService.php` · `Console/Commands/ConnectorHealthCommand.php` · `module.json` · `Resources/lang/pt/lang.php` |
| HRM (Essentials `/hrm`) — painel · licenças · presença · turnos · folha · feriados · metas · configurações (`hrm`, `hrm-*`) | `hrm-page.jsx` · `hrm-extras.jsx` · `hrm-data.jsx` · `hrm-page.css` · rota em `app.jsx` · nav em `data.jsx` | `Modules/Essentials/Routes/web.php` (prefixo `hrm`) · `Http/Controllers/{EssentialsLeave,EssentialsLeaveType,EssentialsHoliday,EssentialsSettings,Attendance,Shift,SalesTarget,Payroll,Dashboard}Controller.php` · `Resources/views/{layouts/nav_hrm,dashboard/hrm_dashboard,leave,leave_type,holiday,attendance,payroll,sales_targets,settings}/**` · `Resources/lang/pt/lang.php` (tradução quebrada, achado A1) |
| Programa de documentação (`programa-doc`) | `programa-doc-page.jsx` · `programa-doc-page.css` | `memory/requisitos/_Governanca/programa-ondas/PLANO-MESTRE.md` § Trilha D |

## Sync history
- 2026-08-06T12:06:38Z — Trilha D do PLANO-MESTRE lida; patch em `cowork-inbox/PLANO-MESTRE-trilha-d-ciclo-completo.md`; tela `programa-doc`.
