---
sessao: "_errata"
titulo: Errata do [CL] ao playbook SINCRONIZAR Hrm — 5 achados medidos contra origin/main
autor: "[CL]"
criado: 2026-09-05
base: wagnerra23/oimpresso.com@main ffe332cadbb7 (2026-09-05 22:30:26 UTC)
regra: este arquivo NÃO edita o pedido do [CC]. É o canal de retorno (§3 do índice — cada sessão escreve só no seu prefixo e no seu arquivo). Os itens 3 e 4 precisam voltar pra FONTE (projeto Cowork), senão o próximo download reintroduz.
---

# Errata — o que mudou embaixo do playbook enquanto ele era escrito

O playbook rev.2 foi lido às 22:26 UTC sobre o commit `45e63465` (22:22). O `main` andou depois disso.
Tudo abaixo foi medido contra `origin/main` = `ffe332ca`, com `git`/`gh`, e conferido pelo próprio `placar-indice.mjs`.

## 1 · Thread 02 (Licenças) já está em produção — as 6 provas passam

`#6868` *"feat(essentials): tela Licenças em Inertia — PR-9 da onda HRM-O7"* mergeou às **22:30:26 UTC**, 4 min depois da base declarada. Entregou o pacote de 8 peças da irmã golden:

| prova do §7 (thread 02) | estado |
|---|---|
| `um_de` tsx | ✅ `resources/js/Pages/Essentials/Licencas/Index.tsx` |
| `um_de` charter | ✅ `Licencas/Index.charter.md` |
| `um_de` casos | ✅ `Licencas/Index.casos.md` |
| `json_com_chaves` contrato (`alvo`, `secoes`) | ✅ `prototipo-ui/contrato/essentials-licencas.contract.json` |
| `arquivo` e2e | ✅ `e2e/essentials-licencas.spec.ts` |
| `contem` D4 render | ✅ `EssentialsLeaveController.php:191` → `Inertia::render('Essentials/Licencas/Index'` |

Fora das provas, também entraram `Modules/Essentials/Tests/Feature/HrmLicencaTest.php`, `memory/requisitos/Essentials/RUNBOOK-licencas.md` e a lane (`.github/workflows/essentials-pest.yml:112` cita `HrmLicencaTest.php`).

**Este é o MESMO defeito que a §5 R4 corrigiu para Metas** (*"Metas estava feita 32 s antes da base declarada e a rev.1 mandava reconstruí-la"*), repetido em Licenças com 4 min. O [CC] não tinha como saber — a rev.2 é de 22:26 e o merge é de 22:30.

**Consequência:** `02-licencas.md` ainda diz `(CRIAR)` e `PASSO A PASSO: 2) criar-tela.mjs`. Quem abrir a thread 02 pelo texto vai reconstruir uma tela viva. O tratamento canônico existe e é o da 04: dono `[CC]`, **PUXAR**, `provas: []`, arquivos do #6868 em `nao_toca`. **Isso é edição do pedido — cabe ao [CC], não a mim.** Até lá, esta errata é o aviso.

O `playbook.schema.json` já diz por que a forma importa: *"Provas explícitas = evidência de trabalho NOVO no repo — nunca arquivo que já existia antes da thread (isso é reuso e vai em `nao_toca`/nota)"*.

## 2 · Os 2 planos de `memory/sessions/` EXISTEM

O §1 e o §3 afirmam, em presente, que não existem e mandam *"não bloqueie neles"*. Medido:

```
f327fde46d  2026-09-05 22:18:22 UTC  #6877  docs(hrm): aterrissa os 2 planos do HRM
  memory/sessions/2026-09-05-como-integrar-ponto-hrm.md
  memory/sessions/2026-09-05-arte-folha-encargos-br.md
```

Nasceram **4 min ANTES** da base que o índice carimba (`45e63465`, 22:22). A medição do [CL] em `a88c66a` (22:11) era honesta na data; o que apodreceu foi ela ter sido reescrita em presente sob uma base mais nova — o padrão do §5 2026-09-01 de `proibicoes.md` (*afirmação de bloqueio em doc canon vira instrução de desistência*). **Quem abrir qualquer thread: leia os dois.** A Emenda [W] os cita de propósito.

## 3 · `placar-indice.mjs` não rodava contra o índice — 2 defeitos, consertados aqui

O §2-bis manda rodar o placar sobre o `00-INDICE.md`. Ele não rodava:

| # | defeito | efeito |
|---|---|---|
| a | `JSON.parse(readFileSync(idxPath))` — lia o `--indice` como JSON puro, sem extrair o bloco ```json embutido | `SyntaxError: No number after minus sign` (tentava parsear o `---` do frontmatter) |
| b | `avaliarProva` não tratava o tipo `um_de` (que o schema declara no enum) e estourava em `resolverPath(prova.path)` com `undefined` | `TypeError: Cannot read properties of undefined` — 18 das 28 provas do índice são `um_de` |

O cabeçalho do próprio script ainda diz `--indice cowork-inbox/hrm/playbook/playbook.json`: ele foi escrito para o desenho **antigo** (JSON solto) e o §0 mudou para embutido sem que ele acompanhasse. Por isso o `#6885` precisou inventar um `playbook.json` — que é o **segundo dono do estado** que o §0 proíbe (*"a fonte da máquina não é arquivo: é o primeiro bloco ```json deste índice"*) e que ia drifar contra o §7. Removido nesta branch.

⚠️ **Os dois consertos precisam voltar para a FONTE** (`cowork-inbox/_scripts/placar-indice.mjs` no projeto Cowork). Enquanto não voltarem, o próximo `get_file` reintroduz os defeitos.

## 4 · `PR-A8` não existe no `COLAR-NO-CODE-AUTOMACAO-DO-PROTOCOLO.md`

O §0 diz que schema e script *"viajam como anexos de `COLAR-NO-CODE-AUTOMACAO-DO-PROTOCOLO.md` (PR-A8)"*. Aquele doc vai até **PR-A7** (`## PR-A1` … `## PR-A7`) e não menciona playbook, schema nem índice. Na prática os dois existem como **arquivos próprios** no Cowork (`cowork-inbox/_schema/playbook.schema.json` · `cowork-inbox/_scripts/placar-indice.mjs`) e é assim que desceram. O destino que o §2-bis manda usar (`scripts/qa/placar-indice.mjs`) **também não existe** no `main` — zero ocorrências de `placar-indice` fora desta pasta. Aterrissaram em `prototipo-ui/design-docs/cowork-inbox/{_schema,_scripts}/`; promovê-los a `scripts/qa/` é decisão [W] (vira máquina de repo, com gate).

## 5 · Thread 09 — a ADR 0014 já está `lifecycle: arquivado`

A prova `{"tipo":"nao_contem", "padrao":"lifecycle: arquivado"}` nasce insatisfeita: `memory/decisions/0014-essentials-pontowr2-integracao.md:8` **já traz** `lifecycle: arquivado` (o `09-presenca-sai.md` inclusive a descreve como arquivada no corpo). Executar a 09 implica **desarquivar** + emendar corpo datado — o que puxa `adr-metadata-normalization` (frontmatter, ADR 0257) **e** `adr-body-edit-W` (corpo, ADR 0377), duas exceções por-label do Append-only. Coerente com o dono ser `[W]+[CL]`, mas quem pegar a 09 bate nisso no primeiro commit.

## Placar real (saída do `placar-indice.mjs` corrigido, contra `ffe332ca`)

```
Hrm: entregue 0 de 11 · próximo 5 · em curso 1 · pendente 4 · bloqueada 1
PRÓXIMO: 01 [CC] · 03 [CL] · 07 [CC->CL] · 08 [CC] · 09 [W+CL]
```

Contra o render do §2-bis (`próximo 6 · em curso 0`): **02 saiu de `proximo` para `em curso`** — as 6 provas passam, falta só o `_saida-02.md`. As outras 10 linhas batem com o índice.

## Colisão medida no momento desta errata (`gh pr list --state open`, o substituto do `whats-active` morto)

| PR | branch | toca | situação |
|---|---|---|---|
| #6867 | `claude/hrm-tipos-tela` | **thread 03** — `Tipos.tsx` + trio + contrato + e2e + lane | **sessão VIVA** — não abrir a 03 |
| #6885 | `claude/hrm-playbook-intake` | esta pasta | sessão parada; assumida por esta branch |
| #6881 | `claude/adr-folha-encargos` | ADR mãe da folha (thread 10) | viva |
| #6880 | `claude/payroll-total-servidor` | `PayrollController` | viva — thread 10 declara `nao_toca` nele |

Ou seja: dos 5 do `PRÓXIMO`, a **03 já está sendo feita**. Livres agora: **01 · 07 · 08 · 09**.
