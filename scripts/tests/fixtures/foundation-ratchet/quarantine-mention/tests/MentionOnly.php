<?php

declare(strict_types=1);

/**
 * Âncora real do caso (Fase 2), NÃO o OutroWaveTest — que é source-grep + @group legacy-quarantine,
 * fora de lane. Esta MENÇÃO em prosa NÃO deve contar como quarentena (FORMA ≠ USO, ADR 0275).
 */
test('mention only NAO conta', fn () => expect(true)->toBeTrue());
