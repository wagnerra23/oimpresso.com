# Copiar para .env e preencher (nunca commitar .env)

# Gerar com: htpasswd -nb admin SUA_SENHA
# No servidor: apt install apache2-utils && htpasswd -nb admin SUA_SENHA
# Escape $$ necessário no compose: trocar cada $ por $$
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$xxxxxxxx$$xxxxxxxxxxxxxxxxxxxxxxxx

# Master key Meilisearch (mesma que no Hostinger .env)
MEILI_MASTER_KEY=TFLfQX3Diuz42MydPn68AYH9Km1JbaBI

# Email para Let's Encrypt
ACME_EMAIL=wagnerra@gmail.com
