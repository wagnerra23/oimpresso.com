# Gabarito selado — rodada 2 calibração do juiz (Fable, sessão fresca, base origin/main)

> Método: `git ls-tree -r origin/main Modules/<X>` (contagens) + `git log origin/main -- Modules/<X>` + `modules_statuses.json` + BRIEFING/SPEC via `git show origin/main:` — veredito decidido do código, não do campo status dos docs. Critério discriminador adotado: `parcial` exige algo REAL rodando em prod (ou motor completo apenas flag-gated); `em-construcao` = incompleto e não serve ninguém.

| # | Módulo | Status (enum) | Justificativa 1-linha com evidência (path/contagem/git) |
|---|--------|---------------|----------|
| 1 | ComunicacaoVisual | em-construcao | 67 files/4 ctrl/20 tests mas só 1 tela real (calculadora m²) + 2 APIs; PCP/FSM/materiais/NFe = "schema órfão" sem controller/rota (BRIEFING textual); zero cliente, piloto só Q3/2026; commits recentes são sweeps (#3660, #4117), não feature. |
| 2 | Compras | em-construcao | Modules/Compras = 21 files/3 ctrl/0 migrations (cockpit fino sobre `transactions` core); BRIEFING 07-03 confirma "NÃO está em produção nem canary pra nenhum business (D5=0)"; motor central (ponte DF-e→compra US-COM-003, matching, recebimento) inexistente no código. |
| 3 | KB | parcial | 126 files/14 ctrl/30 tests, dev ativo até 2026-07-17 (#4465 classificador auto_match); backend LIVE em prod biz=1 (bridge 15-em-15min + CRUD `kb_nodes`) mas a tela flagship /kb/v2 roda 100% mock — Controller nunca ligou dado ao frontend (BRIEFING [W] 07-17). |
| 4 | NFSe | parcial | 57 files com motor de emissão construído (`SnNfseAdapter` + emitir/cancelar instrumentados #2678 + `ImportarCertificadoCommand` + health) porém em homologação SN-NFSe com cert A1 pendente = não emite em prod (padrão PaymentGateway "código real, flag OFF"); zero commit de feature desde 06-14. |
| 5 | ProjectMgmt | meta | 50 files/12 ctrl (Board/Backlog/Roadmap/MyWork/Inbox/Triage/Burndown) = interface visual sobre `mcp_tasks` (ADR 0070) pro time interno — substitui Jira/Linear pra Wagner+Felipe+Maiara+Eliana+Luiz; piloto "time interno (uso próprio)", não é produto de negócio. |
| 6 | Auditoria | shared-infra | 37 files: UI /auditoria per-business (Spatie `auditoria.view/revert.*`, stack UltimatePOS em Routes/web.php) sobre `activity_log` cross-tenant — trait `LogsActivity` padronizado consumido por Models de App\ e Modules (Transaction, Product, Financeiro\Titulo); transversal a todos businesses, não vendável isolado, sem cliente direto. |
| 7 | Accounting | deprecated | `Modules/Accounting` = 0 files em origin/main (drop Onda 5, commit 2026-05-20 "drop Modules/Accounting + shim retention"); `modules_statuses.json` = false (único); ADR 0172 deprecação + DEPRECATION-PLAN 7 ondas; substituto = Modules/Financeiro. |
| 8 | SRS | deprecated | 64 files ainda no repo mas só sweeps (perf #3902); estado ZUMBI: BRIEFING "substituído na prática pelo MCP server, ❌ não investir"; DEPRECATION-PLAN.md com decisão Wagner "Caminho 1 (deprecar)" e sucessores mapeados (KB primário + Jana + Governance). |
| 9 | ADS | meta | 119 files/15 ctrl/22 svc/19 tests — meta-sistema de governança de decisão de agentes (PolicyEngine/RiskEngine/DecisionRouter/HITL, Admin/Skills+Learning+Decisoes controllers), DORMANT pending signal (ADR 0105), infra interna Wagner-only; não fala com cliente final por definição própria. |
| 10 | Manufacturing | shared-infra | 117 files legacy UltimatePOS (BOM/recipes/production_purchase) estável sem produto próprio novo; consumido por OficinaAuto (custeio FSM, piloto prod Martinho biz=164), ComVis e core (`ProductController`/`ProdutoUnificadoController`/`ReportController` referenciam Mfg via git grep); commits só sweeps D-14 (#3903). |
| 11 | Pcp | backlog | `Modules/Pcp` inexistente (git ls-tree vazio, git log vazio); só memory/requisitos/Pcp/ (BRIEFING "🌱 WISH — Não há código", ADR 0152 dormente, porta KL-E2 aguardando sinal qualificado ADR 0105). |
| 12 | Officeimpresso | parcial | 107 files/7 ctrl: licenciamento desktop legacy ATIVO em prod (`LicencaComputadorController` + `LicencaService` + audit append-only + Passport, superadmin-only) servindo clientes Delphi reais; porém o importador Firebird central (`officeimpresso:import`) é mock-only (`pdo_firebird` ausente → `:mock:`) — migração real roda por scripts Python fora do módulo; 14 telas Blade sem migração React. |
