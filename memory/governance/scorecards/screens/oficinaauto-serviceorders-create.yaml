# Gerado por scripts/qa/screen-grade-seed.mjs a partir do baseline canônico
# screen-grades-baseline-2026-05-30.json (método "SCREEN-GRADE-9.75").
# A nota 16-dim é LLM-as-judge (agente screen-qa) — NÃO editar à mão.
# Este YAML é o ponto de partida do ratchet: a nota não pode cair abaixo de baseline_anterior.
screen: OficinaAuto/ServiceOrders/Create
path: resources/js/Pages/OficinaAuto/ServiceOrders/Create.tsx
archetype: form
persona: tecnico
nota: 66
nivel: "Developing"
baseline_anterior: 66   # ratchet — nota não cai abaixo disto sem aprovação
peso_real: 1.0
source: screen-grades-baseline-2026-05-30
graded_at: "2026-05-30"
resumo: "Form de OS em Sheet 720 funcional com referer-close, mas selects/textarea nativos e erros parciais o deixam scaffold."
dimensoes: {}   # 16 dims individuais: a regradeação pelo agente preenche (baseline só tem agregado)
gaps:
  - dim: "Internal-consistency"
    best_of_class: "@/Components/ui"
    fix: "<select> e <textarea> nativos hand-rolled — usar Select/Textarea do DS (Edit usa mesmo padrao raw; padronizar)"
    esforco: medio
    origin: screen-grades-baseline-2026-05-30
  - dim: "Error-recovery"
    best_of_class: "Stripe forms"
    fix: "so vehicle_id mostra erro; status/datas sem render de errors[]; sem foco no 1o campo invalido"
    esforco: medio
    origin: screen-grades-baseline-2026-05-30
  - dim: "Cognitive-load"
    best_of_class: "Linear quick-create"
    fix: "selecao de veiculo por <option> texto plano — combobox com busca de placa (lista grande trava)"
    esforco: medio
    origin: screen-grades-baseline-2026-05-30

# ───────── Onda 0b (ADR 0320): comportamento/valor AO LADO da UX — PLUGAR não fundir ─────────
# NÃO entra na nota 66 (UX). Create é o funil de abertura da OS (roda 1×/veículo-que-chega no
# canary Martinho biz=164). É a de UX mais baixa das 3 (Developing), mas a contradição do
# programa persiste: há Pest cobrindo o store, mas ZERO contrato `.casos.md` que a régua cite.
casos_coverage:
  fonte: null          # sem resources/js/Pages/OficinaAuto/ServiceOrders/Create.casos.md
  guard: scripts/casos-coverage-guard.mjs
  ucs: []
  backlog:             # candidatos sem UC-id (G-2: id só no MESMO PR do teste); o Pest que os defenderia JÁ existe
    - "Abrir OS com veículo existente redireciona pro Show — ServiceOrderCrudTest/ServiceOrderStoreLinksVehicleTest"
    - "order_type rejeita 'locacao' (erradicação ADR 0265) — OficinaFioUsavelAdr0265Test"
    - "vehicle_id/contact_id de outro business rejeitado server-side (Tier 0) — VehicleMultiTenantTest"
    - "OS nasce em status 'aberta' (FSM canon — status não é campo do form) — FsmTransitionTest"
  cobertura_uc: "0%"   # derivado: 0 UC contratado (falta o contrato .casos.md, não o Pest)
d1_calculo:            # Create NÃO computa total — itens (peças/serviços) são pós-criação (charter Non-Goals). Sem cálculo próprio.
  aplica: false        # n/a: a OS nasce sem valor; o dente de cálculo vive em Edit/Show + na onda de cálculo (outro chip)
  nivel: "n/a"
  provas: { property: false, golden: false, cross_check: false }
