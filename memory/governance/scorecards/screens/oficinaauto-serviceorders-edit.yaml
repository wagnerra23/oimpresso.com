# Gerado por scripts/qa/screen-grade-seed.mjs a partir do baseline canônico
# screen-grades-baseline-2026-05-30.json (método "SCREEN-GRADE-9.75").
# A nota 16-dim é LLM-as-judge (agente screen-qa) — NÃO editar à mão.
# Este YAML é o ponto de partida do ratchet: a nota não pode cair abaixo de baseline_anterior.
screen: OficinaAuto/ServiceOrders/Edit
path: resources/js/Pages/OficinaAuto/ServiceOrders/Edit.tsx
archetype: form
persona: tecnico
nota: 73
nivel: "Advanced"
baseline_anterior: 73   # ratchet — nota não cai abaixo disto sem aprovação
peso_real: 1.0
source: screen-grades-baseline-2026-05-30
graded_at: "2026-05-30"
resumo: "Edit com secao Itens inline (optimistic+toast+roxo) supera Create, mas mantem selects nativos e window.confirm fora do DS."
dimensoes: {}   # 16 dims individuais: a regradeação pelo agente preenche (baseline só tem agregado)
gaps:
  - dim: "Internal-consistency"
    best_of_class: "@/Components/ui"
    fix: "select/textarea nativos (vs Input/Label do DS); padronizar com Select/Textarea shadcn"
    esforco: medio
    origin: screen-grades-baseline-2026-05-30
  - dim: "Error-recovery"
    best_of_class: "Notion"
    fix: "delete de item usa window.confirm() nativo — trocar por AlertDialog do DS (consistente com optimistic toast)"
    esforco: medio
    origin: screen-grades-baseline-2026-05-30
  - dim: "Cognitive-load"
    best_of_class: "Linear"
    fix: "4 campos datetime-local lado a lado (entrada/previsao/concluida/entregue) sem agrupamento visual — agrupar em fieldset 'Datas'"
    esforco: medio
    origin: screen-grades-baseline-2026-05-30

# ───────── Onda 0b (ADR 0320): comportamento/valor AO LADO da UX — PLUGAR não fundir ─────────
# NÃO entra na nota 73 (UX). CONTRATO estabelecido 2026-07-03: Edit.casos.md com 4 UCs 🧪 (cada um
# cita um Pest real — G-2). Status 🧪 (não ✅) porque os Pest não estão no manifesto de vereditos —
# subir pra ✅ exige run CT100 (o dente). cobertura_uc 0% honesto — paridade com o golden Sells/Create.
casos_coverage:
  fonte: resources/js/Pages/OficinaAuto/ServiceOrders/Edit.casos.md
  guard: scripts/casos-coverage-guard.mjs
  ucs:
    - { uc: UC-OED-01, desc: "Editar e salvar retorna pro Show", status: "🧪", teste: Modules/OficinaAuto/Tests/Feature/ServiceOrderCrudTest.php }
    - { uc: UC-OED-02, desc: "Adicionar/editar item recalcula o Total OS (Observer, ADR 0192)", status: "🧪", teste: Modules/OficinaAuto/Tests/Feature/ServiceOrderItemTest.php }
    - { uc: UC-OED-03, desc: "Adicionar peça baixa estoque (peça×qty)", status: "🧪", teste: Modules/OficinaAuto/Tests/Feature/ServiceOrderItemStockBaixaTest.php }
    - { uc: UC-OED-05, desc: "vehicle_id de outro business rejeitado (Tier 0)", status: "🧪", teste: Modules/OficinaAuto/Tests/Feature/VehicleMultiTenantTest.php }
  backlog:             # candidatos sem UC-id (G-2: id só no mesmo PR do teste que o cita)
    - "Excluir item com rollback optimistic em erro HTTP — ServiceOrderItemHttpIntegrationTest"
    - "window.confirm → AlertDialog do DS (gap de UI, não comportamento)"
  cobertura_uc: "0%"   # derivado: 0 de 4 UC com ✅ (todos 🧪). Contrato existe; ✅ precisa do run CT100.
d1_calculo:            # Edit monta a OS (itens → Total OS BRL). Recalc é server-side (Observer). NÃO tocar aqui (é o outro chip)
  aplica: true
  nivel: "🔴"          # sem property (parse∘format==id) nem golden de borda (arredondamento/devolução) citáveis; `final_total` mecânica ainda é o buraco da US-OFICINA-027. Dente real = onda de cálculo.
  provas: { property: false, golden: false, cross_check: false }
