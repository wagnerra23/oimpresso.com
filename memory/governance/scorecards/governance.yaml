# Scorecard Governance — meta-módulo (ADR 0156 + ADR 0155).
#
# Wave 23 cross_cutting_infra (2026-05-16): target 90+ via C3 + C5 + C6.

# -- REVISAO 2026-07-26 (decisao [W] · "terceira via") ------------------------
# `last_grade` passa a citar a nota do **v3** (`module:grade`), com a fonte declarada
# em `last_grade_fonte`. Antes era a nota da rubrica v4 (bucket-scoped), semeada a mao
# em 2026-05-16 e nunca revisada -- 71 dias parados, porque o cabecalho deste arquivo
# dizia (falsamente) que um cron o mantinha.
#
# Por que v3 e nao v4: o `module:grade-v4` foi REMOVIDO nesta mesma revisao (0
# invocadores em CI/cron). A nota que MORDE e a do v3 -- roda no CI via
# `module:grade --all` e tem baseline versionado. Trocar a regua em silencio seria
# pior que a nota velha; por isso a fonte fica escrita ao lado do numero.
#
# Este arquivo SEGUE curado a mao (nenhum comando o escreve). Quem mede a idade dele:
# `cron-watchdog.mjs --entrega` + a flag "obra parada" do Daily Brief.

module: "Governance"
slug: "governance"
type: "meta"
fsm_n_a: true
fsm_n_a_reason: "Meta-módulo. Policies têm enabled bool, ADRs têm status enum simples (mcp_memory_documents). Nenhum requer FSM pipeline (ADR 0143)."
last_grade: 88
last_grade_at: "2026-07-26T00:00:00-03:00"
last_grade_fonte: "module:grade (v3) - governance/module-grades-baseline.json"
target_score: 90
bucket: "Bom"

dimensions:
  C1_coerencia:       { weight: 10, target: 10, current: 10 }  # saturado
  C2_reversibilidade: { weight: 10, target: 10, current: 9  }
  C3_reflexividade:   { weight: 13, target: 13, current: 7  }  # +3 Wave 23 (BRIEFING + ADRs referenciadas)
  C4_compliance:      { weight: 10, target: 10, current: 10 }
  C5_cobertura:       { weight: 17, target: 17, current: 4  }  # +9 Wave 23 (4 YAMLs scorecard + snapshot cmd)
  C6_adoption_time:   { weight: 10, target: 10, current: 7  }  # +3 Wave 23 (skill governance-pr-summary)

ownership:
  primary: "wagner"
  secondary: []

adrs_referenciadas:
  - 0094  # Constituição v2 (mãe)
  - 0155  # Rubrica v3 + Gate CI
  - 0156  # Scorecards YAML canon
  - 0157  # bucket fsm_n_a
  - 0158  # Charter audit flow
  - 0159  # ActionGate runtime
  - 0160  # Skill governance-pr-summary

capacidades:
  - "ActionGate runtime + PiiRedactor"
  - "Module Grades v3 (12 dimensões)"
  - "module:grade-snapshot daily 06:00 BRT"
  - "Drift detection (filesystem vs snapshot canon)"
  - "Charter audit (12 charters Inertia)"
  - "Policies CRUD + audit"
