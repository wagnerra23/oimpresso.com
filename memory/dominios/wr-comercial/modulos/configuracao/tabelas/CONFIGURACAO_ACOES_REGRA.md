---
table: CONFIGURACAO_ACOES_REGRA
module: configuracao
created_at_version: 945
last_modified_version: 1121
target_version: 1468
columns_count: 9
auto_generated: true
generated_at: 2026-05-09
generator: scripts/legacy-migration/generate-baseline.py
source: D:/Programas/WR Comercial/Resources/UpdateSQL.txt
---

# `CONFIGURACAO_ACOES_REGRA`

> ⚠️ **Auto-gerado** a partir de `UpdateSQL.txt` blocos UPDATE 1..1468. Não editar manualmente — re-rode `scripts/legacy-migration/generate-baseline.py`. Notas humanas vão em `_notes.md` ao lado.

- **Módulo:** `configuracao` (heurística por prefixo — Wagner refina em `lib/module_classifier.py` se errado)
- **Criada em:** UPDATE 945;
- **Última mudança:** UPDATE 1121;
- **Total colunas (versão 1468):** 9

## Colunas (versão 1468)

| # | Coluna | Tipo | Nullable | Adicionada em | Última mudança |
|---|---|---|---|---|---|
| 1 | `CODIGO` | `INTEGER` | NOT NULL | v945 | v945 |
| 2 | `CODCONFIGURACAO_COMPONENTE` | `INTEGER` | NOT NULL | v945 | v945 |
| 3 | `CODCONFIGURACAO_REGRA` | `INTEGER` | NOT NULL | v945 | v945 |
| 4 | `OBRIGATORIO` | `VARCHAR(1)` | NULL | v945 | v945 |
| 5 | `CODCONFIGURACAO_ACOES` | `INTEGER ALTER TABLE CONFIGURACAO_ACOES_REGRA ADD CONSTRAINT PK_CONFIGURACAO_ACOES_REGRA PRIMARY KEY (CODIGO, CODCONFIGURACAO_ACOES)` | NOT NULL | v949 | v949 |
| 6 | `CONDICAO` | `VARCHAR(500)` | NULL | v945 | v946 |
| 7 | `TEM_PADRAO` | `VARCHAR(1), ADD INFORMACAO VARCHAR(1000)` | NULL | v946 | v946 |
| 8 | `TAB` | `VARCHAR(500), ADD CSS BLOB SUB_TYPE 1 SEGMENT SIZE 80, ADD OBRIGATORIO VARCHAR(1), ADD VALOR_INICIAL VARCHAR(500), ADD ACAO VARCHAR(500), ADD MULTEMPRESA VARCHAR(1), ADD OBRIGATORIO_EXPRESSAO VARCHAR(500), ADD SQLCAMPOSADICIONAIS VARCHAR(500), ADD IMPEDIR_DUPLICIDADE_SQL VARCHAR(1000), ADD MENSAGEM BLOB SUB_TYPE 1 SEGMENT SIZE 80, ADD COR INTEGER, ADD CODCONFIGURACAO_FORM INTEGER, ADD DT_ALTERACAO TIMESTAMP, ADD ATIVO VARCHAR(1)` | NULL | v1040 | v1040 |
| 9 | `CODCONFIGURACAO_FORM` | `INTEGER` | NULL | v1121 | v1121 |

## Evolução

| UPDATE N; | Operação | Detalhe |
|---|---|---|
| 945 | CREATE | CREATE TABLE com 9 colunas |
| 946 | ADD_COL | + CODCONFIGURACAO_ACOES INTEGER |
| 946 | DROP_COL | - ENABLE |
| 946 | RENAME_COL | × ENABLE_CONDICAO → CONDICAO |
| 946 | DROP_COL | - VISIBLE |
| 946 | DROP_COL | - VISIBLE_CONDICAO |
| 946 | DROP_COL | - OBRIGATORIO_CONDICAO |
| 946 | ADD_COL | + TEM_PADRAO VARCHAR(1), ADD INFORMACAO VARCHAR(1000) |
| 949 | ADD_COL | + CODCONFIGURACAO_ACOES INTEGER ALTER TABLE CONFIGURACAO_ACOES_REGRA ADD CONSTRAINT PK_CONFIGURACAO_ACOES_REGRA PRIMARY KEY (CODIGO, CODCONFIGURACAO_ACOES) |
| 1040 | ADD_COL | + TAB VARCHAR(500), ADD CSS BLOB SUB_TYPE 1 SEGMENT SIZE 80, ADD OBRIGATORIO VARCHAR(1), ADD VALOR_INICIAL VARCHAR(500), ADD ACAO VARCHAR(500), ADD MULTEMPRESA VARCHAR(1), ADD OBRIGATORIO_EXPRESSAO VARCHAR(500), ADD SQLCAMPOSADICIONAIS VARCHAR(500), ADD IMPEDIR_DUPLICIDADE_SQL VARCHAR(1000), ADD MENSAGEM BLOB SUB_TYPE 1 SEGMENT SIZE 80, ADD COR INTEGER, ADD CODCONFIGURACAO_FORM INTEGER, ADD DT_ALTERACAO TIMESTAMP, ADD ATIVO VARCHAR(1) |
| 1121 | ADD_COL | + CODCONFIGURACAO_FORM INTEGER |

