---
date: 2026-05-30
hour: "23:50 BRT"
topic: "Loop Cowork↔Code maduro: placar de tarefas (#2017) + teste end-to-end + gate §10.4 (#2020) + ADR 0240 task ledger+antifragilidade (#2022) + F1 cartão de evidência (#2023)"
duration: "~sessão épica · 6 PRs nesta leva (continuação do handoff 2215)"
authors: ["Claude Opus 4.8 (1M)", "Wagner"]
---

# Loop Cowork↔Code maduro — placar + teste + ADR 0240 + F1

## Estado MCP no momento
- **Cycle CYCLE-07** "DS v3 enforced" (~12d, goal alvo 90). my-work @wagner 30 tasks. Trabalho desta sessão foi via **PRs/ADR**, não tasks MCP.

## O que aconteceu (continuação do conserto do loop — handoff 2215 / #2014)
Arco: **placar de tarefas → teste end-to-end do loop → gate anti-stale → sistema de tarefas canônico → 1º tijolo de evidência.**

1. **Placar de tarefas vivo (#2017)** — Wagner: "falta o Design ver tarefa concluída pra saber o que o Code precisa executar ainda". `ds:report:write` gera checklist **✅/☐ por módulo** no `DS_ADOCAO_INDICE.md` (derivado do `ds/*` real, não manual). PROTOCOL §10.2 amarrado.

2. **Teste do loop AO VIVO (Chrome)** — Wagner deu `sync now` (= **comando DIGITADO** no composer do Claude Design, **não botão** — feedback #2019). **PROVA de que o loop fechou:** o Cowork leu o git e gerou um prompt **EVOLUÍDO** que incorporou o §10.4 + `ds:report:write` + git=SSOT, e usou os **MEUS números** (`558`/Sells `42` do placar #2017). Ponta-a-ponta, não teoria.

3. **Mas o Cowork veio STALE no "sync da faxina"** — gerou prompt mandando re-numerar **ADR 0238 (já existe)** + renomear colisões 0235/0236 (append-only proíbe). **NÃO executei** (validei contra git). Saiu: feedback canon **#2019** + gate **§10.4 #2020** (Code valida prompt do Cowork contra git **sozinho** — Wagner: *"se eu responder eu posso errar, isso não pode depender de mim"*).

4. **ADR 0240 (#2021 proposto → #2022 aceito)** — task ledger git-native + handoff-por-tarefa + **fechamento por EVIDÊNCIA, não opinião**. Pesquisa SOTA (agente `estado-da-arte`): Magentic-One 5-perguntas + *Gaming the Judge* (arXiv 2601.14691) **valida cientificamente** o princípio do Wagner. **O próprio agente ficou STALE** (jurou que `ds:report`/§10 não existem) → corrigi com errata (§10.4 aplicado a ele). Wagner: *"vai sobreviver no tempo e evoluir?"* → adicionei a **camada de ANTIFRAGILIDADE** (derivar-nunca-escrever + freshness gate + métrica de saúde do loop).

5. **F1 (#2023)** — `ds:report --module=X --json` emite o **cartão de evidência** `{module, total, by_rule, target, pass, measured_against_sha}`. Testado em Sells (`total 42, pass false, sha b9df926b1`). É o 1º tijolo: quando o módulo zerar, `pass:true` **FECHA** a task no ciclo do 0240.

## Artefatos gerados
- **6 PRs em main** (`812c7d14c`): #2017 (placar) · #2019 (feedback sync-stale) · #2020 (gate §10.4) · #2021 (ADR proposto) · **#2022 (ADR 0240 aceito)** · #2023 (F1).
- ADR **0240** canon · `feedback-cowork-sync-now-prompt-stale` · dossier SOTA (`memory/sessions/2026-05-30-arte-task-system-cowork-code.md`) · `PROTOCOL.md §10.1–§10.4` · `scripts/ds-report.mjs` (cartão de evidência).

## Persistência
- **git:** tudo em main (`812c7d14c`). **MCP:** webhook propaga ~2min.
- **Cowork:** leu o §10 via `sync now` (testado ao vivo). **Pendente:** novo `sync now` pra pegar §10.4 + ADR 0240 + F1.

## Próximos passos (plano ADR 0240)
**F2** anatomia `tasks/T-NNN/` + projeções (`tasks:index`/`project`) · **F3** §10.5 PROTOCOL (5 perguntas) · **F4** `tasks:report` Code · **F5 antifragilidade (freshness gate + métrica de saúde) — é o que CUMPRE a promessa de sobrevivência; sem F5 o 0240 é papel** · F6 hook dormente. Em paralelo: disparar **Onda 1 DS (Sells)** quando Wagner quiser — `ds:report` fresco antes (gate §10.4).

## Lições catalogadas
- **`sync now` = comando digitado** no Claude Design (não botão) — feedback #2019.
- **Cowork pós-sync pode vir STALE** → §10.4 valida contra git (pegou o prompt da faxina **E** o próprio agente de pesquisa — o princípio é universal).
- **"não pode depender de mim"**: decisão com resposta objetiva no git = Code decide e age; só escala o **subjetivo** (estético/estratégico/prioridade).
- **Antifragilidade**: *derivado+enforçado sobrevive; escrito+lembrado apodrece* (cemitério: HANDOFF 15d/SYNC_LOG/CODE_NOTES/auto-mem/R12). O ADR é a **promessa**; o **F5 cumpre**.
- **`measured_against_sha`**: evidência objetiva tem que dizer **contra qual commit** foi medida.

## Pointers detalhados
- ADR 0240 (`memory/decisions/0240-task-ledger-git-native-cowork-code.md`) · dossier SOTA · `prototipo-ui/PROTOCOL.md §10` · `scripts/ds-report.mjs --module X --json` (cartão) · handoff anterior `2026-05-30-2215-conserto-loop-cowork-code-ds-sync.md`
