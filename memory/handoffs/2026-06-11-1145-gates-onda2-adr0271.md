---
date: "2026-06-11"
time: "11:45 BRT"
slug: gates-onda2-adr0271
topic: "Onda 2 dos gates (ADR 0271) executada com MCP consultado — 5 itens codáveis entregues (64→53) via PR #2531 + comandos gh api prontos pros itens 5/6 (Wagner)"
tldr: "PR #2531 (CI verde, aguarda merge Wagner): deleta secrets-governance/ragas-gate/charter-gate/mwart-gate/pr-ui-judge + porta secrets:audit --auto-pr pro governance-drift + always-run no-mock/phpstan/governance-gate/financeiro-pest (skip-as-pass validado ao vivo) + fix self-audit eval_ci_gate. Itens 5/6 = comandos gh api prontos abaixo; CAVEAT: ADR 0216 PR scan ainda path-filtered — NÃO promover a required antes de always-run."
decided_by: [W]
cycle: CYCLE-08
prs: [2531]
next_steps:
  - "Wagner: revisar + mergear PR #2531 (squash)"
  - "Wagner: rodar comando gh api dos itens 5+6 (abaixo) APÓS merge do #2531 — variante A (sem secrets-consolidado) é a segura"
  - "Follow-up pequeno: governance-drift.yml pull_request always-run (pré-requisito pra promover ADR 0216 PR scan a required — variante B)"
  - "Item 7 (fusão 4 gates cor→1): alto risco, por último, coordenar rename + branch protection no MESMO momento"
  - "Wagner: decidir destino do stash@{0} charter-gate-wip-frosty-pre-onda2 (WIP provavelmente obsoleto — onda 2 deletou charter-gate.yml)"
---

# Handoff 2026-06-11 11:45 — Onda 2 dos gates (ADR 0271) — PR #2531

> Sessão [CC] desktop (worktree `frosty-greider-83ab2f`), retomada via `/continuar` pós-merge do #2527. Pré-condição da onda 2 CUMPRIDA: MCP consultado primeiro (`brief-fetch` + `cycles-active` + `my-work` + `my-inbox` — inbox vazia, nada conflitando). Wagner aprovou: branch nova de main limpo + só os 5 itens codáveis + "pode fazer".

## Estado

- **PR #2531** `chore/gates-onda2-adr0271` — **CI 100% verde** (28 pass + 2 skipping corretos: jobs scheduled-only). **+123/−817.** Aguarda merge Wagner (squash).
- **64→53 gates** (onda 1 tinha levado 64→58; esta onda −5).
- Worktree `frosty-greider` tinha WIP charter-gate não-commitado → preservado em `stash@{0}` (`charter-gate-wip-frosty-pre-onda2`) no repo principal. Provavelmente obsoleto (onda 2 deletou o `charter-gate.yml`).

## O que o #2531 contém (itens 1/2/3/4/8 da onda 2)

| Item | Entrega | Validação |
|---|---|---|
| 1 — F3 secrets | `secrets:audit --auto-pr` → job scheduled no `governance-drift.yml`; `secrets-governance.yml` deletado (fecha canary 7d ADR 0216) | job aparece como `skipping` em PR (correto — scheduled-only) |
| 2 — F6 ragas | `ragas-gate.yml` deletado (superseded por `jana-ragas-gate`) | jana-ragas-gate passou no próprio PR |
| 3 — softs | `charter-gate.yml` + `mwart-gate.yml` + `pr-ui-judge.yml` deletados | nenhum era required (verificado via gh api ANTES de deletar) |
| 4 — required-readiness | `no-mock-gate`/`phpstan-gate`/`governance-gate`/`financeiro-pest` com `pull_request: {}` always-run; financeiro com skip-as-pass `dorny/paths-filter` | **validado ao vivo**: financeiro-pest PASS 1m39s SEM rodar MySQL/Pest (PR não toca Financeiro); no-mock/pii/append-only passaram trivial |
| 8 — self-audit | `checkEvalCiGate` → `jana-ragas-gate.yml` (era `eval-recall-gate.yml` inexistente = falso-verde) | 1 arquivo, docblock + check |

**Anti-drift (ADR 0270):** proibicoes §MWART · DESIGN.md §15 · rules/pages.md · skills mwart-process + pr-ui-judge-manual · SecretsScanCommand @see · SecretsGovernanceTest R-SEC-0215-008 (agora asserta a consolidação) · BriefDiarioFaithfulnessTest comment.

## ⏭️ ITENS 5/6 — comandos prontos (Wagner executa APÓS merge do #2531)

### ⚠️ CAVEAT descoberto nesta sessão
O handoff da onda 1 lista "secrets-consolidado" na promoção a required. O check correspondente é **`ADR 0216 PR scan (governance:audit --diff-only)`** do `governance-drift.yml` — que **ainda é path-filtered** no `pull_request` (memory/config/Modules/app/composer/package). Promover a required SEM antes torná-lo always-run = **deadlock "Expected — waiting"** em qualquer PR fora desses paths (ex: PR só de `.github/` ou `resources/js/`). Por isso 2 variantes:

### Variante A (SEGURA — usar agora): itens 5+6 sem o secrets-consolidado

```bash
gh api -X PATCH repos/wagnerra23/oimpresso.com/branches/main/protection/required_status_checks \
  -F strict=false \
  -f "contexts[]=ADR frontmatter" \
  -f "contexts[]=Casos-coverage · ratchet (trio + rastreabilidade)" \
  -f "contexts[]=Conformance · cor-crua ratchet vs baseline" \
  -f "contexts[]=Dominio-dict · ratchet (enum ⇔ dicionário)" \
  -f "contexts[]=Frontend / Vite build" \
  -f "contexts[]=PHP / Pest (Unit)" \
  -f "contexts[]=UI Lint · ratchet vs baseline" \
  -f "contexts[]=No hardcode business_id (Tier 0)" \
  -f "contexts[]=PHP / Pest (Financeiro · MySQL)" \
  -f "contexts[]=PII scan (CPF/CNPJ literal)" \
  -f "contexts[]=Append-only canon (ADRs, handoffs, Constituição)" \
  -f "contexts[]=No-mock-in-prod · ratchet" \
  -f "contexts[]=PHPStan / Larastan · ratchet vs baseline"
```

Efeito: **8→13 required** — entra multi-tenant (nº 1!), financeiro-pest, pii-scan, append-only, no-mock, phpstan; **sai `module-grades-gate`** (item 6 — check mais caro do repo, métrica composta, elogiou o SRS zumbi 73/100; o PATCH substitui a lista inteira, então a remoção é a ausência dele). `strict=false` preserva o comportamento atual (sem exigir branch up-to-date — repo com 150+ worktrees).

> Os nomes são os **job names exatos** confirmados no run do #2531 (case + acentos + ` · ` incluídos). Conferir resultado: `gh api repos/wagnerra23/oimpresso.com/branches/main/protection/required_status_checks --jq '.contexts[]'`

### Variante B (DEPOIS do follow-up): + secrets-consolidado

Pré-requisito: PR pequeno tornando o `pull_request` do `governance-drift.yml` always-run (`pull_request: {branches: [main]}` sem `paths` — o `--diff-only` já passa trivial em diff irrelevante; custo = composer+npm ~2-3min/PR). Depois, repetir o comando da Variante A acrescentando:

```bash
  -f "contexts[]=ADR 0216 PR scan (governance:audit --diff-only)"
```

### Rollback (se algo travar)

```bash
gh api -X PATCH repos/wagnerra23/oimpresso.com/branches/main/protection/required_status_checks \
  -F strict=false \
  -f "contexts[]=ADR frontmatter" \
  -f "contexts[]=Casos-coverage · ratchet (trio + rastreabilidade)" \
  -f "contexts[]=Conformance · cor-crua ratchet vs baseline" \
  -f "contexts[]=Dominio-dict · ratchet (enum ⇔ dicionário)" \
  -f "contexts[]=Frontend / Vite build" \
  -f "contexts[]=PHP / Pest (Unit)" \
  -f "contexts[]=UI Lint · ratchet vs baseline" \
  -f "contexts[]=module-grades-gate"
```

(= snapshot exato dos 8 required de antes, capturado nesta sessão.)

## Item 7 (último, alto risco)
Fusão 4 gates de cor → 1 (conformance + ui-lint + stylelint + CockpitAccentCanon, 1 baseline). **Toca 2 checks required** (Conformance + UI Lint) — coordenar rename do workflow e PATCH da branch protection NO MESMO momento, senão deadlock geral. Recomendação: fazer só depois dos itens 5/6 assentarem 1 semana.

## Pegadinhas pra próxima sessão
- `financeiro-pest` agora tem 2 modos: skip-as-pass (PR sem Financeiro) e full (com). Se adicionar step novo no job, **lembrar do `if: steps.changes.outputs.fin == 'true'`** — step sem guard roda no modo skip e pode falhar sem o setup.
- `governance-gate.yml`, `no-mock-gate.yml`, `phpstan-gate.yml`: NÃO reintroduzir `paths` no `pull_request` — quebraria a required-readiness (deadlock quando promovidos).
- O judge UI agora é **só manual** (`php artisan ui:judge-pr <PR#>` via skill `pr-ui-judge-manual`) — o workflow morreu.
- Enforcement de RUNBOOK MWART agora é **só runtime** (hook `block-mwart-violation.ps1`) — cobertura de tela no CI é do `casos-gate`.
