# LIÇÕES [CODE] — erros de código a não repetir

> Escopo: **código backend/infra** (PHP, Eloquent, jobs, migrations, controllers, services, CI)
> **+ processo/comportamento de agente** (medição, derivação, oráculo, varredura) — o tema é
> *reincidência → defesa mecânica*, e erro de processo é instância disso igual erro de código
> (proposal [two-strikes-cobre-processo](decisions/0344-two-strikes-cobre-processo.md), raio-X 2026-07-20).
> Equivalente de `LICOES_CC.md` (que cobre design/[CC]) pro lado de engenharia.
> Subordinado a `memory/proibicoes.md` (canônico; o §5 de lá é a PROSA-evidência, este é o CONTADOR). **Append-only.**
> Lido no início de toda sessão pelo hook `licoes-code-two-strikes.mjs`.
>
> **Por que existe:** fechar o elo manual do loop de aprendizado — quando uma classe de
> erro de IA-code repete, ela precisa virar **defesa mecânica** (gate/hook/baseline), não
> ficar só na memória de quem lembrou. Origem: sessão 2026-06-06 (Wagner: "meu sistema está
> preparado a evoluir quando esses erros aparecem? quando deve ser acionado o aprendizado?").
>
> ## Regra "two-strikes" (gatilho do aprendizado)
> - **1ª ocorrência** de um erro → conserta o bug. NÃO codifica gate ainda.
> - **2ª ocorrência da mesma `Classe`** → PARA. Vira defesa mecânica (gate/hook/baseline).
> - **Chegou em PROD / cliente pagante** → codifica SEMPRE (mesmo na 1ª). Espelha ADR 0105.
> - **Override de gate usado** → revisa se o gate está errado (aprendizado reverso).
>
> O hook alarma quando uma lição tem `Ocorrências >= 2` **e** `Gate: none`.
>
> ## Formato por entrada (campos lidos pela máquina — não renomear)
> ```
> ## LC-NN — <título curto>
> - **Erro:** o que aconteceu
> - **Sintoma:** como apareceu
> - **Regra:** o que fazer pra não repetir
> - **Classe:** <slug-estável>   (agrupa ocorrências da mesma família)
> - **Ocorrências:** <int>       (incrementar a cada vez que a classe reaparece)
> - **Gate:** none | <nome-do-gate/hook/baseline que já impede>
> - **Ref:** <ADR/PR/sessão>
> ```
> Ao consertar um erro de código: ache a `Classe` aqui. Existe? Incrementa `Ocorrências`.
> Não existe? Cria `LC-NN` novo com `Ocorrências: 1`. Quando virar gate, troca `Gate: none`
> pelo nome do gate e o alarme some sozinho (catraca — só sobe).
>
> **Convenção do campo `Gate` (ADR 0344):** cobertura só-**advisory** (nudge/warn que não bloqueia)
> conta como *sem defesa mecânica* — declare `Gate: advisory — <hooks>` e a classe **segue
> alarmando** até virar sonda que morde. Se o advisory é a decisão **FINAL by-design** (ADR 0224),
> declare `Gate: advisory-terminal (0224) — <hook>`: o marcador `terminal`/`by-design`/`0224` sai
> do alarme. Um gate REAL que só menciona "advisory" entre parênteses (`mutation-gate (advisory,…)`)
> não é advisory pra o contador — só o prefixo declarado.
>
> ## Reconciliação §5↔ledger (auto-feed · proposal [auto-feed-ledger-aprendizado](decisions/proposals/2026-07-20-auto-feed-ledger-aprendizado.md))
> No SessionStart o hook, além de alarmar two-strikes, **reconcilia** este ledger com o §5 de
> `memory/proibicoes.md`: lê as **datas do §5 que a linha `Ocorrências` cita** (o prefixo `§5:` —
> o recibo datado; convenção do "Caminho único" da ADR 0344), calcula `frontier = max(essas datas)`
> e **surfaça** as lápides do §5 que carregam marcador de recorrência do autor (*"reincidência /
> mesma família / EMENDA da lápide / …"*) **além do frontier e fora do ledger** — pra VOCÊ decidir:
> vira LC nova, +1 numa LC, ou é ruído? A **DETECÇÃO** da recorrência-DECLARADA vira mecânica; o
> **julgamento** e o passo erro→lápide seguem HUMANOS (não "lê o erro real"; reconcilia dois docs
> curados à mão). Também flagra **recibo pendurado** (data citada sem lápide no §5 — typo).
> Dry-run contado: `node .claude/hooks/licoes-code-two-strikes.mjs --reconcile`.

---

## LC-01 — Query sem global scope vazando entre tenants
- **Erro:** Eloquent/Service consultando entidade com `business_id` sem o global scope (ou em job na fila que perdeu o tenant).
- **Sintoma:** dado de um business aparecendo pra outro. Pior bug possível do projeto.
- **Regra:** todo model de negócio usa global scope; jobs re-resolvem o tenant; CLI/superadmin trata cross-business explicitamente (skill `multi-tenant-patterns`).
- **Classe:** multi-tenant-scope-missing
- **Ocorrências:** 2
- **Gate:** multi-tenant-gate (.github/workflows/multi-tenant-gate.yml) + skill Tier A
- **Ref:** ADR 0093

## LC-02 — Mock/stub deixado em código de produção
- **Erro:** scaffolding com dados mockados (cowork/demo) sobrevivendo no caminho de produção.
- **Sintoma:** tela "funciona" com dado falso; quebra com dado real.
- **Regra:** mock só em teste/seed. Caminho de prod nunca importa fixture.
- **Classe:** mock-in-prod
- **Ocorrências:** 2
- **Gate:** scripts/no-mock-in-prod.mjs (+ no-mock-baseline.json)
- **Ref:** PR #2262

---

> Abaixo: classes **identificadas no audit 2026-06-06 como sem catraca** (`Gate: none`).
> Ainda em `Ocorrências: 0` — registradas proativamente, viram alarme se reincidirem.
> (Honestidade: 0 = nenhuma reincidência *observada* ainda, não "nunca aconteceu".)

## LC-03 — Teste só de caminho feliz (sem prova de que pega bug)
- **Erro:** suíte Pest verde que não exercita borda/erro; IA tende a gerar exatamente isso.
- **Sintoma:** cobertura "verde" enquanto bug passa. Teste vira teatro.
- **Regra:** teste tem que falhar quando o código quebra. Mutation testing (infection `--min-msi`) prova isso.
- **Classe:** happy-path-only-test
- **Ocorrências:** 0
- **Gate:** mutation-gate (advisory, escopo v1 app/Services via Pest --mutate) — .github/workflows/mutation-gate.yml
- **Ref:** audit 2026-06-06 (gap nº1); gate advisory landeado sessão 2026-06-06. Expandir escopo + promover a bloqueante no CT 100 (MySQL real).

## LC-04 — N+1 / query dentro de loop
- **Erro:** loop com query por iteração (IA adora gerar); sem eager-load.
- **Sintoma:** tela lenta sob dado real; explode com volume.
- **Regra:** eager-load + `Inertia::defer` em props pesadas (skill `inertia-defer-default`). Falta gate de contagem de queries.
- **Classe:** n-plus-one-query
- **Ocorrências:** 0
- **Gate:** none
- **Ref:** audit 2026-06-06 (gap nº5)

## LC-05 — Injeção genérica (SQLi/XSS/path-traversal) fora da regra custom
- **Erro:** input não-sanitizado em caminho sem gate específico (multi-tenant é gateado; injeção genérica não).
- **Sintoma:** vulnerabilidade que phpstan (type-level) não pega.
- **Regra:** prepared statements; escape na borda. Falta SAST/taint (semgrep/psalm-taint).
- **Classe:** injection-generic
- **Ocorrências:** 0
- **Gate:** none
- **Ref:** audit 2026-06-06 (gap nº2)

## LC-06 — Comparação design×prod NO OLHO em vez de MEDIDA
- **Erro:** comparei protótipo (Cowork vivo) × produção por screenshot/olho e declarei "estruturalmente iguais" — deixei passar diferenças MEDÍVEIS: KPI `center` na prod × `left` no design (causa: `<button>` herda center-default × `<div>` é left), dark-mode com texto `stone` invisível (escuro no escuro), e roxo do primary escuro `0.55` × roxinho `0.72` (o design clareia o accent no dark, a prod trava inline). Wagner teve que apontar cada uma.
- **Sintoma:** agente diz "aplicado/igual/pronto"; Wagner acha a divergência que o agente não mediu. "Print igual" esconde center×left, full-reload, contraste.
- **Regra:** comparação design×prod SÓ por MEDIÇÃO (computed style/DOM, MESMA sonda nos dois lados — `prototipo-ui/design-diff.mjs`), NUNCA no olho. Antes de comparar: provar a fonte `SYNC` (`cowork-mirror-freshness`) + validar com uma pista-canário conhecida (o alinhamento foi o canário do Wagner). Screenshot é ilustração, não prova de igualdade. Mesmo tema nos dois lados.
- **Classe:** visual-compare-eyeball
- **Ocorrências:** 2   (strike 1 = comparação v1 rasa "essencialmente igual" que gerou o `PROTOCOLO-COMPARACAO-RUNTIME` em 2026-07-06; strike 2 = esta sessão 2026-07-07, mesma classe)
- **Gate:** `prototipo-ui/design-diff.mjs` — comparador determinístico D2/D4/D6/D8 + probe medida (mesma sonda) + `--selftest` no CI (design-memory-gate). ⚠️ honestidade: a comparação em si é DISPATCH do agente pós-merge de tela (browser + design vivo — CI não renderiza), como o `cowork-mirror-freshness`; o que a máquina garante é que o veredito vem de medida, não do olho. Wirado no protocolo (Regra 0 pós-deploy).
- **Ref:** ADR 0299 (/design-diff previsto), `memory/requisitos/_DesignSystem/PROTOCOLO-COMPARACAO-RUNTIME.md` (D8), sessão 2026-07-07

## LC-07 — Manifest de frescor cobria só as âncoras (cego pras deps do render)
- **Erro:** a rodada de frescor design↔espelho media só os `.jsx` de tela declarados em charter (3 âncoras). O render depende de ~100 arquivos (`app.jsx`, `styles.css`, `ds-v6/tokens.css`, css por módulo) — nenhum era medido. O [W] mudou o PageHeaderNav pra roxo no `app.jsx` do Cowork e a rodada saiu "3 SYNC" verde com o drift passando invisível ("ele não baixou tudo?", 2026-07-07).
- **Sintoma:** rodada de frescor verde + Wagner vê no vivo uma mudança que "não veio" no pull. Verde-parcial lido como verde-total.
- **Regra:** manifest de frescor enumera âncoras **+ deps de render derivadas MECANICAMENTE do shell** (`parseShellDeps` — src/href, strip `?v=`, sem CDN; nunca lista curada na mão). Sem shell → WARN explícito, nunca omissão silenciosa. Rodada mínima: âncoras + deps globais + css do módulo tocado; parcial = UNCHECKED.
- **Classe:** freshness-manifest-partial-coverage
- **Ocorrências:** 1
- **Gate:** `cowork-mirror-freshness.mjs` manifest v3 (kind ancora|dep) + testes §6b no `.test.mjs` (CI design-memory-gate) — o caso `app.jsx` do drift real está travado por assert nomeado.
- **Ref:** sessão 2026-07-07 · PROTOCOLO-COMPARACAO-RUNTIME passo 0 · LC-06 (família: cobertura de medição)

---

> Abaixo: 1ª classe de **PROCESSO/comportamento de agente** promovida ao contador (raio-X 2026-07-20).
> O §5 do `proibicoes.md` já tinha a prosa-evidência; faltava o CONTADOR que torna a reincidência
> visível pro hook. Backfill **forward-only + oportunístico** (a lápide 2026-07-12 do §5 proíbe big-bang):
> só a classe que estava GRITANDO entra agora; as demais viram LC-NN quando reincidirem.

## LC-08 — Afirmar/derivar/medir a partir da FONTE ou MEDIDA errada (sem provar)
- **Erro:** apresentar achado, causa-raiz, número ou veredito derivado da fonte errada — ler código em vez de rodar; medir o atributo (`.hidden`/`offsetTop`) em vez do computado; restatear número que outro sistema (banco/runtime) sabe melhor; parsear o `Kernel.php` em vez de perguntar ao scheduler; `crontab -l` num host que não tem o binário — e chamar de "verificado/medido".
- **Sintoma:** o agente afirma "medido / a raiz é X / verificado"; [W] ou o CI acha que a medida veio do disco/leitura/olho, não do sistema-que-sabe (SELECT no banco, `runsInEnvironment()` no runtime, varredura contada, teste vermelho).
- **Regra:** achado/número/veredito só vira CONCLUSÃO depois de (a) varredura CONTADA (sem `head_limit`, dizendo "N de N"), (b) âncora de contrato citada (UC/SPEC/ADR), (c) oráculo certo (banco/runtime/teste vermelho) — nunca leitura/parse/olho. Recibo DATADO (`query+resultado+data`), não afirmação atemporal. Em dúvida → PERGUNTAR, não inventar.
- **Classe:** afirmar-sem-medir-fonte-certa
- **Ocorrências:** 14   (proibicoes §5: 07-15 achado-sem-varredura · 07-16 medir-propriedade-errada · 07-17 oráculo-errado-restatear-número · 07-17 deduzir-quem-roda-parseando · 07-17 crontab-l-falso-negativo · **07-17 premissa-stale-consertar-medidor-sem-rodar-ao-vivo** (a grade mandou casar custo→PR por SHA/Analytics API pra "derrubar os 96,8% de órfão" do `agent-cost-per-pr`; o número vinha do **snapshot de 13/07** — rodado AO VIVO o join por branch casava **12/20**, e o resíduo decompunha em 3 defeitos de bookkeeping (janela · cauda-de-branch · cap do `gh --limit 200`) que levaram a cobertura a **81,22%** sem tocar em SHA nem em API. Corolário perene: *"antes de consertar um medidor, RODE-O ao vivo — o número que motiva o conserto pode ser um snapshot velho"*) · 07-22 mapa-de-artefatos-de-tela-por-Glob-em-vez-da-porta-viva · 07-22 blindar-candidatos-sem-rodar-a-máquina+afirmar-@import-por-leitura · **07-24 data-de-git-log-em-clone-raso** (reportei como FATO que 3 `casos.md` do Produto nasceram em 23/07 no #4719; nasceram em 15/07/19/07/22/07 — 23/07 era o **piso do clone**, `--depth`, 57 commits alcançáveis. A assinatura estava à vista: o commit-piso `13ec1e06` aparecia como **root, zero parents, 16.081 arquivos "adicionados"**. Corrigido com `git fetch --unshallow` → 5.682 commits. Errata em [session 2026-07-24](sessions/2026-07-24-verificacao-transcript-gestao-documentacao.md) §2 — cometida **dentro do log que denuncia a classe**). · **07-26 node--check-em-contexto-de-modulo-diferente** (comparei `node --check` do arquivo editado DENTRO do projeto (ESM, `"type":"module"` → `return` de topo ilegal → exit 1) com o baseline salvo em `/tmp` (sem `package.json` → CJS → exit 0) e publiquei que o agente tinha quebrado a sintaxe; medido no MESMO contexto, **o baseline falha igual** (1/1/0) — zero regressão. O agente já dizia isso no relatório e eu desconfiei dele em vez de testar.) · **07-26 grep-de-CRLF-que-casa-string-vazia** (baixei o `composer.lock` do CT 100 e "medi" EOL com `grep -c $'\r'` — o padrão resolve pra **string vazia**, que casa TODA linha: reportei "CRLF: 20632" no lock e "163" no `composer.json`, ou seja, exatamente o total de linhas de cada um. Rodei um `tr -d '\r'` que não mudou nada e quase "consertei" um problema inexistente. Os instrumentos certos estavam à mão: `file` (não dizia *"with CRLF line terminators"*) e o próprio `git diff --numstat` — **25/17 linhas** num arquivo de 20.632; se houvesse conversão de EOL o diff seria o arquivo inteiro. O 2º teste (`od -c \| grep -o '\\r'`) também errou: conta os `\r` **escapados dentro das strings JSON**.) · **07-26 exit-code-do-gh-run-watch-lido-como-falha** (reportei "Deploy FALHOU" porque `gh run watch --exit-status` devolveu `X` / exit 1; o veredito real era `conclusion: cancelled` — a fila do deploy, com `cancel-in-progress: false`, mantém só o pendente mais recente e o main recebia merges de sessões paralelas. O oráculo certo é o campo `conclusion` da API (`gh api .../actions/runs/<id>`), não o exit code do watcher, que não distingue `cancelled` de `failure`.) · **07-27 denominador-inventado-e-métrica-de-outro-universo** (grade da máquina SDD vs Swimm: pontuei cobertura como `1 SDD / 40 módulos = 2,5%` — **nenhuma decisão estabelece os 40**; o denominador executável é **1** (a pré-condição da Camada 1, `ANTI-REGRESSAO-*`, existe em 1 módulo) → cobertura **1/1**. E li o `anchor-lint` do Produto (**11,1%**, que mede **US do SPEC**) como veredito do trio (que produz **`casos.md` por tela**), chamando de *"o achado que fecha o diagnóstico"*: das 9 US, **8 são roadmap não-implementado** — 2 delas em voo no brief do dia — logo o campo vazio está **CORRETO**. Ainda agreguei capacidade-do-mecanismo com quantidade-de-artefato numa nota (47/100) cuja maximização = gerar 39 SDDs = big-bang proibido (§5 07-12), e julguei por cobertura uma máquina de **3 dias** (ADR 0351 de 24/07, template de 26/07). A auditoria que eu mesmo recomendei **refutou minha hipótese adversarial** — a Camada 1 mordeu, ≥6 achados incl. bloqueador de migração que zeraria estoque — e a evidência estava nos **corpos** dos commits, não nos títulos que eu tinha lido.) · **07-27 grep-com-padrão-que-não-casa-o-nome-real-do-arquivo** (caçando outra "perna morta" como a que eu tinha acabado de consertar no `exposicao-tier0`, procurei o manifesto do G-7 com `git ls-files \| grep -iE "casos-result\|uc-manifest"`. O arquivo é `casos-test-**results**.json` e o padrão `casos-result` **não casa** — concluí que ele não existia no repo, logo o guard gracioso `if (!manifest) return []` faria o **G-7 dormir sempre**, e os 3 zeros (`status_lies`/`status_unverified`/`status_stale` — a defesa contra `Status: ✅` mentiroso, no gate **REQUIRED** `Casos-coverage · ratchet`) seriam "não medi". Errado: `git cat-file -e origin/main:scripts/casos-test-results.json` → **existe e está fresco** (auto-PR diário do `casos-results-publish.yml`, 8 dias consecutivos mergeados, 32 UC todos `pass`). Uma linha inteira de investigação sobre um gate required, apoiada num falso negativo do meu próprio filtro. Duas irmãs menores na mesma sessão: `grep -ci DRIFT` reportou "39 drifts" porque o padrão casa **"sem drift"** (o certo é `DRIFT em`); e ranqueei o débito Tier-0 contando `num_uf`/`final_total` no **Controller** quando a sentinela já ranqueava por exposição da **tela** em 4 categorias — proxy que escolheu o alvo errado **2×** (Purchase, depois Cliente) até eu rodar o dono do tema. Corolário perene: *quando o resultado de um filtro sustenta uma conclusão forte, teste o filtro contra um caso que você SABE que existe antes de concluir a ausência.*) Classe adjacente `importar-solução-sem-checar-premissa` migrou pra LC-09.)
- **Gate:** parcial (3/9) — `block-instrumento-sem-porta-viva` (PreToolUse **Glob|Grep|Bash**, **BLOQUEIA** exit 2) cobre 3 sub-comportamentos. **P3** (2026-07-24) é `git log` pedindo **data/criação** num repo com a **history truncada** → porta viva `git fetch --unshallow` ou `mcp__github__list_commits`. Duas pernas, e a 2ª é o que zera o FP: o comando pede data (`--diff-filter=A`/`--follow`/`--date`/`--since`/`%ad`) **E** o repo está de fato truncado — em clone completo, o caso normal, **nunca morde**. A perna do repo **espelha** `sdd-scorecard.mjs::isShallowHistory()` (o DONO da regra), inclusive a sutileza: `--is-shallow-repository` sozinho é grosso demais (um `fetch --depth 1` de órfã marca shallow sem truncar o HEAD) → só vale se um boundary do `.git/shallow` for **ancestral do HEAD**. **Mordida provada** em clone raso real (2026-07-24): ruim→exit 2 · `git log --oneline`→0 · comando sem git→0 · escape→0. Os outros 2 pares: **P1** mapa-de-artefatos-de-tela por Glob (ocorrência 07-22 → porta viva `npm run screen:files`) e **P2** deduzir runtime parseando fonte estática (07-17 → `schedule:list`/`route:list`/`config:show`). Forma escolhida por [W] 2026-07-24 sobre 3 alternativas: intercepta o **instrumento errado quando existe porta viva registrada** (precedente `block-test-fora-ct100`), NÃO detecta "afirmação sem evidência" — essa forma foi MEDIDA e deu 130 FP em árvore limpa (§5 2026-07-16). Segue **alarmando de propósito** (prefixo `parcial`): 6 sub-comportamentos sem sonda — **consertar-medidor-sem-rodar-ao-vivo (07-17 — não é instrumento errado, é NÃO-rodar o instrumento certo e confiar em snapshot; `block-instrumento-sem-porta-viva` não cobre)**, achado-sem-varredura-contada (07-15), medir-propriedade-errada (07-16 → **sem alvo aberto, medido 2026-07-24**: o `[hidden]{display:none!important}` já está no CSS da tela do incidente; no app React o atributo `hidden={...}` aparece **5×/1 arquivo** (`Cliente/Index.tsx`) — o "573 em 167" da 1ª contagem era `overflow-hidden` do Tailwind; e no `Design System v4.html`, único outro que seta `.hidden` vivo, os 3 seletores atingidos (`.combobox-list`/`.popover`/`[data-tab-panel]`) **não têm `display:` de classe** → sem colisão, sem bug. Aplicar a defesa lá seria preventiva sem alvo, pagando o protocolo 4b de design-memory por zero risco fechado. Reabrir só se aparecer colisão real), oráculo-errado-restatear-número (07-17 → **ampliar o corpus do Check T `fact-anchor` MEDIDO E REPROVADO 2026-07-24**: dry-run sobre 238 charters + 78 BRIEFINGs deu 14 hits, dos quais **9 são FP (~64%)** — 4× `Modules/<X>` em **Non-Goals** ("escopo futuro", que é o uso CANÔNICO da seção), 1× path da própria tela (`Pages/Modules/Index`), 2× contagem lida como versão ("Pest 6 cenários", "Pest 5+ fixtures"), 1× `Inertia v2` onde o `v2` qualifica a lista. Os 2 primeiros FP são tratáveis; o de Non-Goal **não é** — exige distinguir "afirmo que existe" de "digo que é escopo de outro módulo", que é julgamento semântico. É a MESMA razão pela qual o autor já deixou `ARCHITECTURE`/`INDEX` fora do corpus. Gate é required (ADR 0349) → FP quebraria o main de todos. Reabrir só com critério determinístico pro caso Non-Goal. **Achado colateral REAL da medição** (não é gate, é conteúdo): 5 hits de `Modules/Accounting` afirmado vivo em BRIEFING — deprecado pela [ADR 0172](decisions/0172-deprecar-modulo-accounting-fundir-financeiro.md) — e 1 de `Modules/Copiloto` (inexistente) no `Sells/Index.charter.md`), `crontab -l` falso-negativo (07-17), blindar-sem-rodar-a-máquina (07-22). Advisory pré-existente que NÃO conta: `nudge-diagnosis-without-evidence` + `warn-red-first`.
- **Ref:** `memory/proibicoes.md` §5 (2026-07-15..17, 07-22) · raio-X 2026-07-20 · proposal [two-strikes-cobre-processo](decisions/0344-two-strikes-cobre-processo.md)

## LC-09 — Importar solução de sistema externo sem checar se a premissa vale no nosso
- **Erro:** transplantar decisão de arquitetura/UI/fluxo de um sistema pesquisado (Odoo, Shopify, Akeneo, doc oficial Anthropic, concorrente) direto pro modelo do oimpresso sem verificar se o PROBLEMA que aquela solução resolve existe aqui — copiar a *solução* sem traduzir a *premissa*.
- **Sintoma:** o agente escreve como canon (charter/fluxo) uma decisão que só faz sentido no modelo do sistema-fonte; [W] corta apontando que a premissa não vale ("pra quê a função X?", "isso colide com o canon v2"). Anti-padrão inventado é pior que ausente — parece canon.
- **Regra:** pesquisa de mercado INFORMA a decisão, não a substitui. Antes de transplantar, escrever na mesma frase POR QUE o problema do sistema-fonte é o nosso ("que premissa do modelo DELES sustenta isso, e vale AQUI?"). Quando a fonte é a doc oficial (a mais tentadora), cruzar com o canon existente ANTES — pode já ter sido rejeitado deliberadamente.
- **Classe:** importar-solucao-externa-sem-checar-premissa
- **Ocorrências:** 2   (proibicoes §5: 07-16 importar-solução-sem-checar-premissa (Odoo/Shopify/Akeneo na aba Preço especial) · 07-21 re-importar-Claude-Design-centro (doc oficial Anthropic vs canon v2))
- **Gate:** advisory — `design-agente-ativa` (UserPromptSubmit; cobre só o eixo fonte-de-design = ocorrência #2) + canon always-on `how-trabalhar.md` §ordem-de-fonte 4 (*"Traduzir premissa, não copiar solução"*) + skill `charter-write` (Non-Goals/Anti-hooks `⚠️ NÃO INFERIR` — só [W] preenche). **SEGUE ALARMANDO de propósito** (prefixo `advisory`, convenção ADR 0344): o eixo modelo-de-negócio (ocorrência #1) não tem hook. **Determinismo MEDIDO E REPROVADO 2026-07-25** (corpus **769** = 256 charters + 42 casos + 471 ADRs/proposals, contagem sem head_limit): C1 token de sistema externo **202/769**; C2 token em bloco normativo **26**; C3 (C2 sem tradução-de-premissa) **25 hits, 25/25 FP = 100%** — 9 artefato léxico (`jira` dentro do slug da ADR 0070; `temporal` como adjetivo PT), 5 são o próprio canon que REJEITA fonte externa (0239/0299/0315/0324/0335 — o gate morderia a defesa), 7 import legítimo já traduzido, 1 é o charter da ocorrência #1 **na versão CORRIGIDA**. **O classificador erra o SINAL:** counterfactual dá **0/4** nos textos errados reconstruídos e **2/2** nas correções — porque a assinatura do erro é a **AUSÊNCIA** da citação (charter v3 verbatim: *"O anti-padrão era meu, não do mercado"*). Promovê-lo pressionaria a RETIRAR a fonte dos charters, o oposto da regra. Variantes também caem: presence-gate de citação reprovaria **1.883 de 1.964** bullets ❌ legítimos (95,9% — forma banida §5 2026-07-01/09); nudge por bullet-Non-Goal-novo dispara ~1,3×/dia (114 commits/90d) com precisão ≤1,8%; denylist de nome de sistema é incompleta por construção (§5 2026-06-30) — as 2 ocorrências vieram de universos distintos (ERP de mercado × doc oficial Anthropic). E a ocorrência **#2 não atravessa chokepoint nenhum** (prosa em chat; zero artefato novo em `proposals/`/`prototipo-ui/` na janela 07-21) → sonda ali seria chokepoint fantasma (§5 2026-07-09 `flag:set`). Predicado é semântico por construção ("a premissa deles vale aqui?") → ADR 0224: semântico = advisory. **FORK PENDENTE [W]:** se advisory é o teto by-design (a medição diz que sim — não existe gate a construir), trocar o prefixo por `advisory-terminal (0224)` apaga o alarme; mantido `advisory` até [W] decidir, porque apagar alarme é política, não medição. **Reabrir** só com critério que separe *import traduzido* de *import cru* sem ler a premissa.
- **Ref:** `memory/proibicoes.md` §5 (2026-07-16, 2026-07-21, 2026-07-25) · reconciliação auto-feed 2026-07-22 · **medição de determinismo 2026-07-25 (chip 3)** · ADR 0224 (block vs advisory)

## LC-10 — Artefato AFIRMAR o próprio enforcement em tempo presente
- **Erro:** rotular o estado de enforcement **DENTRO** do artefato, em tempo presente — label de output (`anchor-lint.mjs` imprimindo `Gate de entrada (advisory)`), header de workflow (`nfebrasil-pest.yml:11` "a falha NÃO derruba o PR"), comentário de job, `nome` no `gates-registry.json`, `nota` de contrato. O rótulo apodrece no **primeiro flip** e vira mentira com cara de documentação.
- **Sintoma:** verificador (humano ou adversarial) lê o rótulo e conclui o enforcement **errado** — concluiu "o gate de entrada é advisory" sobre gate `required` desde 2026-06-24. Varredura contada: **8 sites vivos em 5 arquivos**, incl. `nfebrasil-pest.yml` **auto-contraditório no mesmo arquivo** (L11 "não derruba" × L90 "agora MORDE") num gate **fiscal ×150 clientes**, e o `gate-selftest.mjs` — o metaguard que prova que as catracas mordem — se auto-rotulando `(advisory)`.
- **Regra:** o artefato reporta o **NÚMERO** (`Gate de entrada: 264 US sem aceite`); se aquilo bloqueia merge é da **branch protection** (camada de cima) — script afirmando status de gate é violação de camada. Prosa aceita **fato datado em passado** ("promovido a required em 2026-06-24" — história, não apodrece); **nunca** afirmação em presente ("segue advisory", "não bloqueia até o flip"). Dono único de "o que é required" = [`governance/required-checks-baseline.json`](../governance/required-checks-baseline.json) (baseline↔vivo vigiado por `protection-drift.mjs`) — quem precisar falar de enforcement **aponta pro dono, não restateia**. Não construir 2º oráculo de enforcement.
- **Classe:** enforcement-autodeclarado-em-presente
- **Ocorrências:** 2   (proibicoes §5: 07-16 gate-label-afirma-proprio-enforcement — a lápide **numera os dois strikes dentro dela**: Strike 1 = P14, que "consertou os NOMES à mão" quando o nome mentia sobre gates já required; Strike 2 = os labels/comentários que sobraram e enganaram o verificador adversarial, 8 sites vivos em 5 arquivos)
- **Gate:** none
- **Ref:** `memory/proibicoes.md` §5 (2026-07-16) · [session 2026-07-16](sessions/2026-07-16-adversario-ponto-entrada-us-uc.md) · reconciliação auto-feed 2026-07-25 (CHIP 4). **Por que `none` e não "parcial":** as 2 máquinas óbvias foram **MEDIDAS e reprovadas** na própria lápide — (a) *gate de vocabulário* (grep de `advisory|não-required` em job que está no baseline) deu **130 falso-positivos em árvore limpa (100% FP)**, porque a palavra é valor de schema no `memory-health`, é fixture do `gate-selftest` e é modo legítimo do `anchor-lint`; (b) *label derivado do baseline em runtime* mente com certificado de frescor — o baseline **laga o vivo por design** (flip do `anchor-content-check` 08/07 → baseline 12/07 = **4 dias** imprimindo "(advisory)" pra gate required). O dono do FATO (`required-checks-baseline.json` + `protection-drift.mjs`) cobre **baseline↔vivo**, NÃO prosa. Defesa hoje = cultural (§5) + a revogação já aplicada nos sites (`anchor-lint.mjs:935-936`).

## LC-11 — Presence-gate: gate que mede PRESENÇA em vez de COMPORTAMENTO
- **Erro:** propor gate/catraca cuja métrica é **presença** — arquivo apareceu no diff (`charter-sync-gate`), texto está escrito no YAML (selftest que faz `grep` de `--compare`), campo auto-declarado tem data (`last_validated`, `verificado_em`), seção não está vazia (`anchor-lint "§ vazio = falha"`) — como se presença fosse garantia de correção.
- **Sintoma:** gate **verde** enquanto o contrato drifta. "O charter mudou no diff" passa com um typo no changelog; o `.tsx` podia ter tocado o charter **E** deletado o menu → verde. Catraca sobre campo que o próprio agente escreve trava **teatro**, não frescor. É a família do gate-de-teatro (`mwart-gate` `continue-on-error`, `jana-ragas-gate` mock) que a [ADR 0271](decisions/0271-revisao-gates-ci-estado-real-required-e-subtracao-segura.md) **deletou** — somar um rema contra a fase de subtração.
- **Regra:** enforcement de comportamento = **UC + teste que quebra quando o comportamento some** ([ADR 0256](decisions/0256-knowledge-survival-meia-vida-catraca-sentinela.md)/[0264](decisions/0264-governanca-executavel-trio-dominio-e2e.md)), **cego a qual arquivo mudou**. Bump de charter/RUNBOOK é **higiene, nunca o mecanismo**. Presença de TEXTO/CAMPO/SEÇÃO é tão proibida quanto presença no diff. Antes de propor gate: inventariar quem já mede aquilo (`governance/gates-registry.json` + ADR 0264/0271/0314) e **estender o dono do tema**, nunca abrir paralelo.
- **Classe:** presence-gate-em-vez-de-comportamento
- **Ocorrências:** 4   (proibicoes §5: 07-01 charter-sync-gate-presenca-no-diff — a mesma lápide registra sob "Reincidência da classe" mais 3 variantes rejeitadas na origem: selftest-grep-no-YAML · ratchet de `last_validated` · anchor-lint "§ vazio" · 07-09 frescor-`verificado_em`-auto-declarado, declarada explicitamente "presence-gate da mesma família do `last_validated`" · **07-26 presence-gate que ENVELHECEU EM PRODUÇÃO** — o `modulo-preflight-warning` decidia "leu o briefing?" por substring em texto corrido; silencioso em **116 de 116** pares medidos em 417 sessões, **87 (75%) por casamento acidental** de 6.490 chars dentro do system prompt. · **07-27 o banner do próprio loop IA-OS** — `loop-fechar-check.mjs` decidia "o gap P0 está fechado?" por `detect: file_any` = **o arquivo existe**; o item #6 (LGPD purge) tinha `"done": false` no manifesto e mesmo assim saía `[OK]` + **"LOOP FECHADO"** em toda sessão, porque `RetentionPurgeCommand.php` existe — enquanto o DoD real (`JANA_RETENTION_ENABLED=true` pós-canary + sign-off [W]) segue aberto. O escape valve que a própria mensagem anunciava — *"(Para reabrir um item, mude 'done' no manifesto.)"* — era **letra morta em 3 dos 4 itens**, os `file_any`. Defeito nascido com a rotina em **maio/2026** (#1938) que atravessou o **porte fiel** `.ps1`→`.mjs` de **julho/2026** (US-GOV-052) sem ser notado — datas exatas na lápide (aqui só recibo, pra não poluir a gramática da linha). Fix: veto do `false` explícito + 6 asserts novos (bite-test + 3 controles negativos + 2 E2E de contrato), 17/17 verde. Conto **4** = 4 lápides distintas com recibo datado; a massa DECLARADA pelo autor é 6 tentativas — não inflo o contador acima do que o recibo prova)
- **Gate:** parcial — `scripts/governance/gate-selftest.mjs` (fixtures boa+ruim provam que a catraca **morde numa fixture**, não que a **métrica meça comportamento** — um presence-gate passa nesse selftest) + `scripts/governance/design-gate-bites.mjs` / [ADR 0336](decisions/0336-gates-design-promocao-por-mordida-provada-emenda-0314.md) DR-2 (≥2 **mordidas reais** antes de promover — mas só pra gates de **design**). O julgamento "presença ≠ comportamento" segue **humano**. ⚠️ **Ponto cego medido 2026-07-26:** "revisão adversarial na origem" matou as 5 primeiras **porque eram propostas**; a 6ª já estava **mergeada e rodando havia meses** e só apareceu contando **disparo real contra população de gatilho** (116 pares). Revisão na origem não alcança presence-gate que já passou — pra esses, o instrumento é medição de mordida no corpus, não leitura de código. **O ponto cego REINCIDIU em 07-27** (2ª instância em 2 dias, mesma assinatura: mergeado havia meses, achado só ao confrontar a SAÍDA do mecanismo com a fonte que ele diz resolver — banner `[OK]` × manifesto `done:false`). Sinal de que "presence-gate já em produção" é **população**, não caso isolado: os candidatos são os mecanismos que decidem estado por `existsSync`/substring/campo auto-declarado. Varredura ainda **não feita** — se virar chip, o predicado é medir saída-vs-fonte, nunca reler o código.
- **Ref:** `memory/proibicoes.md` §5 (2026-07-01, 2026-07-09, 2026-07-26, 2026-07-27) · L-24 "presença ≠ correção" · [handoff 2026-07-09 15:20](handoffs/2026-07-09-1520-revisao-memoria-processo-estabilizacao.md) · reconciliação auto-feed 2026-07-25 (CHIP 4) · [PR #4797](https://github.com/wagnerra23/oimpresso.com/pull/4797) (fix + antes→depois)

## LC-12 — AGIR sobre estado GLOBAL compartilhado presumindo que é seu
- **Erro:** executar comando que consome/destrói estado **global do repositório** — `git stash pop/apply/drop`, `git worktree remove`, `git branch -D`, `gh api` que troca branch protection, `Cache::forget` em prod — **presumindo que a entry do topo é a sua**, sem listar antes. O estado é do **repo**, não do worktree: neste projeto há **N worktrees e N sessões paralelas** escrevendo na mesma pilha.
- **Sintoma:** o comando "funciona" e traz/apaga trabalho de outra sessão. Ou, com sorte, **conflita** — e o conflito é a única razão de o dano não ter acontecido. Assinatura: `git stash list` mostra a entry do topo com `On <branch-que-não-é-a-sua>`.
- **Regra:** antes de qualquer consumo de estado global, **listar e conferir a posse** — `git stash list` (a entry é `On <sua-branch>`?), `git worktree list`, `gh api .../protection` antes do PATCH. Melhor: **nomear o que você cria** (`git stash push -m <marcador>`) e consumir **por nome**, nunca por posição (`stash@{0}` é ordinal, muda quando outra sessão empilha). Se o worktree está **limpo**, `git stash -u` **não cria entry** — logo o `pop` seguinte come a de outro. Alternativa sem estado global pra ler outra ref: `git show <ref>:<path>` ou `git checkout <ref> -- <path>` seguido de `git checkout HEAD -- <path>`.
- **Classe:** estado-global-compartilhado-presumido-proprio
- **Ocorrências:** 1   (proibicoes §5: **07-27 stash-pop-de-outra-sessão** — medir o efeito de um merge com `git stash -u` → `checkout origin/main -- SPEC.md` → `git stash pop`, num worktree **limpo**: o `-u` não criou entry e o `pop` foi buscar `stash@{0}` = `WIP on claude/pr6-paymentgateway-redistill`, de **outra sessão**. Salvou o **conflito** em `PaymentGateway/BRIEFING.md`, que preservou a entry. A pilha do repo tinha **16 entries** de várias branches — a de cima nunca foi minha.)
- **Gate:** none — **par candidato MEDIDO e pronto (P4 do [`block-instrumento-sem-porta-viva`](../.claude/hooks/block-instrumento-sem-porta-viva.mjs)), não armado por two-strikes ([ADR 0344](decisions/0344-two-strikes-cobre-processo.md): 1ª ocorrência conserta, não codifica; não chegou em prod).** Predicado **determinístico** (não semântico): `git stash list` dá `On <branch>` do topo, `git branch --show-current` dá a sua. População de gatilho medida 2026-07-27 no corpus de transcripts — **2.160 jsonl varridos**, 187 com `git stash`, contando só `tool_use` Bash/PowerShell: **111 comandos**, **98 (88,3%) sem entry explícita** (piso conservador: `pop 2>/dev/null` caiu no balde errado do classificador). ⚠️ **FP REAL achado na medição:** *"topo é de outra branch"* **não basta** — `stash push` em A → `checkout B` → `pop` é fluxo legítimo com topo sempre de outra branch; bloquear puniria o uso correto (doença do allowlist-de-pasta / guard `@scope`). **Forma que sobrevive: advisory** que imprime **de quem é o topo** no `pop|apply` sem entry — informação sempre verdadeira, decisão humana. Armar na 2ª ocorrência.
- **Ref:** `memory/proibicoes.md` §5 (2026-07-27) · adjacente e já em `proibicoes §Ambiente`: `git worktree remove` com junction no Windows (2026-05-11 `vendor/`, 2026-07-14 `node_modules/`) — mesma família (comando que segue estado compartilhado e destrói alvo real), lá com proibição própria
