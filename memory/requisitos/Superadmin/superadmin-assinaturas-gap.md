---
id: requisitos-superadmin-assinaturas-gap
tela: superadmin/Assinaturas/Index (/superadmin/superadmin-subscription)
prototipo: prototipo-ui/cowork/superadmin-page.jsx
tela_viva: Modules/Superadmin/Resources/js/Pages/superadmin/Assinaturas/Index.tsx
gerado_em: 2026-09-06
---

# GAP-SPEC — superadmin/Assinaturas/Index

> Protótipo = porte do Blade legado (superadmin-page.jsx:1-9), anterior às telas React (SA-O1..O3, 2026-08). Charter: `Modules/Superadmin/Resources/js/Pages/superadmin/Assinaturas/Index.charter.md` (Non-Goals/Anti-hooks respeitados, nunca reabertos).

**Veredito:** PARIDADE na lista (KPIs, 3 filtros, tabela de 6 colunas com vigência fundida, ordenação, kebab, gaveta de status/vigência) e VIVO-À-FRENTE em URL/whitelist/vazio/motivo de cancelamento; todas as divergências do mockup (Exportar, Lançar, BulkBar, comprovante, 5 status, 6/página, R$ no subtítulo) já estão declaradas no charter §Divergências / RUNBOOK §7 — **0 itens a decidir** (o feedback pós-ação existe: toast global do `app.tsx` sobre o flash `status` do controller).

| Parte | Estado no vivo | Ação |
|---|---|---|
| PageHeader — título | `PageHeader title="Assinaturas" moduleNav` (Assinaturas/Index.tsx:167); mockup `PageHead titulo="Assinaturas"` (superadmin-page.jsx:1072) | Nada — paridade |
| PageHeader — subtítulo (registros · total aprovado em R$ · pendente de baixa) | `description="Cobrança de todos os negócios da plataforma"` fixa (Assinaturas/Index.tsx:167); as contagens são os 4 KPIs (:277-296) | Nada — decisão já registrada (charter §Divergências "subtítulo com total aprovado em R$ → contagens apenas — MRR tem dono, 2º oráculo seria régua duplicada"; RUNBOOK-assinaturas §7; o "· 1 pendente de baixa" do mockup :1072 é literal fixo = mock) |
| PageHeader — botão "Exportar" | Sem exportação (Assinaturas/Index.tsx:165-251) | Nada — decisão já registrada (RUNBOOK-assinaturas §7 "Exportar / Lançar assinatura — decisão [W], não migração"; mockup superadmin-page.jsx:1074 só dispara toast) |
| PageHeader — botão "Lançar assinatura" | Sem criação na tela; `create()/store()` seguem em Blade e `store()` exige `can('subscribe')` (`SuperadminSubscriptionsController.php:250, :267`) | Nada — decisão já registrada (RUNBOOK-assinaturas §7 "permissão diferente da que guarda esta tela — decisão [W], não migração"; charter Anti-hook "Não escrever `status` direto") |
| KPIs (`superadmin.assinaturas.kpis`) — Ativas · Em trial · Pendentes · Vencidas ou canceladas | `KpiLinha` com 4 cards (Assinaturas/Index.tsx:277-296), fonte `kpisPayload()` (`SuperadminSubscriptionsController.php:124`); nota "bloqueadas por inadimplência, fora desta conta" (:290-294) e "conta pelo fim do teste, não pelo status" (:284); mockup :1078-1083 | Nada — vivo à frente (recorte `declined` dito em texto — charter Anti-hook "Não fundir Bloqueada com Cancelada"; rótulo "Aprovadas"→"Ativas" é divergência declarada no contrato `_papel`) |
| Filtros (`superadmin.assinaturas.filtros`) — Pacote · Status · Criada em + "Limpar" | `FiltroPacote` sob `<Deferred>` + 2 `Select` + Limpar condicional (Assinaturas/Index.tsx:180-218, :299-311); mockup :1085-1095 | Nada — vivo à frente (RUNBOOK-assinaturas §3 "paridade com o F1"; no mockup "Criada em" é inerte — `onChange={() => {}}`, superadmin-page.jsx:1091 — e o status "Bloqueada" não existe como filtro) |
| Filtros + ordenação na URL (partial reload) | `irPara` preserva `pacote/status/periodo/ordem/dir` na query string (Assinaturas/Index.tsx:137-152) | Nada — vivo à frente (charter Goals "sobrevivem a refresh e a link colado"; `UC-SAASS-09`; mockup usa `useState` local :1045-1051) |
| Contador "N de N" | Rodapé da tabela `{inicio}–{fim} de N assinaturas` (Assinaturas/Index.tsx:444-446); mockup na barra de filtros (superadmin-page.jsx:1094) | Nada — paridade (posição adaptada; charter Goals "total dito em texto") |
| Tabela (`superadmin.assinaturas.tabela`) — 6 colunas: Assinatura (#id + "criada") · Negócio (+ pacote) · Status · Vigência fundida (`início → fim` + trial) · Valor · Pagamento (via + transação) | `COLUNAS` + linhas (Assinaturas/Index.tsx:123-130, :382-437); mockup :1105-1149 | Nada — paridade (charter Goals "vigência fundida"; valor formatado do payload via `Intl.NumberFormat` :134, :424) |
| Tabela — ordenação por cabeçalho | `Cabecalho` com `aria-sort` em 4 colunas visíveis + `criado` no default, whitelist `ORDENS` no servidor (Assinaturas/Index.tsx:154-156, :313-338; `SuperadminSubscriptionsController.php:59, :76, :208`); mockup `SortTh` em 5 colunas no cliente (superadmin-page.jsx:1111-1115) | Nada — vivo à frente (server-side + whitelist, `UC-SAASS-08`, charter Anti-hook "Não trocar `orderBy` whitelisted por input direto"; a coluna Assinatura do mockup ordena por `id`, no vivo o default `criado desc` cobre a mesma ordem — `id: null` :124) |
| Tabela — seleção múltipla + BulkBar (Baixar comprovantes · Exportar seleção) | Sem checkbox nem barra de lote (Assinaturas/Index.tsx:382-437) | Nada — decisão já registrada (charter §Divergências "as 2 ações do F1 não existem no backend"; RUNBOOK-assinaturas §7; mockup superadmin-page.jsx:1107-1110, :1155-1159) |
| Tabela — kebab por linha (Ver negócio · Mudar status · Editar vigência) | `Kebab` com `DropdownMenu` (Assinaturas/Index.tsx:481-499); "Ver negócio" navega para `/superadmin/business?q=<negocio_id>` (:490-492); mockup :1137-1144 ("Editar datas" = "Editar vigência"; "Ver negócio" via `window.__selectRoute` :1138 = harness) | Nada — paridade (charter Goals "Ações por linha (SA-O4b)") |
| Tabela — kebab "Baixar comprovante" | Ausente; comentário :476-479 explica (comprovante não existe no sistema) | Nada — decisão já registrada (charter §Divergências "não há comprovante — `payment_transaction_id` é string do gateway, medido"; RUNBOOK-assinaturas §7) |
| Tabela — kebab "Cancelar assinatura" + `Confirm` | Cancelar é a 3ª ação da gaveta de status, com motivo categorizado, observação e o efeito dito antes (Assinaturas/Index.tsx:532-536, :591-622); sem `Confirm` separado | Nada — vivo à frente (charter Goals "cada ação diz o efeito antes de aplicar"; R3 `UC-SAASS-12`; mockup superadmin-page.jsx:1143, :1161-1165 usa confirmação genérica sem motivo) |
| Gaveta (`superadmin.assinaturas.form`) — modo status | `Sheet` com `Select` de 3 ações (Aprovar · Marcar como vencida · Cancelar) + nota de efeito (Assinaturas/Index.tsx:521-537, :577-623), submit `put` para o resource (:560); mockup `Sel` com os 5 status + motivo livre (superadmin-page.jsx:575-580) | Nada — Non-Goal do charter (Anti-hook "Não ampliar a gaveta para os 5 status do F1"; contrato `_nota_recorte`; `UC-SAASS-11`) |
| Gaveta — modo vigência (Início · Fim · Fim do trial + nota "prorroga sem cobrança nova") | 3 inputs + notas (Assinaturas/Index.tsx:625-665), submit `post /superadmin/update-subscription` (:562-563); mockup :583-590 | Nada — paridade (`UC-SA-009`) |
| Gaveta — "misturar" status e datas | Uma gaveta com dois modos (Assinaturas/Index.tsx:543-551); mockup idem via `campo` (superadmin-page.jsx:557-566) | Nada — paridade (contrato `_nota_recorte` explica o desenho) |
| Feedback pós-ação (toast) | A gaveta fecha no `onSuccess` (Assinaturas/Index.tsx:560-563); o controller devolve `back()->with('status', ['success'=>0/1, 'msg'=>…])` (`SuperadminSubscriptionsController.php:414-417, :519-521`); `HandleInertiaRequests.php:99-113` traduz `status.msg`/`status.success` em `flash.success`/`flash.error`, e `resources/js/app.tsx:55-75` renderiza toast global em todo `router.on('success')` (`showFlashToast`). Nem a tela nem o `AppShellV2` precisam renderizar o flash — o toast é do app. | Nada — paridade (mockup superadmin-page.jsx:1168, :1143-1144 confirma com toast local; no vivo o toast é global via `app.tsx:75` + flash do controller — mesma capacidade, outro dono) |
| Paginação (`superadmin.assinaturas.paginacao`) | Server-side, 20/página, Anterior/Próxima + total (Assinaturas/Index.tsx:440-467; `SuperadminSubscriptionsController.php:212`); mockup 6/página no cliente (superadmin-page.jsx:1044, :1151) | Nada — decisão já registrada (charter §Divergências "paginação 6/página → 20 — decisão [W] em aberto") |
| Estado carregando (KPIs · filtro de pacote · tabela) | `KpisEsqueleto` (Assinaturas/Index.tsx:255-263, :175), skeleton do filtro (:181) e da tabela (:221-229) | Nada — vivo à frente (mockup só tem `SkelTable` via `useCarga` harness, superadmin-page.jsx:1053, :1098 — sem skeleton de KPI) |
| Estado vazio | Distingue "Nenhuma assinatura cadastrada" de "Nenhuma assinatura com esses filtros" (Assinaturas/Index.tsx:358-372) | Nada — vivo à frente (mockup só tem o vazio de filtro, superadmin-page.jsx:1099-1101; charter Goals) |

## Recibos de ausência
- `grep -nE 'aprovados|registros ·|pendente de baixa|description=.*R' Modules/Superadmin/Resources/js/Pages/superadmin/Assinaturas/Index.tsx` → 0   (subtítulo sem contagens/R$)
- `grep -nE 'Exportar|CSV|csv' Modules/Superadmin/Resources/js/Pages/superadmin/Assinaturas/Index.tsx` → 0   (sem exportação)
- `grep -nE 'Lançar|lancar|store\(|/create' Modules/Superadmin/Resources/js/Pages/superadmin/Assinaturas/Index.tsx` → 0   (sem criação de assinatura na tela)
- `grep -nE 'Checkbox|checkbox|selecionad|BulkBar|Selecionar a página' Modules/Superadmin/Resources/js/Pages/superadmin/Assinaturas/Index.tsx` → 0   (sem seleção múltipla/BulkBar)
- `grep -nE 'comprovante|Comprovante|download' Modules/Superadmin/Resources/js/Pages/superadmin/Assinaturas/Index.tsx` → 1, só o comentário :476   (sem ação de comprovante)
- `grep -nE 'Cancelar assinatura|Confirm|AlertDialog' Modules/Superadmin/Resources/js/Pages/superadmin/Assinaturas/Index.tsx` → 0   (sem item/confirmação separados — cancelar vive na gaveta :533)
- `grep -nE 'toast|Toast|sonner' Modules/Superadmin/Resources/js/Pages/superadmin/Assinaturas/Index.tsx` → 0   (a tela não tem toast próprio — o toast é global: `grep -n 'showFlashToast' resources/js/app.tsx` → :55, :74)
- `grep -nE '__selectRoute' Modules/Superadmin/Resources/js/Pages/superadmin/Assinaturas/Index.tsx` → 0   (navegação real, não harness)
- `sed -n '1044,1175p' prototipo-ui/cowork/superadmin-page.jsx | grep -cE 'Vazio titulo="Nenhuma assinatura cadastrada|Skeleton|KpiEsqueleto'` → 0   (mockup sem vazio "cadastrada" nem skeleton de KPI)
