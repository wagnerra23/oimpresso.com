---
id: requisitos-governance-dashboard-gap
tela: governance/Dashboard (/governance/dashboard)
prototipo: prototipo-ui/cowork/governance-page.jsx
tela_viva: resources/js/Pages/governance/Dashboard.tsx
gerado_em: 2026-09-06
---

# GAP-SPEC — governance/Dashboard

> Protótipo = porte REVERSO do vivo (governance-page.jsx:1-3 "Espelha as telas vivas"; retrato de ~2026-08-23, já com a seção MCP que o charter v3 absorveu em 2026-08-05). Fase 1 = PARIDADE. Charter: `resources/js/Pages/governance/Dashboard.charter.md` (v3 — Non-Goals e Automation Anti-hooks respeitados, nunca reabertos).

**Veredito:** VIVO-À-FRENTE com 1 item a decidir — o vivo tem badge do ActionGate, intervalo custom na seção MCP, links de drill-down, atalhos e documentos canônicos que o retrato não desenha; o retrato acrescenta só a régua de conformidade por artigo com selo "auto-declarado".

| Parte | Estado no vivo | Ação |
|---|---|---|
| Header / PageHeader | `Dashboard.tsx:745-753` — `<PageHeader icon="shield-check" title="Governança" description=…>` com `Badge` "ActionGate: {modo}" (`modeBadge` `:141`); layout `AppShellV2` em `:1050`. Mockup: `governance-page.jsx:403-418` (h1 `TITULOS.painel` + subtítulo de rota + selo `superadmin · cross-tenant`); badge de modo do ActionGate ausente — `ActionGate／modo aviso` só em prosa (`:98`) e num sub de KPI da vista Políticas (`:325`) | Nada — vivo à frente (o badge do modo off/warn/strict só existe no vivo) |
| Abas do shell (sub-navegação) | `Dashboard.tsx:743` `<GovernancaSubNav active="dashboard" />` (comentário `:740-742`: fonte é o DataController, nunca duplicada — charter §Anti-hooks). Mockup: `governance-page.jsx:24-30` + `:420-424` | Nada — paridade |
| Seletor de cenário (Base completa · Fontes ausentes · Sem permissão MCP) | Vivo: inexistente — `cenario／Cenário／Fontes ausentes` → 0 hits; os estados degradados vêm das props (`health_kpis.* === null` `:854-880`, `mcp_enabled` `:995`). Mockup: `governance-page.jsx:383-387` (`CENARIOS`) + `:410-414` (segmento no header) | Nada — mock/harness do protótipo (troca de cenário só simula o que as props do vivo já carregam) |
| KPIs "Constituição" | `Dashboard.tsx:755-804` — `KpiGrid cols=6`: ADRs pendentes (`href`) · Policies ativas · Skill approvals (`href`) · Actors registrados · Audit highlights 24h (tone dinâmico) · Compliance Constitution (`complianceColor` `:134`). Mockup: `governance-page.jsx:273-275` renderiza `KPIS_CONST` (`governance-data.jsx:24-30`, 5 cards — o 6º vira a régua abaixo) | Nada — paridade (mesmos indicadores; no retrato o compliance sai do grid e vira a régua da linha seguinte) |
| Régua "Conformidade com a Constituição" por artigo + selo "auto-declarado" | Vivo: só o `KpiCard` "Compliance Constitution" (`Dashboard.tsx:797-803`) com `${kpis.compliance_pct}%` e descrição "v1.1.0 — próx revisão …"; o valor é literal no controller — `Modules/Governance/Http/Controllers/DashboardController.php:65` `$compliancePct = (7 * 10) + (2 * 5) + 0; // = 80` (idem `:268`). Não há lista de artigos nem rótulo de proveniência: `ARTIGOS／Art\. N ／pleno／parcial` → 1 hit (`:748`, descrição do header), `auto-declarad／declarado` → 1 hit (`:81`, comentário de código). Mockup: `governance-page.jsx:74-102` (`Conformidade` — % + selo "auto-declarado" + fórmula `plenos×10 + parciais×5 + 0` + lista dos 10 artigos com estado, de `ARTIGOS` `governance-data.jsx:11-22`) | **Decidir.** Régua com breakdown por artigo (pleno/parcial/pendente) e selo de proveniência "auto-declarado" (mockup `governance-page.jsx:74-102`) ausente no vivo (`Dashboard.tsx:797-803` é um card só). Exige fonte por artigo — hoje o controller só tem os 3 somandos literais (`DashboardController.php:65`). Não é Non-Goal (edição/trigger de checks continuam fora). Construir ou rejeitar por escrito. |
| Card "Scorecard SDD" (deferred) | `Dashboard.tsx:806-844` — `<Deferred data="sdd">` com fallback textual (`:810`), 3 `KpiCard`: Composta v1 (k, Δ vs ontem, snapshot) · Métricas vivas N/total · Alertas SDD (1º alerta truncado); estado "Sem snapshot SDD ainda — cron 07:10 BRT" (`:838-842`). Mockup: `governance-page.jsx:104-122` (`CardSdd` — 1 card com composta+delta, "N de M métricas vivas", alertas; `useDefer(900)` + `Esqueleto`) | Nada — vivo à frente (3 KPIs + estado sem snapshot; o skeleton do retrato é harness de defer) |
| KPIs "Saúde do ecossistema" (3 fontes, degradação graciosa) | `Dashboard.tsx:846-882` — Failed jobs 24h · Custo IA 24h (BRL) · Última narrativa; cada um rende `—` + descrição "<tabela> ausente" quando a fonte é `null` (`:854-856`, `:861-866`, `:872-879`). Mockup: `governance-page.jsx:129-134` (`Saude` — 3 KPIs de `SAUDE` `governance-data.jsx:55-59`; cenário "fonte ausente" mostra `—` e `A.Vazio variant="offline"` `:136-137`) | Nada — paridade (a degradação por fonte ausente existe nos dois — charter §Goals "Degradação graciosa") |
| Lista "Narrativas Brain A 24h" | `Dashboard.tsx:959-996` — card próprio com badge de severidade (`severityBadgeClass` `:170`), texto truncado a 80, hora, link "histórico →" (`:966-968`) e `EmptyState` (`:972`). Mockup: `governance-page.jsx:139-146` (lista `NARRATIVAS` dentro da seção Saúde, severidade por classe, hora, texto); sem link — `ver todos／drill-down／histórico →` → 0 hits em `governance-page.jsx` | Nada — vivo à frente (card separado com link de histórico e vazio próprio) |
| Lista "ADRs aguardando decisão" | `Dashboard.tsx:884-916` — contagem no título, link "ver todos →" (`:891-893`), cada ADR com slug linkado (`:904-909`), título e data; `EmptyState` (`:897`). Mockup: `governance-page.jsx:157-167` (`Decisoes` — slug curto, título, "há N dias"; teto 10 declarado no sub `:157`) | Nada — vivo à frente (links por ADR e "ver todos"; "há N dias" vs data é rótulo) |
| Lista "Ocorrências em 24 h" / Audit Highlights | `Dashboard.tsx:918-957` — `audit_highlights.slice(0, 10)` (`:936`), badge de status por token semântico (`:940`), endpoint mono, tool/resource, hora, link "drill-down →" para `/governance/audit` (`:925-927`), `EmptyState` (`:931`). Mockup: `governance-page.jsx:168-180` (hora, ator, alvo, selo do resultado, duração ms) | Nada — paridade (mesma lista de topo com teto 10 — charter §Non-Goals "Histórico longo"; ator/duração por chamada são colunas da vista Auditoria — charter §Non-Goals "Drill-down por chamada individual do MCP (isso é governance/Audit)") |
| Seção MCP — cabeçalho + filtro de período | `Dashboard.tsx:626-696` — título + sub (ADR 0053), `Select` com os presets de `MCP_PRESET_LABEL` (`:204`) **e** intervalo custom De/Até com `Input type="date"` + "Aplicar" (`:656-693`); `router.get` com `only: ['mcp','mcp_filters']` (`:616-624`); status "Atualizando o período…" (`:698-701`). Mockup: `governance-page.jsx:196-204` (segmento de 5 presets de `PERIODOS` `governance-data.jsx:95-99`); sem intervalo custom — `custom／De.*Até／type="date"` → 0 hits em `governance-page.jsx` | Nada — vivo à frente (intervalo custom De/Até + feedback de recarga; o partial reload é o mesmo desenho nos dois — charter §Goals) |
| Seção MCP — KPIs (4) | `Dashboard.tsx:372-400` (`McpPainel`): Chamadas · Taxa de sucesso · Latência p95 (com p50/p99/máx) · Custo. Mockup: `governance-page.jsx:210-215` (mesmos 4) | Nada — paridade |
| Seção MCP — abas in-page (Consumo · Acesso/RBAC · Usuários e tools) | `Dashboard.tsx:403-419` — `<SubNav variant="segmented">` controlado por `aba` (`:364`), itens `consumo`/`acesso`/`usuarios` (`:408-416`). Mockup: `governance-page.jsx:216-220` (`gov-subtabs`, 3 botões `aria-pressed`) | Nada — paridade (no vivo é o `<SubNav>` canônico — charter §UX Anti-patterns proíbe `role="tablist"` hand-rolado) |
| Seção MCP — conteúdo das abas (série diária · resultado · negadas por código · tools · usuários) | `Dashboard.tsx:420-597` — `McpCallsChart` SVG (`:238-260`, com `EmptyState` "Sem dados no período" `:254-256`), Distribuição por resultado (`:448-480`), Negadas por código (`:482-518`), Tools e recursos mais usados (`:521-558`), Quem mais consome (`:560-596`) — cada bloco com `EmptyState` próprio (`:457`, `:489`, `:526`, `:565`). Mockup: `governance-page.jsx:221-259` (`Chart type="area"` ou spark fallback, barras por resultado, lista de códigos, atores, ferramentas; sem vazios por bloco) | Nada — vivo à frente (vazio por bloco só no vivo; o gráfico e as 4 listas são os mesmos) |
| Seção MCP — fonte ausente (`mcp_audit_log` inexistente) | `Dashboard.tsx:704-718` — `<Deferred data="mcp">` obrigatório (comentário `:702-703`, charter §Anti-hooks) + `EmptyState` "Trilha do MCP indisponível" quando `mcp` é null. Mockup: `governance-page.jsx:206-207` (`A.Vazio variant="offline"` "mcp_audit_log não existe nesta base") | Nada — paridade (mesma degradação; o defer do retrato é harness) |
| Seção MCP — sem permissão `jana.mcp.usage.all` | `Dashboard.tsx:992-995` — `{mcp_enabled && <McpSection …>}`: some inteira, título incluso (comentário `:992-994`: "sem ele a prop `mcp` nem é enviada"); nenhum placeholder — `no-perm／Esta seção pede` → 0 hits. Mockup: `governance-page.jsx:284-289` (`A.Vazio variant="no-perm"` explicando o gate herdado) | Nada — decisão já registrada (`Dashboard.tsx:992-995` "Some inteira (título incluso) sem `jana.mcp.usage.all`" + charter §Anti-hooks sobre o gate herdado) |
| Atalhos de governança + Documentos canônicos | `Dashboard.tsx:998-1027` (4 atalhos: Policies · Audit log · Drift alerts · ADRs proposto) e `:1030-1047` (8 links: Constituição, Trust Tiers, Architecture, Identity Mesh, Enforcement, Drift Migration Plan, Audit cascata, KB). Mockup: ausentes — `Atalhos／Documentos canônicos／Trust Tiers` → 0 hits em `governance-page.jsx` | Nada — vivo à frente |

## Recibos de ausência
- `grep -nEi 'cenario|Cenário|Fontes ausentes' resources/js/Pages/governance/Dashboard.tsx` → 0
- `grep -nEi 'ARTIGOS|Art\. [0-9]+ |pleno|parcial' resources/js/Pages/governance/Dashboard.tsx` → 1 (só `:748`, prosa da descrição do header — não é lista por artigo)
- `grep -nEi 'auto-declarad|declarado' resources/js/Pages/governance/Dashboard.tsx` → 1 (só `:81`, comentário de código — não é selo na UI)
- `grep -nEi 'no-perm|Esta seção pede' resources/js/Pages/governance/Dashboard.tsx` → 0
- `grep -nEi 'ActionGate|modo aviso' prototipo-ui/cowork/governance-page.jsx` → 2 (`:98` prosa da régua, `:325` sub de KPI da vista Políticas — nenhum é badge de modo; sustenta "vivo à frente" do header)
- `grep -cEi 'Atalhos|Documentos canônicos|Trust Tiers' prototipo-ui/cowork/governance-page.jsx` → 0 (ausência no mockup)
- `grep -cEi 'custom|De.*Até|type="date"' prototipo-ui/cowork/governance-page.jsx` → 0 (ausência no mockup — intervalo custom da seção MCP)
- `grep -cEi 'ver todos|drill-down|histórico →' prototipo-ui/cowork/governance-page.jsx` → 0 (ausência no mockup — links das 3 listas)
