---
id: requisitos-repair-repair-jobsheet-gap
tela: Repair/JobSheet/Index (/repair/job-sheet)
prototipo: prototipo-ui/cowork/repair-page.jsx
tela_viva: resources/js/Pages/Repair/JobSheet/Index.tsx
gerado_em: 2026-09-06
comparacao: memory/requisitos/Repair/jobsheet-visual-comparison.md
---

# GAP-SPEC — Repair/JobSheet/Index

> Fase 1 do protocolo (`prototipo-ui/PROTOCOL.md`) em modo **PARIDADE**. `repair-page.jsx` é porte REVERSO dos blades (cabeçalho l.1-2; §5 2026-08-28); a aba comparada é `Folhas de OS` (repair-page.jsx:144-256, origem `job_sheet/index.blade.php`). Estado no vivo medido em 2026-09-06 sobre `origin/main` `80bc4ef8b9`, com `grep -n`. Lidos antes: `Index.charter.md` v3 (Non-Goals l.51-55, UX Target l.65, anti-hook l.93, nota l.26-27); a tela **não tem** `Index.casos.md` (trio incompleto, já declarado no charter). O vivo busca a lista no endpoint DataTables AJAX compartilhado com o Blade (`datatable_url`, JobSheetController.php:79-288) e só pode consumir campos escalares desse payload (UX Target l.65). Dado mock do protótipo não é gap.

| Parte | Estado no vivo | Ação |
|---|---|---|
| Header (título · "Nova OS") | `PageHeader` "Ordens de serviço" + botão "Nova OS" → `/repair/job-sheet/create` (Index.tsx:99-108) | Nada — paridade com "Adicionar folha" do shell (repair-page.jsx:705); a permissão de criar é checada no Controller do `create`, não na lista. |
| Abas de estado (Pendentes · Concluídas · Entrega vencida · Todas, com contagem) | Não há abas; status é 1 dropdown por status individual (Index.tsx:113). O endpoint já aceita `is_completed_status=1／0` (JobSheetController.php:155-160), mas a tela não o envia — os params montados são só `location_id`／`status_id`／`contact_id` (Index.tsx:70-73) | **Decidir.** O `TabBar` do mockup (repair-page.jsx:211-222) tem 4 abas com contagem. Pendentes／Concluídas são servíveis hoje sem tocar backend (`is_completed_status`); "Entrega vencida" precisa de parâmetro novo (o Controller não filtra por `delivery_date`, l.135-160); contagem por aba exige chamadas extras ou agregado novo. Construir ou rejeitar por escrito. |
| Filtros (Local · Status · Cliente · Técnico · busca) | 3 `Select` Local／Status／Cliente + "Limpar" (Index.tsx:111-127) e o aviso `is_user_service_staff` (Index.tsx:128-132, paridade com repair-page.jsx:223). A prop `filters.service_staffs` chega (Index.tsx:31; JobSheetController.php:315) e o endpoint aceita `technician` (JobSheetController.php:146), mas não há dropdown de técnico. Não há busca por texto (nenhum `Input` importado, Index.tsx:8-20) | **Decidir.** (a) Técnico (repair-page.jsx:225-226): dado e filtro já existem nos dois lados, falta só o `FilterSelect` — pequeno. (b) Busca por nº／cliente／série／modelo (repair-page.jsx:698-703, 161-165): o Controller não lê busca livre (l.135-160), exige parâmetro novo no endpoint compartilhado com o Blade. Cliente é vivo à frente (o mockup não tem). Construir ou rejeitar por escrito. |
| Tabela | 6 colunas OS／Cliente／Aparelho／Status／Técnico／Local (Index.tsx:161-200). Status com ponto genérico `bg-primary` (Index.tsx:187) embora `status_color` chegue no payload (Index.tsx:50; select em JobSheetController.php:115). `flags.show_serial_no` é recebida (Index.tsx:35) e não usada. No payload, `serial_no` e `status_color` são escalares; `delivery_date`, `estimated_cost` e `service_type` vêm como HTML (`editColumn`／`rawColumns`, JobSheetController.php:240-288) | **Decidir.** O mockup tem 11 colunas (repair-page.jsx:170-182) e o Blade de origem também traz serviço, prazo, série e custo (job_sheet/index.blade.php:75-98). Sem tocar backend: cor real do status e coluna Nº de série sob `flags.show_serial_no` — os dois dados já chegam. Prazo, tipo de serviço e custo estimado exigem o endpoint emitir campo escalar ao lado do HTML (o charter proíbe consumir o HTML, l.65) ou a onda do motor de dados (l.26-27); custo ⚠️ toca valor — só exibir. Pipeline FSM (`FsmStepper`): a FSM Repair existe (ADR 0143) mas o select do endpoint não traz `current_stage_id` (l.115). Construir ou rejeitar por escrito. |
| Ações por linha | Só o nº da OS é link para `/repair/job-sheet/{id}` (Index.tsx:178); nenhuma outra ação por linha | **Decidir.** O mockup tem status／editar／imprimir／excluir por permissão (repair-page.jsx:196-204) e o Blade tinha coluna Ação (job_sheet/index.blade.php:75). Links de navegação (Editar → `/repair/job-sheet/{id}/edit`; Imprimir → rota Blade `job-sheet/print/{id}`, Routes/web.php:32) são compatíveis com o charter — os Non-Goals l.51-52 vetam o inline e o print direto, não o link — e não existem no vivo. "Status" e "excluir" mudam estado a partir da lista, que o charter declara visualização-only (l.54, l.93): ficam fora. Construir só os links, ou rejeitar por escrito. |
| Ações em lote (`BulkBar`) | Sem seleção de linhas (0 `checkbox` no Index.tsx) | Nada — fora do charter. Alterar status／atribuir técnico／excluir em lote (repair-page.jsx:246-255 e o `LoteModal` l.578-602) são mutação a partir de uma lista que o charter fixa como visualização-only (Index.charter.md:54) e read-only (l.93). Reabrir é mudança de charter, não gap desta comparação. |
| Paginação e rodapé | Busca 200 linhas fixas (`length: '200'`, Index.tsx:70); rodapé "N OS exibida(s)" (Index.tsx:204-206); sem paginação | Nada — já registrado. O charter declara a migração do motor de dados (paginator server-side) onda própria, decisão de [W] (Index.charter.md:26-27); o `Pagination` do mockup (repair-page.jsx:240-245) e o teto de 200 são o mesmo item. |
| Drawer da folha | O clique na OS leva à tela própria `JobSheet/Show.tsx` (Index.tsx:178; `Show.charter.md` e `Show.casos.md` existem ao lado) | Nada — paridade em tela própria (PT-03 Detalhe). O `FolhaDrawer` do mockup (repair-page.jsx:427-527, abas Folha／Checklist／Peças／Atividades) é assunto do charter do Show, não desta lista. |
| Estados (carregando · vazio · erro) | Skeleton `aria-busy` (Index.tsx:136-141) e `EmptyState` com variantes erro／vazio／filtro-sem-resultado (Index.tsx:142-159) | Nada — paridade com o `EmptyState` da aba (repair-page.jsx:237-238) e com skeleton／erro do shell (repair-page.jsx:718-729). |
