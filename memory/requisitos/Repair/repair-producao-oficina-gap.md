---
id: requisitos-repair-repair-producao-oficina-gap
tela: Repair/ProducaoOficina/Index (/repair/producao-oficina)
prototipo: prototipo-ui/cowork/repair-page.jsx
tela_viva: resources/js/Pages/Repair/ProducaoOficina/Index.tsx
gerado_em: 2026-09-06
---

# GAP-SPEC — Repair/ProducaoOficina/Index

> Fase 1 do protocolo (`prototipo-ui/PROTOCOL.md`) em modo **PARIDADE**. `repair-page.jsx` é porte REVERSO dos blades (cabeçalho l.1-2; §5 2026-08-28); a aba comparada é `Produção` (repair-page.jsx:103-142, que o próprio mockup liga ao `KanbanProductionService`) — esta tela é greenfield (sem Blade), então aqui o mockup mede uma tela já viva. Estado no vivo medido em 2026-09-06 sobre `origin/main` `80bc4ef8b9`, com `grep -n`. Lidos antes: `Index.charter.md` (Goals l.37-38, Non-Goals l.60-66, UX anti-pattern l.93) e `Index.casos.md` (UC-RPOE-01..06). Dado mock do protótipo não é gap; o `data_source='mock'` do vivo (fallback sem `repair_statuses`) também não.

| Parte | Estado no vivo | Ação |
|---|---|---|
| Cabeçalho e avisos | Sem `PageHeader`; barra fixa com filtros, badge "mock" explicando o fallback (Index.tsx:222-229) e contadores "N OS · N aguardando aprovação" (Index.tsx:230-243) | Nada — paridade. O `Alert` "Kanban derivado do status" (repair-page.jsx:112-114) e a nota de permissão (l.139) são texto explicativo; o vivo explica o fallback e conta o board. |
| Filtros | Chips por `slot_config` + "Limpar filtros" + contador filtrado (Index.tsx:201-244; UC-RPOE-03) | Nada — vivo à frente. A aba do mockup não tem filtro (só a busca do shell, que filtra `folhas`, repair-page.jsx:161-165). |
| Colunas do kanban | 5 colunas com ponto de cor, rótulo, contagem e vazio "Nenhuma OS"／"Soltar aqui" (Index.tsx:248-271, 359-372; UC-RPOE-01) | Nada — paridade com as 5 `BoardColumn` com contagem (repair-page.jsx:117-135; `COLUNAS` em repair-data.jsx:39-45). |
| Card da OS | Código, item · marca, medida de uso, slot／área, executor + ETA, orçamento quando `pending_approval`, `status_label` quando pronto (Index.tsx:417-474). Com dado real o payload manda `code` = série ou nº da OS, `wait` = idade da OS, `slot`／`area`／`usage` nulos e `pending_approval` sempre `false` (ProducaoOficinaController.php:412-425) — o selo "Aprov?" e o banner de aprovação nunca aparecem fora do mock | **Decidir.** O `TaskCard` do mockup (repair-page.jsx:124-133) mostra também cliente, entrega prevista, atraso e o defeito relatado. A folha tem `contact_id`, `delivery_date` e `defects` (JobSheetController.php:115, 419), e o charter não os veta (Non-Goals l.60-66 vetam CRUD, push, atribuição de executor, aprovação e edição inline). Construir ou rejeitar por escrito. |
| Arrastar e soltar | HTML5 nativo + atualização otimista + `POST /repair/producao-oficina/{id}/move` (Index.tsx:136-165; Routes/web.php:44; US-REPAIR-PROD-4) | Nada — vivo à frente. O mockup só move estado local, gated por `repair_status.update` (repair-page.jsx:109-121, 658-663); no vivo a permissão é do endpoint. UC-RPOE-06 já registra que o card não é operável por teclado — defeito medido, dono é o casos. |
| Drawer da OS | `JobDrawer` (Index.tsx:477-578): cabeçalho real (code／item／brand) e `VendaDerivadaCard` real quando pronto (Index.tsx:528-530). O resto é texto fixo no `.tsx`: sintoma (Index.tsx:533-536, com vocabulário automotivo — suspensão, volante, km/h), fotos placeholder (539-550), itens sugeridos com preços literais (552-565), linha do tempo (567-574); banner de aprovação com "enviado há 2h via WhatsApp" fixo e botão "Reenviar" sem `onClick` (Index.tsx:505-519), que nunca renderiza com dado real (`pending_approval` sempre `false`) | **Decidir.** O `FolhaDrawer` do mockup liga cada seção ao dado da folha — defeitos (repair-page.jsx:479), checklist do modelo (490-498), peças (500-512), atividades (515-524). O charter lista o drawer como Goal entregue (l.38, "sintoma + fotos + peças + linha do tempo"), mas o código renderiza copy fixa, não a OS clicada. Ligar aos dados reais (peças ⚠️ toca valor — só exibir) ou declarar no charter que o drawer é mock; UC-RPOE-04 (drawer é o único container) vale nas duas saídas. |
| Modais e paleta ⌘K | Nenhum modal (UC-RPOE-04 exige zero modal e zero `<form>`) | Nada — decisão registrada. `StatusModal`／`ImprimirModal`／`LoteModal` e a paleta (repair-page.jsx:529-630) contrariam o UX anti-pattern do charter (l.93), defendido por teste. |
