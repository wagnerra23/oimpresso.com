---
id: requisitos-oficina-auto-service-orders-board-gap
tela: OficinaAuto/ServiceOrders/Board (/oficina-auto/ordens-servico)
prototipo: prototipo-ui/cowork/oficina-page.jsx
tela_viva: resources/js/Pages/OficinaAuto/ServiceOrders/Board.tsx
gerado_em: 2026-09-06
---

# GAP-SPEC — OficinaAuto/ServiceOrders/Board

> **Âncora conferida contra a revogação.** O charter de `Repair/ProducaoOficina/Index` **revogou** `oficina-page.jsx` como `related_prototype` em 2026-06-30, com o motivo escrito na própria linha: *"MIS-ANCHOR … oficina-page.jsx é o kanban de VEÍCULO da OficinaAuto → ancora em ServiceOrders/Board (parent OficinaAuto)"* (`Index.charter.md:14-16`). Esta tela é o destino nomeado por aquela revogação, e `ancora.mjs OficinaAuto/ServiceOrders/Board` confirma: `âncora ✓ [-page.jsx (bundle · bundle_source)] oficina-page.jsx`.
>
> ⚠️ **Existe um gap.md ANTERIOR sobre o MESMO protótipo** — [`kanban-producao-gap.md`](kanban-producao-gap.md), que o compara com `Repair/ProducaoOficina/Index.tsx`. Ele **não** é duplicata deste: é a comparação contra a **outra** tela, escrita antes da revogação. Fica como registro datado; o inventário de partes dele é bom e foi reusado aqui, mas todo "Estado no vivo" abaixo foi **remedido contra `Board.tsx`**.
>
> **Frescor do espelho (medido 2026-09-06, por HASH — rodada 38 do `.cowork-freshness-ledger.json`):** `oficina-page.jsx` = **STALE**. Espelho `1259f30cf94a…`, vivo `409e02172cd3…`. Diff decodificado: uma única região — o componente `Seg` (`oficina-page.jsx:732-738` no espelho) deixou de ser `<div className="seg">` com botões inline e passou a delegar para `window.CliSeg`. **Adoção de componente compartilhado do DS, não mudança de capacidade** (−4 linhas: 1296 no espelho → 1292 no vivo; o diff é `+1 −5`). Consequência para este documento: as citações **posteriores a `:738`** — `:745`, `:964-973` (com `:970` e `:972`) e `:1227` — estão **deslocadas em 4** em relação ao Cowork vivo; as anteriores (`:9`, `:140-152`, `:176`, `:212`, `:278`, `:342`) coincidem nos dois. **Todas as linhas aqui são do espelho versionado**, que é o que `gerar-map.mjs` hasheia — nenhuma aponta para o vivo.
>
> **Non-Goals do charter — NÃO reabertos aqui.** O charter (`Board.charter.md:83-99`) já decidiu por escrito: não cria OS pelo drag; não emite documento fiscal; a Grade não inventa cobertura serviço×sintoma (gate `no-mock-in-prod`); não mexe no board de caçamba; sem drag para etapas terminais (exceto "Entregar →"); e **"NÃO inventa campo sem lastro: ETA-diag, 'Encomendado: peça chega X' e 'Pago' do protótipo NÃO têm coluna real → omitidos"**. Esses aparecem abaixo como decisão registrada, não como "Decidir".

| Parte | Estado no vivo | Ação |
|---|---|---|
| Header + título | `PageHeader` com "Imprimir fila" (ghost) + "Nova OS"; o toggle de views e o menu Visão foram tirados do header e vivem na barra da busca (`Board.tsx:67-68` documenta a migração; `printOficinaFila` importado em `:88`) | Nada — decisão já registrada. O protótipo tem os mesmos dois botões no `prod-header` (`oficina-page.jsx:964-973`: `Imprimir fila` em `:970`, `Nova OS` em `:972`); a posição do toggle é a canon `.ofc-view-toolbar`, fixada por [W] em 2026-06-11 (charter §Goals). |
| KPIs | 6 cards via `BoardKpiCard`, com sublinha; **5 clicáveis** como filtro client-side e o 6º (`Valor em curso`) só leitura — contado na definição (`Board.tsx:478-483`: os 5 primeiros têm `filterKey`, `valor` tem `filterKey: null`), render em `:709`, estado em `:315` | Nada — paridade. O charter §Goals fixa exatamente esses 6. O protótipo tem a mesma faixa clicável (`kanban-producao-gap.md` §2). |
| Filtros — boxes/elevadores | **Abas** de box com contador por aba, filtro client-side instantâneo (`Board.tsx:54-55`, render em `:723` com `aria-label="Filtrar por box"`) | Nada — vivo à frente. O protótipo usa dropdowns Box/Mecânico; o vivo trocou por abas `.prod-equip-filters` com contador, decisão registrada no cabeçalho do arquivo. |
| Busca livre | `Input` com atalho `/`, contador de resultados e botão de limpar (`Board.tsx:774`, `:782`) | Nada — paridade. |
| Toggle de views | 4 views **todas in-page** sobre o mesmo payload `columns` — Quadro · Lista · Grade · Fila — via `Segmented`, persistidas em `localStorage` e refletidas em `?view=` (`Board.tsx:86`, `:812`; `VIEW_STORAGE_KEY` em `:179`, leitura da URL em `:185`) | Nada — vivo à frente. O comentário `:69-71` registra um estado **anterior** em que Lista/Fila navegavam para a Index; a unificação [W] de 2026-06-11 aposentou a página `Index` e trouxe as 4 para dentro. ⚠️ Quem ler só aquele comentário conclui o contrário do que o código faz hoje — o charter §Goals é a fonte. |
| Menu Visão (foco + densidade) | `Popover` com foco Etapa/Box/Mecânico (re-pivot client-side) e densidade compacto/padrão/detalhe (`Board.tsx:850-874`, estado em `:322-335`) | Nada — paridade. |
| Card da OS | Foto real (sem placeholder de texto), contador DVI x/y, placa Mercosul (`MercosulPlate`, `:89`), valor, mecânico, prazo, km de entrada, barra de progresso do DVI e linha "últ." com a última transição FSM **auditada** (`Board.tsx:19`, `:56`) | Nada — paridade, com a diferença que importa: a linha "últ." do protótipo é mock (`LAST_ACTIVITY`, `oficina-page.jsx:140-152`), a do vivo é transição FSM real. |
| **StageGate no card** | **Ausente do card.** `ServiceOrderStageGate` existe (`_components/ServiceOrderStageGate.tsx`) mas, por varredura contada (`git grep -l -F ServiceOrderStageGate -- '*.tsx'` → 2 arquivos), só é consumido pelo `ServiceOrderRichSheet` — o **drawer**. `Board.tsx` tem 0 ocorrências de `StageGate` | **Decidir.** O protótipo põe o `StageGateMini` **dentro do card** (`oficina-page.jsx:176`, no `CardExtra`), como checklist de bloqueio visível antes de arrastar — e o cabeçalho registra que *"cliente curtiu m0193"* (`:9`). No vivo o mesmo gate só aparece depois de abrir o drawer. Não é capacidade ausente: é **onde** ela aparece. Construir ou rejeitar por escrito. |
| Duas portas para a máquina FSM | Drag entre colunas **e** botão de ação no card, ambos → confirmação → `ExecuteStageActionService` com audit em `sale_stage_history` (charter §Goals; `KanbanDndProvider` e `DragConfirmDialog` em `Board.tsx:90-93`) | Nada — vivo à frente. No protótipo o avanço é estado React local sobre mock: os botões do card chamam `onAdvance` (`oficina-page.jsx:212`, `:278`, `:342`) e o handler faz `setOsList(list => list.map(...stage: nextStage))` (`:1227`; a lista é `useState(OS_LIST)` em `:745`). Nada de FSM, nada de audit. |
| Drawer da OS | `ServiceOrderRichSheet` / `ServiceOrderRichBody` — DVI semáforo, Fotos & Laudo, Peças & mão-de-obra, Checklist de etapa, Pipeline FSM e Linha do tempo, editável inline; **o mesmo corpo** serve o drawer e o detalhe da view Fila (`Board.tsx:94`; charter §Goals "1 corpo, 2 chrome, zero duplicação") | Nada — vivo à frente. |
| Impressão de fila | `printOficinaFila` (`Board.tsx:88`, `:45-46`) | Nada — paridade. |
| Grade veículo × etapa | Varredura client-side com a marca na célula da etapa FSM atual, legenda data-driven, respeitando busca + KPI-filtro + aba de box (`Board.tsx:72-76`) | Nada — decisão já registrada. O charter (§Non-Goals) proíbe explicitamente importar a **heurística serviço×sintoma** do protótipo: *"a marca espelha só a etapa FSM real da OS"*, sob o gate `no-mock-in-prod`. Não reabrir. |
| ETA-diag · "Encomendado: peça chega X" · "Pago" | Ausentes | Nada — decisão já registrada. Charter §Non-Goals: *"NÃO inventa campo sem lastro … NÃO têm coluna real → omitidos (gate `no-mock-in-prod`). Reentram quando houver schema de estoque/pagamento."* O gap é de **schema**, não de tela. |
| Criar OS | "Nova OS" navega; sem criação inline pelo drag | Nada — Non-Goal do charter (*"NÃO cria OS inline pelo drag (usar 'Nova OS')"*). O protótipo tem o `OsCreateDrawer` (`oficina-forms.jsx`), que é a âncora de **outras** telas (`ServiceOrders/Create` e `/Edit`) — não desta. |
| Vocabulário de domínio | Mecânica pesada: veículo, placa, km, mecânico, box, DVI | Nada — Tier 0 já enforçado. [ADR 0194](../../decisions/0194-correcao-dominio-oficinaauto-martinho-mecanica-pesada.md) + gate `dominio:check`: proibido vocabulário de locação/caçamba (m³, diária, recolhimento). O protótipo está limpo disso (verificado no `kanban-producao-gap.md` §Restrições). |
| Multi-tenant e FSM | Colunas data-driven do payload `columns`; nunca `UPDATE` direto em `current_stage_id` (charter §Anti-hooks) | Nada — vivo à frente. |
