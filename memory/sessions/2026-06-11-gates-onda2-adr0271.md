---
date: "2026-06-11"
hour: "11:45 BRT"
duration: "1.5h"
topic: "Onda 2 dos gates (ADR 0271) executada — consolida/deleta 5 workflows + required-readiness + fix self-audit (64→53)"
authors: [C, W]
prs: [2531]
related_adrs: [0271-revisao-gates-ci-estado-real-required-e-subtracao-segura, 0270-ciclo-de-vida-da-informacao-porta-unica-destilacao-decaimento, 0264-governanca-executavel-trio-dominio-e2e, 0216-governance-drift-framework-driftchecker-plugavel]
---

# Sessão 2026-06-11 — Onda 2 dos gates (ADR 0271) — PR #2531

**Papel:** [CC desktop, com MCP] · **Pedido [W]:** continuar pós-#2527 — "tudo na main, MCP sincronizando, handoff pronto pra próxima sessão executar a onda 2" → `/continuar` → aprovado "Branch nova de main limpo" + "Só os 5 codáveis" → "pode fazer".

## TL;DR
Onda 2 da ADR 0271 executada com a pré-condição cumprida (MCP consultado: `brief-fetch` + `cycles-active` + `my-work` + `my-inbox` ANTES de agir — era o motivo do fechamento da sessão nuvem). **64→53 gates** via PR #2531 (CI 28 pass + 2 skipping corretos). Itens 5/6/7 (branch protection) preparados como comandos prontos pro Wagner — com 1 caveat novo descoberto (ADR 0216 PR scan ainda é path-filtered; promover a required antes de always-run = deadlock).

## O que foi feito (itens 1, 2, 3, 4, 8 da onda 2)
1. **F3 secrets:** job `secrets:audit --auto-pr` portado pro `governance-drift.yml` (scheduled) + `secrets-governance.yml` DELETADO. Fecha o canary 7d da ADR 0216 (nunca encerrado). Scan de PR já coberto por `governance:audit` (SecretsDriftChecker no `--diff-only`).
2. **F6 ragas:** `ragas-gate.yml` DELETADO (W22 MVP superseded por `jana-ragas-gate`, nunca exit 1). Trio→2.
3. **Softs superseded DELETADOS:** `charter-gate.yml` + `mwart-gate.yml` (teatro `continue-on-error`, régua migrou pro `casos-gate` required ADR 0264) + `pr-ui-judge.yml` (dormente, kill-switch OFF — judge segue manual via skill `pr-ui-judge-manual`).
4. **Required-readiness:** `no-mock-gate` / `phpstan-gate` / `governance-gate` / `financeiro-pest` com `pull_request: {}` (always-run, sem paths). `financeiro-pest` ganhou skip-as-pass interno (`dorny/paths-filter` — sem mudança no Financeiro pula MySQL+migrate+Pest e conclui verde; **validado ao vivo no #2531: pass em 1m39s sem rodar o pesado**). `push` segue path-filtered (custo contido no main).
5. **Item 8:** `SystemAuditCommand::checkEvalCiGate` apontava pra `eval-recall-gate.yml` INEXISTENTE (self-audit falso-verde) → `jana-ragas-gate.yml` real.

## Anti-drift (ADR 0270 — deletar sem deixar canon mentindo)
Propagado em: `memory/proibicoes.md` §MWART · `DESIGN.md` §15 (tabela gates + nota de deleção) · `.claude/rules/pages.md` · skills `mwart-process` (camada 3 → casos-gate) + `pr-ui-judge-manual` (judge agora só manual) · `SecretsScanCommand` `@see` · testes `SecretsGovernanceTest` (R-SEC-0215-008 agora asserta a CONSOLIDAÇÃO: secrets-governance.yml ausente + governance-drift.yml com `secrets:audit --auto-pr`) + comentário `BriefDiarioFaithfulnessTest`.

## Verificações de segurança pré-merge
- **Nenhum dos 5 workflows deletados é required check** na branch protection (verificado via `gh api .../required_status_checks`) — deletar não trava merge de ninguém.
- Required atuais (8): ADR frontmatter · Casos-coverage · Conformance cor-crua · Dominio-dict · Frontend/Vite · Pest (Unit) · UI Lint · module-grades-gate.

## Decisões
- **Worktree `frosty-greider` (branch `chore/charter-gate-on-main`):** WIP de charter-gate 100% não-commitado preservado em `stash@{0}` (`charter-gate-wip-frosty-pre-onda2`) — provavelmente obsoleto (a onda 2 deletou o `charter-gate.yml` que ele evoluía), Wagner decide descartar ou revisar.
- **Caveat NOVO (descoberto nesta sessão):** o item 5 da onda 2 lista "secrets-consolidado" pra promoção a required, mas o check `ADR 0216 PR scan (governance:audit --diff-only)` do `governance-drift.yml` **ainda é path-filtered** no `pull_request`. Promover a required SEM antes torná-lo always-run = deadlock "Expected — waiting" em PR fora dos paths. Comandos preparados em 2 variantes (ver handoff).

## Erros + correção
- 1º `git add` falhou por path relativo (cwd do worktree resolve pro repo principal) — corrigido rodando da raiz `D:\oimpresso.com`.

## Residual
- **Itens 5/6/7 (Wagner):** flip branch protection LEI-11 + module-grades→ratchet + fusão 4 gates cor→1 (alto risco, por último, coordenar rename+protection no mesmo momento). Comandos prontos no handoff `2026-06-11-1145-gates-onda2-adr0271`.
- **Backlog ADR 0270/0271 (pós-onda 2):** portas nos 22 módulos órfãos · `knowledge-drift.mjs` como check/cron · F2-F5 ADR 0270 (CT 100) · matar Cockpit mock · RAGAS real diário · deprecação SRS · propagação-ou-falha de renames · `governance-drift.yml` always-run (pré-requisito da promoção do secrets-consolidado).

## Refs
PR #2531 · ADR 0271 onda 2 · ADR 0270 · handoff anterior `2026-06-11-0758-elefante-branco-adr0270-0271-gates-onda1`
