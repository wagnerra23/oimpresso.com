---
date: "2026-07-30"
topic: "PR 5069 — refutação adversarial do lote, rodada 1"
type: session
status: closed
module: governance
pr: 5069
verdict: reprovado
---

# PR #5069 — refutação adversarial, rodada 1

Refutador `opus-5` (Claude Code), sessão fresca — zero contexto de conversa do gerador
(`codex`, branch `codex/module-documentation-activation`). Verificação contra
`origin/main@667e734029` e a branch `8c46d802ea`, sempre no código real: `php -l`,
`git ls-files`, execução dos scripts de governança e leitura dos arquivos citados —
nunca a prosa do PR.

Ressalva de independência declarada: depois de já ter isolado o achado 1 por conta
própria (`php -l` nos dois lados), inspecionei o worktree do gerador em
`C:/tmp/oimpresso-postmerge-main` e encontrei ali a correção **não-commitada** do mesmo
defeito. O achado não veio dele; a coincidência está registrada por honestidade.

## Amostragem

- Anchors/paths: **100%**. Todos os paths citados no `SPEC.md` novo de VozDoCliente
  (12/12 existem), a âncora de heading `US-INFRA-002` em `Infra/SPEC.md:67`, e todos os
  arquivos de runtime tocados pelo diff.
- Prosa: **100%**, não amostra. Os 41 `SUPERFICIE.md` são projeção determinística e foram
  verificados mecanicamente (`module-surface --all --check` = 39 contextos, exit 0); os 9
  documentos escritos à mão foram lidos integralmente.
- PII: CPF 0, CNPJ 0, e-mail de cliente 0, telefone 0.

## Refutações

1. **[FATAL] `Modules/Jana/Services/Reconcile/Reconcilers/IndexReconciler.php` não parseia.**
   O docblock reescrito neste PR contém `` `Modules/*/module.json` `` — a sequência `*/`
   **fecha o comentário** e o backtick seguinte cai como código PHP.
   Prova: `php -l` na versão de `origin/main` → `No syntax errors detected`; na versão do PR →
   `syntax error, unexpected token "\`" ... on line 46`. É a causa dos dois checks vermelhos
   **PHP / Pest (Unit)** (`exit 124`, sem `junit.xml`) e **PHPStan / Larastan · ratchet**.
   O corpo do PR declara *"PHP não estava disponível no desktop; os checks PHP/Pest do CI são
   a prova do ambiente canônico"* — a prova foi produzida e é vermelha.

2. **Contagem do inventário errada no corpo do PR.** O corpo afirma `16.237/16.237`.
   O real é **16.236**: `git ls-files | wc -l` = 16236 e o próprio
   `documentation-loop --impact-ref origin/main --json` devolve
   `repository_inventory.total = 16236, classified = 16236, unclassified = []`.
   O session log do PR está correto (16.236) — quem diverge é o corpo.

3. **Contagem dos testes Node errada no corpo do PR.** O corpo afirma `42/42`.
   O conjunto que o próprio workflow do PR executa
   (`module-surface.test.mjs` + `catalog-graph.test.mjs` + `qa/page-path.test.mjs`) devolve
   `tests 44 · pass 44 · fail 0`. O session log está correto (44/44); o corpo subconta 2.

## Confirmados (não refutados)

- `documentation-loop --selftest`: 17 `[OK]`, inclusive o bite-test novo
  *"detector distingue module.json novo presente de path deletado no diff"*.
- `module-surface --all --check`: **39** contextos, todos `sem drift`, exit 0.
- `catalog-graph --check`: 38 módulos · 612 nós · 935 arestas · 0 pendurados · freshness OK.
- Frota 35/35: `memory/modulos/INDEX.md` tem exatamente os 35 módulos com `module.json`
  no disco (set-diff dos dois lados = vazio).
- Fecho transitivo com profundidade explícita: `related_module_depth` traz `{module, depth}`.
- `Modules/Financeiro/SCOPE.md` → `depends_on: [Sells, Compras]` é real: 32 arquivos do
  módulo referenciam `Transaction`, incluindo `Observers/TransactionObserver.php` e
  `Jobs/CriarTituloDeVendaJob.php`.
- Zero marcadores de conflito commitados.
- `memory/requisitos/VozDoCliente/SPEC.md` (novo): os 8 critérios de aceite batem com o
  código — `min:5|max:2000` e `between:0,4` e `max:500` em `StoreSinalRequest.php:20-23`;
  `business_id` da sessão + `abort_if($businessId <= 0, 403)` em `SinalController.php:57-61`;
  `unique(['business_id','hash_origem'])` na migration:95; `sha256($businessId.'|'.$normalizado)`
  em `Sinal.php:88`; pendentes-primeiro em `SinalController.php:48`. A prova citada
  **executa mesmo** em CI: `SinalCrossTenantTest.php` está em `.github/ci-sqlite-pest.list:287`.

## Verificado e NÃO é erro

- **Deleção de `Modules/NfeBrasil/Resources/lang/pt-br/nfebrasil.php`.** Era duplicata de
  casing: `origin/main` rastreava `pt-br/` e `pt-BR/` com blob idêntico (`diff` vazio), e
  não há **nenhum** consumidor de `nfebrasil::nfebrasil.*` no repo (o único hit é um
  comentário em `DataController.php:21`). A remoção do minúsculo é inócua.

## Ressalva (não contada como erro)

Após merge com `origin/main@667e734029` (3 commits posteriores ao último run do CI),
`system-map --check` reprova com *"PAINEL-SISTEMA.md desatualizado"*. Controle negativo:
`origin/main` **puro também reprova** o mesmo check (*"PATH MORTO … Modules/SRS"*) — logo é
dívida herdada, não introduzida aqui. O job `drift` do `module-surface.yml`, onde este PR
passa a chamar o check, não consta em `governance/required-checks-baseline.json`: é
advisory, não bloqueia merge. Fica o registro de que o check nasce vermelho.

## Métrica e veredito

- Itens verificados: **58** (13 claims do corpo do PR · 8 evidências do session log ·
  26 anchors/critérios/invariantes do SPEC novo · 11 verificações estruturais).
- Erros confirmados: **3**.
- `error_rate_pct` = **5,17** (aceite: < 2).
- **Veredito: REPROVADO** na rodada 1.

Correção mínima para a rodada 2: (a) o docblock não pode conter `*/` — a forma já validada
por `php -l` é *"os manifestos `module.json` sob `Modules/<Modulo>/`"*; (b) corpo do PR
`16.237 → 16.236`; (c) corpo do PR `42/42 → 44/44`. Como esta sessão aplicou (a) e propôs
(b)/(c), ela vira geradora da correção: **a rodada 2 exige refutador de sessão fresca e
independente desta** — o `ledger-check` só libera com `veredito: aprovado` e
`error_rate_pct < 2`.
