# SPEC — DemoMod (fixture BOA: falar do módulo sem o token de path)

O jeito que de fato solta é o TOKEN deixar de ser `Modules/<X>`. Este doc fala
do módulo GhostNovo em prosa, no passado, sem afirmar caminho vivo — e a catraca
tem que passar (exit 0).

O módulo GhostNovo foi removido; o acervo dele virou responsabilidade de outro
dono. Nenhum caminho `Modules/` é afirmado aqui.
