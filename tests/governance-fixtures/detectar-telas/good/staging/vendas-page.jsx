// vendas-page.jsx — mockup do bundle SEM charter, resolvido via ALIAS map de
// detectar-telas.mjs → resources/js/Pages/Sells/Index.tsx. Como o .jsx ≠ .tsx,
// o status é SEMANTICO (não diffável) — NÃO é órfão. Único screen-source do
// staging good ⇒ 0 telas órfãs ⇒ exit 0 ("OK — 0 telas órfãs").
export function VendasPage() {
  return null;
}
