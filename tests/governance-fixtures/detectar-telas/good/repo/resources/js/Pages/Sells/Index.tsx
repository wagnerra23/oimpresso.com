// Sells/Index — alvo declarado pelo ALIAS de vendas-page.jsx. Precisa EXISTIR no
// repo da fixture, senão o status cai pra ALVO-PENDENTE (que não conta como órfão,
// mas deixaria o good "semanticamente sujo"). Com ele presente → SEMANTICO limpo.
export default function SellsIndex() {
  return null;
}
