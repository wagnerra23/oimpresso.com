// mistero-page.jsx — mockup SEM charter-âncora e SEM entrada no ALIAS map →
// não casa nenhuma das 5 regras de resolução de detectar-telas.mjs → status ORFAO.
// É o "tela perdida em silêncio" que o gate TEM que pegar: stderr "GATE FALHOU:
// 1 screen-source(s) ÓRFÃO/AMBÍGUO" + exit 1. Se um dia o gate parar de morder
// (mistero passar a resolver/sumir), o caso bad sai 0 e o gate-selftest avermelha.
export function MisteroPage() {
  return null;
}
