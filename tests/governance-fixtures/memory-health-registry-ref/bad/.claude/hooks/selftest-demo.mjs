// Fixture do Check P (memory-health) — o hook PORTADO (.ps1→.mjs).
// A fixture ruim tem ESTE arquivo, mas o registry ainda cita `selftest-demo.ps1`:
// é exatamente o drift que o check pega (o porte aconteceu, o registry não soube).
