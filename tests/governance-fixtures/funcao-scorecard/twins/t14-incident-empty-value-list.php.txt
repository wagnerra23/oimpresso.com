/**
 * @return string[] slugs distintos das tags (a coluna slug é nullable).
 */
public function optionValues(): array
{
    return Tag::where('business_id', session('user.business_id'))
        ->distinct()->pluck('slug')->all();
}
