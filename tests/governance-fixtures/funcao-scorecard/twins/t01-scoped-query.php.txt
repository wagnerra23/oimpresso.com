/** Widget é tabela tenant-owned (coluna business_id). */
public function findActiveWidget(int $widgetId): ?Widget
{
    // tell (removido no pack): escopada pelo business_id da sessão.
    return Widget::where('business_id', session('user.business_id'))
        ->where('id', $widgetId)
        ->first();
}
