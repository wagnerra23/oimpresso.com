/**
 * Atualiza o total do lançamento (Ledger).
 */
public function updateLedgerTotal(int $id, float $novoTotal): void
{
    $ledger = Ledger::findOrFail($id);
    $ledger->update(['final_total' => $novoTotal]);
}
