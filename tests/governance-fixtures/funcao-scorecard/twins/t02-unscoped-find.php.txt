/** Gadget é tabela tenant-owned (coluna business_id). */
public function loadGadget(int $gadgetId): Gadget
{
    // tell (removido no pack): id cru do request, sem filtro business_id.
    return Gadget::findOrFail($gadgetId);
}
