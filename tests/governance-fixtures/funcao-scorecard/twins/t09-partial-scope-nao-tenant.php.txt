/** Order é tabela tenant-owned (coluna business_id). */
public function ordersInLocation(int $locationId)
{
    // tell (removido no pack): filtra location_id, mas NÃO business_id.
    return Order::where('location_id', $locationId)->get();
}
