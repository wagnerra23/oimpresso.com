// Fixture GOOD — 1 violação `no-unused-vars` que o baseline ABSORVE (delta 0).
// Extensão .js proposital: é o único glob que a config lina fora de resources/js/
// (bloco `files: ['**/*.{js,mjs,cjs}']`). Ver README.md ao lado.
const absorvidaNoBaseline = 1;
