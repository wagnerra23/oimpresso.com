// Fixture BAD — violação `no-unused-vars` NOVA, ausente do baseline → regressão (delta +1).
// É a "uma violação a mais que o baseline" que a catraca deve MORDER (exit 1 + acusação).
const violacaoNova = 1;
