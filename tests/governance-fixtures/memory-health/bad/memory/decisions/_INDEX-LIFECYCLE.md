# _INDEX-LIFECYCLE (fixture bad) — registro INCOMPLETO de propósito

Repare: a colisão entre `demo-foo` e `demo-bar` NÃO está registrada em
`governance/adr-collisions-baseline.json` (ausente neste sandbox — ADR 0274 §3).
Por isso o Check A (colisão ADR não-registrada) deve MORDER. (Este índice não é
mais lido pelo Check A — fica só como conteúdo realista de memory/decisions/.)

| ADR  | slug         | lifecycle |
|------|--------------|-----------|
| 0002 | demo-segunda | ativo     |
