# _INDEX-LIFECYCLE (fixture bad) — registro INCOMPLETO de propósito

Repare: a colisão de número entre `demo-foo` e `demo-bar` NÃO aparece aqui.
Por isso o Check A (colisão ADR não-registrada) deve MORDER. Só o ADR de
número único abaixo está registrado.

| ADR  | slug         | lifecycle |
|------|--------------|-----------|
| 0002 | demo-segunda | ativo     |
